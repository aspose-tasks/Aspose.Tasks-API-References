---
title: "TaskKey"
second_title: "مرجع API لـ Aspose.Tasks for Java"
description: "يمثل قائمة بالحقول المدعومة للمهمة."
type: docs
weight: 294
url: /ar/java/com.aspose.tasks/taskkey/
---

**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class TaskKey extends System.Enum
```

يمثل قائمة بالحقول المدعومة للمهمة.
## الحقول

| حقل | الوصف |
| --- | --- |
| [ACWP](#ACWP) | يمثل حقل ACWP (المهمة). |
| [ActivityId](#ActivityId) | يمثل حقل ActivityId (ينطبق فقط على مشاريع Primavera). |
| [ActualCost](#ActualCost) | يمثل حقل ActualCost (المهمة). |
| [ActualDuration](#ActualDuration) | يمثل حقل ActualDuration (المهمة). |
| [ActualFinish](#ActualFinish) | يمثل حقل ActualFinish (المهمة). |
| [ActualOvertimeCost](#ActualOvertimeCost) | يمثل حقل ActualOvertimeCost (المهمة). |
| [ActualOvertimeWork](#ActualOvertimeWork) | يمثل حقل ActualOvertimeWork (المهمة). |
| [ActualOvertimeWorkProtected](#ActualOvertimeWorkProtected) | يمثل حقل ActualOvertimeWorkProtected (المهمة). |
| [ActualStart](#ActualStart) | يمثل حقل ActualStart (المهمة). |
| [ActualWork](#ActualWork) | يمثل الحقل ActualWork (Task). |
| [ActualWorkProtected](#ActualWorkProtected) | يمثل الحقل ActualWorkProtected (Task). |
| [BCWP](#BCWP) | يمثل الحقل BCWP (Task). |
| [BCWS](#BCWS) | يمثل الحقل BCWS (Task). |
| [BudgetCost](#BudgetCost) | يمثل الحقل BudgetCost (Task). |
| [BudgetWork](#BudgetWork) | يمثل الحقل BudgetWork (Task). |
| [CV](#CV) | يمثل الحقل CV (Task). |
| [Calendar](#Calendar) | يمثل الحقل Calendar (Task). |
| [CalendarUid](#CalendarUid) | يمثل الحقل CalendarUid (Task). |
| [CommitmentFinish](#CommitmentFinish) | يمثل الحقل CommitmentFinish (Task). |
| [CommitmentStart](#CommitmentStart) | يمثل الحقل CommitmentStart (Task). |
| [CommitmentType](#CommitmentType) | يمثل الحقل CommitmentType (Task). |
| [ConstraintDate](#ConstraintDate) | يمثل الحقل ConstraintDate (Task). |
| [ConstraintType](#ConstraintType) | يمثل الحقل ConstraintType (Task). |
| [Contact](#Contact) | يمثل الحقل Contact (Task). |
| [Cost](#Cost) | يمثل الحقل Cost (Task). |
| [CostVariance](#CostVariance) | يمثل الحقل CostVariance (Task). |
| [Created](#Created) | يمثل الحقل Created (Task). |
| [Deadline](#Deadline) | يمثل الحقل Deadline (Task). |
| [DisplayAsSummary](#DisplayAsSummary) | يمثل الحقل DisplayAsSummary (Task). |
| [DisplayOnTimeline](#DisplayOnTimeline) | يمثل الحقل DisplayOnTimeline (Task). |
| [Duration](#Duration) | يمثل الحقل Duration (Task). |
| [DurationText](#DurationText) | يمثل الحقل DurationText (Task). |
| [DurationVariance](#DurationVariance) | يمثل الحقل DurationVariance (Task). |
| [EarlyFinish](#EarlyFinish) | يمثل الحقل EarlyFinish (Task). |
| [EarlyStart](#EarlyStart) | يمثل حقل EarlyStart (Task). |
| [EarnedValueMethod](#EarnedValueMethod) | يمثل حقل EarnedValueMethod (Task). |
| [ExternalId](#ExternalId) | يمثل حقل ExternalId (Task). |
| [ExternalTaskProject](#ExternalTaskProject) | يمثل حقل ExternalTaskProject (Task). |
| [ExternalUid](#ExternalUid) | يمثل Unique Id للمهمة الخارجية. |
| [Finish](#Finish) | يمثل حقل Finish (Task). |
| [FinishSlack](#FinishSlack) | يمثل حقل FinishSlack (Task). |
| [FinishSlackTimeSpan](#FinishSlackTimeSpan) | يمثل حقل FinishSlack (Task). |
| [FinishText](#FinishText) | يمثل حقل FinishText (Task). |
| [FinishVariance](#FinishVariance) | يمثل حقل FinishVariance (Task). |
| [FixedCost](#FixedCost) | يمثل حقل FixedCost (Task). |
| [FixedCostAccrual](#FixedCostAccrual) | يمثل حقل FixedCostAccrual (Task). |
| [FreeSlack](#FreeSlack) | يمثل حقل FreeSlack (Task). |
| [FreeSlackTimeSpan](#FreeSlackTimeSpan) | يمثل حقل FreeSlack (Task). |
| [Guid](#Guid) | يمثل حقل Guid (Task). |
| [HasOverallocatedResource](#HasOverallocatedResource) | يمثل حقل HasOverallocatedResource (Task). |
| [HideBar](#HideBar) | يمثل حقل HideBar (Task). |
| [Hyperlink](#Hyperlink) | يمثل حقل Hyperlink (Task). |
| [HyperlinkAddress](#HyperlinkAddress) | يمثل حقل HyperlinkAddress (Task). |
| [HyperlinkSubAddress](#HyperlinkSubAddress) | يمثل حقل HyperlinkSubAddress (Task). |
| [Id](#Id) | يمثل حقل Id (Task). |
| [IgnoreResourceCalendar](#IgnoreResourceCalendar) | يمثل حقل IgnoreResourceCalendar (Task). |
| [IgnoreWarnings](#IgnoreWarnings) | يمثل حقل IgnoreWarnings (Task). |
| [IsActive](#IsActive) | يمثل حقل IsActive (Task). |
| [IsCritical](#IsCritical) | يمثل حقل IsCritical (Task). |
| [IsEffortDriven](#IsEffortDriven) | يمثل حقل IsEffortDriven (Task). |
| [IsEstimated](#IsEstimated) | يمثل حقل IsEstimated (Task). |
| [IsExpanded](#IsExpanded) | يمثل حقل IsExpanded (Task). |
| [IsExternalTask](#IsExternalTask) | يمثل حقل IsExternalTask (Task). |
| [IsManual](#IsManual) | يمثل حقل IsManual (Task). |
| [IsMarked](#IsMarked) | يمثل حقل IsMarked (Task). |
| [IsMilestone](#IsMilestone) | يمثل حقل IsMilestone (Task). |
| [IsNull](#IsNull) | يمثل حقل IsNull (Task). |
| [IsOverallocated](#IsOverallocated) | يمثل حقل IsOverallocated (Task). |
| [IsPublished](#IsPublished) | يمثل حقل IsPublished (Task). |
| [IsRecurring](#IsRecurring) | يمثل حقل IsRecurring (Task). |
| [IsResumeValid](#IsResumeValid) | يمثل حقل IsResumeValid (Task). |
| [IsRollup](#IsRollup) | يمثل حقل IsRollup (Task). |
| [IsScheduled](#IsScheduled) | يمثل حقل IsScheduled (Task). |
| [IsShowBeforeProjectStartDateWarning](#IsShowBeforeProjectStartDateWarning) | يمثل العلامة التي تجعل MSP تعرض التحذير بأن المهمة تبدأ قبل تاريخ بدء المشروع. |
| [IsSubproject](#IsSubproject) | يمثل حقل IsSubproject (Task). |
| [IsSubprojectReadOnly](#IsSubprojectReadOnly) | يمثل حقل IsSubprojectReadOnly (Task). |
| [IsSummary](#IsSummary) | يمثل حقل IsSummary (Task). |
| [LateFinish](#LateFinish) | يمثل حقل LateFinish (Task). |
| [LateStart](#LateStart) | يمثل حقل LateStart (Task). |
| [LevelAssignments](#LevelAssignments) | يمثل حقل LevelAssignments (Task). |
| [LevelingCanSplit](#LevelingCanSplit) | يمثل حقل LevelingCanSplit (Task). |
| [LevelingDelay](#LevelingDelay) | يمثل حقل LevelingDelay (Task). |
| [ManualDuration](#ManualDuration) | يمثل حقل ManualDuration (Task). |
| [ManualFinish](#ManualFinish) | يمثل حقل ManualFinish (Task). |
| [ManualStart](#ManualStart) | يمثل حقل ManualStart (Task). |
| [Name](#Name) | يمثل حقل Name (Task). |
| [NotesRTF](#NotesRTF) | يمثل حقل NotesRTF (Task). |
| [NotesText](#NotesText) | يمثل حقل NotesText (Task). |
| [OutlineLevel](#OutlineLevel) | يمثل حقل OutlineLevel (Task). |
| [OutlineNumber](#OutlineNumber) | يمثل حقل OutlineNumber (Task). |
| [OvertimeCost](#OvertimeCost) | يمثل حقل OvertimeCost (Task). |
| [OvertimeWork](#OvertimeWork) | يمثل حقل OvertimeWork (Task). |
| [ParentTaskGuid](#ParentTaskGuid) | يمثل حقل ParentTaskGuid (Task). |
| [ParentTaskUid](#ParentTaskUid) | يمثل حقل ParentTaskUid (Task). |
| [PercentComplete](#PercentComplete) | يمثل حقل PercentComplete (Task). |
| [PercentWorkComplete](#PercentWorkComplete) | يمثل حقل PercentWorkComplete (Task). |
| [PhysicalPercentComplete](#PhysicalPercentComplete) | يمثل حقل PhysicalPercentComplete (Task). |
| [PreleveledFinish](#PreleveledFinish) | يمثل حقل PreleveledFinish (Task). |
| [PreleveledStart](#PreleveledStart) | يمثل حقل PreleveledStart (Task). |
| [Priority](#Priority) | يمثل حقل Priority (Task). |
| [RegularWork](#RegularWork) | يمثل حقل RegularWork (Task). |
| [RemainingCost](#RemainingCost) | يمثل حقل RemainingCost (Task). |
| [RemainingDuration](#RemainingDuration) | يمثل حقل RemainingDuration (Task). |
| [RemainingOvertimeCost](#RemainingOvertimeCost) | يمثل حقل RemainingOvertimeCost (Task). |
| [RemainingOvertimeWork](#RemainingOvertimeWork) | يمثل حقل RemainingOvertimeWork (Task). |
| [RemainingWork](#RemainingWork) | يمثل حقل RemainingWork (Task). |
| [Resume](#Resume) | يمثل حقل Resume (Task). |
| [SV](#SV) | يمثل حقل SV (Task). |
| [Start](#Start) | يمثل حقل Start (Task). |
| [StartSlack](#StartSlack) | يمثل حقل StartSlack (Task). |
| [StartSlackTimeSpan](#StartSlackTimeSpan) | يمثل حقل StartSlack (Task). |
| [StartText](#StartText) | يمثل حقل StartText (Task). |
| [StartVariance](#StartVariance) | يمثل حقل StartVariance (Task). |
| [StatusManager](#StatusManager) | يمثل حقل StatusManager (Task). |
| [Stop](#Stop) | يمثل حقل Stop (Task). |
| [SubprojectName](#SubprojectName) | يمثل حقل SubprojectName (Task). |
| [SummaryProgress](#SummaryProgress) | يمثل حقل Summary Progress (Task). |
| [TaskIsAssigned](#TaskIsAssigned) | يمثل حقل TaskIsAssigned (Task). |
| [TaskOriginalFinish](#TaskOriginalFinish) | يمثل حقل original finish (Task). |
| [TaskOriginalStart](#TaskOriginalStart) | يمثل حقل original start (Task). |
| [TotalSlack](#TotalSlack) | يمثل حقل TotalSlack (Task). |
| [TotalSlackTimeSpan](#TotalSlackTimeSpan) | يمثل حقل TotalSlack (Task). |
| [Type](#Type) | يمثل حقل Type (Task). |
| [Uid](#Uid) | يمثل حقل Unique ID (Task). |
| [WBS](#WBS) | يمثل حقل WBS (Task). |
| [WBSLevel](#WBSLevel) | يمثل حقل WBSLevel (Task). |
| [Warning](#Warning) | يمثل العلامة التي تشير إلى أن المهمة لديها اختلافات في الجدول الزمني. |
| [Work](#Work) | يمثل حقل Work (Task). |
| [WorkVariance](#WorkVariance) | يمثل حقل WorkVariance (Task). |
### ACWP {#ACWP}
```
public static final byte ACWP
```


يمثل حقل ACWP (المهمة).

### ActivityId {#ActivityId}
```
public static final byte ActivityId
```


يمثل حقل ActivityId (ينطبق فقط على مشاريع Primavera).

### ActualCost {#ActualCost}
```
public static final byte ActualCost
```


يمثل حقل ActualCost (المهمة).

### ActualDuration {#ActualDuration}
```
public static final byte ActualDuration
```


يمثل حقل ActualDuration (المهمة).

### ActualFinish {#ActualFinish}
```
public static final byte ActualFinish
```


يمثل حقل ActualFinish (المهمة).

### ActualOvertimeCost {#ActualOvertimeCost}
```
public static final byte ActualOvertimeCost
```


يمثل حقل ActualOvertimeCost (المهمة).

### ActualOvertimeWork {#ActualOvertimeWork}
```
public static final byte ActualOvertimeWork
```


يمثل حقل ActualOvertimeWork (المهمة).

### ActualOvertimeWorkProtected {#ActualOvertimeWorkProtected}
```
public static final byte ActualOvertimeWorkProtected
```


يمثل حقل ActualOvertimeWorkProtected (المهمة).

### ActualStart {#ActualStart}
```
public static final byte ActualStart
```


يمثل حقل ActualStart (المهمة).

### ActualWork {#ActualWork}
```
public static final byte ActualWork
```


يمثل الحقل ActualWork (Task).

### ActualWorkProtected {#ActualWorkProtected}
```
public static final byte ActualWorkProtected
```


يمثل الحقل ActualWorkProtected (Task).

### BCWP {#BCWP}
```
public static final byte BCWP
```


يمثل الحقل BCWP (Task).

### BCWS {#BCWS}
```
public static final byte BCWS
```


يمثل الحقل BCWS (Task).

### BudgetCost {#BudgetCost}
```
public static final byte BudgetCost
```


يمثل الحقل BudgetCost (Task).

### BudgetWork {#BudgetWork}
```
public static final byte BudgetWork
```


يمثل الحقل BudgetWork (Task).

### CV {#CV}
```
public static final byte CV
```


يمثل الحقل CV (Task).

### Calendar {#Calendar}
```
public static final byte Calendar
```


يمثل الحقل Calendar (Task).

### CalendarUid {#CalendarUid}
```
public static final byte CalendarUid
```


يمثل الحقل CalendarUid (Task).

### CommitmentFinish {#CommitmentFinish}
```
public static final byte CommitmentFinish
```


يمثل الحقل CommitmentFinish (Task).

### CommitmentStart {#CommitmentStart}
```
public static final byte CommitmentStart
```


يمثل الحقل CommitmentStart (Task).

### CommitmentType {#CommitmentType}
```
public static final byte CommitmentType
```


يمثل الحقل CommitmentType (Task).

### ConstraintDate {#ConstraintDate}
```
public static final byte ConstraintDate
```


يمثل الحقل ConstraintDate (Task).

### ConstraintType {#ConstraintType}
```
public static final byte ConstraintType
```


يمثل الحقل ConstraintType (Task).

### Contact {#Contact}
```
public static final byte Contact
```


يمثل الحقل Contact (Task).

### Cost {#Cost}
```
public static final byte Cost
```


يمثل الحقل Cost (Task).

### CostVariance {#CostVariance}
```
public static final byte CostVariance
```


يمثل الحقل CostVariance (Task).

### Created {#Created}
```
public static final byte Created
```


يمثل الحقل Created (Task).

### Deadline {#Deadline}
```
public static final byte Deadline
```


يمثل الحقل Deadline (Task).

### DisplayAsSummary {#DisplayAsSummary}
```
public static final byte DisplayAsSummary
```


يمثل الحقل DisplayAsSummary (Task).

### DisplayOnTimeline {#DisplayOnTimeline}
```
public static final byte DisplayOnTimeline
```


يمثل الحقل DisplayOnTimeline (Task).

### Duration {#Duration}
```
public static final byte Duration
```


يمثل الحقل Duration (Task).

### DurationText {#DurationText}
```
public static final byte DurationText
```


يمثل الحقل DurationText (Task).

### DurationVariance {#DurationVariance}
```
public static final byte DurationVariance
```


يمثل الحقل DurationVariance (Task).

### EarlyFinish {#EarlyFinish}
```
public static final byte EarlyFinish
```


يمثل الحقل EarlyFinish (Task).

### EarlyStart {#EarlyStart}
```
public static final byte EarlyStart
```


يمثل حقل EarlyStart (Task).

### EarnedValueMethod {#EarnedValueMethod}
```
public static final byte EarnedValueMethod
```


يمثل حقل EarnedValueMethod (Task).

### ExternalId {#ExternalId}
```
public static final byte ExternalId
```


يمثل حقل ExternalId (Task).

### ExternalTaskProject {#ExternalTaskProject}
```
public static final byte ExternalTaskProject
```


يمثل حقل ExternalTaskProject (Task).

### ExternalUid {#ExternalUid}
```
public static final byte ExternalUid
```


يمثل Unique Id للمهمة الخارجية.

### Finish {#Finish}
```
public static final byte Finish
```


يمثل حقل Finish (Task).

### FinishSlack {#FinishSlack}
```
public static final byte FinishSlack
```


يمثل حقل FinishSlack (Task).

### FinishSlackTimeSpan {#FinishSlackTimeSpan}
```
public static final byte FinishSlackTimeSpan
```


يمثل حقل FinishSlack (Task).

### FinishText {#FinishText}
```
public static final byte FinishText
```


يمثل حقل FinishText (Task).

### FinishVariance {#FinishVariance}
```
public static final byte FinishVariance
```


يمثل حقل FinishVariance (Task).

### FixedCost {#FixedCost}
```
public static final byte FixedCost
```


يمثل حقل FixedCost (Task).

### FixedCostAccrual {#FixedCostAccrual}
```
public static final byte FixedCostAccrual
```


يمثل حقل FixedCostAccrual (Task).

### FreeSlack {#FreeSlack}
```
public static final byte FreeSlack
```


يمثل حقل FreeSlack (Task).

### FreeSlackTimeSpan {#FreeSlackTimeSpan}
```
public static final byte FreeSlackTimeSpan
```


يمثل حقل FreeSlack (Task).

### Guid {#Guid}
```
public static final byte Guid
```


يمثل حقل Guid (Task).

### HasOverallocatedResource {#HasOverallocatedResource}
```
public static final byte HasOverallocatedResource
```


يمثل حقل HasOverallocatedResource (Task).

### HideBar {#HideBar}
```
public static final byte HideBar
```


يمثل حقل HideBar (Task).

### Hyperlink {#Hyperlink}
```
public static final byte Hyperlink
```


يمثل حقل Hyperlink (Task).

### HyperlinkAddress {#HyperlinkAddress}
```
public static final byte HyperlinkAddress
```


يمثل حقل HyperlinkAddress (Task).

### HyperlinkSubAddress {#HyperlinkSubAddress}
```
public static final byte HyperlinkSubAddress
```


يمثل حقل HyperlinkSubAddress (Task).

### Id {#Id}
```
public static final byte Id
```


يمثل حقل Id (Task).

### IgnoreResourceCalendar {#IgnoreResourceCalendar}
```
public static final byte IgnoreResourceCalendar
```


يمثل حقل IgnoreResourceCalendar (Task).

### IgnoreWarnings {#IgnoreWarnings}
```
public static final byte IgnoreWarnings
```


يمثل حقل IgnoreWarnings (Task).

### IsActive {#IsActive}
```
public static final byte IsActive
```


يمثل حقل IsActive (Task).

### IsCritical {#IsCritical}
```
public static final byte IsCritical
```


يمثل حقل IsCritical (Task).

### IsEffortDriven {#IsEffortDriven}
```
public static final byte IsEffortDriven
```


يمثل حقل IsEffortDriven (Task).

### IsEstimated {#IsEstimated}
```
public static final byte IsEstimated
```


يمثل حقل IsEstimated (Task).

### IsExpanded {#IsExpanded}
```
public static final byte IsExpanded
```


يمثل حقل IsExpanded (Task).

### IsExternalTask {#IsExternalTask}
```
public static final byte IsExternalTask
```


يمثل حقل IsExternalTask (Task).

### IsManual {#IsManual}
```
public static final byte IsManual
```


يمثل حقل IsManual (Task).

### IsMarked {#IsMarked}
```
public static final byte IsMarked
```


يمثل حقل IsMarked (Task).

### IsMilestone {#IsMilestone}
```
public static final byte IsMilestone
```


يمثل حقل IsMilestone (Task).

### IsNull {#IsNull}
```
public static final byte IsNull
```


يمثل حقل IsNull (Task).

### IsOverallocated {#IsOverallocated}
```
public static final byte IsOverallocated
```


يمثل حقل IsOverallocated (Task).

### IsPublished {#IsPublished}
```
public static final byte IsPublished
```


يمثل حقل IsPublished (Task).

### IsRecurring {#IsRecurring}
```
public static final byte IsRecurring
```


يمثل حقل IsRecurring (Task).

### IsResumeValid {#IsResumeValid}
```
public static final byte IsResumeValid
```


يمثل حقل IsResumeValid (Task).

### IsRollup {#IsRollup}
```
public static final byte IsRollup
```


يمثل حقل IsRollup (Task).

### IsScheduled {#IsScheduled}
```
public static final byte IsScheduled
```


يمثل حقل IsScheduled (Task).

### IsShowBeforeProjectStartDateWarning {#IsShowBeforeProjectStartDateWarning}
```
public static final byte IsShowBeforeProjectStartDateWarning
```


يمثل العلامة التي تجعل MSP تعرض التحذير بأن المهمة تبدأ قبل تاريخ بدء المشروع.

### IsSubproject {#IsSubproject}
```
public static final byte IsSubproject
```


يمثل حقل IsSubproject (Task).

### IsSubprojectReadOnly {#IsSubprojectReadOnly}
```
public static final byte IsSubprojectReadOnly
```


يمثل حقل IsSubprojectReadOnly (Task).

### IsSummary {#IsSummary}
```
public static final byte IsSummary
```


يمثل حقل IsSummary (Task).

### LateFinish {#LateFinish}
```
public static final byte LateFinish
```


يمثل حقل LateFinish (Task).

### LateStart {#LateStart}
```
public static final byte LateStart
```


يمثل حقل LateStart (Task).

### LevelAssignments {#LevelAssignments}
```
public static final byte LevelAssignments
```


يمثل حقل LevelAssignments (Task).

### LevelingCanSplit {#LevelingCanSplit}
```
public static final byte LevelingCanSplit
```


يمثل حقل LevelingCanSplit (Task).

### LevelingDelay {#LevelingDelay}
```
public static final byte LevelingDelay
```


يمثل حقل LevelingDelay (Task).

### ManualDuration {#ManualDuration}
```
public static final byte ManualDuration
```


يمثل حقل ManualDuration (Task).

### ManualFinish {#ManualFinish}
```
public static final byte ManualFinish
```


يمثل حقل ManualFinish (Task).

### ManualStart {#ManualStart}
```
public static final byte ManualStart
```


يمثل حقل ManualStart (Task).

### Name {#Name}
```
public static final byte Name
```


يمثل حقل Name (Task).

### NotesRTF {#NotesRTF}
```
public static final byte NotesRTF
```


يمثل حقل NotesRTF (Task).

### NotesText {#NotesText}
```
public static final byte NotesText
```


يمثل حقل NotesText (Task).

### OutlineLevel {#OutlineLevel}
```
public static final byte OutlineLevel
```


يمثل حقل OutlineLevel (Task).

### OutlineNumber {#OutlineNumber}
```
public static final byte OutlineNumber
```


يمثل حقل OutlineNumber (Task).

### OvertimeCost {#OvertimeCost}
```
public static final byte OvertimeCost
```


يمثل حقل OvertimeCost (Task).

### OvertimeWork {#OvertimeWork}
```
public static final byte OvertimeWork
```


يمثل حقل OvertimeWork (Task).

### ParentTaskGuid {#ParentTaskGuid}
```
public static final byte ParentTaskGuid
```


يمثل حقل ParentTaskGuid (Task).

### ParentTaskUid {#ParentTaskUid}
```
public static final byte ParentTaskUid
```


يمثل حقل ParentTaskUid (Task).

### PercentComplete {#PercentComplete}
```
public static final byte PercentComplete
```


يمثل حقل PercentComplete (Task).

### PercentWorkComplete {#PercentWorkComplete}
```
public static final byte PercentWorkComplete
```


يمثل حقل PercentWorkComplete (Task).

### PhysicalPercentComplete {#PhysicalPercentComplete}
```
public static final byte PhysicalPercentComplete
```


يمثل حقل PhysicalPercentComplete (Task).

### PreleveledFinish {#PreleveledFinish}
```
public static final byte PreleveledFinish
```


يمثل حقل PreleveledFinish (Task).

### PreleveledStart {#PreleveledStart}
```
public static final byte PreleveledStart
```


يمثل حقل PreleveledStart (Task).

### Priority {#Priority}
```
public static final byte Priority
```


يمثل حقل Priority (Task).

### RegularWork {#RegularWork}
```
public static final byte RegularWork
```


يمثل حقل RegularWork (Task).

### RemainingCost {#RemainingCost}
```
public static final byte RemainingCost
```


يمثل حقل RemainingCost (Task).

### RemainingDuration {#RemainingDuration}
```
public static final byte RemainingDuration
```


يمثل حقل RemainingDuration (Task).

### RemainingOvertimeCost {#RemainingOvertimeCost}
```
public static final byte RemainingOvertimeCost
```


يمثل حقل RemainingOvertimeCost (Task).

### RemainingOvertimeWork {#RemainingOvertimeWork}
```
public static final byte RemainingOvertimeWork
```


يمثل حقل RemainingOvertimeWork (Task).

### RemainingWork {#RemainingWork}
```
public static final byte RemainingWork
```


يمثل حقل RemainingWork (Task).

### Resume {#Resume}
```
public static final byte Resume
```


يمثل حقل Resume (Task).

### SV {#SV}
```
public static final byte SV
```


يمثل حقل SV (Task).

### Start {#Start}
```
public static final byte Start
```


يمثل حقل Start (Task).

### StartSlack {#StartSlack}
```
public static final byte StartSlack
```


يمثل حقل StartSlack (Task).

### StartSlackTimeSpan {#StartSlackTimeSpan}
```
public static final byte StartSlackTimeSpan
```


يمثل حقل StartSlack (Task).

### StartText {#StartText}
```
public static final byte StartText
```


يمثل حقل StartText (Task).

### StartVariance {#StartVariance}
```
public static final byte StartVariance
```


يمثل حقل StartVariance (Task).

### StatusManager {#StatusManager}
```
public static final byte StatusManager
```


يمثل حقل StatusManager (Task).

### Stop {#Stop}
```
public static final byte Stop
```


يمثل حقل Stop (Task).

### SubprojectName {#SubprojectName}
```
public static final byte SubprojectName
```


يمثل حقل SubprojectName (Task).

### SummaryProgress {#SummaryProgress}
```
public static final byte SummaryProgress
```


يمثل حقل Summary Progress (Task).

### TaskIsAssigned {#TaskIsAssigned}
```
public static final byte TaskIsAssigned
```


يمثل حقل TaskIsAssigned (Task).

### TaskOriginalFinish {#TaskOriginalFinish}
```
public static final byte TaskOriginalFinish
```


يمثل حقل original finish (Task).

### TaskOriginalStart {#TaskOriginalStart}
```
public static final byte TaskOriginalStart
```


يمثل حقل original start (Task).

### TotalSlack {#TotalSlack}
```
public static final byte TotalSlack
```


يمثل حقل TotalSlack (Task).

### TotalSlackTimeSpan {#TotalSlackTimeSpan}
```
public static final byte TotalSlackTimeSpan
```


يمثل حقل TotalSlack (Task).

### Type {#Type}
```
public static final byte Type
```


يمثل حقل Type (Task).

### Uid {#Uid}
```
public static final byte Uid
```


يمثل حقل Unique ID (Task).

### WBS {#WBS}
```
public static final byte WBS
```


يمثل حقل WBS (Task).

### WBSLevel {#WBSLevel}
```
public static final byte WBSLevel
```


يمثل حقل WBSLevel (Task).

### Warning {#Warning}
```
public static final byte Warning
```


يمثل العلامة التي تشير إلى أن المهمة لديها اختلافات في الجدول الزمني.

### Work {#Work}
```
public static final byte Work
```


يمثل حقل Work (Task).

### WorkVariance {#WorkVariance}
```
public static final byte WorkVariance
```


يمثل حقل WorkVariance (Task).

