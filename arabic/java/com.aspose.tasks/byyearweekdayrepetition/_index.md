---
title: "ByYearWeekDayRepetition"
second_title: "مرجع API لـ Aspose.Tasks for Java"
description: "يمثل نمطًا يعتمد على موقع يوم الأسبوع في شهر."
type: docs
weight: 38
url: /ar/java/com.aspose.tasks/byyearweekdayrepetition/
---

**Inheritance:**
java.lang.Object, [com.aspose.tasks.YearlyRepetitionBase](../../com.aspose.tasks/yearlyrepetitionbase)
```
public class ByYearWeekDayRepetition extends YearlyRepetitionBase
```

يمثل نمطًا يعتمد على موقع يوم الأسبوع في شهر.
## المنشئات

| منشئ | الوصف |
| --- | --- |
| [ByYearWeekDayRepetition()](#ByYearWeekDayRepetition--) | يُهيئ نسخة جديدة من فئة [ByYearWeekDayRepetition](../../com.aspose.tasks/byyearweekdayrepetition). |
## الطرق

| طريقة | الوصف |
| --- | --- |
| [getMonth()](#getMonth--) | يحصل على الشهر الذي يجب أن تتكرر فيه المهمة. |
| [getPosition()](#getPosition--) | يحصل على موضع اليوم في أسبوع من شهر يجب أن تتكرر فيه المهمة. |
| [getWeekDay()](#getWeekDay--) | يحصل على نوع يوم الأسبوع الذي يجب أن تتكرر فيه المهمة. |
| [setMonth(int value)](#setMonth-int-) | يضبط الشهر الذي يجب أن تتكرر فيه المهمة. |
| [setPosition(int value)](#setPosition-int-) | يضبط موضع اليوم في أسبوع من شهر يجب أن تتكرر فيه المهمة. |
| [setWeekDay(int value)](#setWeekDay-int-) | يضبط نوع يوم الأسبوع الذي يجب أن تتكرر فيه المهمة. |
### ByYearWeekDayRepetition() {#ByYearWeekDayRepetition--}
```
public ByYearWeekDayRepetition()
```


يُهيئ نسخة جديدة من فئة [ByYearWeekDayRepetition](../../com.aspose.tasks/byyearweekdayrepetition).

### getMonth() {#getMonth--}
```
public final int getMonth()
```


يحصل على الشهر الذي يجب أن تتكرر فيه المهمة.

قراءة/كتابة `Month`.

**Returns:**
int - شهر يجب أن تتكرر فيه المهمة.
### getPosition() {#getPosition--}
```
public final int getPosition()
```


يحصل على موضع اليوم في أسبوع من شهر يجب أن تتكرر فيه المهمة.

قراءة/كتابة `OrdinalNumber`.

**Returns:**
int - موضع اليوم في أسبوع من شهر يجب أن تتكرر فيه المهمة.
### getWeekDay() {#getWeekDay--}
```
public final int getWeekDay()
```


يحصل على نوع يوم الأسبوع الذي يجب أن تتكرر فيه المهمة.

قراءة/كتابة `DayOfWeek`.

**Returns:**
int - نوع يوم الأسبوع الذي يجب أن تتكرر فيه المهمة.
### setMonth(int value) {#setMonth-int-}
```
public final void setMonth(int value)
```


يضبط الشهر الذي يجب أن تتكرر فيه المهمة.

قراءة/كتابة `Month`.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| القيمة | int | شهر يجب أن تتكرر فيه المهمة. |

### setPosition(int value) {#setPosition-int-}
```
public final void setPosition(int value)
```


يضبط موضع اليوم في أسبوع من شهر يجب أن تتكرر فيه المهمة.

قراءة/كتابة `OrdinalNumber`.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| القيمة | int | موضع اليوم في أسبوع من شهر يجب أن تتكرر فيه المهمة. |

### setWeekDay(int value) {#setWeekDay-int-}
```
public final void setWeekDay(int value)
```


يضبط نوع يوم الأسبوع الذي يجب أن تتكرر فيه المهمة.

قراءة/كتابة `DayOfWeek`.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| القيمة | int | نوع يوم الأسبوع الذي يجب أن تتكرر فيه المهمة. |

