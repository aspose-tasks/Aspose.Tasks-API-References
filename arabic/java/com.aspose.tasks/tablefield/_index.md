---
title: "TableField"
second_title: "مرجع API لـ Aspose.Tasks for Java"
description: "يمثل حقلًا في جدول بالمشروع."
type: docs
weight: 286
url: /ar/java/com.aspose.tasks/tablefield/
---

**Inheritance:**
java.lang.Object
```
public class TableField
```

يمثل حقلًا في جدول بالمشروع.
## المنشئات

| منشئ | الوصف |
| --- | --- |
| [TableField()](#TableField--) | يُنشئ مثيلاً جديدًا من الفئة [TableField](../../com.aspose.tasks/tablefield). |
## الطرق

| طريقة | الوصف |
| --- | --- |
| [getAlignData()](#getAlignData--) | يحصل على محاذاة البيانات في حقل جدول. |
| [getAlignTitle()](#getAlignTitle--) | يحصل على محاذاة العنوان في حقل جدول. |
| [getField()](#getField--) | يحصل على نوع حقل جدول. |
| [getTitle()](#getTitle--) | يحصل على عنوان الحقل في جدول. |
| [getWidth()](#getWidth--) | يحصل على عرض العمود في الحقل بوحدة النقاط في جدول. |
| [getWrapHeader()](#getWrapHeader--) | يحصل على قيمة تشير إلى ما إذا كان عنوان عمود الجدول يمكن أن يلتف إلى عدة أسطر، أو إذا كان يجب قطعه عندما يتجاوز عرض العمود. |
| [getWrapText()](#getWrapText--) | يحصل على قيمة تشير إلى ما إذا كان نص العمود يمكن أن يلتف إلى عدة أسطر، أو إذا كان يجب قطعه عندما يتجاوز عرض العمود. |
| [setAlignData(int value)](#setAlignData-int-) | يضبط محاذاة البيانات في حقل جدول. |
| [setAlignTitle(int value)](#setAlignTitle-int-) | يضبط محاذاة العنوان في حقل جدول. |
| [setField(int value)](#setField-int-) | يضبط نوع حقل جدول. |
| [setTitle(String value)](#setTitle-java.lang.String-) | يضبط عنوان الحقل في جدول. |
| [setWidth(int value)](#setWidth-int-) | يضبط عرض العمود في الحقل بوحدة النقاط في جدول. |
| [setWrapHeader(boolean value)](#setWrapHeader-boolean-) | يضبط قيمة تشير إلى ما إذا كان عنوان عمود الجدول يمكن أن يلتف إلى عدة أسطر، أو إذا كان يجب قطعه عندما يتجاوز عرض العمود. |
| [setWrapText(boolean value)](#setWrapText-boolean-) | يضبط قيمة تشير إلى ما إذا كان نص العمود يمكن أن يلتف إلى عدة أسطر، أو إذا كان يجب قطعه عندما يتجاوز عرض العمود. |
### TableField() {#TableField--}
```
public TableField()
```


يُنشئ مثيلاً جديدًا من الفئة [TableField](../../com.aspose.tasks/tablefield).

### getAlignData() {#getAlignData--}
```
public final int getAlignData()
```


يحصل على محاذاة البيانات في حقل جدول.

**Returns:**
int - محاذاة البيانات في حقل جدول.
### getAlignTitle() {#getAlignTitle--}
```
public final int getAlignTitle()
```


يحصل على محاذاة العنوان في حقل جدول.

**Returns:**
int - محاذاة العنوان في حقل جدول.
### getField() {#getField--}
```
public final int getField()
```


يحصل على نوع حقل جدول.

**Returns:**
int - نوع حقل جدول.
### getTitle() {#getTitle--}
```
public final String getTitle()
```


يحصل على عنوان الحقل في جدول.

**Returns:**
java.lang.String - عنوان الحقل في جدول.
### getWidth() {#getWidth--}
```
public final int getWidth()
```


يحصل على عرض العمود في الحقل بوحدة النقاط في جدول.

**Returns:**
int - عرض العمود في الحقل بوحدة النقاط في جدول.
### getWrapHeader() {#getWrapHeader--}
```
public final boolean getWrapHeader()
```


يحصل على قيمة تشير إلى ما إذا كان عنوان عمود الجدول يمكن أن يلتف إلى عدة أسطر، أو إذا كان يجب قطعه عندما يتجاوز عرض العمود.

**Returns:**
boolean - قيمة تشير إلى ما إذا كان عنوان عمود الجدول يمكن أن يلتف إلى عدة أسطر، أو إذا كان يجب قطعه عندما يتجاوز عرض العمود.
### getWrapText() {#getWrapText--}
```
public final boolean getWrapText()
```


يحصل على قيمة تشير إلى ما إذا كان نص العمود يمكن أن يلتف إلى عدة أسطر، أو إذا كان يجب قطعه عندما يتجاوز عرض العمود.

--------------------

مدعوم من نسخة MSP 2010 وما بعدها.

**Returns:**
boolean - قيمة تشير إلى ما إذا كان نص العمود يمكن أن يلتف إلى عدة أسطر، أو إذا كان يجب قطعه عندما يتجاوز عرض العمود.
### setAlignData(int value) {#setAlignData-int-}
```
public final void setAlignData(int value)
```


يضبط محاذاة البيانات في حقل جدول.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| القيمة | int | محاذاة البيانات في حقل جدول. |

### setAlignTitle(int value) {#setAlignTitle-int-}
```
public final void setAlignTitle(int value)
```


يضبط محاذاة العنوان في حقل جدول.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| القيمة | int | محاذاة العنوان في حقل جدول. |

### setField(int value) {#setField-int-}
```
public final void setField(int value)
```


يضبط نوع حقل جدول.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| القيمة | int | نوع حقل جدول. |

### setTitle(String value) {#setTitle-java.lang.String-}
```
public final void setTitle(String value)
```


يضبط عنوان الحقل في جدول.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| القيمة | java.lang.String | عنوان الحقل في جدول. |

### setWidth(int value) {#setWidth-int-}
```
public final void setWidth(int value)
```


يضبط عرض العمود في الحقل بوحدة النقاط في جدول.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| القيمة | int | عرض عمود الحقل بالنقاط في جدول. |

### setWrapHeader(boolean value) {#setWrapHeader-boolean-}
```
public final void setWrapHeader(boolean value)
```


يضبط قيمة تشير إلى ما إذا كان عنوان عمود الجدول يمكن أن يلتف إلى عدة أسطر، أو إذا كان يجب قطعه عندما يتجاوز عرض العمود.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| القيمة | منطقي | قيمة تشير إلى ما إذا كان عنوان عمود الجدول يمكن أن يلتف إلى عدة أسطر، أو إذا يجب قطعه عندما يتجاوز عرض العمود. |

### setWrapText(boolean value) {#setWrapText-boolean-}
```
public final void setWrapText(boolean value)
```


يضبط قيمة تشير إلى ما إذا كان نص العمود يمكن أن يلتف إلى عدة أسطر، أو إذا كان يجب قطعه عندما يتجاوز عرض العمود.

--------------------

مدعوم من نسخة MSP 2010 وما بعدها.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| القيمة | منطقي | قيمة تشير إلى ما إذا كان نص العمود يمكن أن يلتف إلى عدة أسطر، أو إذا يجب قطعه عندما يتجاوز عرض العمود. |

