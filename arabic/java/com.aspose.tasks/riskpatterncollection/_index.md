---
title: "RiskPatternCollection"
second_title: "مرجع API لـ Aspose.Tasks for Java"
description: "يمثل مجموعة تحتوي على مثيلات فئة RiskPattern."
type: docs
weight: 269
url: /ar/java/com.aspose.tasks/riskpatterncollection/
---

**Inheritance:**
java.lang.Object, java.util.AbstractCollection
```
public class RiskPatternCollection extends AbstractCollection<RiskPattern>
```

يمثل مجموعة تحتوي على مثيلات الفئة `RiskPattern`.
## الطرق

| طريقة | الوصف |
| --- | --- |
| [add(RiskPattern item)](#add-com.aspose.tasks.RiskPattern-) | يضيف مثيلًا من الفئة `RiskPattern` إلى هذه المجموعة. |
| [clear()](#clear--) | يزيل جميع العناصر من هذه المجموعة. |
| [contains(Object item)](#contains-java.lang.Object-) | يرجع true إذا تم العثور على العنصر المحدد في هذه المجموعة؛ وإلا، false. |
| [copyTo(RiskPattern[] array, int arrayIndex)](#copyTo-com.aspose.tasks.RiskPattern---int-) | ينسخ عناصر هذه المجموعة إلى المصفوفة المحددة، بدءًا من الفهرس المحدد للمصفوفة. |
| [get_Item(Task task)](#get-Item-com.aspose.tasks.Task-) | يحصل على المثيل من الفئة `RiskPattern` للمهمة المحددة. |
| [iterator()](#iterator--) | يرجع مُعدِّدًا لهذه المجموعة. |
| [remove(Object item)](#remove-java.lang.Object-) | يزيل أول ظهور لكائن محدد من هذه المجموعة. |
| [size()](#size--) | يعيد عدد العناصر الموجودة في هذه المجموعة. |
### add(RiskPattern item) {#add-com.aspose.tasks.RiskPattern-}
```
public boolean add(RiskPattern item)
```


يضيف مثيلًا من الفئة `RiskPattern` إلى هذه المجموعة.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| item | [RiskPattern](../../com.aspose.tasks/riskpattern) | مثيل من الفئة `RiskPattern` لإضافته إلى هذه المجموعة. |

**Returns:**
boolean - يعيد true إذا تم إضافة مثيل من الفئة `RiskPattern` بنجاح؛ وإلا false.
### clear() {#clear--}
```
public void clear()
```


يزيل جميع العناصر من هذه المجموعة.

### contains(Object item) {#contains-java.lang.Object-}
```
public boolean contains(Object item)
```


يرجع true إذا تم العثور على العنصر المحدد في هذه المجموعة؛ وإلا، false.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| عنصر | java.lang.Object | العنصر المحدد للبحث عنه. |

**Returns:**
منطقي - true إذا تم العثور على العنصر المحدد في هذه المجموعة؛ وإلا، false.
### copyTo(RiskPattern[] array, int arrayIndex) {#copyTo-com.aspose.tasks.RiskPattern---int-}
```
public void copyTo(RiskPattern[] array, int arrayIndex)
```


ينسخ عناصر هذه المجموعة إلى المصفوفة المحددة، بدءًا من الفهرس المحدد للمصفوفة.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| array | [RiskPattern\[\]](../../com.aspose.tasks/riskpattern) | المصفوفة الأحادية البُعد المحددة لنسخ العناصر إليها |
| arrayIndex | int | الفهرس الصفري للمصفوفة المحددة الذي يبدأ عنده النسخ. |

### get_Item(Task task) {#get-Item-com.aspose.tasks.Task-}
```
public RiskPattern get_Item(Task task)
```


يحصل على المثيل من الفئة `RiskPattern` للمهمة المحددة.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| task | [Task](../../com.aspose.tasks/task) | المهمة المحددة. |

**Returns:**
[RiskPattern](../../com.aspose.tasks/riskpattern) - the pattern for the specified task.
### iterator() {#iterator--}
```
public Iterator<RiskPattern> iterator()
```


يرجع مُعدِّدًا لهذه المجموعة.

**Returns:**
java.util.Iterator&lt;com.aspose.tasks.RiskPattern&gt; - مُعدِّـد لهذه المجموعة.
### remove(Object item) {#remove-java.lang.Object-}
```
public boolean remove(Object item)
```


يزيل أول ظهور لكائن محدد من هذه المجموعة.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| عنصر | java.lang.Object | الكائن المحدد للإزالة. |

**Returns:**
boolean - true إذا تم إزالة الكائن المحدد بنجاح من هذا التجميع؛ وإلا false.
### size() {#size--}
```
public int size()
```


يعيد عدد العناصر الموجودة في هذه المجموعة.

**Returns:**
int - عدد العناصر الموجودة في هذه المجموعة.
