---
title: "PageSavingArgs"
second_title: "مرجع API لـ Aspose.Tasks for Java"
description: "تمثل هذه الفئة مجموعة من البيانات المتعلقة بحفظ صفحة المستندات إلى تدفق."
type: docs
weight: 180
url: /ar/java/com.aspose.tasks/pagesavingargs/
---

**Inheritance:**
java.lang.Object
```
public final class PageSavingArgs
```

تمثل هذه الفئة مجموعة من البيانات المتعلقة بحفظ صفحة المستند إلى تدفق.
## الطرق

| طريقة | الوصف |
| --- | --- |
| [getKeepStreamOpen()](#getKeepStreamOpen--) | يحصل على قيمة تشير إلى ما إذا كان يجب على روتين العرض إبقاء `Stream`([getStream()](../../com.aspose.tasks/pagesavingargs\#getStream--)/[setStream(java.io.OutputStream)](../../com.aspose.tasks/pagesavingargs\#setStream-java.io.OutputStream-)) مفتوحًا بعد كتابة الصفحة. |
| [getPageNumber()](#getPageNumber--) | يحصل على رقم الصفحة التي سيتم كتابتها. |
| [getStream()](#getStream--) | يحصل على تدفق لكتابة صفحة. |
| [setKeepStreamOpen(boolean value)](#setKeepStreamOpen-boolean-) | يضبط قيمة تشير إلى ما إذا كان روتين العرض يجب أن يحتفظ بـ `Stream`([getStream()](../../com.aspose/tasks/pagesavingargs\#getStream--)/[setStream(java.io.OutputStream)](../../com.aspose/tasks/pagesavingargs\#setStream-java.io.OutputStream-)) مفتوحًا بعد كتابة الصفحة. |
| [setStream(OutputStream value)](#setStream-java.io.OutputStream-) | يضبط تدفقًا لكتابة صفحة. |
### getKeepStreamOpen() {#getKeepStreamOpen--}
```
public final boolean getKeepStreamOpen()
```


يحصل على قيمة تشير إلى ما إذا كان يجب على روتين العرض إبقاء `Stream`([getStream()](../../com.aspose.tasks/pagesavingargs\#getStream--)/[setStream(java.io.OutputStream)](../../com.aspose.tasks/pagesavingargs\#setStream-java.io.OutputStream-)) مفتوحًا بعد كتابة الصفحة.

**Returns:**
boolean - قيمة تشير إلى ما إذا كان روتين العرض يجب أن يحتفظ بـ `Stream`([getStream()](../../com.aspose/tasks/pagesavingargs\#getStream--)/[setStream(java.io.OutputStream)](../../com.aspose/tasks/pagesavingargs\#setStream-java.io.OutputStream-)) مفتوحًا بعد كتابة الصفحة.
### getPageNumber() {#getPageNumber--}
```
public final int getPageNumber()
```


يحصل على رقم الصفحة التي سيتم كتابتها.

**Returns:**
int - عدد الصفحات التي سيتم كتابتها.
### getStream() {#getStream--}
```
public final OutputStream getStream()
```


يحصل على تدفق لكتابة صفحة.

**Returns:**
java.io.OutputStream - تدفق لكتابة صفحة.
### setKeepStreamOpen(boolean value) {#setKeepStreamOpen-boolean-}
```
public final void setKeepStreamOpen(boolean value)
```


يضبط قيمة تشير إلى ما إذا كان روتين العرض يجب أن يحتفظ بـ `Stream`([getStream()](../../com.aspose/tasks/pagesavingargs\#getStream--)/[setStream(java.io.OutputStream)](../../com.aspose/tasks/pagesavingargs\#setStream-java.io.OutputStream-)) مفتوحًا بعد كتابة الصفحة.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| value | boolean | قيمة تشير إلى ما إذا كان روتين العرض يجب أن يحتفظ بـ `Stream`([getStream()](../../com.aspose/tasks/pagesavingargs\#getStream--)/[setStream(java.io.OutputStream)](../../com.aspose/tasks/pagesavingargs\#setStream-java.io.OutputStream-)) مفتوحًا بعد كتابة الصفحة. |

### setStream(OutputStream value) {#setStream-java.io.OutputStream-}
```
public final void setStream(OutputStream value)
```


يضبط تدفقًا لكتابة صفحة.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| القيمة | java.io.OutputStream | تدفق لكتابة صفحة. |

