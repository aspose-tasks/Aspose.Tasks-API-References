---
title: "PdfPermissions"
second_title: "مرجع API لـ Aspose.Tasks for Java"
description: "يحدد الأذونات المستخدمة للوصول إلى مستند PDF."
type: docs
weight: 190
url: /ar/java/com.aspose.tasks/pdfpermissions/
---

**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class PdfPermissions extends System.Enum
```

يحدد الأذونات المستخدمة للوصول إلى مستند PDF.
## الحقول

| حقل | الوصف |
| --- | --- |
| [AllowAll](#AllowAll) | يحدد الأذونات المستخدمة للوصول إلى مستند PDF. |
| [ContentCopy](#ContentCopy) | يحدد الأذونات المستخدمة للوصول إلى مستند PDF. |
| [ContentCopyForAccessibility](#ContentCopyForAccessibility) | يحدد الأذونات المستخدمة للوصول إلى مستند PDF. |
| [DocumentAssembly](#DocumentAssembly) | يحدد الأذونات المستخدمة للوصول إلى مستند PDF. |
| [FillIn](#FillIn) | يحدد الأذونات المستخدمة للوصول إلى مستند PDF. |
| [HighResolutionPrinting](#HighResolutionPrinting) | يحدد الأذونات المستخدمة للوصول إلى مستند PDF. |
| [ModifyAnnotations](#ModifyAnnotations) | يحدد الأذونات المستخدمة للوصول إلى مستند PDF. |
| [ModifyContents](#ModifyContents) | يحدد الأذونات المستخدمة للوصول إلى مستند PDF. |
| [None](#None) | يحدد الأذونات المستخدمة للوصول إلى مستند PDF. |
| [Printing](#Printing) | يحدد الأذونات المستخدمة للوصول إلى مستند PDF. |
### AllowAll {#AllowAll}
```
public static final int AllowAll
```


يحدد الأذونات المستخدمة للوصول إلى مستند PDF.

### ContentCopy {#ContentCopy}
```
public static final int ContentCopy
```


يحدد الأذونات المستخدمة للوصول إلى مستند PDF.

### ContentCopyForAccessibility {#ContentCopyForAccessibility}
```
public static final int ContentCopyForAccessibility
```


يحدد الأذونات المستخدمة للوصول إلى مستند PDF.

### DocumentAssembly {#DocumentAssembly}
```
public static final int DocumentAssembly
```


يحدد الأذونات المستخدمة للوصول إلى مستند PDF.

### FillIn {#FillIn}
```
public static final int FillIn
```


يحدد الأذونات المستخدمة للوصول إلى مستند PDF.

### HighResolutionPrinting {#HighResolutionPrinting}
```
public static final int HighResolutionPrinting
```


يحدد الأذونات المستخدمة للوصول إلى مستند PDF.

### ModifyAnnotations {#ModifyAnnotations}
```
public static final int ModifyAnnotations
```


يحدد الأذونات المستخدمة للوصول إلى مستند PDF.

### ModifyContents {#ModifyContents}
```
public static final int ModifyContents
```


يحدد الأذونات المستخدمة للوصول إلى مستند PDF.

### None {#None}
```
public static final int None
```


يحدد الأذونات المستخدمة للوصول إلى مستند PDF.

### Printing {#Printing}
```
public static final int Printing
```


يحدد الأذونات المستخدمة للوصول إلى مستند PDF.

