---
title: "ByMonthWeekDayRepetition"
second_title: "مرجع API لـ Aspose.Tasks for Java"
description: "يمثل نمطًا يعتمد على موقع يوم الأسبوع في شهر."
type: docs
weight: 36
url: /ar/java/com.aspose.tasks/bymonthweekdayrepetition/
---

**Inheritance:**
java.lang.Object, [com.aspose.tasks.MonthlyRepetitionBase](../../com.aspose.tasks/monthlyrepetitionbase)
```
public class ByMonthWeekDayRepetition extends MonthlyRepetitionBase
```

يمثل نمطًا يعتمد على موقع يوم الأسبوع في شهر.
## المنشئات

| منشئ | الوصف |
| --- | --- |
| [ByMonthWeekDayRepetition()](#ByMonthWeekDayRepetition--) | ينشئ مثيلًا جديدًا للفئة [ByMonthWeekDayRepetition](../../com.aspose.tasks/bymonthweekdayrepetition). |
## الطرق

| طريقة | الوصف |
| --- | --- |
| [getPosition()](#getPosition--) | يحصل على موضع يوم الأسبوع في الشهر الذي يجب أن تتكرر فيه المهمة. |
| [getWeekDay()](#getWeekDay--) | يحصل على نوع يوم الأسبوع الذي يجب أن تتكرر فيه المهمة. |
| [setPosition(int value)](#setPosition-int-) | يضبط موضع يوم الأسبوع في الشهر الذي يجب أن تتكرر فيه المهمة. |
| [setWeekDay(int value)](#setWeekDay-int-) | يضبط نوع يوم الأسبوع الذي يجب أن تتكرر فيه المهمة. |
### ByMonthWeekDayRepetition() {#ByMonthWeekDayRepetition--}
```
public ByMonthWeekDayRepetition()
```


ينشئ مثيلًا جديدًا للفئة [ByMonthWeekDayRepetition](../../com.aspose.tasks/bymonthweekdayrepetition).

### getPosition() {#getPosition--}
```
public final int getPosition()
```


يحصل على موضع يوم الأسبوع في الشهر الذي يجب أن تتكرر فيه المهمة.

قراءة/كتابة `OrdinalNumber`.

**Returns:**
int - موضع يوم الأسبوع في الشهر الذي يجب أن تتكرر فيه المهمة.
### getWeekDay() {#getWeekDay--}
```
public final int getWeekDay()
```


يحصل على نوع يوم الأسبوع الذي يجب أن تتكرر فيه المهمة.

قراءة/كتابة `DayOfWeek`.

**Returns:**
int - نوع يوم الأسبوع الذي يجب أن تتكرر فيه المهمة.
### setPosition(int value) {#setPosition-int-}
```
public final void setPosition(int value)
```


يضبط موضع يوم الأسبوع في الشهر الذي يجب أن تتكرر فيه المهمة.

قراءة/كتابة `OrdinalNumber`.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| القيمة | int | موضع يوم الأسبوع في الشهر الذي يجب أن تتكرر فيه المهمة. |

### setWeekDay(int value) {#setWeekDay-int-}
```
public final void setWeekDay(int value)
```


يضبط نوع يوم الأسبوع الذي يجب أن تتكرر فيه المهمة.

قراءة/كتابة `DayOfWeek`.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| القيمة | int | نوع يوم الأسبوع الذي يجب أن تتكرر فيه المهمة. |

