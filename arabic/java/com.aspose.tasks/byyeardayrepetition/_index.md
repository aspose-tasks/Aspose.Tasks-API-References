---
title: "ByYearDayRepetition"
second_title: "مرجع API لـ Aspose.Tasks for Java"
description: "يمثل نمطًا يعتمد على الموقع المطلق ليوم في شهر."
type: docs
weight: 37
url: /ar/java/com.aspose.tasks/byyeardayrepetition/
---

**Inheritance:**
java.lang.Object, [com.aspose.tasks.YearlyRepetitionBase](../../com.aspose.tasks/yearlyrepetitionbase)
```
public class ByYearDayRepetition extends YearlyRepetitionBase
```

يمثل نمطًا يعتمد على الموقع المطلق ليوم في شهر.
## المنشئات

| منشئ | الوصف |
| --- | --- |
| [ByYearDayRepetition()](#ByYearDayRepetition--) | يُنشئ مثيلاً جديداً من الفئة [ByYearDayRepetition](../../com.aspose.tasks/byyeardayrepetition). |
## الطرق

| طريقة | الوصف |
| --- | --- |
| [getDayPosition()](#getDayPosition--) | يحصل على موضع اليوم في الشهر الذي يجب أن تتكرر فيه المهمة. |
| [getMonth()](#getMonth--) | يحصل على الشهر الذي يجب أن تتكرر فيه المهمة. |
| [setDayPosition(int value)](#setDayPosition-int-) | يضبط موضع اليوم في الشهر الذي يجب أن تتكرر فيه المهمة. |
| [setMonth(int value)](#setMonth-int-) | يضبط الشهر الذي يجب أن تتكرر فيه المهمة. |
### ByYearDayRepetition() {#ByYearDayRepetition--}
```
public ByYearDayRepetition()
```


يُنشئ مثيلاً جديداً من الفئة [ByYearDayRepetition](../../com.aspose.tasks/byyeardayrepetition).

### getDayPosition() {#getDayPosition--}
```
public final int getDayPosition()
```


يحصل على موضع اليوم في الشهر الذي يجب أن تتكرر فيه المهمة.

**Returns:**
int - موضع اليوم في الشهر الذي يجب أن تتكرر فيه المهمة.
### getMonth() {#getMonth--}
```
public final int getMonth()
```


يحصل على الشهر الذي يجب أن تتكرر فيه المهمة.

قراءة/كتابة `Month`.

**Returns:**
int - شهر يجب أن تتكرر فيه المهمة.
### setDayPosition(int value) {#setDayPosition-int-}
```
public final void setDayPosition(int value)
```


يضبط موضع اليوم في الشهر الذي يجب أن تتكرر فيه المهمة.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| القيمة | int | موضع اليوم في الشهر الذي يجب أن تتكرر فيه المهمة. |

### setMonth(int value) {#setMonth-int-}
```
public final void setMonth(int value)
```


يضبط الشهر الذي يجب أن تتكرر فيه المهمة.

قراءة/كتابة `Month`.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| القيمة | int | شهر يجب أن تتكرر فيه المهمة. |

