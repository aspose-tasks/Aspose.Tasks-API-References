---
title: "Aspose::Tasks::Visualization::TextStyle::TextStyle منشئ"
linktitle: "TextStyle"
articleTitle: "TextStyle"
second_title: "Aspose.Tasks لـ C++"
description: "ينشئ مثيلاً جديدًا لفئة TextStyle بالإعدادات الافتراضية."
type: docs
weight: 10
url: /ar/cpp/aspose.tasks.visualization/textstyle/textstyle/
---

## TextStyle (1 of 4) {#textstyle_1}

ينشئ مثيلاً جديدًا لفئة TextStyle بالإعدادات الافتراضية.

**Returns:** Aspose::Tasks::Visualization::

```cpp
TextStyle()
```

---

## TextStyle (2 of 4) {#textstyle_2}

يقوم بتهيئة نسخة جديدة من الفئة TextStyle باستخدام الخط الافتراضي وحجم الخط والنمط المحددين.

**Returns:** Aspose::Tasks::Visualization::

```cpp
TextStyle(float fontSize, FontStyles fontStyle)
```

| معامل | الوصف |
| --- | --- |
| fontSize | حجم الخط في TextStyle. |
| fontStyle | نمط الخط في TextStyle. |

---

## TextStyle (3 of 4) {#textstyle_3}

يقوم بتهيئة نسخة جديدة من الفئة TextStyle باستخدام الخط الافتراضي والنمط المحدد للخط.

**Returns:** Aspose::Tasks::Visualization::

```cpp
TextStyle(FontStyles fontStyle)
```

| معامل | الوصف |
| --- | --- |
| fontStyle | نمط الخط لتطبيقه على الخط الافتراضي. |

---

## TextStyle (4 of 4) {#textstyle_4}

يقوم بتهيئة نسخة جديدة من الفئة TextStyle باستخدام إعدادات الخط المحددة.

**Returns:** Aspose::Tasks::Visualization::

```cpp
TextStyle(const System::SharedPtr< FontDescriptor > & font)
```

| معامل | الوصف |
| --- | --- |
| font | خط TextStyle. |

