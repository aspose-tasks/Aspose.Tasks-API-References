---
title: "Aspose::Tasks::Visualization::TableTextStyle::TableTextStyle منشئ"
linktitle: "TableTextStyle"
articleTitle: "TableTextStyle"
second_title: "Aspose.Tasks لـ C++"
description: "يُنشئ مثيلاً جديدًا لفئة TableTextStyle."
type: docs
weight: 10
url: /ar/cpp/aspose.tasks.visualization/tabletextstyle/tabletextstyle/
---

## TableTextStyle (1 of 4) {#tabletextstyle_1}

يُنشئ مثيلاً جديدًا لفئة TableTextStyle.

**Returns:** Aspose::Tasks::Visualization::

```cpp
TableTextStyle(int32_t rowUid)
```

| معامل | الوصف |
| --- | --- |
| rowUid | معرف صف فريد محدد. |

---

## TableTextStyle (2 of 4) {#tabletextstyle_2}

يُنشئ نسخة جديدة من الفئة TableTextStyle باستخدام الخط المحدد.

**Returns:** Aspose::Tasks::Visualization::

```cpp
TableTextStyle(int32_t rowUid, const System::SharedPtr< FontDescriptor > & font)
```

| معامل | الوصف |
| --- | --- |
| rowUid | معرف صف فريد محدد. |
| font | خط يُستند إليه النمط النصي. |

---

## TableTextStyle (3 of 4) {#tabletextstyle_3}

يُنشئ نسخة جديدة من الفئة TableTextStyle باستخدام حجم الخط المحدد ونمط الخط.

**Returns:** Aspose::Tasks::Visualization::

```cpp
TableTextStyle(int32_t rowUid, float fontSize, FontStyles fontStyle)
```

| معامل | الوصف |
| --- | --- |
| rowUid | معرف صف فريد محدد. |
| fontSize | حجم الخط الذي يُستند إليه النمط النصي. |

---

## TableTextStyle (4 of 4) {#tabletextstyle_4}

يُنشئ نسخة جديدة من الفئة TableTextStyle باستخدام إعدادات الخط الافتراضية والنمط المحدد للخط.

**Returns:** Aspose::Tasks::Visualization::

```cpp
TableTextStyle(int32_t rowUid, FontStyles fontStyle)
```

| معامل | الوصف |
| --- | --- |
| rowUid | معرف صف فريد محدد. |

