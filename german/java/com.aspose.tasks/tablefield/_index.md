---
title: "TableField"
second_title: "Aspose.Tasks for Java API Reference"
description: "Stellt ein Feld einer Tabelle in einem Projekt dar."
type: docs
weight: 286
url: /de/java/com.aspose.tasks/tablefield/
---

**Inheritance:**
java.lang.Object
```
public class TableField
```

Stellt ein Feld einer Tabelle in einem Projekt dar.
## Konstruktoren

| Konstruktor | Beschreibung |
| --- | --- |
| [TableField()](#TableField--) | Initialisiert eine neue Instanz der [TableField](../../com.aspose.tasks/tablefield)-Klasse. |
## Methoden

| Methode | Beschreibung |
| --- | --- |
| [getAlignData()](#getAlignData--) | Liefert die Ausrichtung der Daten in einem Tabellenfeld. |
| [getAlignTitle()](#getAlignTitle--) | Liefert die Ausrichtung des Titels in einem Tabellenfeld. |
| [getField()](#getField--) | Liefert den Typ eines Tabellenfelds. |
| [getTitle()](#getTitle--) | Liefert den Titel des Feldes in einer Tabelle. |
| [getWidth()](#getWidth--) | Liefert die Breite in Punkten der Feldspalte in einer Tabelle. |
| [getWrapHeader()](#getWrapHeader--) | Liefert einen Wert, der angibt, ob die Tabellenüberschrift in mehrere Zeilen umbrochen werden kann oder ob sie abgeschnitten werden soll, wenn sie die Spaltenbreite überschreitet. |
| [getWrapText()](#getWrapText--) | Liefert einen Wert, der angibt, ob der Spaltentext in mehrere Zeilen umbrochen werden kann oder ob er abgeschnitten werden soll, wenn er die Spaltenbreite überschreitet. |
| [setAlignData(int value)](#setAlignData-int-) | Setzt die Ausrichtung der Daten in einem Tabellenfeld. |
| [setAlignTitle(int value)](#setAlignTitle-int-) | Setzt die Ausrichtung des Titels in einem Tabellenfeld. |
| [setField(int value)](#setField-int-) | Setzt den Typ eines Tabellenfelds. |
| [setTitle(String value)](#setTitle-java.lang.String-) | Legt den Titel des Feldes in einer Tabelle fest. |
| [setWidth(int value)](#setWidth-int-) | Legt die Breite in Punkten der Feldspalte in einer Tabelle fest. |
| [setWrapHeader(boolean value)](#setWrapHeader-boolean-) | Legt einen Wert fest, der angibt, ob die Tabellenspaltenüberschrift in mehrere Zeilen umbrechen kann oder ob sie abgeschnitten werden soll, wenn sie die Spaltenbreite überschreitet. |
| [setWrapText(boolean value)](#setWrapText-boolean-) | Legt einen Wert fest, der angibt, ob der Spaltentext in mehrere Zeilen umbrechen kann oder ob er abgeschnitten werden soll, wenn er die Spaltenbreite überschreitet. |
### TableField() {#TableField--}
```
public TableField()
```


Initialisiert eine neue Instanz der [TableField](../../com.aspose.tasks/tablefield)-Klasse.

### getAlignData() {#getAlignData--}
```
public final int getAlignData()
```


Liefert die Ausrichtung der Daten in einem Tabellenfeld.

**Returns:**
int - die Ausrichtung der Daten in einem Tabellenfeld.
### getAlignTitle() {#getAlignTitle--}
```
public final int getAlignTitle()
```


Liefert die Ausrichtung des Titels in einem Tabellenfeld.

**Returns:**
int - die Ausrichtung des Titels in einem Tabellenfeld.
### getField() {#getField--}
```
public final int getField()
```


Liefert den Typ eines Tabellenfelds.

**Returns:**
int - der Typ eines Tabellenfeldes.
### getTitle() {#getTitle--}
```
public final String getTitle()
```


Liefert den Titel des Feldes in einer Tabelle.

**Returns:**
java.lang.String - der Titel des Feldes in einer Tabelle.
### getWidth() {#getWidth--}
```
public final int getWidth()
```


Liefert die Breite in Punkten der Feldspalte in einer Tabelle.

**Returns:**
int - die Breite in Punkten der Feldspalte in einer Tabelle.
### getWrapHeader() {#getWrapHeader--}
```
public final boolean getWrapHeader()
```


Liefert einen Wert, der angibt, ob die Tabellenüberschrift in mehrere Zeilen umbrochen werden kann oder ob sie abgeschnitten werden soll, wenn sie die Spaltenbreite überschreitet.

**Returns:**
boolean - ein Wert, der angibt, ob die Tabellenspaltenüberschrift in mehrere Zeilen umbrechen kann oder ob sie abgeschnitten werden soll, wenn sie die Spaltenbreite überschreitet.
### getWrapText() {#getWrapText--}
```
public final boolean getWrapText()
```


Liefert einen Wert, der angibt, ob der Spaltentext in mehrere Zeilen umbrochen werden kann oder ob er abgeschnitten werden soll, wenn er die Spaltenbreite überschreitet.

--------------------

Unterstützt ab der Version MSP 2010 und später.

**Returns:**
boolean - ein Wert, der angibt, ob der Spaltentext in mehrere Zeilen umbrechen kann oder ob er abgeschnitten werden soll, wenn er die Spaltenbreite überschreitet.
### setAlignData(int value) {#setAlignData-int-}
```
public final void setAlignData(int value)
```


Setzt die Ausrichtung der Daten in einem Tabellenfeld.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert | int | die Ausrichtung der Daten in einem Tabellenfeld. |

### setAlignTitle(int value) {#setAlignTitle-int-}
```
public final void setAlignTitle(int value)
```


Setzt die Ausrichtung des Titels in einem Tabellenfeld.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert | int | die Ausrichtung des Titels in einem Tabellenfeld. |

### setField(int value) {#setField-int-}
```
public final void setField(int value)
```


Setzt den Typ eines Tabellenfelds.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert | int | der Typ eines Tabellenfeldes. |

### setTitle(String value) {#setTitle-java.lang.String-}
```
public final void setTitle(String value)
```


Legt den Titel des Feldes in einer Tabelle fest.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert | java.lang.String | der Titel des Feldes in einer Tabelle. |

### setWidth(int value) {#setWidth-int-}
```
public final void setWidth(int value)
```


Legt die Breite in Punkten der Feldspalte in einer Tabelle fest.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert | int | die Breite in Punkten der Feldspalte in einer Tabelle. |

### setWrapHeader(boolean value) {#setWrapHeader-boolean-}
```
public final void setWrapHeader(boolean value)
```


Legt einen Wert fest, der angibt, ob die Tabellenspaltenüberschrift in mehrere Zeilen umbrechen kann oder ob sie abgeschnitten werden soll, wenn sie die Spaltenbreite überschreitet.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert | boolean | ein Wert, der angibt, ob die Tabellenspaltenüberschrift in mehrere Zeilen umbrechen kann oder ob sie abgeschnitten werden soll, wenn sie die Spaltenbreite überschreitet. |

### setWrapText(boolean value) {#setWrapText-boolean-}
```
public final void setWrapText(boolean value)
```


Legt einen Wert fest, der angibt, ob der Spaltentext in mehrere Zeilen umbrechen kann oder ob er abgeschnitten werden soll, wenn er die Spaltenbreite überschreitet.

--------------------

Unterstützt ab der Version MSP 2010 und später.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert | boolean | ein Wert, der angibt, ob der Spaltentext in mehrere Zeilen umbrechen kann oder ob er abgeschnitten werden soll, wenn er die Spaltenbreite überschreitet. |

