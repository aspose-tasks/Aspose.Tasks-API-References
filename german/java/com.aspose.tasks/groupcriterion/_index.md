---
title: "GroupCriterion"
second_title: "Aspose.Tasks for Java API Reference"
description: "Stellt ein Kriterium in einer Gruppendefinition dar."
type: docs
weight: 124
url: /de/java/com.aspose.tasks/groupcriterion/
---

**Inheritance:**
java.lang.Object
```
public class GroupCriterion
```

Stellt ein Kriterium in einer Gruppendefinition dar. Das GroupCriterion-Objekt ist ein Mitglied der [GroupCriterionCollection](../../com.aspose.tasks/groupcriterioncollection) Sammlung.
## Konstruktoren

| Konstruktor | Beschreibung |
| --- | --- |
| [GroupCriterion()](#GroupCriterion--) |  |
## Methoden

| Methode | Beschreibung |
| --- | --- |
| [equals(Object obj)](#equals-java.lang.Object-) | Gibt einen Wert zurück, der angibt, ob diese Instanz einem angegebenen Objekt gleich ist. |
| [getAscending()](#getAscending--) | Liefert einen Wert, der angibt, ob ein als Kriterium in einer Gruppendefinition verwendetes Feld aufsteigend sortiert ist. |
| [getCellColor()](#getCellColor--) | Liefert die Farbe des Zellhintergrunds für ein als Kriterium in einer Gruppendefinition verwendetes Feld. |
| [getField()](#getField--) | Liefert das Feld, nach dem gruppiert wird. |
| [getFont()](#getFont--) | Liefert die Schriftart für ein Kriterium in einer Gruppendefinition. |
| [getFontColor()](#getFontColor--) | Liefert die Farbe der Schrift für ein als Kriterium in einer Gruppendefinition verwendetes Feld. |
| [getGroupInterval()](#getGroupInterval--) | Liefert das Intervall für ein als Kriterium in einer Gruppendefinition verwendetes Feld. |
| [getGroupOn()](#getGroupOn--) | Liefert den Gruppierungstyp für ein als Kriterium in einer Gruppendefinition verwendetes Feld. |
| [getPattern()](#getPattern--) | Liefert das Muster der Zelle für ein als Kriterium in einer Gruppendefinition verwendetes Feld. |
| [getStartAt()](#getStartAt--) | Liefert den Start der Intervalle für ein als Kriterium in einer Gruppendefinition verwendetes Feld. |
| [hashCode()](#hashCode--) | Dient als Hash-Funktion für einen bestimmten Typ. |
| [setAscending(boolean value)](#setAscending-boolean-) | Setzt einen Wert, der angibt, ob ein als Kriterium in einer Gruppendefinition verwendetes Feld aufsteigend sortiert ist. |
| [setCellColor(Color value)](#setCellColor-java.awt.Color-) | Setzt die Farbe des Zellhintergrunds für ein als Kriterium in einer Gruppendefinition verwendetes Feld. |
| [setField(int value)](#setField-int-) | Setzt das Feld, nach dem gruppiert wird. |
| [setFont(FontDescriptor value)](#setFont-com.aspose.tasks.FontDescriptor-) | Setzt die Schriftart für ein Kriterium in einer Gruppendefinition. |
| [setFontColor(Color value)](#setFontColor-java.awt.Color-) | Setzt die Farbe der Schrift für ein als Kriterium in einer Gruppendefinition verwendetes Feld. |
| [setGroupInterval(Object value)](#setGroupInterval-java.lang.Object-) | Setzt das Intervall für ein als Kriterium in einer Gruppendefinition verwendetes Feld. |
| [setGroupOn(int value)](#setGroupOn-int-) | Setzt den Gruppierungstyp für ein als Kriterium in einer Gruppendefinition verwendetes Feld. |
| [setPattern(int value)](#setPattern-int-) | Setzt das Muster der Zelle für ein als Kriterium in einer Gruppendefinition verwendetes Feld. |
| [setStartAt(Object value)](#setStartAt-java.lang.Object-) | Setzt den Start der Intervalle für ein als Kriterium in einer Gruppendefinition verwendetes Feld. |
### GroupCriterion() {#GroupCriterion--}
```
public GroupCriterion()
```


### equals(Object obj) {#equals-java.lang.Object-}
```
public boolean equals(Object obj)
```


Gibt einen Wert zurück, der angibt, ob diese Instanz einem angegebenen Objekt gleich ist.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| obj | java.lang.Object | Das Objekt, das mit dieser Instanz verglichen wird. |

**Returns:**
boolescher Wert - **True** wenn o ein GroupCriterion ist, das denselben UID-Wert wie diese Instanz hat; andernfalls **false**.
### getAscending() {#getAscending--}
```
public final boolean getAscending()
```


Gibt einen Wert zurück, der angibt, ob ein Feld, das als Kriterium in einer Gruppendefinition verwendet wird, aufsteigend sortiert ist. Falsch, wenn das Feld absteigend sortiert ist.

**Returns:**
boolean – ein Wert, der angibt, ob ein Feld, das als Kriterium in einer Gruppendefinition verwendet wird, aufsteigend sortiert ist.
### getCellColor() {#getCellColor--}
```
public final Color getCellColor()
```


Liefert die Farbe des Zellhintergrunds für ein als Kriterium in einer Gruppendefinition verwendetes Feld.

**Returns:**
java.awt.Color – die Farbe des Zellenhintergrunds für ein Feld, das als Kriterium in einer Gruppendefinition verwendet wird.
### getField() {#getField--}
```
public final int getField()
```


Liefert das Feld, nach dem gruppiert wird.

**Returns:**
int – das Feld, nach dem gruppiert wird.
### getFont() {#getFont--}
```
public final FontDescriptor getFont()
```


Liefert die Schriftart für ein Kriterium in einer Gruppendefinition.

**Returns:**
[FontDescriptor](../../com.aspose.tasks/fontdescriptor) - the font for a criterion in a group definition.
### getFontColor() {#getFontColor--}
```
public final Color getFontColor()
```


Liefert die Farbe der Schrift für ein als Kriterium in einer Gruppendefinition verwendetes Feld.

**Returns:**
java.awt.Color – die Farbe der Schrift für ein Feld, das als Kriterium in einer Gruppendefinition verwendet wird.
### getGroupInterval() {#getGroupInterval--}
```
public final Object getGroupInterval()
```


Liefert das Intervall für ein als Kriterium in einer Gruppendefinition verwendetes Feld.

**Returns:**
java.lang.Object – das Intervall für ein Feld, das als Kriterium in einer Gruppendefinition verwendet wird.
### getGroupOn() {#getGroupOn--}
```
public final int getGroupOn()
```


Liefert den Gruppierungstyp für ein als Kriterium in einer Gruppendefinition verwendetes Feld.

**Returns:**
int – der Gruppierungstyp für ein Feld, das als Kriterium in einer Gruppendefinition verwendet wird.
### getPattern() {#getPattern--}
```
public final int getPattern()
```


Liefert das Muster der Zelle für ein als Kriterium in einer Gruppendefinition verwendetes Feld.

**Returns:**
int – das Muster der Zelle für ein Feld, das als Kriterium in einer Gruppendefinition verwendet wird.
### getStartAt() {#getStartAt--}
```
public final Object getStartAt()
```


Liefert den Start der Intervalle für ein als Kriterium in einer Gruppendefinition verwendetes Feld.

**Returns:**
java.lang.Object – der Beginn der Intervalle für ein Feld, das als Kriterium in einer Gruppendefinition verwendet wird.
### hashCode() {#hashCode--}
```
public int hashCode()
```


Dient als Hash-Funktion für einen bestimmten Typ.

**Returns:**
int – ein Hashcode für das aktuelle Objekt.
### setAscending(boolean value) {#setAscending-boolean-}
```
public final void setAscending(boolean value)
```


Legt einen Wert fest, der angibt, ob ein Feld, das als Kriterium in einer Gruppendefinition verwendet wird, aufsteigend sortiert ist. Falsch, wenn das Feld absteigend sortiert ist.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert | boolean | ein Wert, der angibt, ob ein Feld, das als Kriterium in einer Gruppendefinition verwendet wird, aufsteigend sortiert ist. |

### setCellColor(Color value) {#setCellColor-java.awt.Color-}
```
public final void setCellColor(Color value)
```


Setzt die Farbe des Zellhintergrunds für ein als Kriterium in einer Gruppendefinition verwendetes Feld.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert | java.awt.Color | die Farbe des Zellenhintergrunds für ein Feld, das als Kriterium in einer Gruppendefinition verwendet wird. |

### setField(int value) {#setField-int-}
```
public final void setField(int value)
```


Setzt das Feld, nach dem gruppiert wird.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert | int | das Feld, nach dem gruppiert wird. |

### setFont(FontDescriptor value) {#setFont-com.aspose.tasks.FontDescriptor-}
```
public final void setFont(FontDescriptor value)
```


Setzt die Schriftart für ein Kriterium in einer Gruppendefinition.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | [FontDescriptor](../../com.aspose.tasks/fontdescriptor) | die Schrift für ein Kriterium in einer Gruppendefinition. |

### setFontColor(Color value) {#setFontColor-java.awt.Color-}
```
public final void setFontColor(Color value)
```


Setzt die Farbe der Schrift für ein als Kriterium in einer Gruppendefinition verwendetes Feld.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert | java.awt.Color | die Farbe der Schrift für ein Feld, das als Kriterium in einer Gruppendefinition verwendet wird. |

### setGroupInterval(Object value) {#setGroupInterval-java.lang.Object-}
```
public final void setGroupInterval(Object value)
```


Setzt das Intervall für ein als Kriterium in einer Gruppendefinition verwendetes Feld.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert | java.lang.Object | das Intervall für ein Feld, das als Kriterium in einer Gruppendefinition verwendet wird. |

### setGroupOn(int value) {#setGroupOn-int-}
```
public final void setGroupOn(int value)
```


Setzt den Gruppierungstyp für ein als Kriterium in einer Gruppendefinition verwendetes Feld.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert | int | der Gruppierungstyp für ein Feld, das als Kriterium in einer Gruppendefinition verwendet wird. |

### setPattern(int value) {#setPattern-int-}
```
public final void setPattern(int value)
```


Setzt das Muster der Zelle für ein als Kriterium in einer Gruppendefinition verwendetes Feld.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert | int | das Muster der Zelle für ein Feld, das als Kriterium in einer Gruppendefinition verwendet wird. |

### setStartAt(Object value) {#setStartAt-java.lang.Object-}
```
public final void setStartAt(Object value)
```


Setzt den Start der Intervalle für ein als Kriterium in einer Gruppendefinition verwendetes Feld.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert | java.lang.Object | der Beginn der Intervalle für ein Feld, das als Kriterium in einer Gruppendefinition verwendet wird. |

