---
title: "TimelineView"
second_title: "Aspose.Tasks for Java API Reference"
description: "Stellt eine Zeitachsenansicht eines Projekts dar."
type: docs
weight: 319
url: /de/java/com.aspose.tasks/timelineview/
---

**Inheritance:**
java.lang.Object, [com.aspose.tasks.View](../../com.aspose.tasks/view)
```
public class TimelineView extends View
```

Stellt eine Zeitachsenansicht eines Projekts dar.
## Konstruktoren

| Konstruktor | Beschreibung |
| --- | --- |
| [TimelineView()](#TimelineView--) | Initialisiert eine neue Instanz der Klasse [TimelineView](../../com.aspose.tasks/timelineview). |
## Methoden

| Methode | Beschreibung |
| --- | --- |
| [getDateFormat()](#getDateFormat--) | Ermittelt einen Wert, der angibt, wie Daten in der Timeline-Ansicht formatiert werden. |
| [getDisplayOverlapped()](#getDisplayOverlapped--) | Ermittelt einen Wert, der angibt, ob überlappende Aufgaben in mehreren Zeilen angezeigt werden. |
| [getShowDates()](#getShowDates--) | Ermittelt einen Wert, der angibt, ob Daten angezeigt werden. |
| [getShowPanZoom()](#getShowPanZoom--) | Ermittelt einen Wert, der angibt, ob die Schwenk‑ und Zoom‑Steuerung angezeigt wird. |
| [getShowTimescale()](#getShowTimescale--) | Ermittelt einen Wert, der angibt, ob die Zeitskala angezeigt wird. |
| [getShowToday()](#getShowToday--) | Ermittelt einen Wert, der angibt, ob eine Linie, die den heutigen Tag darstellt, angezeigt wird. |
| [getTextLinesCount()](#getTextLinesCount--) | Ermittelt einen Wert, der angibt, wie viele Zeilen zur Anzeige von Aufgaben in einer Timeline verwendet werden. |
| [setDateFormat(int value)](#setDateFormat-int-) | Legt einen Wert fest, der angibt, wie Daten in der Timeline-Ansicht formatiert werden. |
| [setDisplayOverlapped(boolean value)](#setDisplayOverlapped-boolean-) | Legt einen Wert fest, der angibt, ob überlappende Aufgaben in mehreren Zeilen angezeigt werden. |
| [setShowPanZoom(boolean value)](#setShowPanZoom-boolean-) | Legt einen Wert fest, der angibt, ob die Schwenk‑ und Zoom‑Steuerung angezeigt wird. |
| [setShowTimescale(boolean value)](#setShowTimescale-boolean-) | Legt einen Wert fest, der angibt, ob die Zeitskala angezeigt wird. |
| [setShowToday(boolean value)](#setShowToday-boolean-) | Legt einen Wert fest, der angibt, ob eine Linie, die den heutigen Tag darstellt, angezeigt werden soll. |
| [setTextLinesCount(int value)](#setTextLinesCount-int-) | Legt einen Wert fest, der angibt, wie viele Zeilen zur Anzeige von Aufgaben in einer Zeitleiste verwendet werden. |
### TimelineView() {#TimelineView--}
```
public TimelineView()
```


Initialisiert eine neue Instanz der Klasse [TimelineView](../../com.aspose.tasks/timelineview).

### getDateFormat() {#getDateFormat--}
```
public final int getDateFormat()
```


Ermittelt einen Wert, der angibt, wie Daten in der Timeline-Ansicht formatiert werden.

**Returns:**
int - ein Wert, der angibt, wie Daten in der Zeitleistenansicht formatiert werden.
### getDisplayOverlapped() {#getDisplayOverlapped--}
```
public final boolean getDisplayOverlapped()
```


Ermittelt einen Wert, der angibt, ob überlappende Aufgaben in mehreren Zeilen angezeigt werden.

**Returns:**
boolean - ein Wert, der angibt, ob überlappende Aufgaben in mehreren Zeilen angezeigt werden.
### getShowDates() {#getShowDates--}
```
public final boolean getShowDates()
```


Ermittelt einen Wert, der angibt, ob Daten angezeigt werden.

**Returns:**
boolean - ein Wert, der angibt, ob Datumsangaben angezeigt werden.
### getShowPanZoom() {#getShowPanZoom--}
```
public final boolean getShowPanZoom()
```


Ermittelt einen Wert, der angibt, ob die Schwenk‑ und Zoom‑Steuerung angezeigt wird.

**Returns:**
boolean - ein Wert, der angibt, ob die Schwenk- und Zoom-Steuerung angezeigt wird.
### getShowTimescale() {#getShowTimescale--}
```
public final boolean getShowTimescale()
```


Ermittelt einen Wert, der angibt, ob die Zeitskala angezeigt wird.

**Returns:**
boolean - ein Wert, der angibt, ob die Zeitskala angezeigt wird.
### getShowToday() {#getShowToday--}
```
public final boolean getShowToday()
```


Ermittelt einen Wert, der angibt, ob eine Linie, die den heutigen Tag darstellt, angezeigt wird.

**Returns:**
boolean - ein Wert, der angibt, ob eine Linie, die den heutigen Tag darstellt, angezeigt wird.
### getTextLinesCount() {#getTextLinesCount--}
```
public final int getTextLinesCount()
```


Ermittelt einen Wert, der angibt, wie viele Zeilen zur Anzeige von Aufgaben in einer Timeline verwendet werden.

**Returns:**
int - ein Wert, der angibt, wie viele Zeilen zur Anzeige von Aufgaben in einer Zeitleiste verwendet werden.
### setDateFormat(int value) {#setDateFormat-int-}
```
public final void setDateFormat(int value)
```


Legt einen Wert fest, der angibt, wie Daten in der Timeline-Ansicht formatiert werden.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert | int | ein Wert, der angibt, wie Daten in der Zeitleistenansicht formatiert werden. |

### setDisplayOverlapped(boolean value) {#setDisplayOverlapped-boolean-}
```
public final void setDisplayOverlapped(boolean value)
```


Legt einen Wert fest, der angibt, ob überlappende Aufgaben in mehreren Zeilen angezeigt werden.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert | boolean | ein Wert, der angibt, ob überlappende Aufgaben in mehreren Zeilen angezeigt werden. |

### setShowPanZoom(boolean value) {#setShowPanZoom-boolean-}
```
public final void setShowPanZoom(boolean value)
```


Legt einen Wert fest, der angibt, ob die Schwenk‑ und Zoom‑Steuerung angezeigt wird.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert | boolean | ein Wert, der angibt, ob die Schwenk- und Zoom-Steuerung angezeigt wird. |

### setShowTimescale(boolean value) {#setShowTimescale-boolean-}
```
public final void setShowTimescale(boolean value)
```


Legt einen Wert fest, der angibt, ob die Zeitskala angezeigt wird.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert | boolean | ein Wert, der angibt, ob die Zeitskala angezeigt wird. |

### setShowToday(boolean value) {#setShowToday-boolean-}
```
public final void setShowToday(boolean value)
```


Legt einen Wert fest, der angibt, ob eine Linie, die den heutigen Tag darstellt, angezeigt werden soll.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert | boolean | ein Wert, der angibt, ob eine Linie, die den heutigen Tag darstellt, angezeigt wird. |

### setTextLinesCount(int value) {#setTextLinesCount-int-}
```
public final void setTextLinesCount(int value)
```


Legt einen Wert fest, der angibt, wie viele Zeilen zur Anzeige von Aufgaben in einer Zeitleiste verwendet werden.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert | int | ein Wert, der angibt, wie viele Zeilen zur Anzeige von Aufgaben in einer Zeitleiste verwendet werden. |

