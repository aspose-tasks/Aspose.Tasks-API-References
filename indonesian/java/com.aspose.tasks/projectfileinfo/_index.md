---
title: "ProjectFileInfo"
second_title: "Referensi API Aspose.Tasks for Java"
description: "Instansi kelas berisi informasi tentang format file proyek dan versi Microsoft Project tempat file dibuat."
type: docs
weight: 222
url: /id/java/com.aspose.tasks/projectfileinfo/
---

**Inheritance:**
java.lang.Object

**All Implemented Interfaces:**
com.aspose.ms.System.IEquatable
```
public final class ProjectFileInfo implements System.IEquatable<ProjectFileInfo>
```

Instansi kelas berisi informasi tentang format file proyek dan versi Microsoft Project tempat file dibuat.

--------------------

Gunakan properti CanRead untuk menentukan bahwa perpustakaan dapat memproses file proyek.
## Metode

| Metode | Deskripsi |
| --- | --- |
| [canRead()](#canRead--) | Mendapatkan nilai yang menunjukkan apakah Aspose.Tasks dapat memproses file proyek. |
| [equals(ProjectFileInfo other)](#equals-com.aspose.tasks.ProjectFileInfo-) | Mengembalikan nilai yang menunjukkan apakah instance ini sama dengan objek yang ditentukan. |
| [equals(Object obj)](#equals-java.lang.Object-) | Mengembalikan nilai yang menunjukkan apakah instance ini sama dengan objek yang ditentukan. |
| [getProjectApplicationInfo()](#getProjectApplicationInfo--) | Mendapatkan info aplikasi file proyek. |
| [getProjectFileFormat()](#getProjectFileFormat--) | Mendapatkan format file proyek. |
| [hashCode()](#hashCode--) | Mengembalikan nilai kode hash untuk instance dari kelas [ProjectFileInfo](../../com.aspose/tasks/projectfileinfo). |
| [isPasswordProtected()](#isPasswordProtected--) | Mendapatkan nilai yang menunjukkan apakah sebuah proyek dilindungi kata sandi. |
### canRead() {#canRead--}
```
public final boolean canRead()
```


Mendapatkan nilai yang menunjukkan apakah Aspose.Tasks dapat memproses file proyek.

**Returns:**
boolean - nilai yang menunjukkan apakah Aspose dapat.
### equals(ProjectFileInfo other) {#equals-com.aspose.tasks.ProjectFileInfo-}
```
public final boolean equals(ProjectFileInfo other)
```


Mengembalikan nilai yang menunjukkan apakah instance ini sama dengan objek yang ditentukan.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| other | [ProjectFileInfo](../../com.aspose.tasks/projectfileinfo) | Objek yang ditentukan untuk dibandingkan dengan instance ini. |

**Returns:**
boolean - mengembalikan true jika ProjectFileInfo yang ditentukan dan instance ini memiliki format file dan informasi aplikasi yang sama.
### equals(Object obj) {#equals-java.lang.Object-}
```
public boolean equals(Object obj)
```


Mengembalikan nilai yang menunjukkan apakah instance ini sama dengan objek yang ditentukan.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| obj | java.lang.Object | Objek yang ditentukan untuk dibandingkan dengan instance ini. |

**Returns:**
boolean - mengembalikan true jika ProjectFileInfo yang ditentukan dan instance ini memiliki format file dan informasi aplikasi yang sama.
### getProjectApplicationInfo() {#getProjectApplicationInfo--}
```
public final int getProjectApplicationInfo()
```


Mendapatkan info aplikasi file proyek.

**Returns:**
int - informasi aplikasi file proyek.
### getProjectFileFormat() {#getProjectFileFormat--}
```
public final int getProjectFileFormat()
```


Mendapatkan format file proyek.

**Returns:**
int - format file proyek.
### hashCode() {#hashCode--}
```
public int hashCode()
```


Mengembalikan nilai kode hash untuk instance dari kelas [ProjectFileInfo](../../com.aspose/tasks/projectfileinfo).

**Returns:**
int - mengembalikan nilai kode hash untuk objek ini.
### isPasswordProtected() {#isPasswordProtected--}
```
public final boolean isPasswordProtected()
```


Mendapatkan nilai yang menunjukkan apakah sebuah proyek dilindungi kata sandi.

**Returns:**
boolean - nilai yang menunjukkan apakah sebuah proyek dilindungi kata sandi.
