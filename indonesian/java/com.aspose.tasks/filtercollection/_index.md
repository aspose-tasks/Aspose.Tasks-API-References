---
title: "FilterCollection"
second_title: "Referensi API Aspose.Tasks for Java"
description: "Berisi daftar objek."
type: docs
weight: 92
url: /id/java/com.aspose.tasks/filtercollection/
---

**Inheritance:**
java.lang.Object, java.util.AbstractCollection
```
public class FilterCollection extends AbstractCollection<Filter>
```

Berisi daftar objek [Filter](../../com.aspose.tasks/filter). Mengimplementasikan antarmuka ICollection&lt;Filter&gt;.
## Metode

| Metode | Deskripsi |
| --- | --- |
| [add(Filter item)](#add-com.aspose.tasks.Filter-) |  |
| [clear()](#clear--) | Menghapus semua elemen dari koleksi ini (operasi opsional). |
| [contains(Filter item)](#contains-com.aspose.tasks.Filter-) | Mengembalikan true jika koleksi ini berisi item yang ditentukan. |
| [copyTo(Filter[] array, int arrayIndex)](#copyTo-com.aspose.tasks.Filter---int-) | Menyalin elemen dari array yang ditentukan ke koleksi ini mulai dari indeks yang ditentukan. |
| [iterator()](#iterator--) | Mengembalikan iterator atas elemen yang terdapat dalam koleksi ini. |
| [remove(Filter item)](#remove-com.aspose.tasks.Filter-) | Menghapus item yang ditentukan dari koleksi ini. |
| [size()](#size--) | Mendapatkan jumlah elemen yang terdapat dalam koleksi ini. |
| [toList()](#toList--) | Mengonversi koleksi filter menjadi daftar objek `Filter`. |
### add(Filter item) {#add-com.aspose.tasks.Filter-}
```
public boolean add(Filter item)
```




**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| item | [Filter](../../com.aspose.tasks/filter) |  |

**Returns:**
boolean
### clear() {#clear--}
```
public void clear()
```


Menghapus semua elemen dari koleksi ini (operasi opsional).

### contains(Filter item) {#contains-com.aspose.tasks.Filter-}
```
public final boolean contains(Filter item)
```


Mengembalikan true jika koleksi ini berisi item yang ditentukan.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| item | [Filter](../../com.aspose.tasks/filter) | item yang ditentukan. |

**Returns:**
boolean - true jika koleksi berisi item yang ditentukan.
### copyTo(Filter[] array, int arrayIndex) {#copyTo-com.aspose.tasks.Filter---int-}
```
public final void copyTo(Filter[] array, int arrayIndex)
```


Menyalin elemen dari array yang ditentukan ke koleksi ini mulai dari indeks yang ditentukan.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| array | [Filter\[\]](../../com.aspose.tasks/filter) | array satu dimensi yang ditentukan untuk menyalin elemen ke |
| arrayIndex | int | indeks berbasis nol dari array yang ditentukan di mana penyalinan dimulai. |

### iterator() {#iterator--}
```
public Iterator<Filter> iterator()
```


Mengembalikan iterator atas elemen yang terdapat dalam koleksi ini.

**Returns:**
java.util.Iterator&lt;com.aspose.tasks.Filter&gt; - iterator koleksi.
### remove(Filter item) {#remove-com.aspose.tasks.Filter-}
```
public final boolean remove(Filter item)
```


Menghapus item yang ditentukan dari koleksi ini.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| item | [Filter](../../com.aspose.tasks/filter) | item yang ditentukan. |

**Returns:**
boolean - true jika operasi berhasil.
### size() {#size--}
```
public final int size()
```


Mendapatkan jumlah elemen yang terdapat dalam koleksi ini.

**Returns:**
int - jumlah elemen yang terdapat dalam koleksi ini.
### toList() {#toList--}
```
public List<Filter> toList()
```


Mengonversi koleksi filter menjadi daftar objek `Filter`.

**Returns:**
java.util.List&lt;com.aspose.tasks.Filter&gt; - Daftar generik objek `Filter`.
