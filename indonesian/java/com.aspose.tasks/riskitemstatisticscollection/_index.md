---
title: "RiskItemStatisticsCollection"
second_title: "Referensi API Aspose.Tasks for Java"
description: "Mewakili koleksi yang berisi instance dari kelas tersebut."
type: docs
weight: 266
url: /id/java/com.aspose.tasks/riskitemstatisticscollection/
---

**Inheritance:**
java.lang.Object

**All Implemented Interfaces:**
java.util.Map, java.lang.Iterable
```
public class RiskItemStatisticsCollection implements Map<Task,RiskItemStatistics>, Iterable<RiskItemStatistics>
```

Mewakili koleksi yang berisi instance dari kelas [RiskItemStatistics](../../com.aspose/tasks/riskitemstatistics).
## Metode

| Metode | Deskripsi |
| --- | --- |
| [clear()](#clear--) | Menghapus semua item dari koleksi. |
| [containsKey(Object key)](#containsKey-java.lang.Object-) | Mengembalikan true jika peta ini berisi pemetaan untuk kunci yang ditentukan. |
| [containsValue(Object value)](#containsValue-java.lang.Object-) | Mengembalikan true jika peta ini memetakan satu atau lebih kunci ke nilai yang ditentukan. |
| [entrySet()](#entrySet--) | Mengembalikan tampilan Set dari pemetaan yang terdapat dalam peta ini. |
| [get(Object task)](#get-java.lang.Object-) | Mengembalikan sebuah instance dari kelas `RiskItemStatistics` yang terdapat dalam koleksi ini dan terkait dengan objek Task yang ditentukan; null jika item tidak ditemukan. |
| [isEmpty()](#isEmpty--) | Mengembalikan true jika peta ini tidak berisi pemetaan kunci-nilai. |
| [iterator()](#iterator--) | Mengembalikan enumerator untuk koleksi ini. |
| [keySet()](#keySet--) | Mengembalikan tampilan Set dari kunci yang terdapat dalam peta ini. |
| [put(Task key, RiskItemStatistics value)](#put-com.aspose.tasks.Task-com.aspose.tasks.RiskItemStatistics-) | Mengaitkan nilai yang ditentukan dengan kunci yang ditentukan dalam peta ini. |
| [putAll(Map&lt;? extends Task,? extends RiskItemStatistics&gt; m)](#putAll-java.util.Map---extends-com.aspose.tasks.Task---extends-com.aspose.tasks.RiskItemStatistics--) | Menyalin semua pemetaan dari peta yang ditentukan ke peta ini. |
| [remove(Object key)](#remove-java.lang.Object-) | Menghapus pemetaan untuk sebuah kunci dari peta ini jika ada. |
| [size()](#size--) | Mengembalikan jumlah elemen dalam koleksi ini. |
| [values()](#values--) | Mengembalikan tampilan Collection dari nilai yang terdapat dalam peta ini. |
### clear() {#clear--}
```
public void clear()
```


Menghapus semua item dari koleksi.

### containsKey(Object key) {#containsKey-java.lang.Object-}
```
public boolean containsKey(Object key)
```


Mengembalikan true jika peta ini berisi pemetaan untuk kunci yang ditentukan.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| key | java.lang.Object | \{@inheritDoc\} |

**Returns:**
boolean - true jika peta ini berisi pemetaan untuk kunci yang ditentukan.
### containsValue(Object value) {#containsValue-java.lang.Object-}
```
public boolean containsValue(Object value)
```


Mengembalikan true jika peta ini memetakan satu atau lebih kunci ke nilai yang ditentukan.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| nilai | java.lang.Object | \{@inheritDoc\} |

**Returns:**
boolean - true jika peta ini memetakan satu atau lebih kunci ke nilai yang ditentukan.
### entrySet() {#entrySet--}
```
public Set<Map.Entry<Task,RiskItemStatistics>> entrySet()
```


Mengembalikan tampilan Set dari pemetaan yang terdapat dalam peta ini.

**Returns:**
java.util.Set&lt;java.util.Map.Entry&lt;com.aspose.tasks.Task,com.aspose.tasks.RiskItemStatistics&gt;&gt; - \{@inheritDoc\}
### get(Object task) {#get-java.lang.Object-}
```
public RiskItemStatistics get(Object task)
```


Mengembalikan sebuah instance dari kelas `RiskItemStatistics` yang terdapat dalam koleksi ini dan terkait dengan objek Task yang ditentukan; null jika item tidak ditemukan.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| tugas | java.lang.Object | instance yang ditentukan dari kelas `Task`. |

**Returns:**
[RiskItemStatistics](../../com.aspose.tasks/riskitemstatistics) - risk item which is associated with the specified task object if found; null otherwise.
### isEmpty() {#isEmpty--}
```
public boolean isEmpty()
```


Mengembalikan true jika peta ini tidak berisi pemetaan kunci-nilai.

**Returns:**
boolean - true jika peta ini tidak berisi pasangan kunci-nilai
### iterator() {#iterator--}
```
public Iterator<RiskItemStatistics> iterator()
```


Mengembalikan enumerator untuk koleksi ini.

**Returns:**
java.util.Iterator&lt;com.aspose.tasks.RiskItemStatistics&gt; - enumerator untuk koleksi ini.
### keySet() {#keySet--}
```
public Set<Task> keySet()
```


Mengembalikan tampilan Set dari kunci yang terdapat dalam peta ini.

**Returns:**
java.util.Set&lt;com.aspose.tasks.Task&gt; - tampilan set dari kunci yang terdapat dalam peta ini.
### put(Task key, RiskItemStatistics value) {#put-com.aspose.tasks.Task-com.aspose.tasks.RiskItemStatistics-}
```
public RiskItemStatistics put(Task key, RiskItemStatistics value)
```


Mengaitkan nilai yang ditentukan dengan kunci yang ditentukan dalam peta ini.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| key | [Task](../../com.aspose.tasks/task) | \{@inheritDoc\} |
| value | [RiskItemStatistics](../../com.aspose.tasks/riskitemstatistics) | \{@inheritDoc\} |

**Returns:**
[RiskItemStatistics](../../com.aspose.tasks/riskitemstatistics) - \{@inheritDoc\}
### putAll(Map&lt;? extends Task,? extends RiskItemStatistics&gt; m) {#putAll-java.util.Map---extends-com.aspose.tasks.Task---extends-com.aspose.tasks.RiskItemStatistics--}
```
public void putAll(Map<? extends Task,? extends RiskItemStatistics> m)
```


Menyalin semua pemetaan dari peta yang ditentukan ke peta ini.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| m | java.util.Map&lt;? extends com.aspose.tasks.Task,? extends com.aspose.tasks.RiskItemStatistics&gt; | \{@inheritDoc\} |

### remove(Object key) {#remove-java.lang.Object-}
```
public RiskItemStatistics remove(Object key)
```


Menghapus pemetaan untuk sebuah kunci dari peta ini jika ada.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| key | java.lang.Object | \{@inheritDoc\} |

**Returns:**
[RiskItemStatistics](../../com.aspose.tasks/riskitemstatistics) - \{@inheritDoc\}
### size() {#size--}
```
public int size()
```


Mengembalikan jumlah elemen dalam koleksi ini.

**Returns:**
int - jumlah elemen dalam koleksi ini.
### values() {#values--}
```
public Collection<RiskItemStatistics> values()
```


Mengembalikan tampilan Collection dari nilai yang terdapat dalam peta ini.

**Returns:**
java.util.Collection&lt;com.aspose.tasks.RiskItemStatistics&gt; - tampilan koleksi dari nilai yang terdapat dalam peta ini.
