---
title: "ExtendedAttributeCollection"
second_title: "Referensi API Aspose.Tasks for Java"
description: "Mewakili koleksi objek."
type: docs
weight: 82
url: /id/java/com.aspose.tasks/extendedattributecollection/
---

**Inheritance:**
java.lang.Object, java.util.AbstractCollection, java.util.AbstractList, com.aspose.tasks.AbstractList
```
public class ExtendedAttributeCollection extends AbstractList<ExtendedAttribute>
```

Mewakili koleksi objek [ExtendedAttribute](../../com.aspose.tasks/extendedattribute).
## Metode

| Metode | Deskripsi |
| --- | --- |
| [add(ExtendedAttribute item)](#add-com.aspose.tasks.ExtendedAttribute-) | Menambahkan item yang ditentukan ke koleksi ini. |
| [clear()](#clear--) | Menghapus semua item dari koleksi ini. |
| [contains(ExtendedAttribute item)](#contains-com.aspose.tasks.ExtendedAttribute-) | Mengembalikan true jika item yang ditentukan ditemukan dalam koleksi ini; jika tidak, false. |
| [copyTo(ExtendedAttribute[] array, int arrayIndex)](#copyTo-com.aspose.tasks.ExtendedAttribute---int-) | Menyalin elemen-elemen koleksi ini ke array yang ditentukan, mulai dari indeks array yang ditentukan. |
| [get(int index)](#get-int-) | Mendapatkan elemen pada indeks yang ditentukan. |
| [indexOf(ExtendedAttribute item)](#indexOf-com.aspose.tasks.ExtendedAttribute-) | Menentukan indeks dari item yang ditentukan dalam koleksi ini. |
| [insert(int index, ExtendedAttribute item)](#insert-int-com.aspose.tasks.ExtendedAttribute-) | Menyisipkan item yang ditentukan pada indeks yang ditentukan. |
| [iterator()](#iterator--) | Mengembalikan iterator atas elemen dari koleksi ini. |
| [remove(int index)](#remove-int-) | Menghapus item pada indeks yang ditentukan. |
| [remove(Object item)](#remove-java.lang.Object-) | Menghapus elemen dari koleksi. |
| [set(int index, ExtendedAttribute value)](#set-int-com.aspose.tasks.ExtendedAttribute-) | Mengganti elemen pada posisi yang ditentukan dalam daftar ini dengan elemen yang ditentukan. |
| [size()](#size--) | Mendapatkan jumlah elemen yang terdapat dalam koleksi ini. |
### add(ExtendedAttribute item) {#add-com.aspose.tasks.ExtendedAttribute-}
```
public final boolean add(ExtendedAttribute item)
```


Menambahkan item yang ditentukan ke koleksi ini.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| item | [ExtendedAttribute](../../com.aspose.tasks/extendedattribute) | item yang ditentukan untuk ditambahkan ke koleksi ini. |

**Returns:**
boolean - true jika item yang ditentukan berhasil ditambahkan; jika tidak, false.
### clear() {#clear--}
```
public final void clear()
```


Menghapus semua item dari koleksi ini.

### contains(ExtendedAttribute item) {#contains-com.aspose.tasks.ExtendedAttribute-}
```
public final boolean contains(ExtendedAttribute item)
```


Mengembalikan true jika item yang ditentukan ditemukan dalam koleksi ini; jika tidak, false.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| item | [ExtendedAttribute](../../com.aspose.tasks/extendedattribute) | item yang ditentukan untuk dicari. |

**Returns:**
boolean - true jika item yang ditentukan ditemukan dalam koleksi ini; jika tidak, false.
### copyTo(ExtendedAttribute[] array, int arrayIndex) {#copyTo-com.aspose.tasks.ExtendedAttribute---int-}
```
public final void copyTo(ExtendedAttribute[] array, int arrayIndex)
```


Menyalin elemen-elemen koleksi ini ke array yang ditentukan, mulai dari indeks array yang ditentukan.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| array | [ExtendedAttribute\[\]](../../com.aspose.tasks/extendedattribute) | array satu dimensi yang ditentukan untuk menyalin elemen ke |
| arrayIndex | int | indeks berbasis nol dari array yang ditentukan di mana penyalinan dimulai. |

### get(int index) {#get-int-}
```
public final ExtendedAttribute get(int index)
```


Mendapatkan elemen pada indeks yang ditentukan.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| index | int | Indeks berbasis nol dari elemen yang akan diambil atau diatur. |

**Returns:**
[ExtendedAttribute](../../com.aspose.tasks/extendedattribute) - the element at the specified index.
### indexOf(ExtendedAttribute item) {#indexOf-com.aspose.tasks.ExtendedAttribute-}
```
public final int indexOf(ExtendedAttribute item)
```


Menentukan indeks dari item yang ditentukan dalam koleksi ini.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| item | [ExtendedAttribute](../../com.aspose.tasks/extendedattribute) | item yang ditentukan untuk ditemukan dalam koleksi ini. |

**Returns:**
int - indeks dari item yang ditentukan jika ditemukan; jika tidak, -1.
### insert(int index, ExtendedAttribute item) {#insert-int-com.aspose.tasks.ExtendedAttribute-}
```
public final void insert(int index, ExtendedAttribute item)
```


Menyisipkan item yang ditentukan pada indeks yang ditentukan.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| index | int | indeks berbasis nol yang ditentukan di mana item harus disisipkan. |
| item | [ExtendedAttribute](../../com.aspose.tasks/extendedattribute) | item yang ditentukan untuk disisipkan ke dalam koleksi ini. |

### iterator() {#iterator--}
```
public Iterator<ExtendedAttribute> iterator()
```


Mengembalikan iterator atas elemen dari koleksi ini.

**Returns:**
java.util.Iterator&lt;com.aspose.tasks.ExtendedAttribute&gt; - iterator atas elemen dari koleksi ini.
### remove(int index) {#remove-int-}
```
public final ExtendedAttribute remove(int index)
```


Menghapus item pada indeks yang ditentukan.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| index | int | indeks berbasis nol yang ditentukan untuk menghapus sebuah item. |

**Returns:**
[ExtendedAttribute](../../com.aspose.tasks/extendedattribute) - the element that was removed from the collection.
### remove(Object item) {#remove-java.lang.Object-}
```
public final boolean remove(Object item)
```


Menghapus elemen dari koleksi.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| item | java.lang.Object | elemen yang akan dihapus. |

**Returns:**
boolean - true jika elemen dihapus dari koleksi; jika tidak false.
### set(int index, ExtendedAttribute value) {#set-int-com.aspose.tasks.ExtendedAttribute-}
```
public final ExtendedAttribute set(int index, ExtendedAttribute value)
```


Mengganti elemen pada posisi yang ditentukan dalam daftar ini dengan elemen yang ditentukan.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| index | int | posisi yang ditentukan untuk mengganti elemen. |
| value | [ExtendedAttribute](../../com.aspose.tasks/extendedattribute) | elemen yang ditentukan untuk mengganti elemen pada posisi yang ditentukan. |

**Returns:**
[ExtendedAttribute](../../com.aspose.tasks/extendedattribute) - the element replaced by the specified element at the specified position.
### size() {#size--}
```
public final int size()
```


Mendapatkan jumlah elemen yang terdapat dalam koleksi ini.

**Returns:**
int - jumlah elemen yang terdapat dalam koleksi ini.
