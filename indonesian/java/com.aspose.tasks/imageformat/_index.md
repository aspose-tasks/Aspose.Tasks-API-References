---
title: "ImageFormat"
second_title: "Referensi API Aspose.Tasks for Java"
description: "Menentukan format berkas gambar."
type: docs
weight: 133
url: /id/java/com.aspose.tasks/imageformat/
---

**Inheritance:**
java.lang.Object
```
public final class ImageFormat
```

Menentukan format file gambar. Tidak dapat diperluas.
## Konstruktor

| Konstruktor | Deskripsi |
| --- | --- |
| [ImageFormat(String guid)](#ImageFormat-java.lang.String-) | Menginisialisasi instance baru dari kelas ImageFormat dengan menggunakan string Guid yang ditentukan. |
## Metode

| Metode | Deskripsi |
| --- | --- |
| [equals(Object o)](#equals-java.lang.Object-) | Mengembalikan nilai yang menunjukkan apakah objek yang ditentukan adalah objek ImageFormat yang setara dengan objek ImageFormat ini. |
| [getBmp()](#getBmp--) | Mendapatkan format gambar bitmap (BMP). |
| [getEmf()](#getEmf--) | Mendapatkan format gambar enhanced metafile (EMF). |
| [getExif()](#getExif--) | Mendapatkan format Exchangeable Image File (Exif). |
| [getGif()](#getGif--) | Mendapatkan format Graphics Interchange Format (GIF). |
| [getGuid()](#getGuid--) | Mendapatkan string Guid yang mewakili objek ImageFormat ini. |
| [getIcon()](#getIcon--) | Mendapatkan format gambar ikon Windows. |
| [getJpeg()](#getJpeg--) | Mendapatkan format Joint Photographic Experts Group (JPEG). |
| [getMemoryBmp()](#getMemoryBmp--) | Mendapatkan format bitmap dalam memori. |
| [getPng()](#getPng--) | Mendapatkan format W3C Portable Network Graphics (PNG). |
| [getTiff()](#getTiff--) | Mendapatkan format Tagged Image File Format (TIFF). |
| [getWmf()](#getWmf--) | Mendapatkan format Windows metafile (WMF). |
| [hashCode()](#hashCode--) | Mengembalikan nilai kode hash yang mewakili objek ini. |
| [isBitmapType()](#isBitmapType--) | Menentukan apakah format gambar adalah tipe bitmap. |
| [isMetafileType()](#isMetafileType--) | Menentukan apakah format gambar adalah tipe metafile. |
| [isUnknownType()](#isUnknownType--) | Menentukan apakah format gambar adalah tipe tidak diketahui. |
| [toString()](#toString--) | Mengonversi objek ImageFormat ini menjadi string yang dapat dibaca manusia. |
### ImageFormat(String guid) {#ImageFormat-java.lang.String-}
```
public ImageFormat(String guid)
```


Menginisialisasi instance baru dari kelas ImageFormat dengan menggunakan string Guid yang ditentukan.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| guid | java.lang.String | String Guid yang menentukan format gambar tertentu. |

### equals(Object o) {#equals-java.lang.Object-}
```
public boolean equals(Object o)
```


Mengembalikan nilai yang menunjukkan apakah objek yang ditentukan adalah objek ImageFormat yang setara dengan objek ImageFormat ini.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| o | java.lang.Object | Objek yang akan diuji. |

**Returns:**
boolean - true jika o adalah objek ImageFormat yang setara dengan objek ImageFormat ini; jika tidak, false.
### getBmp() {#getBmp--}
```
public static ImageFormat getBmp()
```


Mendapatkan format gambar bitmap (BMP).

**Returns:**
[ImageFormat](../../com.aspose.tasks/imageformat) - An ImageFormat object that indicates the bitmap image format.
### getEmf() {#getEmf--}
```
public static ImageFormat getEmf()
```


Mendapatkan format gambar enhanced metafile (EMF).

**Returns:**
[ImageFormat](../../com.aspose.tasks/imageformat) - An ImageFormat object that indicates the enhanced metafile image format.
### getExif() {#getExif--}
```
public static ImageFormat getExif()
```


Mendapatkan format Exchangeable Image File (Exif).

**Returns:**
[ImageFormat](../../com.aspose.tasks/imageformat) - An ImageFormat object that indicates the Exif format.
### getGif() {#getGif--}
```
public static ImageFormat getGif()
```


Mendapatkan format Graphics Interchange Format (GIF).

**Returns:**
[ImageFormat](../../com.aspose.tasks/imageformat) - An ImageFormat object that indicates the GIF image format.
### getGuid() {#getGuid--}
```
public String getGuid()
```


Mendapatkan string Guid yang mewakili objek ImageFormat ini.

**Returns:**
java.lang.String - Sebuah string Guid yang mewakili objek ImageFormat ini.
### getIcon() {#getIcon--}
```
public static ImageFormat getIcon()
```


Mendapatkan format gambar ikon Windows.

**Returns:**
[ImageFormat](../../com.aspose.tasks/imageformat) - An ImageFormat object that indicates the Windows icon image format.
### getJpeg() {#getJpeg--}
```
public static ImageFormat getJpeg()
```


Mendapatkan format Joint Photographic Experts Group (JPEG).

**Returns:**
[ImageFormat](../../com.aspose.tasks/imageformat) - An ImageFormat object that indicates the JPEG image format.
### getMemoryBmp() {#getMemoryBmp--}
```
public static ImageFormat getMemoryBmp()
```


Mendapatkan format bitmap dalam memori.

**Returns:**
[ImageFormat](../../com.aspose.tasks/imageformat) - An ImageFormat object that indicates the format of a bitmap in memory.
### getPng() {#getPng--}
```
public static ImageFormat getPng()
```


Mendapatkan format W3C Portable Network Graphics (PNG).

**Returns:**
[ImageFormat](../../com.aspose.tasks/imageformat) - An ImageFormat object that indicates the PNG image format.
### getTiff() {#getTiff--}
```
public static ImageFormat getTiff()
```


Mendapatkan format Tagged Image File Format (TIFF).

**Returns:**
[ImageFormat](../../com.aspose.tasks/imageformat) - An ImageFormat object that indicates the TIFF image format.
### getWmf() {#getWmf--}
```
public static ImageFormat getWmf()
```


Mendapatkan format Windows metafile (WMF).

**Returns:**
[ImageFormat](../../com.aspose.tasks/imageformat) - An ImageFormat object that indicates the Windows metafile image format.
### hashCode() {#hashCode--}
```
public int hashCode()
```


Mengembalikan nilai kode hash yang mewakili objek ini.

**Returns:**
int - Sebuah kode hash yang mewakili objek ini.
### isBitmapType() {#isBitmapType--}
```
public boolean isBitmapType()
```


Menentukan apakah format gambar adalah tipe bitmap.

**Returns:**
boolean - true jika format gambar adalah tipe bitmap; jika tidak, false.
### isMetafileType() {#isMetafileType--}
```
public boolean isMetafileType()
```


Menentukan apakah format gambar adalah tipe metafile.

**Returns:**
boolean - true jika format gambar adalah tipe metafile; jika tidak, false.
### isUnknownType() {#isUnknownType--}
```
public boolean isUnknownType()
```


Menentukan apakah format gambar adalah tipe tidak diketahui.

**Returns:**
boolean - true jika format gambar adalah tipe tidak diketahui; jika tidak, false.
### toString() {#toString--}
```
public String toString()
```


Mengonversi objek ImageFormat ini menjadi string yang dapat dibaca manusia.

**Returns:**
java.lang.String - Sebuah string yang mewakili objek ImageFormat ini.
