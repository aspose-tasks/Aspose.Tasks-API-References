---
title: "RiskPatternCollection"
second_title: "Referensi API Aspose.Tasks for Java"
description: "Mewakili koleksi yang berisi instance kelas RiskPattern."
type: docs
weight: 269
url: /id/java/com.aspose.tasks/riskpatterncollection/
---

**Inheritance:**
java.lang.Object, java.util.AbstractCollection
```
public class RiskPatternCollection extends AbstractCollection<RiskPattern>
```

Mewakili kumpulan yang berisi instance kelas `RiskPattern`.
## Metode

| Metode | Deskripsi |
| --- | --- |
| [add(RiskPattern item)](#add-com.aspose.tasks.RiskPattern-) | Menambahkan sebuah instance dari kelas `RiskPattern` ke koleksi ini. |
| [clear()](#clear--) | Menghapus semua item dari koleksi ini. |
| [contains(Object item)](#contains-java.lang.Object-) | Mengembalikan true jika item yang ditentukan ditemukan dalam koleksi ini; jika tidak, false. |
| [copyTo(RiskPattern[] array, int arrayIndex)](#copyTo-com.aspose.tasks.RiskPattern---int-) | Menyalin elemen-elemen koleksi ini ke array yang ditentukan, mulai dari indeks array yang ditentukan. |
| [get_Item(Task task)](#get-Item-com.aspose.tasks.Task-) | Mendapatkan instance kelas `RiskPattern` untuk tugas yang ditentukan. |
| [iterator()](#iterator--) | Mengembalikan enumerator untuk koleksi ini. |
| [remove(Object item)](#remove-java.lang.Object-) | Menghapus kemunculan pertama dari objek tertentu dalam koleksi ini. |
| [size()](#size--) | Mengembalikan jumlah elemen yang terdapat dalam koleksi ini. |
### add(RiskPattern item) {#add-com.aspose.tasks.RiskPattern-}
```
public boolean add(RiskPattern item)
```


Menambahkan sebuah instance dari kelas `RiskPattern` ke koleksi ini.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| item | [RiskPattern](../../com.aspose.tasks/riskpattern) | sebuah instance dari kelas `RiskPattern` untuk ditambahkan ke koleksi ini. |

**Returns:**
boolean - mengembalikan true jika sebuah instance dari kelas `RiskPattern` berhasil ditambahkan; jika tidak, false.
### clear() {#clear--}
```
public void clear()
```


Menghapus semua item dari koleksi ini.

### contains(Object item) {#contains-java.lang.Object-}
```
public boolean contains(Object item)
```


Mengembalikan true jika item yang ditentukan ditemukan dalam koleksi ini; jika tidak, false.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| item | java.lang.Object | item yang ditentukan untuk dicari. |

**Returns:**
boolean - true jika item yang ditentukan ditemukan dalam koleksi ini; jika tidak, false.
### copyTo(RiskPattern[] array, int arrayIndex) {#copyTo-com.aspose.tasks.RiskPattern---int-}
```
public void copyTo(RiskPattern[] array, int arrayIndex)
```


Menyalin elemen-elemen koleksi ini ke array yang ditentukan, mulai dari indeks array yang ditentukan.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| array | [RiskPattern\[\]](../../com.aspose.tasks/riskpattern) | array satu dimensi yang ditentukan untuk menyalin elemen ke |
| arrayIndex | int | indeks berbasis nol dari array yang ditentukan di mana penyalinan dimulai. |

### get_Item(Task task) {#get-Item-com.aspose.tasks.Task-}
```
public RiskPattern get_Item(Task task)
```


Mendapatkan instance kelas `RiskPattern` untuk tugas yang ditentukan.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| task | [Task](../../com.aspose.tasks/task) | tugas yang ditentukan. |

**Returns:**
[RiskPattern](../../com.aspose.tasks/riskpattern) - the pattern for the specified task.
### iterator() {#iterator--}
```
public Iterator<RiskPattern> iterator()
```


Mengembalikan enumerator untuk koleksi ini.

**Returns:**
java.util.Iterator&lt;com.aspose.tasks.RiskPattern&gt; - enumerator untuk koleksi ini.
### remove(Object item) {#remove-java.lang.Object-}
```
public boolean remove(Object item)
```


Menghapus kemunculan pertama dari objek tertentu dalam koleksi ini.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| item | java.lang.Object | objek yang ditentukan untuk dihapus. |

**Returns:**
boolean - true jika objek yang ditentukan berhasil dihapus dari koleksi ini; jika tidak, false.
### size() {#size--}
```
public int size()
```


Mengembalikan jumlah elemen yang terdapat dalam koleksi ini.

**Returns:**
int - jumlah elemen yang terdapat dalam koleksi ini.
