---
title: "Aspose::Tasks::Project::Project konstruktor"
linktitle: "Proyek"
articleTitle: "Proyek"
second_title: "Aspose.Tasks untuk C++"
description: "Menginisialisasi instance baru dari kelas Project."
type: docs
weight: 10
url: /id/cpp/aspose.tasks/project/project/
---

## Project (1 of 13) {#project_1}

Menginisialisasi instance baru dari kelas Project.

**Returns:** Aspose::Tasks::

```cpp
Project()
```

---

## Project (2 of 13) {#project_2}

Menginisialisasi instance baru dari kelas Project dari templat yang dilindungi kata sandi (file mpp atau mpt yang ada).

**Returns:** Aspose::Tasks::

```cpp
Project(const System::String & projectTemplate, const System::String & protectionPassword)
```

| Parameter | Deskripsi |
| --- | --- |
| projectTemplate | Jalur ke templat untuk membuat proyek dari. |
| protectionPassword | Kata sandi perlindungan. |

---

## Project (3 of 13) {#project_3}

Menginisialisasi instance baru dari kelas Project dari templat (file mpp atau mpt yang ada).

**Returns:** Aspose::Tasks::

```cpp
Project(const System::String & projectTemplate)
```

| Parameter | Deskripsi |
| --- | --- |
| projectTemplate | Jalur ke templat untuk membuat proyek dari. |

---

## Project (4 of 13) {#project_4}

Menginisialisasi instance baru dari kelas Project dari Stream dengan instance yang ditentukan dari kelas PrimaveraReadOptions.

**Returns:** Aspose::Tasks::

```cpp
Project(const System::SharedPtr< System::IO::Stream > & stream, const System::SharedPtr< PrimaveraReadOptions > & options)
```

| Parameter | Deskripsi |
| --- | --- |
| stream | Stream dari kelas Project System::IO::Stream |
| options | instance yang ditentukan dari kelas PrimaveraReadOptions yang memungkinkan menyesuaikan pembacaan format Primavera (XER atau XML). |

---

## Project (5 of 13) {#project_5}

Menginisialisasi instance baru dari kelas Project dari templat (file mpp atau mpt yang ada).

**Returns:** Aspose::Tasks::

```cpp
Project(const System::String & projectTemplate, ParseErrorCallback parseErrorHandler)
```

| Parameter | Deskripsi |
| --- | --- |
| projectTemplate | Jalur ke templat untuk membuat proyek dari. |
| parseErrorHandler | metode callback yang ditentukan untuk menangani kesalahan parsing xml. |

---

## Project (6 of 13) {#project_6}

Menginisialisasi instance baru dari kelas Project dari sebuah stream.

**Returns:** Aspose::Tasks::

```cpp
Project(const System::SharedPtr< System::IO::Stream > & stream)
```

| Parameter | Deskripsi |
| --- | --- |
| stream | Stream untuk memuat templat dari. |

---

## Project (7 of 13) {#project_7}

Menginisialisasi instance baru dari kelas Project dari instance StreamReader.

**Returns:** Aspose::Tasks::

```cpp
Project(const System::SharedPtr< System::IO::StreamReader > & reader)
```

| Parameter | Deskripsi |
| --- | --- |
| reader | Pembaca aliran tempat memuat templat dari. |

---

## Project (8 of 13) {#project_8}

Menginisialisasi instance baru dari kelas Project dari templat (file MPP atau MPT yang ada) dengan instance yang ditentukan dari kelas PrimaveraReadOptions.

**Returns:** Aspose::Tasks::

```cpp
Project(const System::String & projectTemplate, const System::SharedPtr< PrimaveraReadOptions > & options)
```

| Parameter | Deskripsi |
| --- | --- |
| projectTemplate | Jalur ke templat untuk membuat proyek dari |
| options | instance yang ditentukan dari kelas PrimaveraReadOptions. |

---

## Project (9 of 13) {#project_9}

Menginisialisasi instance baru dari kelas Project untuk membaca data dari basis data yang ditentukan oleh instance dari kelas DbSettings.

**Returns:** Aspose::Tasks::

```cpp
Project(const System::SharedPtr< Connectivity::DbSettings > & settings)
```

| Parameter | Deskripsi |
| --- | --- |
| pengaturan | instance yang ditentukan dari kelas DbSettings. |

---

## Project (10 of 13) {#project_10}

Menginisialisasi instance baru dari kelas Project dari templat (file mpp atau mpt yang ada).

**Returns:** Aspose::Tasks::

```cpp
Project(const System::SharedPtr< System::IO::Stream > & stream, ParseErrorCallback parseErrorHandler)
```

| Parameter | Deskripsi |
| --- | --- |
| stream | Stream untuk memuat templat dari. |
| parseErrorHandler | metode callback yang ditentukan untuk menangani kesalahan parsing xml. |

---

## Project (11 of 13) {#project_11}

Menginisialisasi instance baru dari kelas Project dari templat (file mpp atau mpt yang ada).

**Returns:** Aspose::Tasks::

```cpp
Project(const System::SharedPtr< System::IO::Stream > & stream, const System::String & protectionPassword)
```

| Parameter | Deskripsi |
| --- | --- |
| stream | Stream untuk memuat templat dari. |
| protectionPassword | Kata sandi perlindungan. |

---

## Project (12 of 13) {#project_12}

Menginisialisasi instance baru dari kelas Project dari templat (file mpp atau mpt yang ada) dengan instance yang ditentukan dari kelas LoadOptions.

**Returns:** Aspose::Tasks::

```cpp
Project(const System::String & projectTemplate, const System::SharedPtr< LoadOptions > & options)
```

| Parameter | Deskripsi |
| --- | --- |
| projectTemplate | Jalur ke templat untuk membuat proyek dari |
| options | instance yang ditentukan dari kelas LoadOptions. |

---

## Project (13 of 13) {#project_13}

Menginisialisasi instance baru dari kelas Project dari Stream dengan instance yang ditentukan dari kelas LoadOptions.

**Returns:** Aspose::Tasks::

```cpp
Project(const System::SharedPtr< System::IO::Stream > & stream, const System::SharedPtr< LoadOptions > & options)
```

| Parameter | Deskripsi |
| --- | --- |
| stream | Stream dari kelas Project System::IO::Stream |
| options | instance yang ditentukan dari kelas LoadOptions |

