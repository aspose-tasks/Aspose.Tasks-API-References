---
title: "Aspose::Tasks::ExtendedAttributeDefinition::CreateExtendedAttribute metode"
linktitle: "CreateExtendedAttribute"
articleTitle: "CreateExtendedAttribute"
second_title: "Aspose.Tasks untuk C++"
description: "Membuat atribut ekstensi baru dengan ID bidang yang sama dengan nilai ID bidang objek ini."
type: docs
weight: 20
url: /id/cpp/aspose.tasks/extendedattributedefinition/createextendedattribute/
---

## CreateExtendedAttribute (1 of 7) {#createextendedattribute_1}

Membuat atribut ekstensi baru dengan ID bidang yang sama dengan nilai ID bidang objek ini.

**Returns:** returns created instance of the ExtendedAttribute class with the fieldID which equals to this object's fieldID value.

```cpp
CreateExtendedAttribute()
```

---

## CreateExtendedAttribute (2 of 7) {#createextendedattribute_2}

Membuat atribut ekstensi baru dengan ID bidang yang sama dengan nilai ID bidang objek ini dan nilai flag yang ditentukan.

**Returns:** returns created instance of the ExtendedAttribute class with the fieldID which equals to this object's fieldID value.

```cpp
CreateExtendedAttribute(bool flagValue)
```

| Parameter | Deskripsi |
| --- | --- |
| flagValue | Nilai flag yang ditentukan. |

---

## CreateExtendedAttribute (3 of 7) {#createextendedattribute_3}

Membuat atribut ekstensi baru yang terhubung dengan item Value yang ditentukan.

**Returns:** returns created instance of the ExtendedAttribute class linked with specified Value item.

```cpp
CreateExtendedAttribute(const System::SharedPtr< Value > & lookupValue)
```

| Parameter | Deskripsi |
| --- | --- |
| lookupValue | Item Value yang ditentukan. |

---

## CreateExtendedAttribute (4 of 7) {#createextendedattribute_4}

Membuat atribut ekstensi baru dengan ID bidang yang sama dengan nilai ID bidang objek ini dan nilai teks yang ditentukan.

**Returns:** returns created instance of the ExtendedAttribute class with the fieldID which equals to this object's fieldID value.

```cpp
CreateExtendedAttribute(const System::String & textValue)
```

| Parameter | Deskripsi |
| --- | --- |
| textValue | Nilai teks yang ditentukan. |

---

## CreateExtendedAttribute (5 of 7) {#createextendedattribute_5}

Membuat atribut ekstensi baru dengan ID bidang yang sama dengan nilai ID bidang objek ini dan nilai durasi yang ditentukan.

**Returns:** returns created instance of the ExtendedAttribute class with the fieldID which equals to this object's fieldID value.

```cpp
CreateExtendedAttribute(Duration durationValue)
```

| Parameter | Deskripsi |
| --- | --- |
| durationValue | Nilai durasi yang ditentukan. |

---

## CreateExtendedAttribute (6 of 7) {#createextendedattribute_6}

Membuat atribut ekstensi baru dengan ID bidang yang sama dengan nilai ID bidang objek ini dan nilai tanggal yang ditentukan.

**Returns:** returns created instance of the ExtendedAttribute class with the fieldID which equals to this object's fieldID value.

```cpp
CreateExtendedAttribute(System::DateTime dateTimeValue)
```

| Parameter | Deskripsi |
| --- | --- |
| dateTimeValue | Nilai tanggal dan waktu yang ditentukan. |

---

## CreateExtendedAttribute (7 of 7) {#createextendedattribute_7}

Membuat atribut ekstensi baru dengan ID bidang yang sama dengan nilai ID bidang objek ini dan nilai numerik yang ditentukan.

**Returns:** returns created instance of the ExtendedAttribute class with the fieldID which equals to this object's fieldID value.

```cpp
CreateExtendedAttribute(System::Decimal numericValue)
```

| Parameter | Deskripsi |
| --- | --- |
| numericValue | Nilai numerik yang ditentukan. |

