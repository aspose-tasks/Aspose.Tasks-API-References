---
title: "Aspose::Tasks::RiskAnalysis::RiskItemStatisticsCollection::Get metode"
linktitle: "Get"
articleTitle: "Get"
second_title: "Aspose.Tasks untuk C++"
description: "Mengembalikan sebuah instance dari kelas RiskItemStatistics yang terdapat dalam koleksi ini dan terkait dengan objek Task yang ditentukan; null jika item tidak ada"
type: docs
weight: 10
url: /id/cpp/aspose.tasks.riskanalysis/riskitemstatisticscollection/get/
---

## Get {#get}

Mengembalikan sebuah instance dari kelas RiskItemStatistics yang terdapat dalam koleksi ini dan terkait dengan objek Task yang ditentukan; null jika item tidak ditemukan.

**Returns:** risk item which is associated with the specified task object if found; null otherwise.

```cpp
Get(const System::SharedPtr< Task > & task)
```

| Parameter | Deskripsi |
| --- | --- |
| tugas | instance yang ditentukan dari kelas Task. |

