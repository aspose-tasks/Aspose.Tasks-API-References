---
title: "Enum TaskKey"
second_title: "Riferimento API di Aspose.Tasks per .NET"
description: "Enum Aspose.Tasks.TaskKey. Rappresenta un elenco di campi attività supportati"
type: docs
weight: 2400
url: /it/net/aspose.tasks/taskkey/
---
## TaskKey enumeration

Rappresenta un elenco di campi di attività supportati.

```csharp
public enum TaskKey : byte
```

### Valori

| Nome | Valore | Descrizione |
| --- | --- | --- |
| Uid | `0` | Rappresenta il campo Unique ID (Task). |
| Id | `1` | Rappresenta il campo Id (Task). |
| Name | `2` | Rappresenta il campo Name (Task). |
| Type | `3` | Rappresenta il campo Type (Task). |
| IsNull | `4` | Rappresenta il campo IsNull (Task). |
| Created | `5` | Rappresenta il campo Created (Task). |
| Contact | `6` | Rappresenta il campo Contact (Task). |
| WBS | `7` | Rappresenta il campo WBS (Task). |
| WBSLevel | `8` | Rappresenta il campo WBSLevel (Task). |
| OutlineNumber | `9` | Rappresenta il campo OutlineNumber (Task). |
| OutlineLevel | `10` | Rappresenta il campo OutlineLevel (Task). |
| Priority | `11` | Rappresenta il campo Priority (Task). |
| Start | `12` | Rappresenta il campo Start (Task). |
| Finish | `13` | Rappresenta il campo Finish (Task). |
| Duration | `14` | Rappresenta il campo Duration (Task). |
| DurationVariance | `15` | Rappresenta il campo DurationVariance (Task). |
| Work | `16` | Rappresenta il campo Work (Task). |
| Stop | `17` | Rappresenta il campo Stop (Task). |
| Resume | `18` | Rappresenta il campo Resume (Task). |
| IsResumeValid | `19` | Rappresenta il campo IsResumeValid (Task). |
| IsEffortDriven | `20` | Rappresenta il campo IsEffortDriven (Task). |
| IsRecurring | `21` | Rappresenta il campo IsRecurring (Task). |
| IsOverallocated | `22` | Rappresenta il campo IsOverallocated (Task). |
| HasOverallocatedResource | `23` | Rappresenta il campo HasOverallocatedResource (Task). |
| IsEstimated | `24` | Rappresenta il campo IsEstimated (Task). |
| IsMilestone | `25` | Rappresenta il campo IsMilestone (Task). |
| IsCritical | `26` | Rappresenta il campo IsCritical (Task). |
| IsSubproject | `27` | Rappresenta il campo IsSubproject (Task). |
| IsSubprojectReadOnly | `28` | Rappresenta il campo IsSubprojectReadOnly (Task). |
| IsMarked | `29` | Rappresenta il campo IsMarked (Task). |
| IgnoreWarnings | `30` | Rappresenta il campo IgnoreWarnings (Task). |
| SubprojectName | `31` | Rappresenta il campo SubprojectName (Task). |
| IsExternalTask | `32` | Rappresenta il campo IsExternalTask (Task). |
| IsSummary | `33` | Rappresenta il campo IsSummary (Task). |
| ExternalTaskProject | `34` | Rappresenta il campo ExternalTaskProject (Task). |
| ExternalId | `35` | Rappresenta il campo ExternalId (Task). |
| EarlyStart | `36` | Rappresenta il campo EarlyStart (Task). |
| EarlyFinish | `37` | Rappresenta il campo EarlyFinish (Task). |
| LateStart | `38` | Rappresenta il campo LateStart (Task). |
| LateFinish | `39` | Rappresenta il campo LateFinish (Task). |
| StartVariance | `40` | Rappresenta il campo StartVariance (Task). |
| FinishVariance | `41` | Rappresenta il campo FinishVariance (Task). |
| WorkVariance | `42` | Rappresenta il campo WorkVariance (Task). |
| CostVariance | `43` | Rappresenta il campo CostVariance (Task). |
| FreeSlack | `44` | Rappresenta il campo FreeSlack (Task). |
| TotalSlack | `45` | Rappresenta il campo TotalSlack (Task). |
| StartSlack | `46` | Rappresenta il campo StartSlack (Task). |
| FinishSlack | `47` | Rappresenta il campo FinishSlack (Task). |
| FixedCost | `48` | Rappresenta il campo FixedCost (Task). |
| FixedCostAccrual | `49` | Rappresenta il campo FixedCostAccrual (Task). |
| PercentComplete | `50` | Rappresenta il campo PercentComplete (Task). |
| PercentWorkComplete | `51` | Rappresenta il campo PercentWorkComplete (Task). |
| Cost | `52` | Rappresenta il campo Cost (Task). |
| OvertimeCost | `53` | Rappresenta il campo OvertimeCost (Task). |
| ActualStart | `54` | Rappresenta il campo ActualStart (Task). |
| ActualFinish | `55` | Rappresenta il campo ActualFinish (Task). |
| ActualDuration | `56` | Rappresenta il campo ActualDuration (Task). |
| ActualCost | `57` | Rappresenta il campo ActualCost (Task). |
| ActualOvertimeCost | `58` | Rappresenta il campo ActualOvertimeCost (Task). |
| ActualWork | `59` | Rappresenta il campo ActualWork (Task). |
| ActualOvertimeWork | `60` | Rappresenta il campo ActualOvertimeWork (Task). |
| OvertimeWork | `61` | Rappresenta il campo OvertimeWork (Task). |
| RegularWork | `62` | Rappresenta il campo RegularWork (Task). |
| RemainingDuration | `63` | Rappresenta il campo RemainingDuration (Task). |
| RemainingCost | `64` | Rappresenta il campo RemainingCost (Task). |
| RemainingWork | `65` | Rappresenta il campo RemainingWork (Task). |
| RemainingOvertimeWork | `66` | Rappresenta il campo RemainingOvertimeWork (Task). |
| RemainingOvertimeCost | `67` | Rappresenta il campo RemainingOvertimeCost (Task). |
| ACWP | `68` | Rappresenta il campo ACWP (Task). |
| CV | `69` | Rappresenta il campo CV (Task). |
| SV | `70` | Rappresenta il campo SV (Task). |
| ConstraintType | `71` | Rappresenta il campo ConstraintType (Task). |
| Calendar | `72` | Rappresenta il campo Calendar (Task). |
| ConstraintDate | `73` | Rappresenta il campo ConstraintDate (Task). |
| Deadline | `74` | Rappresenta il campo Deadline (Task). |
| LevelAssignments | `75` | Rappresenta il campo LevelAssignments (Task). |
| LevelingCanSplit | `76` | Rappresenta il campo LevelingCanSplit (Task). |
| LevelingDelay | `77` | Rappresenta il campo LevelingDelay (Task). |
| PreleveledStart | `78` | Rappresenta il campo PreleveledStart (Task). |
| PreleveledFinish | `79` | Rappresenta il campo PreleveledFinish (Task). |
| Hyperlink | `80` | Rappresenta il campo Hyperlink (Task). |
| HyperlinkAddress | `81` | Rappresenta il campo HyperlinkAddress (Task). |
| HyperlinkSubAddress | `82` | Rappresenta il campo HyperlinkSubAddress (Task). |
| IgnoreResourceCalendar | `83` | Rappresenta il campo IgnoreResourceCalendar (Task). |
| HideBar | `84` | Rappresenta il campo HideBar (Task). |
| IsRollup | `85` | Rappresenta il campo IsRollup (Task). |
| BCWS | `86` | Rappresenta il campo BCWS (Task). |
| BCWP | `87` | Rappresenta il campo BCWP (Task). |
| PhysicalPercentComplete | `88` | Rappresenta il campo PhysicalPercentComplete (Task). |
| EarnedValueMethod | `89` | Rappresenta il campo EarnedValueMethod (Task). |
| ActualWorkProtected | `90` | Rappresenta il campo ActualWorkProtected (Task). |
| ActualOvertimeWorkProtected | `91` | Rappresenta il campo ActualOvertimeWorkProtected (Task). |
| IsPublished | `92` | Rappresenta il campo IsPublished (Task). |
| IsScheduled | `93` | Rappresenta il campo IsScheduled (Task). |
| StatusManager | `94` | Rappresenta il campo StatusManager (Task). |
| CommitmentStart | `95` | Rappresenta il campo CommitmentStart (Task). |
| CommitmentFinish | `96` | Rappresenta il campo CommitmentFinish (Task). |
| CommitmentType | `97` | Rappresenta il campo CommitmentType (Task). |
| IsManual | `98` | Rappresenta il campo IsManual (Task). |
| IsExpanded | `99` | Rappresenta il campo IsExpanded (Task). |
| Guid | `100` | Rappresenta il campo Guid (Task). |
| NotesText | `101` | Rappresenta il campo NotesText (Task). |
| NotesRTF | `102` | Rappresenta il campo NotesRTF (Task). |
| ManualStart | `103` | Rappresenta il campo ManualStart (Task). |
| ManualFinish | `104` | Rappresenta il campo ManualFinish (Task). |
| ManualDuration | `105` | Rappresenta il campo ManualDuration (Task). |
| BudgetWork | `106` | Rappresenta il campo BudgetWork (Task). |
| BudgetCost | `107` | Rappresenta il campo BudgetCost (Task). |
| DisplayAsSummary | `108` | Rappresenta il campo DisplayAsSummary (Task). |
| SummaryProgress | `109` | Rappresenta il campo Summary Progress (Task). |
| IsActive | `110` | Rappresenta il campo IsActive (Task). |
| StartText | `111` | Rappresenta il campo StartText (Task). |
| FinishText | `112` | Rappresenta il campo FinishText (Task). |
| DurationText | `113` | Rappresenta il campo DurationText (Task). |
| CalendarUid | `114` | Rappresenta il campo CalendarUid (Task). |
| ParentTaskUid | `115` | Rappresenta il campo ParentTaskUid (Task). |
| DisplayOnTimeline | `116` | Rappresenta il campo DisplayOnTimeline (Task). |
| TaskIsAssigned | `117` | Rappresenta il campo TaskIsAssigned (Task). |
| TaskOriginalStart | `118` | Rappresenta il campo inizio originale (Task). |
| TaskOriginalFinish | `119` | Rappresenta il campo fine originale (Task). |
| IsShowBeforeProjectStartDateWarning | `120` | Rappresenta il flag che fa sì che MSP mostri l'avviso che un task inizia prima della data di inizio del progetto. |
| Warning | `121` | Rappresenta il flag che indica che il task ha discrepanze di programmazione. |
| ParentTaskGuid | `122` | Rappresenta il campo ParentTaskGuid (Task). |
| ActivityId | `123` | Rappresenta il campo ActivityId (solo applicabile ai progetti Primavera). |
| FreeSlackTimeSpan | `124` | Rappresenta il campo FreeSlack (Task). |
| TotalSlackTimeSpan | `125` | Rappresenta il campo TotalSlack (Task). |
| StartSlackTimeSpan | `126` | Rappresenta il campo StartSlack (Task). |
| FinishSlackTimeSpan | `127` | Rappresenta il campo FinishSlack (Task). |
| ExternalUid | `128` | Rappresenta l'Unique Id del task esterno. |

## Esempi

Mostra come leggere/scrivere le proprietà del task.

```csharp
var project = new Project();

// Aggiungi un task e imposta le proprietà del task
var task = project.RootTask.Children.Add();
task.Set(Tsk.Name, "Task1");
task.Set(Tsk.Start, new DateTime(2020, 3, 31, 8, 0, 0));
task.Set(Tsk.Finish, new DateTime(2020, 3, 31, 17, 0, 0));

var collector = new ChildTasksCollector();
TaskUtils.Apply(project.RootTask, collector, 0);

// Analizza tutti i task raccolti
foreach (var tsk in collector.Tasks)
{
    Console.WriteLine("Task Id: {0}", tsk.Get(Tsk.Id));
    Console.WriteLine("Task Uid: {0}", tsk.Get(Tsk.Uid));
    Console.WriteLine("Task Name: {0}", tsk.Get(Tsk.Name));
    Console.WriteLine("Task Start: {0}", tsk.Get(Tsk.Start));
    Console.WriteLine("Task Finish: {0}", tsk.Get(Tsk.Finish));
}
```

### Vedi anche

* namespace [Aspose.Tasks](../../aspose.tasks/)
* assembly [Aspose.Tasks](../../)


