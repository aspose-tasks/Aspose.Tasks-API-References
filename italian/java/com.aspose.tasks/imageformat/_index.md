---
title: "ImageFormat"
second_title: "Aspose.Tasks for Java API Reference"
description: "Specifica il formato file dell'immagine."
type: docs
weight: 133
url: /it/java/com.aspose.tasks/imageformat/
---

**Inheritance:**
java.lang.Object
```
public final class ImageFormat
```

Specifica il formato file dell'immagine. Non può essere esteso.
## Costruttori

| Costruttore | Descrizione |
| --- | --- |
| [ImageFormat(String guid)](#ImageFormat-java.lang.String-) | Inizializza una nuova istanza della classe ImageFormat utilizzando la stringa Guid specificata. |
## Metodi

| Metodo | Descrizione |
| --- | --- |
| [equals(Object o)](#equals-java.lang.Object-) | Restituisce un valore che indica se l'oggetto specificato è un oggetto ImageFormat equivalente a questo oggetto ImageFormat. |
| [getBmp()](#getBmp--) | Ottiene il formato immagine bitmap (BMP). |
| [getEmf()](#getEmf--) | Ottiene il formato immagine enhanced metafile (EMF). |
| [getExif()](#getExif--) | Ottiene il formato Exchangeable Image File (Exif). |
| [getGif()](#getGif--) | Ottiene il formato Graphics Interchange Format (GIF). |
| [getGuid()](#getGuid--) | Ottiene una stringa Guid che rappresenta questo oggetto ImageFormat. |
| [getIcon()](#getIcon--) | Ottiene il formato immagine icona Windows. |
| [getJpeg()](#getJpeg--) | Ottiene il formato Joint Photographic Experts Group (JPEG). |
| [getMemoryBmp()](#getMemoryBmp--) | Ottiene il formato di un bitmap in memoria. |
| [getPng()](#getPng--) | Ottiene il formato W3C Portable Network Graphics (PNG). |
| [getTiff()](#getTiff--) | Ottiene il formato Tagged Image File Format (TIFF). |
| [getWmf()](#getWmf--) | Ottiene il formato Windows metafile (WMF). |
| [hashCode()](#hashCode--) | Restituisce un valore di hash code che rappresenta questo oggetto. |
| [isBitmapType()](#isBitmapType--) | Determina se un formato immagine è di tipo bitmap. |
| [isMetafileType()](#isMetafileType--) | Determina se un formato immagine è di tipo metafile. |
| [isUnknownType()](#isUnknownType--) | Determina se un formato immagine è di tipo sconosciuto. |
| [toString()](#toString--) | Converte questo oggetto ImageFormat in una stringa leggibile dall'uomo. |
### ImageFormat(String guid) {#ImageFormat-java.lang.String-}
```
public ImageFormat(String guid)
```


Inizializza una nuova istanza della classe ImageFormat utilizzando la stringa Guid specificata.

**Parameters:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| guid | java.lang.String | La stringa Guid che specifica un particolare formato immagine. |

### equals(Object o) {#equals-java.lang.Object-}
```
public boolean equals(Object o)
```


Restituisce un valore che indica se l'oggetto specificato è un oggetto ImageFormat equivalente a questo oggetto ImageFormat.

**Parameters:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| o | java.lang.Object | L'oggetto da testare. |

**Returns:**
boolean - true se o è un oggetto ImageFormat equivalente a questo oggetto ImageFormat; altrimenti, false.
### getBmp() {#getBmp--}
```
public static ImageFormat getBmp()
```


Ottiene il formato immagine bitmap (BMP).

**Returns:**
[ImageFormat](../../com.aspose.tasks/imageformat) - An ImageFormat object that indicates the bitmap image format.
### getEmf() {#getEmf--}
```
public static ImageFormat getEmf()
```


Ottiene il formato immagine enhanced metafile (EMF).

**Returns:**
[ImageFormat](../../com.aspose.tasks/imageformat) - An ImageFormat object that indicates the enhanced metafile image format.
### getExif() {#getExif--}
```
public static ImageFormat getExif()
```


Ottiene il formato Exchangeable Image File (Exif).

**Returns:**
[ImageFormat](../../com.aspose.tasks/imageformat) - An ImageFormat object that indicates the Exif format.
### getGif() {#getGif--}
```
public static ImageFormat getGif()
```


Ottiene il formato Graphics Interchange Format (GIF).

**Returns:**
[ImageFormat](../../com.aspose.tasks/imageformat) - An ImageFormat object that indicates the GIF image format.
### getGuid() {#getGuid--}
```
public String getGuid()
```


Ottiene una stringa Guid che rappresenta questo oggetto ImageFormat.

**Returns:**
java.lang.String - Una stringa Guid che rappresenta questo oggetto ImageFormat.
### getIcon() {#getIcon--}
```
public static ImageFormat getIcon()
```


Ottiene il formato immagine icona Windows.

**Returns:**
[ImageFormat](../../com.aspose.tasks/imageformat) - An ImageFormat object that indicates the Windows icon image format.
### getJpeg() {#getJpeg--}
```
public static ImageFormat getJpeg()
```


Ottiene il formato Joint Photographic Experts Group (JPEG).

**Returns:**
[ImageFormat](../../com.aspose.tasks/imageformat) - An ImageFormat object that indicates the JPEG image format.
### getMemoryBmp() {#getMemoryBmp--}
```
public static ImageFormat getMemoryBmp()
```


Ottiene il formato di un bitmap in memoria.

**Returns:**
[ImageFormat](../../com.aspose.tasks/imageformat) - An ImageFormat object that indicates the format of a bitmap in memory.
### getPng() {#getPng--}
```
public static ImageFormat getPng()
```


Ottiene il formato W3C Portable Network Graphics (PNG).

**Returns:**
[ImageFormat](../../com.aspose.tasks/imageformat) - An ImageFormat object that indicates the PNG image format.
### getTiff() {#getTiff--}
```
public static ImageFormat getTiff()
```


Ottiene il formato Tagged Image File Format (TIFF).

**Returns:**
[ImageFormat](../../com.aspose.tasks/imageformat) - An ImageFormat object that indicates the TIFF image format.
### getWmf() {#getWmf--}
```
public static ImageFormat getWmf()
```


Ottiene il formato Windows metafile (WMF).

**Returns:**
[ImageFormat](../../com.aspose.tasks/imageformat) - An ImageFormat object that indicates the Windows metafile image format.
### hashCode() {#hashCode--}
```
public int hashCode()
```


Restituisce un valore di hash code che rappresenta questo oggetto.

**Returns:**
int - Un codice hash che rappresenta questo oggetto.
### isBitmapType() {#isBitmapType--}
```
public boolean isBitmapType()
```


Determina se un formato immagine è di tipo bitmap.

**Returns:**
boolean - true se un formato immagine è di tipo bitmap; altrimenti, false.
### isMetafileType() {#isMetafileType--}
```
public boolean isMetafileType()
```


Determina se un formato immagine è di tipo metafile.

**Returns:**
boolean - true se un formato immagine è di tipo metafile; altrimenti, false.
### isUnknownType() {#isUnknownType--}
```
public boolean isUnknownType()
```


Determina se un formato immagine è di tipo sconosciuto.

**Returns:**
boolean - true se un formato immagine è di tipo sconosciuto; altrimenti, false.
### toString() {#toString--}
```
public String toString()
```


Converte questo oggetto ImageFormat in una stringa leggibile dall'uomo.

**Returns:**
java.lang.String - Una stringa che rappresenta questo oggetto ImageFormat.
