---
title: "SaveTemplateOptions"
second_title: "Aspose.Tasks for Java API Reference"
description: "Consente di specificare opzioni aggiuntive quando si salva un progetto come modello."
type: docs
weight: 275
url: /it/java/com.aspose.tasks/savetemplateoptions/
---

**Inheritance:**
java.lang.Object
```
public class SaveTemplateOptions
```

Consente di specificare opzioni aggiuntive quando si salva un progetto come modello.
## Costruttori

| Costruttore | Descrizione |
| --- | --- |
| [SaveTemplateOptions()](#SaveTemplateOptions--) |  |
## Metodi

| Metodo | Descrizione |
| --- | --- |
| [getRemoveActualValues()](#getRemoveActualValues--) | Ottiene un valore che indica se tutti i valori effettivi da un modello di progetto devono essere rimossi. |
| [getRemoveBaselineValues()](#getRemoveBaselineValues--) | Ottiene un valore che indica se tutti i valori di baseline da un modello di progetto devono essere rimossi. |
| [getRemoveFixedCosts()](#getRemoveFixedCosts--) | Ottiene un valore che indica se tutti i costi fissi da un modello di progetto devono essere rimossi. |
| [getRemoveResourceRates()](#getRemoveResourceRates--) | Ottiene un valore che indica se le tariffe delle risorse da un modello di progetto devono essere rimosse. |
| [setRemoveActualValues(boolean value)](#setRemoveActualValues-boolean-) | Imposta un valore che indica se tutti i valori effettivi da un modello di progetto devono essere rimossi. |
| [setRemoveBaselineValues(boolean value)](#setRemoveBaselineValues-boolean-) | Imposta un valore che indica se tutti i valori di baseline da un modello di progetto devono essere rimossi. |
| [setRemoveFixedCosts(boolean value)](#setRemoveFixedCosts-boolean-) | Imposta un valore che indica se tutti i costi fissi da un modello di progetto devono essere rimossi. |
| [setRemoveResourceRates(boolean value)](#setRemoveResourceRates-boolean-) | Imposta un valore che indica se le tariffe delle risorse da un modello di progetto devono essere rimosse. |
### SaveTemplateOptions() {#SaveTemplateOptions--}
```
public SaveTemplateOptions()
```


### getRemoveActualValues() {#getRemoveActualValues--}
```
public final boolean getRemoveActualValues()
```


Ottiene un valore che indica se tutti i valori effettivi da un modello di progetto devono essere rimossi.

**Returns:**
boolean - un valore che indica se tutti i valori effettivi da un modello di progetto devono essere rimossi.
### getRemoveBaselineValues() {#getRemoveBaselineValues--}
```
public final boolean getRemoveBaselineValues()
```


Ottiene un valore che indica se tutti i valori di baseline da un modello di progetto devono essere rimossi.

**Returns:**
boolean - un valore che indica se tutti i valori di baseline da un modello di progetto devono essere rimossi.
### getRemoveFixedCosts() {#getRemoveFixedCosts--}
```
public final boolean getRemoveFixedCosts()
```


Ottiene un valore che indica se tutti i costi fissi da un modello di progetto devono essere rimossi.

**Returns:**
boolean - un valore che indica se tutti i costi fissi da un modello di progetto devono essere rimossi.
### getRemoveResourceRates() {#getRemoveResourceRates--}
```
public final boolean getRemoveResourceRates()
```


Ottiene un valore che indica se le tariffe delle risorse da un modello di progetto devono essere rimosse.

**Returns:**
boolean - un valore che indica se le tariffe delle risorse da un modello di progetto devono essere rimosse.
### setRemoveActualValues(boolean value) {#setRemoveActualValues-boolean-}
```
public final void setRemoveActualValues(boolean value)
```


Imposta un valore che indica se tutti i valori effettivi da un modello di progetto devono essere rimossi.

**Parameters:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| valore | boolean | un valore che indica se tutti i valori effettivi da un modello di progetto devono essere rimossi. |

### setRemoveBaselineValues(boolean value) {#setRemoveBaselineValues-boolean-}
```
public final void setRemoveBaselineValues(boolean value)
```


Imposta un valore che indica se tutti i valori di baseline da un modello di progetto devono essere rimossi.

**Parameters:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| valore | boolean | un valore che indica se tutti i valori di baseline da un modello di progetto devono essere rimossi. |

### setRemoveFixedCosts(boolean value) {#setRemoveFixedCosts-boolean-}
```
public final void setRemoveFixedCosts(boolean value)
```


Imposta un valore che indica se tutti i costi fissi da un modello di progetto devono essere rimossi.

**Parameters:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| valore | boolean | un valore che indica se tutti i costi fissi da un modello di progetto devono essere rimossi. |

### setRemoveResourceRates(boolean value) {#setRemoveResourceRates-boolean-}
```
public final void setRemoveResourceRates(boolean value)
```


Imposta un valore che indica se le tariffe delle risorse da un modello di progetto devono essere rimosse.

**Parameters:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| valore | boolean | un valore che indica se le tariffe delle risorse da un modello di progetto devono essere rimosse. |

