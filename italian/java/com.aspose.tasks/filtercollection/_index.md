---
title: "FilterCollection"
second_title: "Aspose.Tasks for Java API Reference"
description: "Contiene un elenco di oggetti."
type: docs
weight: 92
url: /it/java/com.aspose.tasks/filtercollection/
---

**Inheritance:**
java.lang.Object, java.util.AbstractCollection
```
public class FilterCollection extends AbstractCollection<Filter>
```

Contiene un elenco di oggetti [Filter](../../com.aspose.tasks/filter). Implementa l'interfaccia ICollection&lt;Filter&gt;.
## Metodi

| Metodo | Descrizione |
| --- | --- |
| [add(Filter item)](#add-com.aspose.tasks.Filter-) |  |
| [clear()](#clear--) | Rimuove tutti gli elementi da questa collezione (operazione opzionale). |
| [contains(Filter item)](#contains-com.aspose.tasks.Filter-) | Restituisce true se questa collezione contiene l'elemento specificato. |
| [copyTo(Filter[] array, int arrayIndex)](#copyTo-com.aspose.tasks.Filter---int-) | Copia gli elementi dall'array specificato a questa collezione a partire dall'indice specificato. |
| [iterator()](#iterator--) | Restituisce un iteratore sugli elementi contenuti in questa collezione. |
| [remove(Filter item)](#remove-com.aspose.tasks.Filter-) | Rimuove l'elemento specificato da questa collezione. |
| [size()](#size--) | Ottiene il numero di elementi contenuti in questa raccolta. |
| [toList()](#toList--) | Converte una raccolta di filtri in un elenco di oggetti `Filter`. |
### add(Filter item) {#add-com.aspose.tasks.Filter-}
```
public boolean add(Filter item)
```




**Parameters:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| item | [Filter](../../com.aspose.tasks/filter) |  |

**Returns:**
boolean
### clear() {#clear--}
```
public void clear()
```


Rimuove tutti gli elementi da questa collezione (operazione opzionale).

### contains(Filter item) {#contains-com.aspose.tasks.Filter-}
```
public final boolean contains(Filter item)
```


Restituisce true se questa collezione contiene l'elemento specificato.

**Parameters:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| item | [Filter](../../com.aspose.tasks/filter) | l'elemento specificato. |

**Returns:**
boolean - true se la collezione contiene l'elemento specificato.
### copyTo(Filter[] array, int arrayIndex) {#copyTo-com.aspose.tasks.Filter---int-}
```
public final void copyTo(Filter[] array, int arrayIndex)
```


Copia gli elementi dall'array specificato a questa collezione a partire dall'indice specificato.

**Parameters:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| array | [Filter\[\]](../../com.aspose.tasks/filter) | l'array monodimensionale specificato in cui copiare gli elementi |
| arrayIndex | int | l'indice basato su zero dell'array specificato a partire dal quale inizia la copia. |

### iterator() {#iterator--}
```
public Iterator<Filter> iterator()
```


Restituisce un iteratore sugli elementi contenuti in questa collezione.

**Returns:**
java.util.Iterator&lt;com.aspose.tasks.Filter&gt; - iteratore della raccolta.
### remove(Filter item) {#remove-com.aspose.tasks.Filter-}
```
public final boolean remove(Filter item)
```


Rimuove l'elemento specificato da questa collezione.

**Parameters:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| item | [Filter](../../com.aspose.tasks/filter) | l'elemento specificato. |

**Returns:**
boolean - true se l'operazione è riuscita.
### size() {#size--}
```
public final int size()
```


Ottiene il numero di elementi contenuti in questa raccolta.

**Returns:**
int - il numero di elementi contenuti in questa raccolta.
### toList() {#toList--}
```
public List<Filter> toList()
```


Converte una raccolta di filtri in un elenco di oggetti `Filter`.

**Returns:**
java.util.List&lt;com.aspose.tasks.Filter&gt; - elenco generico di oggetti `Filter`.
