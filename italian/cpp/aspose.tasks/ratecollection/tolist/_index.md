---
title: "Aspose::Tasks::RateCollection::ToList metodo"
linktitle: "ToList"
articleTitle: "ToList"
second_title: "Aspose.Tasks per C++"
description: "Converte l'oggetto RateCollection in un elenco di oggetti Rate."
type: docs
weight: 80
url: /it/cpp/aspose.tasks/ratecollection/tolist/
---

## ToList (1 of 2) {#tolist_1}

Converte l'oggetto RateCollection in un elenco di oggetti Rate.

**Returns:** List of Rate objects.

```cpp
ToList()
```

---

## ToList (2 of 2) {#tolist_2}

Converte l'oggetto RateCollection in un elenco di oggetti Rate filtrati per il tipo RateType specificato.

**Returns:** a list of Rate objects.

```cpp
ToList(RateType type)
```

| Parametro | Descrizione |
| --- | --- |
| type | Il tipo su cui filtrare. |

