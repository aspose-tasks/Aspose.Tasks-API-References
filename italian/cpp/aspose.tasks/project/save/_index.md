---
title: "Aspose::Tasks::Project::Save metodo"
linktitle: "Save"
articleTitle: "Save"
second_title: "Aspose.Tasks per C++"
description: "Salva il progetto su uno stream usando le opzioni di salvataggio specificate."
type: docs
weight: 1190
url: /it/cpp/aspose.tasks/project/save/
---

## Save (1 of 5) {#save_1}

Salva il progetto su uno stream usando le opzioni di salvataggio specificate.

**Returns:** void Aspose::Tasks::

```cpp
Save(const System::SharedPtr< System::IO::Stream > & stream, const System::SharedPtr< Saving::SimpleSaveOptions > & options)
```

| Parametro | Descrizione |
| --- | --- |
| stream | Il flusso. |
| options | Le opzioni di salvataggio. |

---

## Save (2 of 5) {#save_2}

Salva i dati del progetto nel flusso.

**Returns:** void Aspose::Tasks::

```cpp
Save(const System::SharedPtr< System::IO::Stream > & stream, Saving::SaveFileFormat format)
```

| Parametro | Descrizione |
| --- | --- |
| stream | Il flusso. |
| formato | il formato di file di salvataggio specificato. SaveFileFormat |

---

## Save (3 of 5) {#save_3}

Salva i dati del progetto nel file in formato mpp.

**Returns:** void Aspose::Tasks::

```cpp
Save(const System::String & filename)
```

| Parametro | Descrizione |
| --- | --- |
| nomefile | Il nome file. |

---

## Save (4 of 5) {#save_4}

Salva il documento in un file utilizzando le opzioni di salvataggio specificate.

**Returns:** void Aspose::Tasks::

```cpp
Save(const System::String & filename, const System::SharedPtr< Saving::SimpleSaveOptions > & options)
```

| Parametro | Descrizione |
| --- | --- |
| nomefile | Il nome file. |
| options | Le opzioni di salvataggio. |

---

## Save (5 of 5) {#save_5}

Salva i dati del progetto nel file.

**Returns:** void Aspose::Tasks::

```cpp
Save(const System::String & filename, Saving::SaveFileFormat format)
```

| Parametro | Descrizione |
| --- | --- |
| nomefile | Il nome file. |
| formato | Il formato di file di salvataggio. |

