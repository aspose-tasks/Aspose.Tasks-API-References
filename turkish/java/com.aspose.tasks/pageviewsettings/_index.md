---
title: "PageViewSettings"
second_title: "Aspose.Tasks for Java API Referansı"
description: "Proje görünümü için baskı ayarlarını temsil eder."
type: docs
weight: 183
url: /tr/java/com.aspose.tasks/pageviewsettings/
---

**Inheritance:**
java.lang.Object
```
public class PageViewSettings
```

Proje görünümü için baskı ayarlarını temsil eder.
## Yapıcılar

| Yapıcı | Açıklama |
| --- | --- |
| [PageViewSettings()](#PageViewSettings--) |  |
## Yöntemler

| Yöntem | Açıklama |
| --- | --- |
| [getFirstColumnsCount()](#getFirstColumnsCount--) | Tüm sayfalarda yazdırılacak ilk sütunların sayısını alır. |
| [getFitTimescaleToEndOfPage()](#getFitTimescaleToEndOfPage--) | Yazdırma sırasında zaman ölçeğini bir sayfanın sonuna sığdırıp sığdırmayacağını gösteren bir değeri alır. |
| [getPrintAllSheetColumns()](#getPrintAllSheetColumns--) | Bir görünümün tüm sayfa sütunlarını yazdırıp yazdırmayacağını gösteren bir değeri alır. |
| [getPrintBlankPages()](#getPrintBlankPages--) | Bir görünümün boş sayfalarını yazdırıp yazdırmayacağını gösteren bir değeri alır. |
| [getPrintFirstColumnsCountOnAllPages()](#getPrintFirstColumnsCountOnAllPages--) | Tüm sayfalarda belirli sayıda ilk sütunu yazdırıp yazdırmayacağını gösteren bir değeri alır. |
| [getPrintNotes()](#getPrintNotes--) | Notları yazdırıp yazdırmayacağını gösteren bir değeri alır. |
| [setFirstColumnsCount(int value)](#setFirstColumnsCount-int-) | Tüm sayfalarda yazdırılacak ilk sütunların sayısını ayarlar. |
| [setFitTimescaleToEndOfPage(boolean value)](#setFitTimescaleToEndOfPage-boolean-) | Yazdırma sırasında zaman ölçeğini bir sayfanın sonuna sığdırıp sığdırmayacağını gösteren bir değeri ayarlar. |
| [setPrintAllSheetColumns(boolean value)](#setPrintAllSheetColumns-boolean-) | Bir görünümdeki tüm sayfa sütunlarının yazdırılıp yazdırılmayacağını gösteren bir değeri ayarlar. |
| [setPrintBlankPages(boolean value)](#setPrintBlankPages-boolean-) | Bir görünümdeki boş sayfaların yazdırılıp yazdırılmayacağını gösteren bir değeri ayarlar. |
| [setPrintFirstColumnsCountOnAllPages(boolean value)](#setPrintFirstColumnsCountOnAllPages-boolean-) | Tüm sayfalarda belirli sayıda ilk sütunun yazdırılıp yazdırılmayacağını gösteren bir değeri ayarlar. |
| [setPrintNotes(boolean value)](#setPrintNotes-boolean-) | Notların yazdırılıp yazdırılmayacağını gösteren bir değeri ayarlar. |
### PageViewSettings() {#PageViewSettings--}
```
public PageViewSettings()
```


### getFirstColumnsCount() {#getFirstColumnsCount--}
```
public final int getFirstColumnsCount()
```


Tüm sayfalarda yazdırılacak ilk sütunların sayısını alır.

**Returns:**
int - tüm sayfalarda yazdırılacak ilk sütunların sayısı.
### getFitTimescaleToEndOfPage() {#getFitTimescaleToEndOfPage--}
```
public final boolean getFitTimescaleToEndOfPage()
```


Yazdırma sırasında zaman ölçeğini bir sayfanın sonuna sığdırıp sığdırmayacağını gösteren bir değeri alır.

**Returns:**
boolean - yazdırırken zaman ölçeğinin bir sayfanın sonuna sığdırılıp sığdırılmayacağını gösteren bir değer.
### getPrintAllSheetColumns() {#getPrintAllSheetColumns--}
```
public final boolean getPrintAllSheetColumns()
```


Bir görünümün tüm sayfa sütunlarını yazdırıp yazdırmayacağını gösteren bir değeri alır.

**Returns:**
boolean - bir görünümdeki tüm sayfa sütunlarının yazdırılıp yazdırılmayacağını gösteren bir değer.
### getPrintBlankPages() {#getPrintBlankPages--}
```
public final boolean getPrintBlankPages()
```


Bir görünümün boş sayfalarını yazdırıp yazdırmayacağını gösteren bir değeri alır.

**Returns:**
boolean - bir görünümdeki boş sayfaların yazdırılıp yazdırılmayacağını gösteren bir değer.
### getPrintFirstColumnsCountOnAllPages() {#getPrintFirstColumnsCountOnAllPages--}
```
public final boolean getPrintFirstColumnsCountOnAllPages()
```


Tüm sayfalarda belirli sayıda ilk sütunu yazdırıp yazdırmayacağını gösteren bir değeri alır.

**Returns:**
boolean - tüm sayfalarda belirli sayıda ilk sütunun yazdırılıp yazdırılmayacağını gösteren bir değer.
### getPrintNotes() {#getPrintNotes--}
```
public final boolean getPrintNotes()
```


Notları yazdırıp yazdırmayacağını gösteren bir değeri alır.

**Returns:**
boolean - notların yazdırılıp yazdırılmayacağını gösteren bir değer.
### setFirstColumnsCount(int value) {#setFirstColumnsCount-int-}
```
public final void setFirstColumnsCount(int value)
```


Tüm sayfalarda yazdırılacak ilk sütunların sayısını ayarlar.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| değer | int | tüm sayfalarda yazdırılacak ilk sütunların sayısı. |

### setFitTimescaleToEndOfPage(boolean value) {#setFitTimescaleToEndOfPage-boolean-}
```
public final void setFitTimescaleToEndOfPage(boolean value)
```


Yazdırma sırasında zaman ölçeğini bir sayfanın sonuna sığdırıp sığdırmayacağını gösteren bir değeri ayarlar.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| değer | boolean | yazdırırken zaman ölçeğinin bir sayfanın sonuna sığdırılıp sığdırılmayacağını gösteren bir değer. |

### setPrintAllSheetColumns(boolean value) {#setPrintAllSheetColumns-boolean-}
```
public final void setPrintAllSheetColumns(boolean value)
```


Bir görünümdeki tüm sayfa sütunlarının yazdırılıp yazdırılmayacağını gösteren bir değeri ayarlar.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| değer | boolean | bir görünümdeki tüm sayfa sütunlarının yazdırılıp yazdırılmayacağını gösteren bir değer. |

### setPrintBlankPages(boolean value) {#setPrintBlankPages-boolean-}
```
public final void setPrintBlankPages(boolean value)
```


Bir görünümdeki boş sayfaların yazdırılıp yazdırılmayacağını gösteren bir değeri ayarlar.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| değer | boolean | bir görünümdeki boş sayfaların yazdırılıp yazdırılmayacağını gösteren bir değer. |

### setPrintFirstColumnsCountOnAllPages(boolean value) {#setPrintFirstColumnsCountOnAllPages-boolean-}
```
public final void setPrintFirstColumnsCountOnAllPages(boolean value)
```


Tüm sayfalarda belirli sayıda ilk sütunun yazdırılıp yazdırılmayacağını gösteren bir değeri ayarlar.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| değer | boolean | tüm sayfalarda belirli sayıda ilk sütunun yazdırılıp yazdırılmayacağını gösteren bir değer. |

### setPrintNotes(boolean value) {#setPrintNotes-boolean-}
```
public final void setPrintNotes(boolean value)
```


Notların yazdırılıp yazdırılmayacağını gösteren bir değeri ayarlar.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| değer | boolean | notların yazdırılıp yazdırılmayacağını gösteren bir değer. |

