---
title: "ProjectFileInfo"
second_title: "Aspose.Tasks for Java API Referansı"
description: "Sınıf örneği, dosyanın oluşturulduğu Microsoft Project sürümü ve proje dosya formatı hakkında bilgi içerir."
type: docs
weight: 222
url: /tr/java/com.aspose.tasks/projectfileinfo/
---

**Inheritance:**
java.lang.Object

**All Implemented Interfaces:**
com.aspose.ms.System.IEquatable
```
public final class ProjectFileInfo implements System.IEquatable<ProjectFileInfo>
```

Sınıf örneği, dosyanın oluşturulduğu Microsoft Project sürümü ve proje dosya formatı hakkında bilgi içerir.

--------------------

Kütüphanenin proje dosyasını işleyebileceğini tanımlamak için CanRead özelliğini kullanın.
## Yöntemler

| Yöntem | Açıklama |
| --- | --- |
| [canRead()](#canRead--) | Aspose.Tasks'in proje dosyasını işleyip işleyemeyeceğini gösteren bir değeri alır. |
| [equals(ProjectFileInfo other)](#equals-com.aspose.tasks.ProjectFileInfo-) | Bu örneğin belirtilen nesneye eşit olup olmadığını belirten bir değeri döndürür. |
| [equals(Object obj)](#equals-java.lang.Object-) | Bu örneğin belirtilen nesneye eşit olup olmadığını belirten bir değeri döndürür. |
| [getProjectApplicationInfo()](#getProjectApplicationInfo--) | Proje dosyasının uygulama bilgilerini alır. |
| [getProjectFileFormat()](#getProjectFileFormat--) | Proje dosyasının formatını alır. |
| [hashCode()](#hashCode--) | Sınıfın [ProjectFileInfo](../../com.aspose/tasks/projectfileinfo) örneği için bir hash kod değeri döndürür. |
| [isPasswordProtected()](#isPasswordProtected--) | Bir projenin şifre korumalı olup olmadığını gösteren bir değeri alır. |
### canRead() {#canRead--}
```
public final boolean canRead()
```


Aspose.Tasks'in proje dosyasını işleyip işleyemeyeceğini gösteren bir değeri alır.

**Returns:**
boolean - bir değeri gösterir, Aspose'un tanımlayıp tanımlamadığını.
### equals(ProjectFileInfo other) {#equals-com.aspose.tasks.ProjectFileInfo-}
```
public final boolean equals(ProjectFileInfo other)
```


Bu örneğin belirtilen nesneye eşit olup olmadığını belirten bir değeri döndürür.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| other | [ProjectFileInfo](../../com.aspose.tasks/projectfileinfo) | Bu örnek ile karşılaştırılacak belirtilen nesne. |

**Returns:**
boolean - belirtilen ProjectFileInfo ve bu örnek eşit dosya formatına ve uygulama bilgisine sahipse true döndürür.
### equals(Object obj) {#equals-java.lang.Object-}
```
public boolean equals(Object obj)
```


Bu örneğin belirtilen nesneye eşit olup olmadığını belirten bir değeri döndürür.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| obj | java.lang.Object | Bu örnek ile karşılaştırılacak belirtilen nesne. |

**Returns:**
boolean - belirtilen ProjectFileInfo ve bu örnek eşit dosya formatına ve uygulama bilgisine sahipse true döndürür.
### getProjectApplicationInfo() {#getProjectApplicationInfo--}
```
public final int getProjectApplicationInfo()
```


Proje dosyasının uygulama bilgilerini alır.

**Returns:**
int - proje dosyası uygulama bilgisi.
### getProjectFileFormat() {#getProjectFileFormat--}
```
public final int getProjectFileFormat()
```


Proje dosyasının formatını alır.

**Returns:**
int - proje dosyası formatı.
### hashCode() {#hashCode--}
```
public int hashCode()
```


Sınıfın [ProjectFileInfo](../../com.aspose/tasks/projectfileinfo) örneği için bir hash kod değeri döndürür.

**Returns:**
int - bu nesne için bir karma kod değeri döndürür.
### isPasswordProtected() {#isPasswordProtected--}
```
public final boolean isPasswordProtected()
```


Bir projenin şifre korumalı olup olmadığını gösteren bir değeri alır.

**Returns:**
boolean - bir projenin şifre korumalı olup olmadığını gösteren değer.
