---
title: "GraphicalIndicatorCriteriaValue"
second_title: "Aspose.Tasks for Java API Referansı"
description: "Grafik göstergeler ölçütünün koşul kontrolünde kullanılan bir değeri temsil eder."
type: docs
weight: 117
url: /tr/java/com.aspose.tasks/graphicalindicatorcriteriavalue/
---

**Inheritance:**
java.lang.Object
```
public final class GraphicalIndicatorCriteriaValue
```

Grafik göstergeler ölçütünün koşul kontrolünde kullanılan bir değeri temsil eder.
## Yapıcılar

| Yapıcı | Açıklama |
| --- | --- |
| [GraphicalIndicatorCriteriaValue(BigDecimal value)](#GraphicalIndicatorCriteriaValue-java.math.BigDecimal-) | GraphicalIndicatorCriteriaValue sınıfının sabit BigDecimal değeriyle bir örneğini oluşturur. |
| [GraphicalIndicatorCriteriaValue(Date dateValue)](#GraphicalIndicatorCriteriaValue-java.util.Date-) | GraphicalIndicatorCriteriaValue sınıfının sabit Date değeriyle bir örneğini oluşturur. |
| [GraphicalIndicatorCriteriaValue(String textValue)](#GraphicalIndicatorCriteriaValue-java.lang.String-) | GraphicalIndicatorCriteriaValue sınıfının sabit String değeriyle bir örneğini oluşturur. |
| [GraphicalIndicatorCriteriaValue(Duration durationValue)](#GraphicalIndicatorCriteriaValue-com.aspose.tasks.Duration-) | GraphicalIndicatorCriteriaValue sınıfının sabit Duration değeriyle bir örneğini oluşturur. |
| [GraphicalIndicatorCriteriaValue(boolean flagValue)](#GraphicalIndicatorCriteriaValue-boolean-) | GraphicalIndicatorCriteriaValue sınıfının sabit bayrak (boolean) değeriyle bir örneğini oluşturur. |
## Yöntemler

| Yöntem | Açıklama |
| --- | --- |
| [createFieldLink(int field)](#createFieldLink-int-) | Belirtilen MS Project alanının değerini temsil eden GraphicalIndicatorCriteriaValue sınıfının bir örneğini oluşturur. |
| [getRawValue()](#getRawValue--) | Alan değerinin temel sabitini alır. |
| [isFieldLink()](#isFieldLink--) | Mevcut örneğin bir alan bağlantısı olup olmadığını alır (bir alanın değerini temsil eder). |
| [toString()](#toString--) | Geçerli nesneyi temsil eden bir dize döndürür. |
### GraphicalIndicatorCriteriaValue(BigDecimal value) {#GraphicalIndicatorCriteriaValue-java.math.BigDecimal-}
```
public GraphicalIndicatorCriteriaValue(BigDecimal value)
```


GraphicalIndicatorCriteriaValue sınıfının sabit BigDecimal değeriyle bir örneğini oluşturur.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| değer | java.math.BigDecimal | BigDecimal değeri |

### GraphicalIndicatorCriteriaValue(Date dateValue) {#GraphicalIndicatorCriteriaValue-java.util.Date-}
```
public GraphicalIndicatorCriteriaValue(Date dateValue)
```


GraphicalIndicatorCriteriaValue sınıfının sabit Date değeriyle bir örneğini oluşturur.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| dateValue | java.util.Date | Date değeri |

### GraphicalIndicatorCriteriaValue(String textValue) {#GraphicalIndicatorCriteriaValue-java.lang.String-}
```
public GraphicalIndicatorCriteriaValue(String textValue)
```


GraphicalIndicatorCriteriaValue sınıfının sabit String değeriyle bir örneğini oluşturur.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| textValue | java.lang.String | String değeri |

### GraphicalIndicatorCriteriaValue(Duration durationValue) {#GraphicalIndicatorCriteriaValue-com.aspose.tasks.Duration-}
```
public GraphicalIndicatorCriteriaValue(Duration durationValue)
```


GraphicalIndicatorCriteriaValue sınıfının sabit Duration değeriyle bir örneğini oluşturur.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| durationValue | [Duration](../../com.aspose.tasks/duration) | Duration değeri |

### GraphicalIndicatorCriteriaValue(boolean flagValue) {#GraphicalIndicatorCriteriaValue-boolean-}
```
public GraphicalIndicatorCriteriaValue(boolean flagValue)
```


GraphicalIndicatorCriteriaValue sınıfının sabit bayrak (boolean) değeriyle bir örneğini oluşturur.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| flagValue | boolean | bayrak (boolean) değeri |

### createFieldLink(int field) {#createFieldLink-int-}
```
public static GraphicalIndicatorCriteriaValue createFieldLink(int field)
```


Belirtilen MS Project alanının değerini temsil eden GraphicalIndicatorCriteriaValue sınıfının bir örneğini oluşturur.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| alan | int | belirtilen alan |

**Returns:**
[GraphicalIndicatorCriteriaValue](../../com.aspose.tasks/graphicalindicatorcriteriavalue) - instance of GraphicalIndicatorCriteriaValue class representing the value of the specified field
### getRawValue() {#getRawValue--}
```
public final Object getRawValue()
```


Alan değerinin temel sabitini alır.

**Returns:**
java.lang.Object - Field değerinin temel sabiti
### isFieldLink() {#isFieldLink--}
```
public final boolean isFieldLink()
```


Mevcut örneğin bir alan bağlantısı olup olmadığını alır (bir alanın değerini temsil eder).

**Returns:**
boolean - geçerli örneğin bir alan bağlantısı olup olmadığını (bir alanın değerini temsil eder)
### toString() {#toString--}
```
public String toString()
```


Geçerli nesneyi temsil eden bir dize döndürür.

**Returns:**
java.lang.String - geçerli nesneyi temsil eden bir dize
