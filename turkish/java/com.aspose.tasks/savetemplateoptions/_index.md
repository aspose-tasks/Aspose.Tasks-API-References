---
title: "SaveTemplateOptions"
second_title: "Aspose.Tasks for Java API Referansı"
description: "Bir projeyi şablon olarak kaydederken ek seçenekler belirtmeye izin verir."
type: docs
weight: 275
url: /tr/java/com.aspose.tasks/savetemplateoptions/
---

**Inheritance:**
java.lang.Object
```
public class SaveTemplateOptions
```

Bir projeyi şablon olarak kaydederken ek seçenekler belirtmeye izin verir.
## Yapıcılar

| Yapıcı | Açıklama |
| --- | --- |
| [SaveTemplateOptions()](#SaveTemplateOptions--) |  |
## Yöntemler

| Yöntem | Açıklama |
| --- | --- |
| [getRemoveActualValues()](#getRemoveActualValues--) | Bir proje şablonundaki tüm gerçek değerlerin kaldırılıp kaldırılmayacağını gösteren bir değer alınır. |
| [getRemoveBaselineValues()](#getRemoveBaselineValues--) | Bir proje şablonundaki tüm temel değerlerin kaldırılıp kaldırılmayacağını gösteren bir değer alınır. |
| [getRemoveFixedCosts()](#getRemoveFixedCosts--) | Bir proje şablonundaki tüm sabit maliyetlerin kaldırılıp kaldırılmayacağını gösteren bir değer alınır. |
| [getRemoveResourceRates()](#getRemoveResourceRates--) | Bir proje şablonundaki kaynak oranlarının kaldırılıp kaldırılmayacağını gösteren bir değer alınır. |
| [setRemoveActualValues(boolean value)](#setRemoveActualValues-boolean-) | Bir proje şablonundaki tüm gerçek değerlerin kaldırılıp kaldırılmayacağını belirten bir değer ayarlar. |
| [setRemoveBaselineValues(boolean value)](#setRemoveBaselineValues-boolean-) | Bir proje şablonundaki tüm temel değerlerin kaldırılıp kaldırılmayacağını belirten bir değer ayarlar. |
| [setRemoveFixedCosts(boolean value)](#setRemoveFixedCosts-boolean-) | Bir proje şablonundaki tüm sabit maliyetlerin kaldırılıp kaldırılmayacağını belirten bir değer ayarlar. |
| [setRemoveResourceRates(boolean value)](#setRemoveResourceRates-boolean-) | Bir proje şablonundan kaynak oranlarının kaldırılıp kaldırılmayacağını gösteren bir değeri ayarlar. |
### SaveTemplateOptions() {#SaveTemplateOptions--}
```
public SaveTemplateOptions()
```


### getRemoveActualValues() {#getRemoveActualValues--}
```
public final boolean getRemoveActualValues()
```


Bir proje şablonundaki tüm gerçek değerlerin kaldırılıp kaldırılmayacağını gösteren bir değer alınır.

**Returns:**
boolean - bir proje şablonundan tüm gerçek değerlerin kaldırılıp kaldırılmayacağını gösteren bir değer.
### getRemoveBaselineValues() {#getRemoveBaselineValues--}
```
public final boolean getRemoveBaselineValues()
```


Bir proje şablonundaki tüm temel değerlerin kaldırılıp kaldırılmayacağını gösteren bir değer alınır.

**Returns:**
boolean - bir proje şablonundan tüm temel değerlerin kaldırılıp kaldırılmayacağını gösteren bir değer.
### getRemoveFixedCosts() {#getRemoveFixedCosts--}
```
public final boolean getRemoveFixedCosts()
```


Bir proje şablonundaki tüm sabit maliyetlerin kaldırılıp kaldırılmayacağını gösteren bir değer alınır.

**Returns:**
boolean - bir proje şablonundan tüm sabit maliyetlerin kaldırılıp kaldırılmayacağını gösteren bir değer.
### getRemoveResourceRates() {#getRemoveResourceRates--}
```
public final boolean getRemoveResourceRates()
```


Bir proje şablonundaki kaynak oranlarının kaldırılıp kaldırılmayacağını gösteren bir değer alınır.

**Returns:**
boolean - bir proje şablonundan kaynak oranlarının kaldırılıp kaldırılmayacağını gösteren bir değer.
### setRemoveActualValues(boolean value) {#setRemoveActualValues-boolean-}
```
public final void setRemoveActualValues(boolean value)
```


Bir proje şablonundaki tüm gerçek değerlerin kaldırılıp kaldırılmayacağını belirten bir değer ayarlar.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| değer | boolean | bir proje şablonundan tüm gerçek değerlerin kaldırılıp kaldırılmayacağını gösteren bir değer. |

### setRemoveBaselineValues(boolean value) {#setRemoveBaselineValues-boolean-}
```
public final void setRemoveBaselineValues(boolean value)
```


Bir proje şablonundaki tüm temel değerlerin kaldırılıp kaldırılmayacağını belirten bir değer ayarlar.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| değer | boolean | bir proje şablonundan tüm temel değerlerin kaldırılıp kaldırılmayacağını gösteren bir değer. |

### setRemoveFixedCosts(boolean value) {#setRemoveFixedCosts-boolean-}
```
public final void setRemoveFixedCosts(boolean value)
```


Bir proje şablonundaki tüm sabit maliyetlerin kaldırılıp kaldırılmayacağını belirten bir değer ayarlar.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| değer | boolean | bir proje şablonundan tüm sabit maliyetlerin kaldırılıp kaldırılmayacağını gösteren bir değer. |

### setRemoveResourceRates(boolean value) {#setRemoveResourceRates-boolean-}
```
public final void setRemoveResourceRates(boolean value)
```


Bir proje şablonundan kaynak oranlarının kaldırılıp kaldırılmayacağını gösteren bir değeri ayarlar.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| değer | boolean | bir proje şablonundan kaynak oranlarının kaldırılıp kaldırılmayacağını gösteren bir değer. |

