---
title: "Aspose::Tasks::View::CompareTo metodu"
linktitle: "CompareTo"
articleTitle: "CompareTo"
second_title: "C++ için Aspose.Tasks"
description: "Mevcut örneği aynı tipteki başka bir nesneyle karşılaştırır ve mevcut örnek önce mi, sonra mı veya ..."
type: docs
weight: 10
url: /tr/cpp/aspose.tasks/view/compareto/
---

## CompareTo {#compareto}

Mevcut örneği aynı türdeki başka bir nesneyle karşılaştırır ve mevcut örneğin diğer nesneye göre sıralama düzeninde önce mi, sonra mı yoksa aynı konumda mı olduğunu belirten bir tam sayı döndürür.

**Returns:** A 32-bit signed integer that indicates the relative order of the objects being compared. The return value has these meanings: Value Meaning Less than zero This instance precedes other in the sort order. Zero This instance occurs in the same position in the sort order as other . Greater than zero This instance follows other in the sort order.

```cpp
CompareTo(System::SharedPtr< View > other)
```

| Parametre | Açıklama |
| --- | --- |
| other | bu örneği karşılaştırmak için belirtilen View nesnesi. |

