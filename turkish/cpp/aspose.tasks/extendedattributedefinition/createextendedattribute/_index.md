---
title: "Aspose::Tasks::ExtendedAttributeDefinition::CreateExtendedAttribute metodu"
linktitle: "CreateExtendedAttribute"
articleTitle: "CreateExtendedAttribute"
second_title: "C++ için Aspose.Tasks"
description: "Bu nesnenin alan kimliği değerine eşit olan alan kimliğiyle yeni bir genişletilmiş öznitelik oluşturur."
type: docs
weight: 20
url: /tr/cpp/aspose.tasks/extendedattributedefinition/createextendedattribute/
---

## CreateExtendedAttribute (1 of 7) {#createextendedattribute_1}

Bu nesnenin alan kimliği değerine eşit olan alan kimliğiyle yeni bir genişletilmiş öznitelik oluşturur.

**Returns:** returns created instance of the ExtendedAttribute class with the fieldID which equals to this object's fieldID value.

```cpp
CreateExtendedAttribute()
```

---

## CreateExtendedAttribute (2 of 7) {#createextendedattribute_2}

Bu nesnenin field ID değerine eşit olan field ID ve belirtilen bayrak değeri ile yeni bir genişletilmiş öznitelik oluşturur.

**Returns:** returns created instance of the ExtendedAttribute class with the fieldID which equals to this object's fieldID value.

```cpp
CreateExtendedAttribute(bool flagValue)
```

| Parametre | Açıklama |
| --- | --- |
| flagValue | Belirtilen bayrak değeri. |

---

## CreateExtendedAttribute (3 of 7) {#createextendedattribute_3}

Belirtilen Değer öğesiyle bağlantılı yeni bir genişletilmiş öznitelik oluşturur.

**Returns:** returns created instance of the ExtendedAttribute class linked with specified Value item.

```cpp
CreateExtendedAttribute(const System::SharedPtr< Value > & lookupValue)
```

| Parametre | Açıklama |
| --- | --- |
| lookupValue | Belirtilen Değer öğesi. |

---

## CreateExtendedAttribute (4 of 7) {#createextendedattribute_4}

Bu nesnenin field ID değerine eşit olan field ID ve belirtilen metin değeri ile yeni bir genişletilmiş öznitelik oluşturur.

**Returns:** returns created instance of the ExtendedAttribute class with the fieldID which equals to this object's fieldID value.

```cpp
CreateExtendedAttribute(const System::String & textValue)
```

| Parametre | Açıklama |
| --- | --- |
| textValue | Belirtilen metin değeri. |

---

## CreateExtendedAttribute (5 of 7) {#createextendedattribute_5}

Bu nesnenin field ID değerine eşit olan field ID ve belirtilen süre değeri ile yeni bir genişletilmiş öznitelik oluşturur.

**Returns:** returns created instance of the ExtendedAttribute class with the fieldID which equals to this object's fieldID value.

```cpp
CreateExtendedAttribute(Duration durationValue)
```

| Parametre | Açıklama |
| --- | --- |
| durationValue | Belirtilen süre değeri. |

---

## CreateExtendedAttribute (6 of 7) {#createextendedattribute_6}

Bu nesnenin field ID değerine eşit olan field ID ve belirtilen tarih değeri ile yeni bir genişletilmiş öznitelik oluşturur.

**Returns:** returns created instance of the ExtendedAttribute class with the fieldID which equals to this object's fieldID value.

```cpp
CreateExtendedAttribute(System::DateTime dateTimeValue)
```

| Parametre | Açıklama |
| --- | --- |
| dateTimeValue | Belirtilen tarih saat değeri. |

---

## CreateExtendedAttribute (7 of 7) {#createextendedattribute_7}

Bu nesnenin field ID değerine eşit olan field ID ve belirtilen sayısal değer ile yeni bir genişletilmiş öznitelik oluşturur.

**Returns:** returns created instance of the ExtendedAttribute class with the fieldID which equals to this object's fieldID value.

```cpp
CreateExtendedAttribute(System::Decimal numericValue)
```

| Parametre | Açıklama |
| --- | --- |
| numericValue | Belirtilen sayısal değer. |

