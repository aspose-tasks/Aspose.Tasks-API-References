---
title: "TableField"
second_title: "Référence API d'Aspose.Tasks pour Java"
description: "Représente un champ d'une table dans un projet."
type: docs
weight: 286
url: /fr/java/com.aspose.tasks/tablefield/
---

**Inheritance:**
java.lang.Object
```
public class TableField
```

Représente un champ d'une table dans un projet.
## Constructeurs

| Constructeur | Description |
| --- | --- |
| [TableField()](#TableField--) | Initialise une nouvelle instance de la classe [TableField](../../com.aspose.tasks/tablefield). |
## Méthodes

| Méthode | Description |
| --- | --- |
| [getAlignData()](#getAlignData--) | Obtient l'alignement des données dans un champ de tableau. |
| [getAlignTitle()](#getAlignTitle--) | Obtient l'alignement du titre dans un champ de tableau. |
| [getField()](#getField--) | Obtient le type d'un champ de tableau. |
| [getTitle()](#getTitle--) | Obtient le titre du champ dans un tableau. |
| [getWidth()](#getWidth--) | Obtient la largeur en points de la colonne du champ dans un tableau. |
| [getWrapHeader()](#getWrapHeader--) | Obtient une valeur indiquant si l'en-tête de colonne du tableau peut être renvoyé à la ligne sur plusieurs lignes, ou s'il doit être tronqué lorsqu'il dépasse la largeur de la colonne. |
| [getWrapText()](#getWrapText--) | Obtient une valeur indiquant si le texte de la colonne peut être renvoyé à la ligne sur plusieurs lignes, ou s'il doit être tronqué lorsqu'il dépasse la largeur de la colonne. |
| [setAlignData(int value)](#setAlignData-int-) | Définit l'alignement des données dans un champ de tableau. |
| [setAlignTitle(int value)](#setAlignTitle-int-) | Définit l'alignement du titre dans un champ de tableau. |
| [setField(int value)](#setField-int-) | Définit le type d'un champ de tableau. |
| [setTitle(String value)](#setTitle-java.lang.String-) | Définit le titre du champ dans un tableau. |
| [setWidth(int value)](#setWidth-int-) | Définit la largeur en points de la colonne du champ dans un tableau. |
| [setWrapHeader(boolean value)](#setWrapHeader-boolean-) | Définit une valeur indiquant si l'en-tête de colonne du tableau peut être renvoyé à la ligne sur plusieurs lignes, ou s'il doit être tronqué lorsqu'il dépasse la largeur de la colonne. |
| [setWrapText(boolean value)](#setWrapText-boolean-) | Définit une valeur indiquant si le texte de la colonne peut être renvoyé à la ligne sur plusieurs lignes, ou s'il doit être tronqué lorsqu'il dépasse la largeur de la colonne. |
### TableField() {#TableField--}
```
public TableField()
```


Initialise une nouvelle instance de la classe [TableField](../../com.aspose.tasks/tablefield).

### getAlignData() {#getAlignData--}
```
public final int getAlignData()
```


Obtient l'alignement des données dans un champ de tableau.

**Returns:**
int - l'alignement des données dans un champ de tableau.
### getAlignTitle() {#getAlignTitle--}
```
public final int getAlignTitle()
```


Obtient l'alignement du titre dans un champ de tableau.

**Returns:**
int - l'alignement du titre dans un champ de tableau.
### getField() {#getField--}
```
public final int getField()
```


Obtient le type d'un champ de tableau.

**Returns:**
int - le type d'un champ de tableau.
### getTitle() {#getTitle--}
```
public final String getTitle()
```


Obtient le titre du champ dans un tableau.

**Returns:**
java.lang.String - le titre du champ dans un tableau.
### getWidth() {#getWidth--}
```
public final int getWidth()
```


Obtient la largeur en points de la colonne du champ dans un tableau.

**Returns:**
int - la largeur en points de la colonne du champ dans un tableau.
### getWrapHeader() {#getWrapHeader--}
```
public final boolean getWrapHeader()
```


Obtient une valeur indiquant si l'en-tête de colonne du tableau peut être renvoyé à la ligne sur plusieurs lignes, ou s'il doit être tronqué lorsqu'il dépasse la largeur de la colonne.

**Returns:**
boolean - une valeur indiquant si l'en-tête de colonne du tableau peut être renvoyé à la ligne sur plusieurs lignes, ou s'il doit être tronqué lorsqu'il dépasse la largeur de la colonne.
### getWrapText() {#getWrapText--}
```
public final boolean getWrapText()
```


Obtient une valeur indiquant si le texte de la colonne peut être renvoyé à la ligne sur plusieurs lignes, ou s'il doit être tronqué lorsqu'il dépasse la largeur de la colonne.

--------------------

Pris en charge à partir de la version MSP 2010 et ultérieure.

**Returns:**
boolean - une valeur indiquant si le texte de la colonne peut être renvoyé à la ligne sur plusieurs lignes, ou s'il doit être tronqué lorsqu'il dépasse la largeur de la colonne.
### setAlignData(int value) {#setAlignData-int-}
```
public final void setAlignData(int value)
```


Définit l'alignement des données dans un champ de tableau.

**Parameters:**
| Paramètre | Type | Description |
| --- | --- | --- |
| valeur | int | l'alignement des données dans un champ de tableau. |

### setAlignTitle(int value) {#setAlignTitle-int-}
```
public final void setAlignTitle(int value)
```


Définit l'alignement du titre dans un champ de tableau.

**Parameters:**
| Paramètre | Type | Description |
| --- | --- | --- |
| valeur | int | l'alignement du titre dans un champ de tableau. |

### setField(int value) {#setField-int-}
```
public final void setField(int value)
```


Définit le type d'un champ de tableau.

**Parameters:**
| Paramètre | Type | Description |
| --- | --- | --- |
| valeur | int | le type d'un champ de tableau. |

### setTitle(String value) {#setTitle-java.lang.String-}
```
public final void setTitle(String value)
```


Définit le titre du champ dans un tableau.

**Parameters:**
| Paramètre | Type | Description |
| --- | --- | --- |
| valeur | java.lang.String | le titre du champ dans un tableau. |

### setWidth(int value) {#setWidth-int-}
```
public final void setWidth(int value)
```


Définit la largeur en points de la colonne du champ dans un tableau.

**Parameters:**
| Paramètre | Type | Description |
| --- | --- | --- |
| valeur | int | la largeur en points de la colonne du champ dans un tableau. |

### setWrapHeader(boolean value) {#setWrapHeader-boolean-}
```
public final void setWrapHeader(boolean value)
```


Définit une valeur indiquant si l'en-tête de colonne du tableau peut être renvoyé à la ligne sur plusieurs lignes, ou s'il doit être tronqué lorsqu'il dépasse la largeur de la colonne.

**Parameters:**
| Paramètre | Type | Description |
| --- | --- | --- |
| valeur | booléen | une valeur indiquant si l’en-tête de colonne du tableau peut s’enrouler sur plusieurs lignes, ou si elle doit être tronquée lorsqu’elle dépasse la largeur de la colonne. |

### setWrapText(boolean value) {#setWrapText-boolean-}
```
public final void setWrapText(boolean value)
```


Définit une valeur indiquant si le texte de la colonne peut être renvoyé à la ligne sur plusieurs lignes, ou s'il doit être tronqué lorsqu'il dépasse la largeur de la colonne.

--------------------

Pris en charge à partir de la version MSP 2010 et ultérieure.

**Parameters:**
| Paramètre | Type | Description |
| --- | --- | --- |
| valeur | booléen | une valeur indiquant si le texte de la colonne peut s’enrouler sur plusieurs lignes, ou si elle doit être tronquée lorsqu’elle dépasse la largeur de la colonne. |

