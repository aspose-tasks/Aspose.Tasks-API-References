---
title: "Enum TaskKey"
second_title: "Référence de l'API Aspose.Tasks for .NET"
description: "Enum Aspose.Tasks.TaskKey. Représente une liste de champs de tâche pris en charge"
type: docs
weight: 2400
url: /fr/net/aspose.tasks/taskkey/
---
## TaskKey enumeration

Représente une liste des champs de tâche pris en charge.

```csharp
public enum TaskKey : byte
```

### Valeurs

| Nom | Valeur | Description |
| --- | --- | --- |
| Uid | `0` | Représente le champ Unique ID (Task). |
| Id | `1` | Représente le champ Id (Task). |
| Name | `2` | Représente le champ Name (Task). |
| Type | `3` | Représente le champ Type (Task). |
| IsNull | `4` | Représente le champ IsNull (Task). |
| Created | `5` | Représente le champ Created (Task). |
| Contact | `6` | Représente le champ Contact (Task). |
| WBS | `7` | Représente le champ WBS (Task). |
| WBSLevel | `8` | Représente le champ WBSLevel (Task). |
| OutlineNumber | `9` | Représente le champ OutlineNumber (Task). |
| OutlineLevel | `10` | Représente le champ OutlineLevel (Task). |
| Priority | `11` | Représente le champ Priority (Task). |
| Start | `12` | Représente le champ Start (Task). |
| Finish | `13` | Représente le champ Finish (Task). |
| Duration | `14` | Représente le champ Duration (Task). |
| DurationVariance | `15` | Représente le champ DurationVariance (Task). |
| Work | `16` | Représente le champ Work (Task). |
| Stop | `17` | Représente le champ Stop (Task). |
| Resume | `18` | Représente le champ Resume (Task). |
| IsResumeValid | `19` | Représente le champ IsResumeValid (Task). |
| IsEffortDriven | `20` | Représente le champ IsEffortDriven (Task). |
| IsRecurring | `21` | Représente le champ IsRecurring (Task). |
| IsOverallocated | `22` | Représente le champ IsOverallocated (Task). |
| HasOverallocatedResource | `23` | Représente le champ HasOverallocatedResource (Task). |
| IsEstimated | `24` | Représente le champ IsEstimated (Task). |
| IsMilestone | `25` | Représente le champ IsMilestone (Task). |
| IsCritical | `26` | Représente le champ IsCritical (Task). |
| IsSubproject | `27` | Représente le champ IsSubproject (Task). |
| IsSubprojectReadOnly | `28` | Représente le champ IsSubprojectReadOnly (Task). |
| IsMarked | `29` | Représente le champ IsMarked (Task). |
| IgnoreWarnings | `30` | Représente le champ IgnoreWarnings (Task). |
| SubprojectName | `31` | Représente le champ SubprojectName (Task). |
| IsExternalTask | `32` | Représente le champ IsExternalTask (Task). |
| IsSummary | `33` | Représente le champ IsSummary (Task). |
| ExternalTaskProject | `34` | Représente le champ ExternalTaskProject (Task). |
| ExternalId | `35` | Représente le champ ExternalId (Task). |
| EarlyStart | `36` | Représente le champ EarlyStart (Task). |
| EarlyFinish | `37` | Représente le champ EarlyFinish (Task). |
| LateStart | `38` | Représente le champ LateStart (Task). |
| LateFinish | `39` | Représente le champ LateFinish (Task). |
| StartVariance | `40` | Représente le champ StartVariance (Task). |
| FinishVariance | `41` | Représente le champ FinishVariance (Task). |
| WorkVariance | `42` | Représente le champ WorkVariance (Task). |
| CostVariance | `43` | Représente le champ CostVariance (Task). |
| FreeSlack | `44` | Représente le champ FreeSlack (Task). |
| TotalSlack | `45` | Représente le champ TotalSlack (Task). |
| StartSlack | `46` | Représente le champ StartSlack (Task). |
| FinishSlack | `47` | Représente le champ FinishSlack (Task). |
| FixedCost | `48` | Représente le champ FixedCost (Task). |
| FixedCostAccrual | `49` | Représente le champ FixedCostAccrual (Task). |
| PercentComplete | `50` | Représente le champ PercentComplete (Task). |
| PercentWorkComplete | `51` | Représente le champ PercentWorkComplete (Task). |
| Cost | `52` | Représente le champ Cost (Task). |
| OvertimeCost | `53` | Représente le champ OvertimeCost (Task). |
| ActualStart | `54` | Représente le champ ActualStart (Task). |
| ActualFinish | `55` | Représente le champ ActualFinish (Task). |
| ActualDuration | `56` | Représente le champ ActualDuration (Task). |
| ActualCost | `57` | Représente le champ ActualCost (Task). |
| ActualOvertimeCost | `58` | Représente le champ ActualOvertimeCost (Task). |
| ActualWork | `59` | Représente le champ ActualWork (Task). |
| ActualOvertimeWork | `60` | Représente le champ ActualOvertimeWork (Task). |
| OvertimeWork | `61` | Représente le champ OvertimeWork (Task). |
| RegularWork | `62` | Représente le champ RegularWork (Task). |
| RemainingDuration | `63` | Représente le champ RemainingDuration (Task). |
| RemainingCost | `64` | Représente le champ RemainingCost (Task). |
| RemainingWork | `65` | Représente le champ RemainingWork (Task). |
| RemainingOvertimeWork | `66` | Représente le champ RemainingOvertimeWork (Task). |
| RemainingOvertimeCost | `67` | Représente le champ RemainingOvertimeCost (Task). |
| ACWP | `68` | Représente le champ ACWP (Task). |
| CV | `69` | Représente le champ CV (Task). |
| SV | `70` | Représente le champ SV (Task). |
| ConstraintType | `71` | Représente le champ ConstraintType (Task). |
| Calendar | `72` | Représente le champ Calendar (Task). |
| ConstraintDate | `73` | Représente le champ ConstraintDate (Task). |
| Deadline | `74` | Représente le champ Deadline (Task). |
| LevelAssignments | `75` | Représente le champ LevelAssignments (Task). |
| LevelingCanSplit | `76` | Représente le champ LevelingCanSplit (Task). |
| LevelingDelay | `77` | Représente le champ LevelingDelay (Task). |
| PreleveledStart | `78` | Représente le champ PreleveledStart (Task). |
| PreleveledFinish | `79` | Représente le champ PreleveledFinish (Task). |
| Hyperlink | `80` | Représente le champ Hyperlink (Task). |
| HyperlinkAddress | `81` | Représente le champ HyperlinkAddress (Task). |
| HyperlinkSubAddress | `82` | Représente le champ HyperlinkSubAddress (Task). |
| IgnoreResourceCalendar | `83` | Représente le champ IgnoreResourceCalendar (Task). |
| HideBar | `84` | Représente le champ HideBar (Task). |
| IsRollup | `85` | Représente le champ IsRollup (Task). |
| BCWS | `86` | Représente le champ BCWS (Task). |
| BCWP | `87` | Représente le champ BCWP (Task). |
| PhysicalPercentComplete | `88` | Représente le champ PhysicalPercentComplete (Task). |
| EarnedValueMethod | `89` | Représente le champ EarnedValueMethod (Task). |
| ActualWorkProtected | `90` | Représente le champ ActualWorkProtected (Task). |
| ActualOvertimeWorkProtected | `91` | Représente le champ ActualOvertimeWorkProtected (Task). |
| IsPublished | `92` | Représente le champ IsPublished (Task). |
| IsScheduled | `93` | Représente le champ IsScheduled (Task). |
| StatusManager | `94` | Représente le champ StatusManager (Task). |
| CommitmentStart | `95` | Représente le champ CommitmentStart (Task). |
| CommitmentFinish | `96` | Représente le champ CommitmentFinish (Task). |
| CommitmentType | `97` | Représente le champ CommitmentType (Task). |
| IsManual | `98` | Représente le champ IsManual (Task). |
| IsExpanded | `99` | Représente le champ IsExpanded (Task). |
| Guid | `100` | Représente le champ Guid (Task). |
| NotesText | `101` | Représente le champ NotesText (Task). |
| NotesRTF | `102` | Représente le champ NotesRTF (Task). |
| ManualStart | `103` | Représente le champ ManualStart (Task). |
| ManualFinish | `104` | Représente le champ ManualFinish (Task). |
| ManualDuration | `105` | Représente le champ ManualDuration (Task). |
| BudgetWork | `106` | Représente le champ BudgetWork (Task). |
| BudgetCost | `107` | Représente le champ BudgetCost (Task). |
| DisplayAsSummary | `108` | Représente le champ DisplayAsSummary (Task). |
| SummaryProgress | `109` | Représente le champ Summary Progress (Task). |
| IsActive | `110` | Représente le champ IsActive (Task). |
| StartText | `111` | Représente le champ StartText (Task). |
| FinishText | `112` | Représente le champ FinishText (Task). |
| DurationText | `113` | Représente le champ DurationText (Task). |
| CalendarUid | `114` | Représente le champ CalendarUid (Task). |
| ParentTaskUid | `115` | Représente le champ ParentTaskUid (Task). |
| DisplayOnTimeline | `116` | Représente le champ DisplayOnTimeline (Task). |
| TaskIsAssigned | `117` | Représente le champ TaskIsAssigned (Task). |
| TaskOriginalStart | `118` | Représente le champ départ original (Task). |
| TaskOriginalFinish | `119` | Représente le champ fin original (Task). |
| IsShowBeforeProjectStartDateWarning | `120` | Représente le drapeau qui fait que MSP affiche l'avertissement qu'une tâche commence avant la date de début du projet. |
| Warning | `121` | Représente le drapeau qui indique que la tâche a des écarts de planification. |
| ParentTaskGuid | `122` | Représente le champ ParentTaskGuid (Task). |
| ActivityId | `123` | Représente le champ ActivityId (uniquement applicable aux projets Primavera). |
| FreeSlackTimeSpan | `124` | Représente le champ FreeSlack (Task). |
| TotalSlackTimeSpan | `125` | Représente le champ TotalSlack (Task). |
| StartSlackTimeSpan | `126` | Représente le champ StartSlack (Task). |
| FinishSlackTimeSpan | `127` | Représente le champ FinishSlack (Task). |
| ExternalUid | `128` | Représente l'identifiant unique de la tâche externe. |

## Exemples

Montre comment lire/écrire les propriétés des tâches.

```csharp
var project = new Project();

// Ajouter une tâche et définir les propriétés de la tâche
var task = project.RootTask.Children.Add();
task.Set(Tsk.Name, "Task1");
task.Set(Tsk.Start, new DateTime(2020, 3, 31, 8, 0, 0));
task.Set(Tsk.Finish, new DateTime(2020, 3, 31, 17, 0, 0));

var collector = new ChildTasksCollector();
TaskUtils.Apply(project.RootTask, collector, 0);

// Analyser toutes les tâches collectées
foreach (var tsk in collector.Tasks)
{
    Console.WriteLine("Task Id: {0}", tsk.Get(Tsk.Id));
    Console.WriteLine("Task Uid: {0}", tsk.Get(Tsk.Uid));
    Console.WriteLine("Task Name: {0}", tsk.Get(Tsk.Name));
    Console.WriteLine("Task Start: {0}", tsk.Get(Tsk.Start));
    Console.WriteLine("Task Finish: {0}", tsk.Get(Tsk.Finish));
}
```

### Voir aussi

* namespace [Aspose.Tasks](../../aspose.tasks/)
* assembly [Aspose.Tasks](../../)


