---
title: "Aspose::Tasks::View::CompareTo méthode"
linktitle: "CompareTo"
articleTitle: "CompareTo"
second_title: "Aspose.Tasks pour C++"
description: "Compare l'instance actuelle avec un autre objet du même type et renvoie un entier qui indique si l'instance actuelle précède, suit, ou occu"
type: docs
weight: 10
url: /fr/cpp/aspose.tasks/view/compareto/
---

## CompareTo {#compareto}

Compare l'instance actuelle avec un autre objet du même type et renvoie un entier qui indique si l'instance actuelle précède, suit ou se trouve à la même position dans l'ordre de tri que l'autre objet.

**Returns:** A 32-bit signed integer that indicates the relative order of the objects being compared. The return value has these meanings: Value Meaning Less than zero This instance precedes other in the sort order. Zero This instance occurs in the same position in the sort order as other . Greater than zero This instance follows other in the sort order.

```cpp
CompareTo(System::SharedPtr< View > other)
```

| Paramètre | Description |
| --- | --- |
| autre | l'objet View spécifié auquel comparer cette instance. |

