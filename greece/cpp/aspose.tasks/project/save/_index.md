---
title: "Aspose::Tasks::Project::Save μέθοδος"
linktitle: "Save"
articleTitle: "Save"
second_title: "Aspose.Tasks για C++"
description: "Αποθηκεύει το έργο σε ένα stream χρησιμοποιώντας τις καθορισμένες save options."
type: docs
weight: 1190
url: /el/cpp/aspose.tasks/project/save/
---

## Save (1 of 5) {#save_1}

Αποθηκεύει το έργο σε ένα stream χρησιμοποιώντας τις καθορισμένες save options.

**Returns:** void Aspose::Tasks::

```cpp
Save(const System::SharedPtr< System::IO::Stream > & stream, const System::SharedPtr< Saving::SimpleSaveOptions > & options)
```

| Παράμετρος | Περιγραφή |
| --- | --- |
| stream | Η ροή. |
| options | Οι επιλογές αποθήκευσης. |

---

## Save (2 of 5) {#save_2}

Αποθηκεύει τα δεδομένα του έργου στη ροή.

**Returns:** void Aspose::Tasks::

```cpp
Save(const System::SharedPtr< System::IO::Stream > & stream, Saving::SaveFileFormat format)
```

| Παράμετρος | Περιγραφή |
| --- | --- |
| stream | Η ροή. |
| μορφή | η καθορισμένη μορφή αρχείου αποθήκευσης. SaveFileFormat |

---

## Save (3 of 5) {#save_3}

Αποθηκεύει τα δεδομένα του έργου στο αρχείο σε μορφή mpp.

**Returns:** void Aspose::Tasks::

```cpp
Save(const System::String & filename)
```

| Παράμετρος | Περιγραφή |
| --- | --- |
| όνομα αρχείου | Το όνομα αρχείου. |

---

## Save (4 of 5) {#save_4}

Αποθηκεύει το έγγραφο σε αρχείο χρησιμοποιώντας τις καθορισμένες επιλογές αποθήκευσης.

**Returns:** void Aspose::Tasks::

```cpp
Save(const System::String & filename, const System::SharedPtr< Saving::SimpleSaveOptions > & options)
```

| Παράμετρος | Περιγραφή |
| --- | --- |
| όνομα αρχείου | Το όνομα αρχείου. |
| options | Οι επιλογές αποθήκευσης. |

---

## Save (5 of 5) {#save_5}

Αποθηκεύει τα δεδομένα του έργου στο αρχείο.

**Returns:** void Aspose::Tasks::

```cpp
Save(const System::String & filename, Saving::SaveFileFormat format)
```

| Παράμετρος | Περιγραφή |
| --- | --- |
| όνομα αρχείου | Το όνομα αρχείου. |
| μορφή | Η μορφή αρχείου αποθήκευσης. |

