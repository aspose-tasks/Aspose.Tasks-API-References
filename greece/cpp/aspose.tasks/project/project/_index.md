---
title: "Aspose::Tasks::Project::Project κατασκευαστής"
linktitle: "Project"
articleTitle: "Project"
second_title: "Aspose.Tasks για C++"
description: "Αρχικοποιεί μια νέα παρουσία της κλάσης Project."
type: docs
weight: 10
url: /el/cpp/aspose.tasks/project/project/
---

## Project (1 of 13) {#project_1}

Αρχικοποιεί μια νέα παρουσία της κλάσης Project.

**Returns:** Aspose::Tasks::

```cpp
Project()
```

---

## Project (2 of 13) {#project_2}

Αρχικοποιεί ένα νέο αντικείμενο της κλάσης Project από ένα πρότυπο προστατευμένο με κωδικό (υπάρχον αρχείο mpp ή mpt).

**Returns:** Aspose::Tasks::

```cpp
Project(const System::String & projectTemplate, const System::String & protectionPassword)
```

| Παράμετρος | Περιγραφή |
| --- | --- |
| projectTemplate | Διαδρομή προς το πρότυπο από το οποίο θα δημιουργηθεί το έργο. |
| protectionPassword | Κωδικός προστασίας. |

---

## Project (3 of 13) {#project_3}

Αρχικοποιεί ένα νέο αντικείμενο της κλάσης Project από ένα πρότυπο (υπάρχον αρχείο mpp ή mpt).

**Returns:** Aspose::Tasks::

```cpp
Project(const System::String & projectTemplate)
```

| Παράμετρος | Περιγραφή |
| --- | --- |
| projectTemplate | Διαδρομή προς το πρότυπο από το οποίο θα δημιουργηθεί το έργο. |

---

## Project (4 of 13) {#project_4}

Αρχικοποιεί ένα νέο αντικείμενο της κλάσης Project από το Stream με την καθορισμένη παρουσία της κλάσης PrimaveraReadOptions.

**Returns:** Aspose::Tasks::

```cpp
Project(const System::SharedPtr< System::IO::Stream > & stream, const System::SharedPtr< PrimaveraReadOptions > & options)
```

| Παράμετρος | Περιγραφή |
| --- | --- |
| stream | Stream της κλάσης Project System::IO::Stream |
| options | η καθορισμένη παρουσία της κλάσης PrimaveraReadOptions που επιτρέπει την προσαρμογή της ανάγνωσης των μορφών Primavera (XER ή XML). |

---

## Project (5 of 13) {#project_5}

Αρχικοποιεί ένα νέο αντικείμενο της κλάσης Project από ένα πρότυπο (υπάρχον αρχείο mpp ή mpt).

**Returns:** Aspose::Tasks::

```cpp
Project(const System::String & projectTemplate, ParseErrorCallback parseErrorHandler)
```

| Παράμετρος | Περιγραφή |
| --- | --- |
| projectTemplate | Διαδρομή προς το πρότυπο από το οποίο θα δημιουργηθεί το έργο. |
| parseErrorHandler | η καθορισμένη μέθοδος callback για τη διαχείριση σφαλμάτων ανάλυσης xml. |

---

## Project (6 of 13) {#project_6}

Αρχικοποιεί ένα νέο αντικείμενο της κλάσης Project από ένα stream.

**Returns:** Aspose::Tasks::

```cpp
Project(const System::SharedPtr< System::IO::Stream > & stream)
```

| Παράμετρος | Περιγραφή |
| --- | --- |
| stream | Stream για φόρτωση προτύπου. |

---

## Project (7 of 13) {#project_7}

Αρχικοποιεί ένα νέο αντικείμενο της κλάσης Project από μια παρουσία StreamReader.

**Returns:** Aspose::Tasks::

```cpp
Project(const System::SharedPtr< System::IO::StreamReader > & reader)
```

| Παράμετρος | Περιγραφή |
| --- | --- |
| reader | Ο αναγνώστης ροής από τον οποίο θα φορτωθεί ένα πρότυπο. |

---

## Project (8 of 13) {#project_8}

Αρχικοποιεί ένα νέο αντικείμενο της κλάσης Project από ένα πρότυπο (υπάρχον αρχείο MPP ή MPT) με την καθορισμένη παρουσία της κλάσης PrimaveraReadOptions.

**Returns:** Aspose::Tasks::

```cpp
Project(const System::String & projectTemplate, const System::SharedPtr< PrimaveraReadOptions > & options)
```

| Παράμετρος | Περιγραφή |
| --- | --- |
| projectTemplate | Διαδρομή προς το πρότυπο από το οποίο θα δημιουργηθεί το έργο |
| options | η καθορισμένη παρουσία της κλάσης PrimaveraReadOptions. |

---

## Project (9 of 13) {#project_9}

Αρχικοποιεί ένα νέο αντικείμενο της κλάσης Project για να διαβάσει δεδομένα από μια βάση δεδομένων που καθορίζεται από το αντικείμενο της κλάσης DbSettings.

**Returns:** Aspose::Tasks::

```cpp
Project(const System::SharedPtr< Connectivity::DbSettings > & settings)
```

| Παράμετρος | Περιγραφή |
| --- | --- |
| ρυθμίσεις | το καθορισμένο αντικείμενο της κλάσης DbSettings. |

---

## Project (10 of 13) {#project_10}

Αρχικοποιεί ένα νέο αντικείμενο της κλάσης Project από ένα πρότυπο (υπάρχον αρχείο mpp ή mpt).

**Returns:** Aspose::Tasks::

```cpp
Project(const System::SharedPtr< System::IO::Stream > & stream, ParseErrorCallback parseErrorHandler)
```

| Παράμετρος | Περιγραφή |
| --- | --- |
| stream | Stream για φόρτωση προτύπου. |
| parseErrorHandler | η καθορισμένη μέθοδος callback για τη διαχείριση σφαλμάτων ανάλυσης xml. |

---

## Project (11 of 13) {#project_11}

Αρχικοποιεί ένα νέο αντικείμενο της κλάσης Project από ένα πρότυπο (υπάρχον αρχείο mpp ή mpt).

**Returns:** Aspose::Tasks::

```cpp
Project(const System::SharedPtr< System::IO::Stream > & stream, const System::String & protectionPassword)
```

| Παράμετρος | Περιγραφή |
| --- | --- |
| stream | Stream για φόρτωση προτύπου. |
| protectionPassword | Κωδικός προστασίας. |

---

## Project (12 of 13) {#project_12}

Αρχικοποιεί ένα νέο αντικείμενο της κλάσης Project από ένα πρότυπο (υπάρχον αρχείο mpp ή mpt) με το καθορισμένο αντικείμενο της κλάσης LoadOptions.

**Returns:** Aspose::Tasks::

```cpp
Project(const System::String & projectTemplate, const System::SharedPtr< LoadOptions > & options)
```

| Παράμετρος | Περιγραφή |
| --- | --- |
| projectTemplate | Διαδρομή προς το πρότυπο από το οποίο θα δημιουργηθεί το έργο |
| options | το καθορισμένο αντικείμενο της κλάσης LoadOptions. |

---

## Project (13 of 13) {#project_13}

Αρχικοποιεί ένα νέο αντικείμενο της κλάσης Project από το Stream με το καθορισμένο αντικείμενο της κλάσης LoadOptions.

**Returns:** Aspose::Tasks::

```cpp
Project(const System::SharedPtr< System::IO::Stream > & stream, const System::SharedPtr< LoadOptions > & options)
```

| Παράμετρος | Περιγραφή |
| --- | --- |
| stream | Stream της κλάσης Project System::IO::Stream |
| options | το καθορισμένο αντικείμενο της κλάσης LoadOptions |

