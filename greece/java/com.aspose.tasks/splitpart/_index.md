---
title: "SplitPart"
second_title: "Αναφορά API του Aspose.Tasks for Java"
description: "Αντιπροσωπεύει ένα τμήμα εργασίας."
type: docs
weight: 278
url: /el/java/com.aspose.tasks/splitpart/
---

**Inheritance:**
java.lang.Object
```
public class SplitPart
```

Αντιπροσωπεύει ένα τμήμα εργασίας. Το SplitPart είναι μέλος της συλλογής SplitParts της εργασίας.
## Μέθοδοι

| Μέθοδος | Περιγραφή |
| --- | --- |
| [equals(Object obj)](#equals-java.lang.Object-) | Συγκρίνει δύο τμήματα διαίρεσης. |
| [getFinish()](#getFinish--) | Λαμβάνει την ημερομηνία λήξης ενός SplitPart. |
| [getStart()](#getStart--) | Λαμβάνει την ημερομηνία έναρξης ενός SplitPart. |
| [hashCode()](#hashCode--) | Επιστρέφει μια τιμή κώδικα κατακερματισμού για το στιγμιότυπο της κλάσης [SplitPart](../../com.aspose/tasks/splitpart). |
### equals(Object obj) {#equals-java.lang.Object-}
```
public boolean equals(Object obj)
```


Συγκρίνει δύο τμήματα διαίρεσης.

**Parameters:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| obj | java.lang.Object | Αντικείμενο για σύγκριση. |

**Returns:**
boolean - Αληθές εάν το καθορισμένο αντικείμενο είναι ίσο με το τρέχον αντικείμενο· διαφορετικά, ψευδές.
### getFinish() {#getFinish--}
```
public final Date getFinish()
```


Λαμβάνει την ημερομηνία λήξης ενός SplitPart.

**Returns:**
java.util.Date - η ημερομηνία λήξης ενός SplitPart.
### getStart() {#getStart--}
```
public final Date getStart()
```


Λαμβάνει την ημερομηνία έναρξης ενός SplitPart.

**Returns:**
java.util.Date - η ημερομηνία έναρξης ενός SplitPart.
### hashCode() {#hashCode--}
```
public int hashCode()
```


Επιστρέφει μια τιμή κώδικα κατακερματισμού για το στιγμιότυπο της κλάσης [SplitPart](../../com.aspose/tasks/splitpart).

**Returns:**
int - επιστρέφει μια τιμή κώδικα κατακερματισμού για αυτό το αντικείμενο.
