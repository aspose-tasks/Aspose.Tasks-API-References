---
title: "BaselineCollection"
second_title: "Αναφορά API του Aspose.Tasks for Java"
description: "Αντιπροσωπεύει μια συλλογή από αντικείμενα."
type: docs
weight: 27
url: /el/java/com.aspose.tasks/baselinecollection/
---

**Inheritance:**
java.lang.Object, java.util.AbstractCollection, java.util.AbstractList, com.aspose.tasks.AbstractList
```
public class BaselineCollection extends AbstractList<Baseline>
```

Αντιπροσωπεύει μια συλλογή αντικειμένων [Baseline](../../com.aspose.tasks/baseline).
## Μέθοδοι

| Μέθοδος | Περιγραφή |
| --- | --- |
| [add(Baseline item)](#add-com.aspose.tasks.Baseline-) | Αυτή είναι η ψευδο-εφαρμογή της μεθόδου Add του ICollection, η οποία μόνο ρίχνει NotSupportedException. |
| [get(int index)](#get-int-) | Επιστρέφει το baseline στη καθορισμένη θέση. |
| [getParentResource()](#getParentResource--) | Αποκτά το γονικό [Resource](../../com.aspose.tasks/resource) για αυτή τη συλλογή. |
| [remove(int index)](#remove-int-) | Αφαιρεί το στοιχείο στη συγκεκριμένη θέση σε αυτή τη λίστα. |
| [remove(Object item)](#remove-java.lang.Object-) | Αφαιρεί το baseline από αυτή τη συλλογή. |
| [size()](#size--) | Αποκτά τον αριθμό των αντικειμένων που περιέχονται σε αυτό το αντικείμενο BaselineCollection. |
| [toList()](#toList--) | Μετατρέπει το αντικείμενο BaselineCollection σε λίστα αντικειμένων [Baseline](../../com.aspose.tasks/baseline). |
### add(Baseline item) {#add-com.aspose.tasks.Baseline-}
```
public final boolean add(Baseline item)
```


Αυτή είναι η ψευδο-εφαρμογή της μεθόδου Add του ICollection, η οποία μόνο ρίχνει NotSupportedException.

**Parameters:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| item | [Baseline](../../com.aspose.tasks/baseline) | Το στοιχείο προς αφαίρεση. |

**Returns:**
boolean - \{@inheritDoc\}
### get(int index) {#get-int-}
```
public Baseline get(int index)
```


Επιστρέφει το baseline στη καθορισμένη θέση.

**Parameters:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| index | int | δείκτης του στοιχείου που θα επιστραφεί. |

**Returns:**
[Baseline](../../com.aspose.tasks/baseline) - the baseline at the specified position.
### getParentResource() {#getParentResource--}
```
public final Resource getParentResource()
```


Αποκτά το γονικό [Resource](../../com.aspose.tasks/resource) για αυτή τη συλλογή.

**Returns:**
[Resource](../../com.aspose.tasks/resource) - the parent [Resource](../../com.aspose.tasks/resource) for this collection.
### remove(int index) {#remove-int-}
```
public Baseline remove(int index)
```


Αφαιρεί το στοιχείο στη συγκεκριμένη θέση σε αυτή τη λίστα.

**Parameters:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| index | int | \\{@inheritDoc\\} |

**Returns:**
[Baseline](../../com.aspose.tasks/baseline) - \{@inheritDoc\}
### remove(Object item) {#remove-java.lang.Object-}
```
public final boolean remove(Object item)
```


Αφαιρεί το baseline από αυτή τη συλλογή.

**Parameters:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| item | java.lang.Object | [Baseline](../../com.aspose.tasks/baseline) αντίγραφο για αφαίρεση. |

**Returns:**
boolean - true εάν το αντίγραφο [Baseline](../../com.aspose.tasks/baseline) έχει αφαιρεθεί επιτυχώς; διαφορετικά, false
### size() {#size--}
```
public final int size()
```


Αποκτά τον αριθμό των αντικειμένων που περιέχονται σε αυτό το αντικείμενο BaselineCollection.

**Returns:**
int - ο αριθμός των αντικειμένων που περιέχονται σε αυτό το αντικείμενο BaselineCollection.
### toList() {#toList--}
```
public final List<Baseline> toList()
```


Μετατρέπει το αντικείμενο BaselineCollection σε λίστα αντικειμένων [Baseline](../../com.aspose.tasks/baseline).

**Returns:**
java.util.List&lt;com.aspose.tasks.Baseline&gt; - Λίστα αντικειμένων [Baseline](../../com.aspose.tasks/baseline).
