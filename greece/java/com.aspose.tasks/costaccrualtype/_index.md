---
title: "CostAccrualType"
second_title: "Αναφορά API του Aspose.Tasks for Java"
description: "Καθορίζει τον τύπο ενός κόστους συσσωμάτωσης."
type: docs
weight: 54
url: /el/java/com.aspose.tasks/costaccrualtype/
---

**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class CostAccrualType extends System.Enum
```

Καθορίζει τον τύπο ενός κόστους συσσωμάτωσης.

--------------------

Κατά την εξαγωγή σε XML, οι τιμές Undefined θα αφαιρεθούν από το τελικό XML.
## Πεδία

| Πεδίο | Περιγραφή |
| --- | --- |
| [End](#End) | Δείχνει τον τύπο κατανομής κόστους End. |
| [Invalid](#Invalid) | Δείχνει τον τύπο κατανομής κόστους Invalid. |
| [Prorated](#Prorated) | Δείχνει τον τύπο κατανομής κόστους Prorated. |
| [Start](#Start) | Δείχνει τον τύπο συσσώρευσης κόστους Έναρξης. |
| [Undefined](#Undefined) | Δείχνει ότι η μη ορισμένη τιμή σημαίνει ότι το πεδίο δεν ορίστηκε στο αρχικό αρχείο έργου. |
### End {#End}
```
public static final int End
```


Δείχνει τον τύπο κατανομής κόστους End.

### Invalid {#Invalid}
```
public static final int Invalid
```


Δείχνει τον τύπο κατανομής κόστους Invalid.

### Prorated {#Prorated}
```
public static final int Prorated
```


Δείχνει τον τύπο κατανομής κόστους Prorated.

### Start {#Start}
```
public static final int Start
```


Δείχνει τον τύπο συσσώρευσης κόστους Έναρξης.

### Undefined {#Undefined}
```
public static final int Undefined
```


Δείχνει ότι η μη ορισμένη τιμή σημαίνει ότι το πεδίο δεν ορίστηκε στο αρχικό αρχείο έργου.

