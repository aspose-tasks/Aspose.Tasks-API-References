---
title: "ByYearWeekDayRepetition"
second_title: "Αναφορά API του Aspose.Tasks for Java"
description: "Αναπαριστά ένα μοτίβο που βασίζεται στη θέση μιας ημέρας της εβδομάδας σε μήνα."
type: docs
weight: 38
url: /el/java/com.aspose.tasks/byyearweekdayrepetition/
---

**Inheritance:**
java.lang.Object, [com.aspose.tasks.YearlyRepetitionBase](../../com.aspose.tasks/yearlyrepetitionbase)
```
public class ByYearWeekDayRepetition extends YearlyRepetitionBase
```

Αναπαριστά ένα μοτίβο που βασίζεται στη θέση μιας ημέρας της εβδομάδας σε μήνα.
## Κατασκευαστές

| Κατασκευαστής | Περιγραφή |
| --- | --- |
| [ByYearWeekDayRepetition()](#ByYearWeekDayRepetition--) | Αρχικοποιεί ένα νέο στιγμιότυπο της κλάσης [ByYearWeekDayRepetition](../../com.aspose.tasks/byyearweekdayrepetition). |
## Μέθοδοι

| Μέθοδος | Περιγραφή |
| --- | --- |
| [getMonth()](#getMonth--) | Λαμβάνει τον μήνα στον οποίο η εργασία πρέπει να επαναλαμβάνεται. |
| [getPosition()](#getPosition--) | Λαμβάνει τη θέση της ημέρας σε μια εβδομάδα ενός μήνα στην οποία η εργασία πρέπει να επαναλαμβάνεται. |
| [getWeekDay()](#getWeekDay--) | Λαμβάνει τον τύπο της ημέρας της εβδομάδας στην οποία η εργασία πρέπει να επαναλαμβάνεται. |
| [setMonth(int value)](#setMonth-int-) | Ορίζει τον μήνα στον οποίο η εργασία πρέπει να επαναλαμβάνεται. |
| [setPosition(int value)](#setPosition-int-) | Ορίζει τη θέση της ημέρας σε μια εβδομάδα ενός μήνα στην οποία η εργασία πρέπει να επαναλαμβάνεται. |
| [setWeekDay(int value)](#setWeekDay-int-) | Ορίζει τον τύπο της ημέρας της εβδομάδας στην οποία η εργασία πρέπει να επαναλαμβάνεται. |
### ByYearWeekDayRepetition() {#ByYearWeekDayRepetition--}
```
public ByYearWeekDayRepetition()
```


Αρχικοποιεί ένα νέο στιγμιότυπο της κλάσης [ByYearWeekDayRepetition](../../com.aspose.tasks/byyearweekdayrepetition).

### getMonth() {#getMonth--}
```
public final int getMonth()
```


Λαμβάνει τον μήνα στον οποίο η εργασία πρέπει να επαναλαμβάνεται.

Ανάγνωση/Εγγραφή `Month`.

**Returns:**
int - ένας μήνας στον οποίο η εργασία πρέπει να επαναλαμβάνεται.
### getPosition() {#getPosition--}
```
public final int getPosition()
```


Λαμβάνει τη θέση της ημέρας σε μια εβδομάδα ενός μήνα στην οποία η εργασία πρέπει να επαναλαμβάνεται.

Ανάγνωση/Εγγραφή `OrdinalNumber`.

**Returns:**
int - θέση της ημέρας σε μια εβδομάδα ενός μήνα στην οποία η εργασία πρέπει να επαναλαμβάνεται.
### getWeekDay() {#getWeekDay--}
```
public final int getWeekDay()
```


Λαμβάνει τον τύπο της ημέρας της εβδομάδας στην οποία η εργασία πρέπει να επαναλαμβάνεται.

Ανάγνωση/Εγγραφή `DayOfWeek`.

**Returns:**
int - τύπος ημέρας της εβδομάδας στην οποία η εργασία πρέπει να επαναλαμβάνεται.
### setMonth(int value) {#setMonth-int-}
```
public final void setMonth(int value)
```


Ορίζει τον μήνα στον οποίο η εργασία πρέπει να επαναλαμβάνεται.

Ανάγνωση/Εγγραφή `Month`.

**Parameters:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| τιμή | int | μήνας στον οποίο η εργασία πρέπει να επαναλαμβάνεται. |

### setPosition(int value) {#setPosition-int-}
```
public final void setPosition(int value)
```


Ορίζει τη θέση της ημέρας σε μια εβδομάδα ενός μήνα στην οποία η εργασία πρέπει να επαναλαμβάνεται.

Ανάγνωση/Εγγραφή `OrdinalNumber`.

**Parameters:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| τιμή | int | θέση της ημέρας σε μια εβδομάδα ενός μήνα στην οποία η εργασία πρέπει να επαναλαμβάνεται. |

### setWeekDay(int value) {#setWeekDay-int-}
```
public final void setWeekDay(int value)
```


Ορίζει τον τύπο της ημέρας της εβδομάδας στην οποία η εργασία πρέπει να επαναλαμβάνεται.

Ανάγνωση/Εγγραφή `DayOfWeek`.

**Parameters:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| τιμή | int | τύπος ημέρας της εβδομάδας στην οποία η εργασία πρέπει να επαναλαμβάνεται. |

