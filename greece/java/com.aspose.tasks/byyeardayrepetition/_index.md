---
title: "ByYearDayRepetition"
second_title: "Αναφορά API του Aspose.Tasks for Java"
description: "Αναπαριστά ένα μοτίβο που βασίζεται στην απόλυτη θέση μιας ημέρας σε μήνα."
type: docs
weight: 37
url: /el/java/com.aspose.tasks/byyeardayrepetition/
---

**Inheritance:**
java.lang.Object, [com.aspose.tasks.YearlyRepetitionBase](../../com.aspose.tasks/yearlyrepetitionbase)
```
public class ByYearDayRepetition extends YearlyRepetitionBase
```

Αναπαριστά ένα μοτίβο που βασίζεται στην απόλυτη θέση μιας ημέρας σε μήνα.
## Κατασκευαστές

| Κατασκευαστής | Περιγραφή |
| --- | --- |
| [ByYearDayRepetition()](#ByYearDayRepetition--) | Αρχικοποιεί ένα νέο αντικείμενο της [ByYearDayRepetition](../../com.aspose.tasks/byyeardayrepetition) class. |
## Μέθοδοι

| Μέθοδος | Περιγραφή |
| --- | --- |
| [getDayPosition()](#getDayPosition--) | Λαμβάνει τη θέση της ημέρας σε ένα μήνα στην οποία η εργασία πρέπει να επαναλαμβάνεται. |
| [getMonth()](#getMonth--) | Λαμβάνει τον μήνα στον οποίο η εργασία πρέπει να επαναλαμβάνεται. |
| [setDayPosition(int value)](#setDayPosition-int-) | Ορίζει τη θέση της ημέρας σε ένα μήνα στην οποία η εργασία πρέπει να επαναλαμβάνεται. |
| [setMonth(int value)](#setMonth-int-) | Ορίζει τον μήνα στον οποίο η εργασία πρέπει να επαναλαμβάνεται. |
### ByYearDayRepetition() {#ByYearDayRepetition--}
```
public ByYearDayRepetition()
```


Αρχικοποιεί ένα νέο αντικείμενο της [ByYearDayRepetition](../../com.aspose.tasks/byyeardayrepetition) class.

### getDayPosition() {#getDayPosition--}
```
public final int getDayPosition()
```


Λαμβάνει τη θέση της ημέρας σε ένα μήνα στην οποία η εργασία πρέπει να επαναλαμβάνεται.

**Returns:**
int - θέση ημέρας σε μήνα στην οποία η εργασία πρέπει να επαναλαμβάνεται.
### getMonth() {#getMonth--}
```
public final int getMonth()
```


Λαμβάνει τον μήνα στον οποίο η εργασία πρέπει να επαναλαμβάνεται.

Ανάγνωση/Εγγραφή `Month`.

**Returns:**
int - ένας μήνας στον οποίο η εργασία πρέπει να επαναλαμβάνεται.
### setDayPosition(int value) {#setDayPosition-int-}
```
public final void setDayPosition(int value)
```


Ορίζει τη θέση της ημέρας σε ένα μήνα στην οποία η εργασία πρέπει να επαναλαμβάνεται.

**Parameters:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| τιμή | int | θέση ημέρας σε μήνα στην οποία η εργασία πρέπει να επαναλαμβάνεται. |

### setMonth(int value) {#setMonth-int-}
```
public final void setMonth(int value)
```


Ορίζει τον μήνα στον οποίο η εργασία πρέπει να επαναλαμβάνεται.

Ανάγνωση/Εγγραφή `Month`.

**Parameters:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| τιμή | int | μήνας στον οποίο η εργασία πρέπει να επαναλαμβάνεται. |

