---
title: "ImageFormat"
second_title: "Αναφορά API του Aspose.Tasks for Java"
description: "Καθορίζει τη μορφή αρχείου της εικόνας."
type: docs
weight: 133
url: /el/java/com.aspose.tasks/imageformat/
---

**Inheritance:**
java.lang.Object
```
public final class ImageFormat
```

Καθορίζει τη μορφή αρχείου της εικόνας. Δεν μπορεί να επεκταθεί.
## Κατασκευαστές

| Κατασκευαστής | Περιγραφή |
| --- | --- |
| [ImageFormat(String guid)](#ImageFormat-java.lang.String-) | Αρχικοποιεί ένα νέο αντικείμενο της κλάσης ImageFormat χρησιμοποιώντας τη συγκεκριμένη συμβολοσειρά Guid. |
## Μέθοδοι

| Μέθοδος | Περιγραφή |
| --- | --- |
| [equals(Object o)](#equals-java.lang.Object-) | Επιστρέφει μια τιμή που υποδεικνύει εάν το συγκεκριμένο αντικείμενο είναι ένα αντικείμενο ImageFormat που είναι ισοδύναμο με αυτό το αντικείμενο ImageFormat. |
| [getBmp()](#getBmp--) | Λαμβάνει τη μορφή εικόνας bitmap (BMP). |
| [getEmf()](#getEmf--) | Λαμβάνει τη μορφή εικόνας enhanced metafile (EMF). |
| [getExif()](#getExif--) | Λαμβάνει τη μορφή Exchangeable Image File (Exif). |
| [getGif()](#getGif--) | Λαμβάνει τη μορφή Graphics Interchange Format (GIF). |
| [getGuid()](#getGuid--) | Λαμβάνει μια συμβολοσειρά Guid που αντιπροσωπεύει αυτό το αντικείμενο ImageFormat. |
| [getIcon()](#getIcon--) | Λαμβάνει τη μορφή εικόνας εικονιδίου Windows. |
| [getJpeg()](#getJpeg--) | Λαμβάνει τη μορφή Joint Photographic Experts Group (JPEG). |
| [getMemoryBmp()](#getMemoryBmp--) | Λαμβάνει τη μορφή ενός bitmap στη μνήμη. |
| [getPng()](#getPng--) | Λαμβάνει τη μορφή W3C Portable Network Graphics (PNG). |
| [getTiff()](#getTiff--) | Λαμβάνει τη μορφή Tagged Image File Format (TIFF). |
| [getWmf()](#getWmf--) | Λαμβάνει τη μορφή Windows metafile (WMF). |
| [hashCode()](#hashCode--) | Επιστρέφει μια τιμή κώδικα κατακερματισμού που αντιπροσωπεύει αυτό το αντικείμενο. |
| [isBitmapType()](#isBitmapType--) | Καθορίζει εάν μια μορφή εικόνας είναι τύπου bitmap. |
| [isMetafileType()](#isMetafileType--) | Καθορίζει εάν μια μορφή εικόνας είναι τύπου metafile. |
| [isUnknownType()](#isUnknownType--) | Καθορίζει εάν μια μορφή εικόνας είναι άγνωστος τύπος. |
| [toString()](#toString--) | Μετατρέπει αυτό το αντικείμενο ImageFormat σε αναγνώσιμη από άνθρωπο συμβολοσειρά. |
### ImageFormat(String guid) {#ImageFormat-java.lang.String-}
```
public ImageFormat(String guid)
```


Αρχικοποιεί ένα νέο αντικείμενο της κλάσης ImageFormat χρησιμοποιώντας τη συγκεκριμένη συμβολοσειρά Guid.

**Parameters:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| guid | java.lang.String | Η συμβολοσειρά Guid που καθορίζει μια συγκεκριμένη μορφή εικόνας. |

### equals(Object o) {#equals-java.lang.Object-}
```
public boolean equals(Object o)
```


Επιστρέφει μια τιμή που υποδεικνύει εάν το συγκεκριμένο αντικείμενο είναι ένα αντικείμενο ImageFormat που είναι ισοδύναμο με αυτό το αντικείμενο ImageFormat.

**Parameters:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| o | java.lang.Object | Το αντικείμενο προς δοκιμή. |

**Returns:**
boolean - true εάν το o είναι ένα αντικείμενο ImageFormat που είναι ισοδύναμο με αυτό το αντικείμενο ImageFormat· διαφορετικά, false.
### getBmp() {#getBmp--}
```
public static ImageFormat getBmp()
```


Λαμβάνει τη μορφή εικόνας bitmap (BMP).

**Returns:**
[ImageFormat](../../com.aspose.tasks/imageformat) - An ImageFormat object that indicates the bitmap image format.
### getEmf() {#getEmf--}
```
public static ImageFormat getEmf()
```


Λαμβάνει τη μορφή εικόνας enhanced metafile (EMF).

**Returns:**
[ImageFormat](../../com.aspose.tasks/imageformat) - An ImageFormat object that indicates the enhanced metafile image format.
### getExif() {#getExif--}
```
public static ImageFormat getExif()
```


Λαμβάνει τη μορφή Exchangeable Image File (Exif).

**Returns:**
[ImageFormat](../../com.aspose.tasks/imageformat) - An ImageFormat object that indicates the Exif format.
### getGif() {#getGif--}
```
public static ImageFormat getGif()
```


Λαμβάνει τη μορφή Graphics Interchange Format (GIF).

**Returns:**
[ImageFormat](../../com.aspose.tasks/imageformat) - An ImageFormat object that indicates the GIF image format.
### getGuid() {#getGuid--}
```
public String getGuid()
```


Λαμβάνει μια συμβολοσειρά Guid που αντιπροσωπεύει αυτό το αντικείμενο ImageFormat.

**Returns:**
java.lang.String - Μια συμβολοσειρά Guid που αντιπροσωπεύει αυτό το αντικείμενο ImageFormat.
### getIcon() {#getIcon--}
```
public static ImageFormat getIcon()
```


Λαμβάνει τη μορφή εικόνας εικονιδίου Windows.

**Returns:**
[ImageFormat](../../com.aspose.tasks/imageformat) - An ImageFormat object that indicates the Windows icon image format.
### getJpeg() {#getJpeg--}
```
public static ImageFormat getJpeg()
```


Λαμβάνει τη μορφή Joint Photographic Experts Group (JPEG).

**Returns:**
[ImageFormat](../../com.aspose.tasks/imageformat) - An ImageFormat object that indicates the JPEG image format.
### getMemoryBmp() {#getMemoryBmp--}
```
public static ImageFormat getMemoryBmp()
```


Λαμβάνει τη μορφή ενός bitmap στη μνήμη.

**Returns:**
[ImageFormat](../../com.aspose.tasks/imageformat) - An ImageFormat object that indicates the format of a bitmap in memory.
### getPng() {#getPng--}
```
public static ImageFormat getPng()
```


Λαμβάνει τη μορφή W3C Portable Network Graphics (PNG).

**Returns:**
[ImageFormat](../../com.aspose.tasks/imageformat) - An ImageFormat object that indicates the PNG image format.
### getTiff() {#getTiff--}
```
public static ImageFormat getTiff()
```


Λαμβάνει τη μορφή Tagged Image File Format (TIFF).

**Returns:**
[ImageFormat](../../com.aspose.tasks/imageformat) - An ImageFormat object that indicates the TIFF image format.
### getWmf() {#getWmf--}
```
public static ImageFormat getWmf()
```


Λαμβάνει τη μορφή Windows metafile (WMF).

**Returns:**
[ImageFormat](../../com.aspose.tasks/imageformat) - An ImageFormat object that indicates the Windows metafile image format.
### hashCode() {#hashCode--}
```
public int hashCode()
```


Επιστρέφει μια τιμή κώδικα κατακερματισμού που αντιπροσωπεύει αυτό το αντικείμενο.

**Returns:**
int - Ένας κωδικός κατακερματισμού που αντιπροσωπεύει αυτό το αντικείμενο.
### isBitmapType() {#isBitmapType--}
```
public boolean isBitmapType()
```


Καθορίζει εάν μια μορφή εικόνας είναι τύπου bitmap.

**Returns:**
boolean - true εάν ένας τύπος εικόνας είναι bitmap· διαφορετικά, false.
### isMetafileType() {#isMetafileType--}
```
public boolean isMetafileType()
```


Καθορίζει εάν μια μορφή εικόνας είναι τύπου metafile.

**Returns:**
boolean - true εάν ένας τύπος εικόνας είναι metafile· διαφορετικά, false.
### isUnknownType() {#isUnknownType--}
```
public boolean isUnknownType()
```


Καθορίζει εάν μια μορφή εικόνας είναι άγνωστος τύπος.

**Returns:**
boolean - true εάν ένας τύπος εικόνας είναι άγνωστος· διαφορετικά, false.
### toString() {#toString--}
```
public String toString()
```


Μετατρέπει αυτό το αντικείμενο ImageFormat σε αναγνώσιμη από άνθρωπο συμβολοσειρά.

**Returns:**
java.lang.String - Μια συμβολοσειρά που αντιπροσωπεύει αυτό το αντικείμενο ImageFormat.
