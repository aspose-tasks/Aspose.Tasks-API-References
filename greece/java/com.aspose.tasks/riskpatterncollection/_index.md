---
title: "RiskPatternCollection"
second_title: "Αναφορά API του Aspose.Tasks for Java"
description: "Αντιπροσωπεύει μια συλλογή που περιέχει τις εμφανίσεις της κλάσης RiskPattern."
type: docs
weight: 269
url: /el/java/com.aspose.tasks/riskpatterncollection/
---

**Inheritance:**
java.lang.Object, java.util.AbstractCollection
```
public class RiskPatternCollection extends AbstractCollection<RiskPattern>
```

Αντιπροσωπεύει μια συλλογή που περιέχει τις περιπτώσεις της κλάσης `RiskPattern`.
## Μέθοδοι

| Μέθοδος | Περιγραφή |
| --- | --- |
| [add(RiskPattern item)](#add-com.aspose.tasks.RiskPattern-) | Προσθέτει μια εμφάνιση της κλάσης `RiskPattern` σε αυτή τη συλλογή. |
| [clear()](#clear--) | Αφαιρεί όλα τα στοιχεία από αυτή τη συλλογή. |
| [contains(Object item)](#contains-java.lang.Object-) | Επιστρέφει true εάν το καθορισμένο στοιχείο βρεθεί σε αυτή τη συλλογή· διαφορετικά, false. |
| [copyTo(RiskPattern[] array, int arrayIndex)](#copyTo-com.aspose.tasks.RiskPattern---int-) | Αντιγράφει τα στοιχεία αυτής της συλλογής στον καθορισμένο πίνακα, ξεκινώντας από τον καθορισμένο δείκτη πίνακα. |
| [get_Item(Task task)](#get-Item-com.aspose.tasks.Task-) | Λαμβάνει την εμφάνιση της κλάσης `RiskPattern` για την καθορισμένη εργασία. |
| [iterator()](#iterator--) | Επιστρέφει έναν enumerator για αυτή τη συλλογή. |
| [remove(Object item)](#remove-java.lang.Object-) | Αφαιρεί την πρώτη εμφάνιση ενός συγκεκριμένου αντικειμένου από αυτή τη συλλογή. |
| [size()](#size--) | Επιστρέφει τον αριθμό των στοιχείων που περιέχονται σε αυτή τη συλλογή. |
### add(RiskPattern item) {#add-com.aspose.tasks.RiskPattern-}
```
public boolean add(RiskPattern item)
```


Προσθέτει μια εμφάνιση της κλάσης `RiskPattern` σε αυτή τη συλλογή.

**Parameters:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| item | [RiskPattern](../../com.aspose.tasks/riskpattern) | μια εμφάνιση της κλάσης `RiskPattern` για προσθήκη σε αυτή τη συλλογή. |

**Returns:**
boolean - επιστρέφει true εάν μια εμφάνιση της κλάσης `RiskPattern` προστέθηκε επιτυχώς· διαφορετικά, false.
### clear() {#clear--}
```
public void clear()
```


Αφαιρεί όλα τα στοιχεία από αυτή τη συλλογή.

### contains(Object item) {#contains-java.lang.Object-}
```
public boolean contains(Object item)
```


Επιστρέφει true εάν το καθορισμένο στοιχείο βρεθεί σε αυτή τη συλλογή· διαφορετικά, false.

**Parameters:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| στοιχείο | java.lang.Object | το καθορισμένο στοιχείο για εύρεση. |

**Returns:**
boolean - true εάν το καθορισμένο στοιχείο βρεθεί σε αυτή τη συλλογή· διαφορετικά, false.
### copyTo(RiskPattern[] array, int arrayIndex) {#copyTo-com.aspose.tasks.RiskPattern---int-}
```
public void copyTo(RiskPattern[] array, int arrayIndex)
```


Αντιγράφει τα στοιχεία αυτής της συλλογής στον καθορισμένο πίνακα, ξεκινώντας από τον καθορισμένο δείκτη πίνακα.

**Parameters:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| array | [RiskPattern\[\]](../../com.aspose.tasks/riskpattern) | ο καθορισμένος μονοδιάστατος πίνακας στον οποίο θα αντιγραφούν τα στοιχεία |
| arrayIndex | int | ο δείκτης μηδενικής βάσης του καθορισμένου πίνακα στο οποίο αρχίζει η αντιγραφή. |

### get_Item(Task task) {#get-Item-com.aspose.tasks.Task-}
```
public RiskPattern get_Item(Task task)
```


Λαμβάνει την εμφάνιση της κλάσης `RiskPattern` για την καθορισμένη εργασία.

**Parameters:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| task | [Task](../../com.aspose.tasks/task) | η καθορισμένη εργασία. |

**Returns:**
[RiskPattern](../../com.aspose.tasks/riskpattern) - the pattern for the specified task.
### iterator() {#iterator--}
```
public Iterator<RiskPattern> iterator()
```


Επιστρέφει έναν enumerator για αυτή τη συλλογή.

**Returns:**
java.util.Iterator&lt;com.aspose.tasks.RiskPattern&gt; - ένας απαριθμητής για αυτή τη συλλογή.
### remove(Object item) {#remove-java.lang.Object-}
```
public boolean remove(Object item)
```


Αφαιρεί την πρώτη εμφάνιση ενός συγκεκριμένου αντικειμένου από αυτή τη συλλογή.

**Parameters:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| στοιχείο | java.lang.Object | το καθορισμένο αντικείμενο για αφαίρεση. |

**Returns:**
boolean - true εάν το καθορισμένο αντικείμενο αφαιρέθηκε επιτυχώς από αυτή τη συλλογή· διαφορετικά, false.
### size() {#size--}
```
public int size()
```


Επιστρέφει τον αριθμό των στοιχείων που περιέχονται σε αυτή τη συλλογή.

**Returns:**
int - ο αριθμός των στοιχείων που περιέχονται σε αυτή τη συλλογή.
