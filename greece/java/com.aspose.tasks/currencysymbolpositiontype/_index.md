---
title: "CurrencySymbolPositionType"
second_title: "Αναφορά API του Aspose.Tasks for Java"
description: "Καθορίζει τη θέση του συμβόλου νομίσματος."
type: docs
weight: 58
url: /el/java/com.aspose.tasks/currencysymbolpositiontype/
---

**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class CurrencySymbolPositionType extends System.Enum
```

Καθορίζει τη θέση του συμβόλου νομίσματος.

--------------------

Κατά την εξαγωγή σε XML, οι τιμές Undefined θα αφαιρεθούν από το τελικό XML.
## Πεδία

| Πεδίο | Περιγραφή |
| --- | --- |
| [After](#After) | Δείχνει τον τύπο θέσης του συμβόλου νομίσματος μετά. |
| [AfterWithSpace](#AfterWithSpace) | Δείχνει τον τύπο θέσης του συμβόλου νομίσματος AfterWithSpace. |
| [Before](#Before) | Δείχνει τον τύπο θέσης του συμβόλου νομίσματος πριν. |
| [BeforeWithSpace](#BeforeWithSpace) | Δείχνει τον τύπο θέσης του συμβόλου νομίσματος BeforeWithSpace. |
| [Undefined](#Undefined) | Δείχνει ότι η μη ορισμένη τιμή σημαίνει ότι το πεδίο δεν ορίστηκε στο αρχικό αρχείο έργου. |
### After {#After}
```
public static final int After
```


Δείχνει τον τύπο θέσης του συμβόλου νομίσματος μετά.

### AfterWithSpace {#AfterWithSpace}
```
public static final int AfterWithSpace
```


Δείχνει τον τύπο θέσης του συμβόλου νομίσματος AfterWithSpace.

### Before {#Before}
```
public static final int Before
```


Δείχνει τον τύπο θέσης του συμβόλου νομίσματος πριν.

### BeforeWithSpace {#BeforeWithSpace}
```
public static final int BeforeWithSpace
```


Δείχνει τον τύπο θέσης του συμβόλου νομίσματος BeforeWithSpace.

### Undefined {#Undefined}
```
public static final int Undefined
```


Δείχνει ότι η μη ορισμένη τιμή σημαίνει ότι το πεδίο δεν ορίστηκε στο αρχικό αρχείο έργου.

