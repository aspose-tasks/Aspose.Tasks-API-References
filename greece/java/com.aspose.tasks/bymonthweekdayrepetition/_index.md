---
title: "ByMonthWeekDayRepetition"
second_title: "Αναφορά API του Aspose.Tasks for Java"
description: "Αναπαριστά ένα μοτίβο που βασίζεται στη θέση της ημέρας της εβδομάδας σε μήνα."
type: docs
weight: 36
url: /el/java/com.aspose.tasks/bymonthweekdayrepetition/
---

**Inheritance:**
java.lang.Object, [com.aspose.tasks.MonthlyRepetitionBase](../../com.aspose.tasks/monthlyrepetitionbase)
```
public class ByMonthWeekDayRepetition extends MonthlyRepetitionBase
```

Αναπαριστά ένα μοτίβο που βασίζεται στη θέση της ημέρας της εβδομάδας σε μήνα.
## Κατασκευαστές

| Κατασκευαστής | Περιγραφή |
| --- | --- |
| [ByMonthWeekDayRepetition()](#ByMonthWeekDayRepetition--) | Αρχικοποιεί μια νέα παρουσία της κλάσης [ByMonthWeekDayRepetition](../../com.aspose.tasks/bymonthweekdayrepetition). |
## Μέθοδοι

| Μέθοδος | Περιγραφή |
| --- | --- |
| [getPosition()](#getPosition--) | Λαμβάνει τη θέση της ημέρας της εβδομάδας σε έναν μήνα στην οποία η εργασία πρέπει να επαναλαμβάνεται. |
| [getWeekDay()](#getWeekDay--) | Λαμβάνει τον τύπο της ημέρας της εβδομάδα στην οποία η εργασία πρέπει να επαναλαμβάνεται. |
| [setPosition(int value)](#setPosition-int-) | Ορίζει τη θέση της ημέρας της εβδομάδας σε έναν μήνα στην οποία η εργασία πρέπει να επαναλαμβάνεται. |
| [setWeekDay(int value)](#setWeekDay-int-) | Ορίζει τον τύπο της ημέρας της εβδομάδας στην οποία η εργασία πρέπει να επαναλαμβάνεται. |
### ByMonthWeekDayRepetition() {#ByMonthWeekDayRepetition--}
```
public ByMonthWeekDayRepetition()
```


Αρχικοποιεί μια νέα παρουσία της κλάσης [ByMonthWeekDayRepetition](../../com.aspose.tasks/bymonthweekdayrepetition).

### getPosition() {#getPosition--}
```
public final int getPosition()
```


Λαμβάνει τη θέση της ημέρας της εβδομάδας σε έναν μήνα στην οποία η εργασία πρέπει να επαναλαμβάνεται.

Ανάγνωση/Εγγραφή `OrdinalNumber`.

**Returns:**
int - μια θέση της ημέρας της εβδομάδας σε έναν μήνα στην οποία η εργασία πρέπει να επαναλαμβάνεται.
### getWeekDay() {#getWeekDay--}
```
public final int getWeekDay()
```


Λαμβάνει τον τύπο της ημέρας της εβδομάδα στην οποία η εργασία πρέπει να επαναλαμβάνεται.

Ανάγνωση/Εγγραφή `DayOfWeek`.

**Returns:**
int - ένας τύπος της ημέρας της εβδομάδας στην οποία η εργασία πρέπει να επαναλαμβάνεται.
### setPosition(int value) {#setPosition-int-}
```
public final void setPosition(int value)
```


Ορίζει τη θέση της ημέρας της εβδομάδας σε έναν μήνα στην οποία η εργασία πρέπει να επαναλαμβάνεται.

Ανάγνωση/Εγγραφή `OrdinalNumber`.

**Parameters:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| τιμή | int | μια θέση της ημέρας της εβδομάδας σε έναν μήνα στην οποία η εργασία πρέπει να επαναλαμβάνεται. |

### setWeekDay(int value) {#setWeekDay-int-}
```
public final void setWeekDay(int value)
```


Ορίζει τον τύπο της ημέρας της εβδομάδας στην οποία η εργασία πρέπει να επαναλαμβάνεται.

Ανάγνωση/Εγγραφή `DayOfWeek`.

**Parameters:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| τιμή | int | ένας τύπος της ημέρας της εβδομάδας στην οποία η εργασία πρέπει να επαναλαμβάνεται. |

