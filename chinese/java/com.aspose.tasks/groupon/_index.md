---
title: "GroupOn"
second_title: "Aspose.Tasks for Java API 参考"
description: "指定分组的类型。"
type: docs
weight: 126
url: /zh/java/com.aspose.tasks/groupon/
---

**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class GroupOn extends System.Enum
```

指定分组的类型。
## 字段

| 字段 | 描述 |
| --- | --- |
| [DateDay](#DateDay) | 按天对日期进行分组。 |
| [DateEachValue](#DateEachValue) | 对每个值的日期进行分组。 |
| [DateHour](#DateHour) | 按小时对日期进行分组。 |
| [DateMinute](#DateMinute) | 按分钟对日期进行分组。 |
| [DateMonth](#DateMonth) | 按月对日期进行分组。 |
| [DateQtr](#DateQtr) | 按季度对日期进行分组。 |
| [DateThirdOfMonth](#DateThirdOfMonth) | 按每月的三分之一对日期进行分组。 |
| [DateWeek](#DateWeek) | 按周对日期进行分组。 |
| [DateYear](#DateYear) | 按年对日期进行分组。 |
| [DurationDays](#DurationDays) | 按天对持续时间进行分组。 |
| [DurationEachValue](#DurationEachValue) | 对每个值的持续时间进行分组。 |
| [DurationHours](#DurationHours) | 按小时对持续时间进行分组。 |
| [DurationMinutes](#DurationMinutes) | 按分钟对持续时间进行分组。 |
| [DurationMonths](#DurationMonths) | 按月对持续时间进行分组。 |
| [DurationWeeks](#DurationWeeks) | 按周对持续时间进行分组。 |
| [EachValue](#EachValue) | 按每个值进行分组。 |
| [Interval](#Interval) | 按间隔进行分组。 |
| [OutlineEachValue](#OutlineEachValue) | 对每个大纲值进行分组。 |
| [OutlineLevel](#OutlineLevel) | 对大纲级别进行分组。 |
| [Pct110](#Pct110) | 按 10% 完成增量分组。 |
| [Pct125](#Pct125) | 按 25% 完成增量分组。 |
| [Pct150](#Pct150) | 按 50% 完成增量分组。 |
| [Pct199](#Pct199) | 按 99% 完成分组。 |
| [PctEachValue](#PctEachValue) | 按每个值的百分比分组。 |
| [PctInterval](#PctInterval) | 按间隔百分比分组。 |
| [TextEachValue](#TextEachValue) | 按每个文本值分组。 |
| [TextPrefix](#TextPrefix) | 按文本前缀分组。 |
### DateDay {#DateDay}
```
public static final int DateDay
```


按天对日期进行分组。

### DateEachValue {#DateEachValue}
```
public static final int DateEachValue
```


对每个值的日期进行分组。

### DateHour {#DateHour}
```
public static final int DateHour
```


按小时对日期进行分组。

### DateMinute {#DateMinute}
```
public static final int DateMinute
```


按分钟对日期进行分组。

### DateMonth {#DateMonth}
```
public static final int DateMonth
```


按月对日期进行分组。

### DateQtr {#DateQtr}
```
public static final int DateQtr
```


按季度对日期进行分组。

### DateThirdOfMonth {#DateThirdOfMonth}
```
public static final int DateThirdOfMonth
```


按每月的三分之一对日期进行分组。

### DateWeek {#DateWeek}
```
public static final int DateWeek
```


按周对日期进行分组。

### DateYear {#DateYear}
```
public static final int DateYear
```


按年对日期进行分组。

### DurationDays {#DurationDays}
```
public static final int DurationDays
```


按天对持续时间进行分组。

### DurationEachValue {#DurationEachValue}
```
public static final int DurationEachValue
```


对每个值的持续时间进行分组。

### DurationHours {#DurationHours}
```
public static final int DurationHours
```


按小时对持续时间进行分组。

### DurationMinutes {#DurationMinutes}
```
public static final int DurationMinutes
```


按分钟对持续时间进行分组。

### DurationMonths {#DurationMonths}
```
public static final int DurationMonths
```


按月对持续时间进行分组。

### DurationWeeks {#DurationWeeks}
```
public static final int DurationWeeks
```


按周对持续时间进行分组。

### EachValue {#EachValue}
```
public static final int EachValue
```


按每个值进行分组。

### Interval {#Interval}
```
public static final int Interval
```


按间隔进行分组。

### OutlineEachValue {#OutlineEachValue}
```
public static final int OutlineEachValue
```


对每个大纲值进行分组。

### OutlineLevel {#OutlineLevel}
```
public static final int OutlineLevel
```


对大纲级别进行分组。

### Pct110 {#Pct110}
```
public static final int Pct110
```


按 10% 完成增量分组。

### Pct125 {#Pct125}
```
public static final int Pct125
```


按 25% 完成增量分组。

### Pct150 {#Pct150}
```
public static final int Pct150
```


按 50% 完成增量分组。

### Pct199 {#Pct199}
```
public static final int Pct199
```


按 99% 完成分组。

### PctEachValue {#PctEachValue}
```
public static final int PctEachValue
```


按每个值的百分比分组。

### PctInterval {#PctInterval}
```
public static final int PctInterval
```


按间隔百分比分组。

### TextEachValue {#TextEachValue}
```
public static final int TextEachValue
```


按每个文本值分组。

### TextPrefix {#TextPrefix}
```
public static final int TextPrefix
```


按文本前缀分组。

