---
title: "RiskPatternCollection"
second_title: "Aspose.Tasks for Java API 参考"
description: "表示一个包含 RiskPattern 类实例的集合。"
type: docs
weight: 269
url: /zh/java/com.aspose.tasks/riskpatterncollection/
---

**Inheritance:**
java.lang.Object, java.util.AbstractCollection
```
public class RiskPatternCollection extends AbstractCollection<RiskPattern>
```

表示包含 `RiskPattern` 类实例的集合。
## 方法

| 方法 | 描述 |
| --- | --- |
| [add(RiskPattern item)](#add-com.aspose.tasks.RiskPattern-) | 向此集合添加 `RiskPattern` 类的实例。 |
| [clear()](#clear--) | 从此集合中移除所有项。 |
| [contains(Object item)](#contains-java.lang.Object-) | 如果在此集合中找到指定项，则返回 true；否则返回 false。 |
| [copyTo(RiskPattern[] array, int arrayIndex)](#copyTo-com.aspose.tasks.RiskPattern---int-) | 将此集合的元素复制到指定数组中，从指定的数组索引开始。 |
| [get_Item(Task task)](#get-Item-com.aspose.tasks.Task-) | 获取指定任务的 `RiskPattern` 类实例。 |
| [iterator()](#iterator--) | 返回此集合的枚举器。 |
| [remove(Object item)](#remove-java.lang.Object-) | 从此集合中移除特定对象的第一次出现。 |
| [size()](#size--) | 返回此集合中包含的元素数量。 |
### add(RiskPattern item) {#add-com.aspose.tasks.RiskPattern-}
```
public boolean add(RiskPattern item)
```


向此集合添加 `RiskPattern` 类的实例。

**Parameters:**
| 参数 | 类型 | 描述 |
| --- | --- | --- |
| item | [RiskPattern](../../com.aspose.tasks/riskpattern) | 用于添加到此集合的 `RiskPattern` 类实例。 |

**Returns:**
boolean - 如果成功添加了 `RiskPattern` 类的实例则返回 true；否则返回 false。
### clear() {#clear--}
```
public void clear()
```


从此集合中移除所有项。

### contains(Object item) {#contains-java.lang.Object-}
```
public boolean contains(Object item)
```


如果在此集合中找到指定项，则返回 true；否则返回 false。

**Parameters:**
| 参数 | 类型 | 描述 |
| --- | --- | --- |
| 项 | java.lang.Object | 要查找的指定项。 |

**Returns:**
布尔 - 如果在此集合中找到指定项，则为 true；否则为 false。
### copyTo(RiskPattern[] array, int arrayIndex) {#copyTo-com.aspose.tasks.RiskPattern---int-}
```
public void copyTo(RiskPattern[] array, int arrayIndex)
```


将此集合的元素复制到指定数组中，从指定的数组索引开始。

**Parameters:**
| 参数 | 类型 | 描述 |
| --- | --- | --- |
| array | [RiskPattern\[\]](../../com.aspose.tasks/riskpattern) | 指定的一维数组，用于复制元素。 |
| arrayIndex | int | 指定数组中开始复制的零基索引。 |

### get_Item(Task task) {#get-Item-com.aspose.tasks.Task-}
```
public RiskPattern get_Item(Task task)
```


获取指定任务的 `RiskPattern` 类实例。

**Parameters:**
| 参数 | 类型 | 描述 |
| --- | --- | --- |
| task | [Task](../../com.aspose.tasks/task) | 指定的任务。 |

**Returns:**
[RiskPattern](../../com.aspose.tasks/riskpattern) - the pattern for the specified task.
### iterator() {#iterator--}
```
public Iterator<RiskPattern> iterator()
```


返回此集合的枚举器。

**Returns:**
java.util.Iterator&lt;com.aspose.tasks.RiskPattern&gt; - 此集合的枚举器。
### remove(Object item) {#remove-java.lang.Object-}
```
public boolean remove(Object item)
```


从此集合中移除特定对象的第一次出现。

**Parameters:**
| 参数 | 类型 | 描述 |
| --- | --- | --- |
| 项 | java.lang.Object | 要移除的指定对象。 |

**Returns:**
boolean - 如果成功从此集合中移除指定对象则为 true；否则为 false。
### size() {#size--}
```
public int size()
```


返回此集合中包含的元素数量。

**Returns:**
int - 此集合中包含的元素数量。
