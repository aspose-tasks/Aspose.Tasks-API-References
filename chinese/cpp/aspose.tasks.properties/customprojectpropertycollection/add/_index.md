---
title: "Aspose::Tasks::Properties::CustomProjectPropertyCollection::Add 方法"
linktitle: "Add"
articleTitle: "Add"
second_title: "Aspose.Tasks for C++"
description: "创建一个新的自定义属性。"
type: docs
weight: 10
url: /zh/cpp/aspose.tasks.properties/customprojectpropertycollection/add/
---

## Add (1 of 4) {#add_1}

创建一个新的自定义属性。

**Returns:** The newly created property object.

```cpp
Add(const System::String & name, bool value)
```

| 参数 | Aspose::Tasks 命名空间提供核心类和枚举，用于在 C++ 中管理项目数据、资源、分配和基线信息。 |
| --- | --- |
| 名称 | 属性的名称。 |
| 值 | 新创建的属性对象值。 |

---

## Add (2 of 4) {#add_2}

创建一个新的自定义属性。

**Returns:** The newly created property object.

```cpp
Add(const System::String & name, const System::String & value)
```

| 参数 | Aspose::Tasks 命名空间提供核心类和枚举，用于在 C++ 中管理项目数据、资源、分配和基线信息。 |
| --- | --- |
| 名称 | 属性的名称。 |
| 值 | 新创建的属性对象值。 |

---

## Add (3 of 4) {#add_3}

创建一个新的自定义属性。

**Returns:** The newly created property object.

```cpp
Add(const System::String & name, double value)
```

| 参数 | Aspose::Tasks 命名空间提供核心类和枚举，用于在 C++ 中管理项目数据、资源、分配和基线信息。 |
| --- | --- |
| 名称 | 属性的名称。 |
| 值 | 新创建的属性对象值。 |

---

## Add (4 of 4) {#add_4}

创建一个新的自定义属性。

**Returns:** The newly created property object.

```cpp
Add(const System::String & name, System::DateTime value)
```

| 参数 | Aspose::Tasks 命名空间提供核心类和枚举，用于在 C++ 中管理项目数据、资源、分配和基线信息。 |
| --- | --- |
| 名称 | 属性的名称。 |
| 值 | 新创建的属性对象值。 |

