---
title: "Aspose::Tasks::TableFieldCollection::end 方法"
linktitle: "end"
articleTitle: "end"
second_title: "Aspose.Tasks for C++"
description: "获取指向集合的 const 限定实例的最后一个元素之后的位置（如果有）的迭代器。"
type: docs
weight: 80
url: /zh/cpp/aspose.tasks/tablefieldcollection/end/
---

## end (1 of 2) {#end_1}

获取指向集合的 const 限定实例的最后一个元素之后的位置（如果有）的迭代器。

**Returns:** An iterator pointing right after the last element (if any) of the const-qualified instance of the collection

```cpp
end()
```

---

## end (2 of 2) {#end_2}

获取指向集合中最后一个元素（如果有）之后的迭代器。

**Returns:** An iterator pointing right after the last element (if any) of the collection

```cpp
end()
```

