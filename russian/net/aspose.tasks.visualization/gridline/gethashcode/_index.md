---
title: "Gridline.GetHashCode"
second_title: "Справочник API Aspose.Tasks for .NET"
description: "Метод Gridline. Возвращает значение хэш‑кода для экземпляра класса Gridline."
type: docs
weight: 60
url: /ru/net/aspose.tasks.visualization/gridline/gethashcode/
---
## Gridline.GetHashCode method

Возвращает значение хеш‑кода для экземпляра класса [`Gridline`](../).

```csharp
public override int GetHashCode()
```

### Возвращаемое значение

возвращает значение хеш‑кода для этого объекта.

## Примеры

Показывает, как получить хеш‑код gridline.

```csharp
var gridline1 = new Gridline();
var gridline2 = new Gridline();

// Хеш‑код gridline основан на внутреннем поле GUID.
Console.WriteLine("Gridline 1 Hash Code: {0}", gridline1.GetHashCode());
Console.WriteLine("Gridline 2 Hash Code: {0}", gridline2.GetHashCode());
```

### См. также

* class [Gridline](../)
* namespace [Aspose.Tasks.Visualization](../../gridline/)
* assembly [Aspose.Tasks](../../../)


