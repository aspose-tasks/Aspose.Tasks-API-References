---
title: "Gridlines"
second_title: "Aspose.Tasks for Java API Reference"
description: "간트 차트 뷰에 표시되는 그리드 라인을 나타냅니다."
type: docs
weight: 121
url: /ko/java/com.aspose.tasks/gridlines/
---

**Inheritance:**
java.lang.Object
```
public class Gridlines
```

간트 차트 뷰에 표시되는 그리드 라인을 나타냅니다.
## 생성자

| 생성자 | 설명 |
| --- | --- |
| [Gridlines()](#Gridlines--) |  |
## 메서드

| 메서드 | 설명 |
| --- | --- |
| [getInterval()](#getInterval--) | 그리드선 사이 간격을 지정하는 0에서 99 사이의 숫자를 가져옵니다. |
| [getIntervalColor()](#getIntervalColor--) | 보조 그리드선의 색상을 가져옵니다. |
| [getIntervalPattern()](#getIntervalPattern--) | 보조 그리드선의 선 패턴을 가져옵니다. |
| [getNormalColor()](#getNormalColor--) | 일반 그리드선의 색상을 가져옵니다. |
| [getNormalPattern()](#getNormalPattern--) | 일반 그리드선의 선 패턴을 가져옵니다. |
| [getType()](#getType--) | 그리드선 유형을 가져옵니다. |
| [setInterval(int value)](#setInterval-int-) | 그리드선 사이 간격을 지정하는 0에서 99 사이의 숫자를 설정합니다. |
| [setIntervalColor(Color value)](#setIntervalColor-java.awt.Color-) | 보조 그리드선의 색상을 설정합니다. |
| [setIntervalPattern(int value)](#setIntervalPattern-int-) | 보조 그리드선의 선 패턴을 설정합니다. |
| [setNormalColor(Color value)](#setNormalColor-java.awt.Color-) | 일반 그리드선의 색상을 설정합니다. |
| [setNormalPattern(int value)](#setNormalPattern-int-) | 일반 그리드선의 선 패턴을 설정합니다. |
| [setType(int value)](#setType-int-) | 그리드선 유형을 설정합니다. |
### Gridlines() {#Gridlines--}
```
public Gridlines()
```


### getInterval() {#getInterval--}
```
public final int getInterval()
```


그리드선 사이 간격을 지정하는 0에서 99 사이의 숫자를 가져옵니다.

**Returns:**
int - 그리드선 사이 간격을 지정하는 0에서 99 사이의 숫자입니다.
### getIntervalColor() {#getIntervalColor--}
```
public final Color getIntervalColor()
```


보조 그리드선의 색상을 가져옵니다.

**Returns:**
java.awt.Color - 보조 그리드선의 색상입니다.
### getIntervalPattern() {#getIntervalPattern--}
```
public final int getIntervalPattern()
```


보조 그리드선의 선 패턴을 가져옵니다.

**Returns:**
int - 보조 그리드선의 선 패턴입니다.
### getNormalColor() {#getNormalColor--}
```
public final Color getNormalColor()
```


일반 그리드선의 색상을 가져옵니다.

**Returns:**
java.awt.Color - 일반 그리드선의 색상입니다.
### getNormalPattern() {#getNormalPattern--}
```
public final int getNormalPattern()
```


일반 그리드선의 선 패턴을 가져옵니다.

**Returns:**
int - 일반 그리드선의 선 패턴입니다.
### getType() {#getType--}
```
public final int getType()
```


그리드선 유형을 가져옵니다.

**Returns:**
int - 그리드선 유형입니다.
### setInterval(int value) {#setInterval-int-}
```
public final void setInterval(int value)
```


그리드선 사이 간격을 지정하는 0에서 99 사이의 숫자를 설정합니다.

**Parameters:**
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| 값 | int | 그리드선 사이 간격을 지정하는 0에서 99 사이의 숫자입니다. |

### setIntervalColor(Color value) {#setIntervalColor-java.awt.Color-}
```
public final void setIntervalColor(Color value)
```


보조 그리드선의 색상을 설정합니다.

**Parameters:**
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| 값 | java.awt.Color | 보조 그리드선의 색상입니다. |

### setIntervalPattern(int value) {#setIntervalPattern-int-}
```
public final void setIntervalPattern(int value)
```


보조 그리드선의 선 패턴을 설정합니다.

**Parameters:**
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| 값 | int | 보조 그리드선의 선 패턴입니다. |

### setNormalColor(Color value) {#setNormalColor-java.awt.Color-}
```
public final void setNormalColor(Color value)
```


일반 그리드선의 색상을 설정합니다.

**Parameters:**
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| 값 | java.awt.Color | 일반 그리드선의 색상입니다. |

### setNormalPattern(int value) {#setNormalPattern-int-}
```
public final void setNormalPattern(int value)
```


일반 그리드선의 선 패턴을 설정합니다.

**Parameters:**
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| 값 | int | 일반 그리드선의 선 패턴입니다. |

### setType(int value) {#setType-int-}
```
public final void setType(int value)
```


그리드선 유형을 설정합니다.

**Parameters:**
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| 값 | int | 그리드선 유형입니다. |

