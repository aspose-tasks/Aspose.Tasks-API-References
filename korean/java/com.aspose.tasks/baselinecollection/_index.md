---
title: "BaselineCollection"
second_title: "Aspose.Tasks for Java API Reference"
description: "객체 컬렉션을 나타냅니다."
type: docs
weight: 27
url: /ko/java/com.aspose.tasks/baselinecollection/
---

**Inheritance:**
java.lang.Object, java.util.AbstractCollection, java.util.AbstractList, com.aspose.tasks.AbstractList
```
public class BaselineCollection extends AbstractList<Baseline>
```

다음 [Baseline](../../com.aspose.tasks/baseline) 객체들의 컬렉션을 나타냅니다.
## 메서드

| 메서드 | 설명 |
| --- | --- |
| [add(Baseline item)](#add-com.aspose.tasks.Baseline-) | ICollection의 Add 메서드에 대한 스텁 구현으로, NotSupportedException만 발생시킵니다. |
| [get(int index)](#get-int-) | 지정된 위치의 기준선을 반환합니다. |
| [getParentResource()](#getParentResource--) | 이 컬렉션의 상위 [Resource](../../com.aspose.tasks/resource)를 가져옵니다. |
| [remove(int index)](#remove-int-) | 이 목록에서 지정된 위치의 요소를 제거합니다. |
| [remove(Object item)](#remove-java.lang.Object-) | 이 컬렉션에서 기준선을 제거합니다. |
| [size()](#size--) | 이 BaselineCollection 객체에 포함된 객체 수를 가져옵니다. |
| [toList()](#toList--) | BaselineCollection 객체를 [Baseline](../../com.aspose.tasks/baseline) 객체 목록으로 변환합니다. |
### add(Baseline item) {#add-com.aspose.tasks.Baseline-}
```
public final boolean add(Baseline item)
```


ICollection의 Add 메서드에 대한 스텁 구현으로, NotSupportedException만 발생시킵니다.

**Parameters:**
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| item | [Baseline](../../com.aspose.tasks/baseline) | 제거할 항목입니다. |

**Returns:**
boolean - \{@inheritDoc\}
### get(int index) {#get-int-}
```
public Baseline get(int index)
```


지정된 위치의 기준선을 반환합니다.

**Parameters:**
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| index | int | 반환할 요소의 인덱스. |

**Returns:**
[Baseline](../../com.aspose.tasks/baseline) - the baseline at the specified position.
### getParentResource() {#getParentResource--}
```
public final Resource getParentResource()
```


이 컬렉션의 상위 [Resource](../../com.aspose.tasks/resource)를 가져옵니다.

**Returns:**
[Resource](../../com.aspose.tasks/resource) - the parent [Resource](../../com.aspose.tasks/resource) for this collection.
### remove(int index) {#remove-int-}
```
public Baseline remove(int index)
```


이 목록에서 지정된 위치의 요소를 제거합니다.

**Parameters:**
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| index | int | \{@inheritDoc\} |

**Returns:**
[Baseline](../../com.aspose.tasks/baseline) - \{@inheritDoc\}
### remove(Object item) {#remove-java.lang.Object-}
```
public final boolean remove(Object item)
```


이 컬렉션에서 기준선을 제거합니다.

**Parameters:**
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| item | java.lang.Object | 제거할 [Baseline](../../com.aspose.tasks/baseline) 인스턴스. |

**Returns:**
boolean - [Baseline](../../com.aspose.tasks/baseline) 인스턴스가 성공적으로 제거되면 true; 그렇지 않으면 false
### size() {#size--}
```
public final int size()
```


이 BaselineCollection 객체에 포함된 객체 수를 가져옵니다.

**Returns:**
int - 이 BaselineCollection 객체에 포함된 객체 수.
### toList() {#toList--}
```
public final List<Baseline> toList()
```


BaselineCollection 객체를 [Baseline](../../com.aspose.tasks/baseline) 객체 목록으로 변환합니다.

**Returns:**
java.util.List&lt;com.aspose.tasks.Baseline&gt; - [Baseline](../../com.aspose.tasks/baseline) 객체 목록.
