---
title: "VbaModuleCollection"
second_title: "Aspose.Tasks for Java API Reference"
description: "객체 컬렉션을 나타냅니다."
type: docs
weight: 337
url: /ko/java/com.aspose.tasks/vbamodulecollection/
---

**Inheritance:**
java.lang.Object, java.util.AbstractCollection, java.util.AbstractList, com.aspose.tasks.AbstractList
```
public class VbaModuleCollection extends AbstractList<VbaModule>
```

[VbaModule](../../com.aspose.tasks/vbamodule) 객체의 컬렉션을 나타냅니다.
## 메서드

| 메서드 | 설명 |
| --- | --- |
| [addItem(VbaModule item)](#addItem-com.aspose.tasks.VbaModule-) | 지정된 모듈을 이 컬렉션에 추가합니다. |
| [clear()](#clear--) | \{@inheritDoc\} |
| [contains(VbaModule item)](#contains-com.aspose.tasks.VbaModule-) | 컬렉션에 지정된 항목이 포함되어 있는지 확인합니다. |
| [get(int index)](#get-int-) | 지정된 인덱스에 있는 모듈을 가져옵니다. |
| [get(String moduleName)](#get-java.lang.String-) | 지정된 이름을 가진 모듈을 가져옵니다. |
| [isReadOnly()](#isReadOnly--) | 이 컬렉션이 읽기 전용인지 여부를 나타내는 값을 가져옵니다. |
| [iterator()](#iterator--) | 이 컬렉션에 대한 열거자를 반환합니다. |
| [remove(VbaModule item)](#remove-com.aspose.tasks.VbaModule-) | 컬렉션에서 새로 추가된 모듈을 제거합니다. |
| [removeItem(VbaModule item)](#removeItem-com.aspose.tasks.VbaModule-) | 컬렉션에서 모듈을 제거합니다. |
| [size()](#size--) | 컬렉션에 포함된 모듈 수를 가져옵니다. |
| [toList()](#toList--) | 컬렉션 객체를 [VbaModule](../../com.aspose.tasks/vbamodule) 객체 목록으로 변환합니다. |
### addItem(VbaModule item) {#addItem-com.aspose.tasks.VbaModule-}
```
public final void addItem(VbaModule item)
```


지정된 모듈을 이 컬렉션에 추가합니다.

**Parameters:**
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| item | [VbaModule](../../com.aspose.tasks/vbamodule) | 이 컬렉션에 추가되어야 하는 지정된 모듈. |

### clear() {#clear--}
```
public final void clear()
```




### contains(VbaModule item) {#contains-com.aspose.tasks.VbaModule-}
```
public final boolean contains(VbaModule item)
```


컬렉션에 지정된 항목이 포함되어 있는지 확인합니다.

**Parameters:**
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| item | [VbaModule](../../com.aspose.tasks/vbamodule) | 확인할 항목. |

**Returns:**
boolean - 컬렉션에 항목이 포함되어 있으면 true, 그렇지 않으면 false.
### get(int index) {#get-int-}
```
public final VbaModule get(int index)
```


지정된 인덱스에 있는 모듈을 가져옵니다.

**Parameters:**
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| index | int | \{@inheritDoc\} |

**Returns:**
[VbaModule](../../com.aspose.tasks/vbamodule) - \{@inheritDoc\}
### get(String moduleName) {#get-java.lang.String-}
```
public final VbaModule get(String moduleName)
```


지정된 이름을 가진 모듈을 가져옵니다.

**Parameters:**
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| moduleName | java.lang.String | 가져올 모듈의 이름. |

**Returns:**
[VbaModule](../../com.aspose.tasks/vbamodule) - the module instance
### isReadOnly() {#isReadOnly--}
```
public final boolean isReadOnly()
```


이 컬렉션이 읽기 전용인지 여부를 나타내는 값을 가져옵니다.

**Returns:**
boolean - 이 컬렉션이 읽기 전용인지 여부를 나타내는 값.
### iterator() {#iterator--}
```
public final Iterator<VbaModule> iterator()
```


이 컬렉션에 대한 열거자를 반환합니다.

**Returns:**
java.util.Iterator&lt;com.aspose.tasks.VbaModule&gt; - 이 컬렉션에 대한 열거자.
### remove(VbaModule item) {#remove-com.aspose.tasks.VbaModule-}
```
public final boolean remove(VbaModule item)
```


컬렉션에서 새로 추가된 모듈을 제거합니다.

**Parameters:**
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| item | [VbaModule](../../com.aspose.tasks/vbamodule) | 제거될 모듈 |

**Returns:**
boolean - 모듈이 제거되었는지 여부를 나타내는 값
### removeItem(VbaModule item) {#removeItem-com.aspose.tasks.VbaModule-}
```
public final void removeItem(VbaModule item)
```


컬렉션에서 모듈을 제거합니다.

**Parameters:**
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| item | [VbaModule](../../com.aspose.tasks/vbamodule) | 제거될 모듈 |

### size() {#size--}
```
public final int size()
```


컬렉션에 포함된 모듈 수를 가져옵니다.

**Returns:**
int - 컬렉션에 포함된 모듈 수.
### toList() {#toList--}
```
public final List<VbaModule> toList()
```


컬렉션 객체를 [VbaModule](../../com.aspose.tasks/vbamodule) 객체 목록으로 변환합니다.

**Returns:**
java.util.List&lt;com.aspose.tasks.VbaModule&gt; - 객체 목록.
