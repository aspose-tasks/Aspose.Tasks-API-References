---
title: "ProjectFileInfo"
second_title: "Aspose.Tasks for Java API Reference"
description: "클래스 인스턴스는 파일이 생성된 Microsoft Project의 프로젝트 파일 형식 및 버전에 대한 정보를 포함합니다."
type: docs
weight: 222
url: /ko/java/com.aspose.tasks/projectfileinfo/
---

**Inheritance:**
java.lang.Object

**All Implemented Interfaces:**
com.aspose.ms.System.IEquatable
```
public final class ProjectFileInfo implements System.IEquatable<ProjectFileInfo>
```

클래스 인스턴스는 파일이 생성된 Microsoft Project의 프로젝트 파일 형식 및 버전에 대한 정보를 포함합니다.

--------------------

CanRead 속성을 사용하여 라이브러리가 프로젝트 파일을 처리할 수 있음을 정의합니다.
## 메서드

| 메서드 | 설명 |
| --- | --- |
| [canRead()](#canRead--) | Aspose.Tasks가 프로젝트 파일을 처리할 수 있는지 여부를 나타내는 값을 가져옵니다. |
| [equals(ProjectFileInfo other)](#equals-com.aspose.tasks.ProjectFileInfo-) | 이 인스턴스가 지정된 객체와 같은지 여부를 나타내는 값을 반환합니다. |
| [equals(Object obj)](#equals-java.lang.Object-) | 이 인스턴스가 지정된 객체와 같은지 여부를 나타내는 값을 반환합니다. |
| [getProjectApplicationInfo()](#getProjectApplicationInfo--) | 프로젝트 파일 애플리케이션 정보를 가져옵니다. |
| [getProjectFileFormat()](#getProjectFileFormat--) | 프로젝트 파일 형식을 가져옵니다. |
| [hashCode()](#hashCode--) | 인스턴스인 [ProjectFileInfo](../../com.aspose.tasks/projectfileinfo) 클래스에 대한 해시 코드 값을 반환합니다. |
| [isPasswordProtected()](#isPasswordProtected--) | 프로젝트가 비밀번호로 보호되어 있는지 여부를 나타내는 값을 가져옵니다. |
### canRead() {#canRead--}
```
public final boolean canRead()
```


Aspose.Tasks가 프로젝트 파일을 처리할 수 있는지 여부를 나타내는 값을 가져옵니다.

**Returns:**
boolean - Aspose가 수행할 수 있는지 여부를 나타내는 값.
### equals(ProjectFileInfo other) {#equals-com.aspose.tasks.ProjectFileInfo-}
```
public final boolean equals(ProjectFileInfo other)
```


이 인스턴스가 지정된 객체와 같은지 여부를 나타내는 값을 반환합니다.

**Parameters:**
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| other | [ProjectFileInfo](../../com.aspose.tasks/projectfileinfo) | 이 인스턴스와 비교할 지정된 객체입니다. |

**Returns:**
boolean - 지정된 ProjectFileInfo와 이 인스턴스가 동일한 파일 형식 및 애플리케이션 정보를 가지고 있으면 true를 반환합니다.
### equals(Object obj) {#equals-java.lang.Object-}
```
public boolean equals(Object obj)
```


이 인스턴스가 지정된 객체와 같은지 여부를 나타내는 값을 반환합니다.

**Parameters:**
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| obj | java.lang.Object | 이 인스턴스와 비교할 지정된 객체입니다. |

**Returns:**
boolean - 지정된 ProjectFileInfo와 이 인스턴스가 동일한 파일 형식 및 애플리케이션 정보를 가지고 있으면 true를 반환합니다.
### getProjectApplicationInfo() {#getProjectApplicationInfo--}
```
public final int getProjectApplicationInfo()
```


프로젝트 파일 애플리케이션 정보를 가져옵니다.

**Returns:**
int - 프로젝트 파일 애플리케이션 정보.
### getProjectFileFormat() {#getProjectFileFormat--}
```
public final int getProjectFileFormat()
```


프로젝트 파일 형식을 가져옵니다.

**Returns:**
int - 프로젝트 파일 형식.
### hashCode() {#hashCode--}
```
public int hashCode()
```


인스턴스인 [ProjectFileInfo](../../com.aspose.tasks/projectfileinfo) 클래스에 대한 해시 코드 값을 반환합니다.

**Returns:**
int - 이 객체에 대한 해시 코드 값을 반환합니다.
### isPasswordProtected() {#isPasswordProtected--}
```
public final boolean isPasswordProtected()
```


프로젝트가 비밀번호로 보호되어 있는지 여부를 나타내는 값을 가져옵니다.

**Returns:**
boolean - 프로젝트가 비밀번호로 보호되어 있는지 여부를 나타내는 값.
