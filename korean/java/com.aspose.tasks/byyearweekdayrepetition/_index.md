---
title: "ByYearWeekDayRepetition"
second_title: "Aspose.Tasks for Java API Reference"
description: "월 내 요일 위치를 기반으로 하는 패턴을 나타냅니다."
type: docs
weight: 38
url: /ko/java/com.aspose.tasks/byyearweekdayrepetition/
---

**Inheritance:**
java.lang.Object, [com.aspose.tasks.YearlyRepetitionBase](../../com.aspose.tasks/yearlyrepetitionbase)
```
public class ByYearWeekDayRepetition extends YearlyRepetitionBase
```

월 내 요일 위치를 기반으로 하는 패턴을 나타냅니다.
## 생성자

| 생성자 | 설명 |
| --- | --- |
| [ByYearWeekDayRepetition()](#ByYearWeekDayRepetition--) | [ByYearWeekDayRepetition](../../com.aspose.tasks/byyearweekdayrepetition) 클래스의 새 인스턴스를 초기화합니다. |
## 메서드

| 메서드 | 설명 |
| --- | --- |
| [getMonth()](#getMonth--) | 작업이 반복되어야 하는 월을 가져옵니다. |
| [getPosition()](#getPosition--) | 작업이 반복되어야 하는 월의 주 중 요일 위치를 가져옵니다. |
| [getWeekDay()](#getWeekDay--) | 작업이 반복되어야 하는 요일 유형을 가져옵니다. |
| [setMonth(int value)](#setMonth-int-) | 작업이 반복되어야 하는 월을 설정합니다. |
| [setPosition(int value)](#setPosition-int-) | 작업이 반복되어야 하는 월의 주 중 요일 위치를 설정합니다. |
| [setWeekDay(int value)](#setWeekDay-int-) | 작업이 반복되어야 하는 요일 유형을 설정합니다. |
### ByYearWeekDayRepetition() {#ByYearWeekDayRepetition--}
```
public ByYearWeekDayRepetition()
```


[ByYearWeekDayRepetition](../../com.aspose.tasks/byyearweekdayrepetition) 클래스의 새 인스턴스를 초기화합니다.

### getMonth() {#getMonth--}
```
public final int getMonth()
```


작업이 반복되어야 하는 월을 가져옵니다.

`Month`를 읽고 씁니다.

**Returns:**
int - 작업이 반복되어야 하는 월.
### getPosition() {#getPosition--}
```
public final int getPosition()
```


작업이 반복되어야 하는 월의 주 중 요일 위치를 가져옵니다.

읽기/쓰기 `OrdinalNumber`.

**Returns:**
int - 작업이 반복되어야 하는 월의 주 중 요일 위치.
### getWeekDay() {#getWeekDay--}
```
public final int getWeekDay()
```


작업이 반복되어야 하는 요일 유형을 가져옵니다.

읽기/쓰기 `DayOfWeek`.

**Returns:**
int - 작업이 반복되어야 하는 요일 유형.
### setMonth(int value) {#setMonth-int-}
```
public final void setMonth(int value)
```


작업이 반복되어야 하는 월을 설정합니다.

`Month`를 읽고 씁니다.

**Parameters:**
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| 값 | int | 작업이 반복되어야 하는 월. |

### setPosition(int value) {#setPosition-int-}
```
public final void setPosition(int value)
```


작업이 반복되어야 하는 월의 주 중 요일 위치를 설정합니다.

읽기/쓰기 `OrdinalNumber`.

**Parameters:**
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| 값 | int | 작업이 반복되어야 하는 월의 주 중 요일 위치. |

### setWeekDay(int value) {#setWeekDay-int-}
```
public final void setWeekDay(int value)
```


작업이 반복되어야 하는 요일 유형을 설정합니다.

읽기/쓰기 `DayOfWeek`.

**Parameters:**
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| 값 | int | 작업이 반복되어야 하는 요일 유형. |

