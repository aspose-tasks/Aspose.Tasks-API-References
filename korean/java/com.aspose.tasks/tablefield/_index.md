---
title: "TableField"
second_title: "Aspose.Tasks for Java API Reference"
description: "프로젝트의 테이블 필드를 나타냅니다."
type: docs
weight: 286
url: /ko/java/com.aspose.tasks/tablefield/
---

**Inheritance:**
java.lang.Object
```
public class TableField
```

프로젝트의 테이블 필드를 나타냅니다.
## 생성자

| 생성자 | 설명 |
| --- | --- |
| [TableField()](#TableField--) | [TableField](../../com.aspose.tasks/tablefield) 클래스의 새 인스턴스를 초기화합니다. |
## 메서드

| 메서드 | 설명 |
| --- | --- |
| [getAlignData()](#getAlignData--) | 테이블 필드의 데이터 정렬을 가져옵니다. |
| [getAlignTitle()](#getAlignTitle--) | 테이블 필드의 제목 정렬을 가져옵니다. |
| [getField()](#getField--) | 테이블 필드의 유형을 가져옵니다. |
| [getTitle()](#getTitle--) | 테이블에서 필드의 제목을 가져옵니다. |
| [getWidth()](#getWidth--) | 테이블에서 필드 열의 너비(포인트)를 가져옵니다. |
| [getWrapHeader()](#getWrapHeader--) | 테이블 열 머리글이 여러 줄로 자동 줄바꿈될 수 있는지, 또는 열 너비를 초과하면 잘릴지 여부를 나타내는 값을 가져옵니다. |
| [getWrapText()](#getWrapText--) | 열 텍스트가 여러 줄로 자동 줄바꿈될 수 있는지, 또는 열 너비를 초과하면 잘릴지 여부를 나타내는 값을 가져옵니다. |
| [setAlignData(int value)](#setAlignData-int-) | 테이블 필드의 데이터 정렬을 설정합니다. |
| [setAlignTitle(int value)](#setAlignTitle-int-) | 테이블 필드에서 제목의 정렬을 설정합니다. |
| [setField(int value)](#setField-int-) | 테이블 필드의 유형을 설정합니다. |
| [setTitle(String value)](#setTitle-java.lang.String-) | 테이블에서 필드의 제목을 설정합니다. |
| [setWidth(int value)](#setWidth-int-) | 테이블에서 필드 열의 너비(포인트)를 설정합니다. |
| [setWrapHeader(boolean value)](#setWrapHeader-boolean-) | 테이블 열 머리글이 여러 줄로 자동 줄바꿈될 수 있는지, 또는 열 너비를 초과하면 잘릴지를 나타내는 값을 설정합니다. |
| [setWrapText(boolean value)](#setWrapText-boolean-) | 열 텍스트가 여러 줄로 자동 줄바꿈될 수 있는지, 또는 열 너비를 초과하면 잘릴지를 나타내는 값을 설정합니다. |
### TableField() {#TableField--}
```
public TableField()
```


[TableField](../../com.aspose.tasks/tablefield) 클래스의 새 인스턴스를 초기화합니다.

### getAlignData() {#getAlignData--}
```
public final int getAlignData()
```


테이블 필드의 데이터 정렬을 가져옵니다.

**Returns:**
int - 테이블 필드에서 데이터의 정렬.
### getAlignTitle() {#getAlignTitle--}
```
public final int getAlignTitle()
```


테이블 필드의 제목 정렬을 가져옵니다.

**Returns:**
int - 테이블 필드에서 제목의 정렬.
### getField() {#getField--}
```
public final int getField()
```


테이블 필드의 유형을 가져옵니다.

**Returns:**
int - 테이블 필드의 유형.
### getTitle() {#getTitle--}
```
public final String getTitle()
```


테이블에서 필드의 제목을 가져옵니다.

**Returns:**
java.lang.String - 테이블에서 필드의 제목.
### getWidth() {#getWidth--}
```
public final int getWidth()
```


테이블에서 필드 열의 너비(포인트)를 가져옵니다.

**Returns:**
int - 테이블에서 필드 열의 너비(포인트).
### getWrapHeader() {#getWrapHeader--}
```
public final boolean getWrapHeader()
```


테이블 열 머리글이 여러 줄로 자동 줄바꿈될 수 있는지, 또는 열 너비를 초과하면 잘릴지 여부를 나타내는 값을 가져옵니다.

**Returns:**
boolean - 테이블 열 머리글이 여러 줄로 자동 줄바꿈될 수 있는지, 또는 열 너비를 초과하면 잘릴지를 나타내는 값.
### getWrapText() {#getWrapText--}
```
public final boolean getWrapText()
```


열 텍스트가 여러 줄로 자동 줄바꿈될 수 있는지, 또는 열 너비를 초과하면 잘릴지 여부를 나타내는 값을 가져옵니다.

--------------------

MSP 2010 버전 및 이후 버전에서 지원됩니다.

**Returns:**
boolean - 열 텍스트가 여러 줄로 자동 줄바꿈될 수 있는지, 또는 열 너비를 초과하면 잘릴지를 나타내는 값.
### setAlignData(int value) {#setAlignData-int-}
```
public final void setAlignData(int value)
```


테이블 필드의 데이터 정렬을 설정합니다.

**Parameters:**
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| 값 | int | 테이블 필드에서 데이터의 정렬. |

### setAlignTitle(int value) {#setAlignTitle-int-}
```
public final void setAlignTitle(int value)
```


테이블 필드에서 제목의 정렬을 설정합니다.

**Parameters:**
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| 값 | int | 테이블 필드에서 제목의 정렬. |

### setField(int value) {#setField-int-}
```
public final void setField(int value)
```


테이블 필드의 유형을 설정합니다.

**Parameters:**
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| 값 | int | 테이블 필드의 유형. |

### setTitle(String value) {#setTitle-java.lang.String-}
```
public final void setTitle(String value)
```


테이블에서 필드의 제목을 설정합니다.

**Parameters:**
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| 값 | java.lang.String | 테이블에서 필드의 제목. |

### setWidth(int value) {#setWidth-int-}
```
public final void setWidth(int value)
```


테이블에서 필드 열의 너비(포인트)를 설정합니다.

**Parameters:**
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| 값 | int | 테이블에서 필드 열의 너비(포인트). |

### setWrapHeader(boolean value) {#setWrapHeader-boolean-}
```
public final void setWrapHeader(boolean value)
```


테이블 열 머리글이 여러 줄로 자동 줄바꿈될 수 있는지, 또는 열 너비를 초과하면 잘릴지를 나타내는 값을 설정합니다.

**Parameters:**
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| 값 | boolean | 테이블 열 머리글이 여러 줄로 자동 줄바꿈될 수 있는지, 또는 열 너비를 초과하면 잘릴지를 나타내는 값. |

### setWrapText(boolean value) {#setWrapText-boolean-}
```
public final void setWrapText(boolean value)
```


열 텍스트가 여러 줄로 자동 줄바꿈될 수 있는지, 또는 열 너비를 초과하면 잘릴지를 나타내는 값을 설정합니다.

--------------------

MSP 2010 버전 및 이후 버전에서 지원됩니다.

**Parameters:**
| 매개변수 | 형식 | 설명 |
| --- | --- | --- |
| 값 | boolean | 열 텍스트가 여러 줄로 자동 줄바꿈될 수 있는지, 또는 열 너비를 초과하면 잘릴지를 나타내는 값. |

