---
title: "Aspose::Tasks::Properties::CustomProjectPropertyCollection::Add 메서드"
linktitle: "추가"
articleTitle: "추가"
second_title: "C++용 Aspose.Tasks"
description: "새 사용자 정의 속성을 생성합니다."
type: docs
weight: 10
url: /ko/cpp/aspose.tasks.properties/customprojectpropertycollection/add/
---

## Add (1 of 4) {#add_1}

새 사용자 정의 속성을 생성합니다.

**Returns:** The newly created property object.

```cpp
Add(const System::String & name, bool value)
```

| 매개변수 | 설명 |
| --- | --- |
| 이름 | 속성의 이름입니다. |
| 값 | 새로 만든 속성 객체 값입니다. |

---

## Add (2 of 4) {#add_2}

새 사용자 정의 속성을 생성합니다.

**Returns:** The newly created property object.

```cpp
Add(const System::String & name, const System::String & value)
```

| 매개변수 | 설명 |
| --- | --- |
| 이름 | 속성의 이름입니다. |
| 값 | 새로 만든 속성 객체 값입니다. |

---

## Add (3 of 4) {#add_3}

새 사용자 정의 속성을 생성합니다.

**Returns:** The newly created property object.

```cpp
Add(const System::String & name, double value)
```

| 매개변수 | 설명 |
| --- | --- |
| 이름 | 속성의 이름입니다. |
| 값 | 새로 만든 속성 객체 값입니다. |

---

## Add (4 of 4) {#add_4}

새 사용자 정의 속성을 생성합니다.

**Returns:** The newly created property object.

```cpp
Add(const System::String & name, System::DateTime value)
```

| 매개변수 | 설명 |
| --- | --- |
| 이름 | 속성의 이름입니다. |
| 값 | 새로 만든 속성 객체 값입니다. |

