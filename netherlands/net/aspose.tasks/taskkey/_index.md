---
title: TaskKey
second_title: Aspose.Tasks voor .NET API-referentie
description: Vertegenwoordigt een lijst met ondersteunde taakvelden.
type: docs
weight: 2110
url: /nl/net/aspose.tasks/taskkey/
---
## TaskKey enumeration

Vertegenwoordigt een lijst met ondersteunde taakvelden.

```csharp
public enum TaskKey
```

### Waarden

| Naam | Waarde | Beschrijving |
| --- | --- | --- |
| Uid | `0` | Vertegenwoordigt het veld Unieke ID (taak). |
| Id | `1` | Vertegenwoordigt het veld Id (taak). |
| Name | `2` | Vertegenwoordigt het veld Naam (taak). |
| Type | `3` | Vertegenwoordigt het veld Type (taak). |
| IsNull | `4` | Vertegenwoordigt het veld IsNull (taak). |
| Created | `5` | Vertegenwoordigt het veld Aangemaakt (taak). |
| Contact | `6` | Vertegenwoordigt het veld Contactpersoon (taak). |
| WBS | `7` | Vertegenwoordigt het veld WBS (taak). |
| WBSLevel | `8` | Vertegenwoordigt het veld WBSLevel (taak). |
| OutlineNumber | `9` | Vertegenwoordigt het veld Overzichtsnummer (taak). |
| OutlineLevel | `10` | Vertegenwoordigt het veld OutlineLevel (taak). |
| Priority | `11` | Vertegenwoordigt het veld Prioriteit (taak). |
| Start | `12` | Vertegenwoordigt het veld Start (taak). |
| Finish | `13` | Vertegenwoordigt het veld Voltooien (taak). |
| Duration | `14` | Vertegenwoordigt het veld Duur (taak). |
| DurationVariance | `15` | Vertegenwoordigt het veld DurationVariance (taak). |
| DurationFormat | `16` | Vertegenwoordigt het veld DurationFormat (taak). |
| Work | `17` | Vertegenwoordigt het veld Werk (taak). |
| Stop | `18` | Vertegenwoordigt het veld Stop (taak). |
| Resume | `19` | Vertegenwoordigt het veld Hervatten (taak). |
| IsResumeValid | `20` | Vertegenwoordigt het veld IsResumeValid (taak). |
| IsEffortDriven | `21` | Vertegenwoordigt het veld IsEffortDriven (taak). |
| IsRecurring | `22` | Vertegenwoordigt het veld Is terugkerend (taak). |
| IsOverallocated | `23` | Vertegenwoordigt het veld IsOverallocated (Task). |
| HasOverallocatedResource | `24` | Vertegenwoordigt het veld HasOverallocatedResource (Task). |
| IsEstimated | `25` | Vertegenwoordigt het veld IsEstimated (Task). |
| IsMilestone | `26` | Vertegenwoordigt het veld IsMilestone (taak). |
| IsCritical | `27` | Vertegenwoordigt het veld IsCritical (taak). |
| IsSubproject | `28` | Vertegenwoordigt het veld IsSubproject (taak). |
| IsSubprojectReadOnly | `29` | Vertegenwoordigt het veld IsSubprojectReadOnly (taak). |
| IsMarked | `30` | Vertegenwoordigt het veld IsMarked (taak). |
| IgnoreWarnings | `31` | Vertegenwoordigt het veld Negeer waarschuwingen (taak). |
| SubprojectName | `32` | Vertegenwoordigt het veld Subprojectnaam (taak). |
| IsExternalTask | `33` | Vertegenwoordigt het veld IsExternalTask (taak). |
| IsSummary | `34` | Vertegenwoordigt het veld IsSummary (taak). |
| ExternalTaskProject | `35` | Vertegenwoordigt het veld ExternalTaskProject (taak). |
| ExternalId | `36` | Vertegenwoordigt het veld ExternalId (taak). |
| ExternalKey | `37` | Vertegenwoordigt het veld ExternalKey (taak). |
| EarlyStart | `38` | Vertegenwoordigt het veld EarlyStart (taak). |
| EarlyFinish | `39` | Vertegenwoordigt het veld EarlyFinish (taak). |
| LateStart | `40` | Vertegenwoordigt het veld LateStart (taak). |
| LateFinish | `41` | Vertegenwoordigt het veld LateFinish (taak). |
| StartVariance | `42` | Vertegenwoordigt het veld StartVariance (taak). |
| FinishVariance | `43` | Vertegenwoordigt het veld FinishVariance (taak). |
| WorkVariance | `44` | Vertegenwoordigt het veld WorkVariance (taak). |
| CostVariance | `45` | Vertegenwoordigt het veld CostVariance (Task). |
| FreeSlack | `46` | Vertegenwoordigt het veld FreeSlack (taak). |
| TotalSlack | `47` | Vertegenwoordigt het veld TotalSlack (taak). |
| StartSlack | `48` | Vertegenwoordigt het veld StartSlack (taak). |
| FinishSlack | `49` | Vertegenwoordigt het veld FinishSlack (taak). |
| FixedCost | `50` | Vertegenwoordigt het veld FixedCost (Task). |
| FixedCostAccrual | `51` | Vertegenwoordigt het veld FixedCostAccrual (Task). |
| PercentComplete | `52` | Vertegenwoordigt het veld PercentComplete (taak). |
| PercentWorkComplete | `53` | Vertegenwoordigt het veld PercentWorkComplete (Task). |
| Cost | `54` | Vertegenwoordigt het veld Kosten (taak). |
| OvertimeCost | `55` | Vertegenwoordigt het veld OvertimeCost (Task). |
| ActualStart | `56` | Vertegenwoordigt het veld ActualStart (taak). |
| ActualFinish | `57` | Vertegenwoordigt het veld ActualFinish (taak). |
| ActualDuration | `58` | Vertegenwoordigt het veld Werkelijke duur (taak). |
| ActualCost | `59` | Vertegenwoordigt het veld ActualCost (taak). |
| ActualOvertimeCost | `60` | Vertegenwoordigt het veld ActualOvertimeCost (Task). |
| ActualWork | `61` | Vertegenwoordigt het veld ActualWork (taak). |
| ActualOvertimeWork | `62` | Vertegenwoordigt het veld ActualOvertimeWork (Task). |
| OvertimeWork | `63` | Vertegenwoordigt het veld Overwerk (taak). |
| RegularWork | `64` | Vertegenwoordigt het veld RegularWork (taak). |
| RemainingDuration | `65` | Vertegenwoordigt het veld Resterende duur (taak). |
| RemainingCost | `66` | Vertegenwoordigt het veld Resterende kosten (taak). |
| RemainingWork | `67` | Vertegenwoordigt het veld Resterend werk (taak). |
| RemainingOvertimeWork | `68` | Vertegenwoordigt het veld RemainingOvertimeWork (Task). |
| RemainingOvertimeCost | `69` | Vertegenwoordigt het veld RemainingOvertimeCost (Task). |
| ACWP | `70` | Vertegenwoordigt het veld ACWP (taak). |
| CV | `71` | Vertegenwoordigt het veld CV (taak). |
| SV | `72` | Vertegenwoordigt het veld SV (taak). |
| ConstraintType | `73` | Vertegenwoordigt het veld ConstraintType (taak). |
| Calendar | `74` | Vertegenwoordigt het kalenderveld (taak). |
| ConstraintDate | `75` | Vertegenwoordigt het veld ConstraintDate (taak). |
| Deadline | `76` | Vertegenwoordigt het veld Deadline (taak). |
| LevelAssignments | `77` | Vertegenwoordigt het veld LevelAssignments (taak). |
| LevelingCanSplit | `78` | Vertegenwoordigt het veld LevelingCanSplit (taak). |
| LevelingDelay | `79` | Vertegenwoordigt het veld LevelingDelay (taak). |
| LevelingDelayFormat | `80` | Vertegenwoordigt het veld LevelingDelayFormat (taak). |
| PreleveledStart | `81` | Vertegenwoordigt het veld PreleveledStart (taak). |
| PreleveledFinish | `82` | Vertegenwoordigt het veld PreleveledFinish (taak). |
| Hyperlink | `83` | Vertegenwoordigt het veld Hyperlink (taak). |
| HyperlinkAddress | `84` | Vertegenwoordigt het veld HyperlinkAddress (taak). |
| HyperlinkSubAddress | `85` | Vertegenwoordigt het veld HyperlinkSubAddress (taak). |
| IgnoreResourceCalendar | `86` | Vertegenwoordigt het IgnoreResourceCalendar (taak) veld. |
| HideBar | `87` | Vertegenwoordigt het veld HideBar (taak). |
| IsRollup | `88` | Vertegenwoordigt het veld IsRollup (taak). |
| BCWS | `89` | Vertegenwoordigt het BCWS-veld (taak). |
| BCWP | `90` | Vertegenwoordigt het veld GKUW (taak). |
| PhysicalPercentComplete | `91` | Vertegenwoordigt het veld PhysicalPercentComplete (Task). |
| EarnedValueMethod | `92` | Vertegenwoordigt het veld EarnedValueMethod (taak). |
| ActualWorkProtected | `93` | Vertegenwoordigt het veld ActualWorkProtected (taak). |
| ActualOvertimeWorkProtected | `94` | Vertegenwoordigt het veld ActualOvertimeWorkProtected (Task). |
| IsPublished | `95` | Vertegenwoordigt het veld IsPublished (taak). |
| IsScheduled | `96` | Vertegenwoordigt het veld IsScheduled (Task). |
| StatusManager | `97` | Vertegenwoordigt het veld StatusManager (taak). |
| CommitmentStart | `98` | Vertegenwoordigt het veld CommitmentStart (taak). |
| CommitmentFinish | `99` | Vertegenwoordigt het veld CommitmentFinish (taak). |
| CommitmentType | `100` | Vertegenwoordigt het veld CommitmentType (taak). |
| IsManual | `101` | Vertegenwoordigt het veld IsManual (taak). |
| IsExpanded | `102` | Vertegenwoordigt het veld IsUitgevouwen (taak). |
| Guid | `103` | Vertegenwoordigt het veld Guid (taak). |
| NotesText | `104` | Vertegenwoordigt het NotesText (taak) veld. |
| NotesRTF | `105` | Vertegenwoordigt het veld NotesRTF (taak). |
| ManualStart | `106` | Vertegenwoordigt het veld Handmatig starten (taak). |
| ManualFinish | `107` | Vertegenwoordigt het veld ManualFinish (taak). |
| ManualDuration | `108` | Vertegenwoordigt het veld ManualDuration (Task). |
| BudgetWork | `109` | Vertegenwoordigt het veld BudgetWork (taak). |
| BudgetCost | `110` | Vertegenwoordigt het veld BudgetCost (taak). |
| DisplayAsSummary | `111` | Vertegenwoordigt het veld DisplayAsSummary (taak). |
| SummaryProgress | `112` | Vertegenwoordigt het veld Samenvattende voortgang (taak). |
| IsActive | `113` | Vertegenwoordigt het veld IsActief (taak). |
| StartText | `114` | Vertegenwoordigt het veld StartText (taak). |
| FinishText | `115` | Vertegenwoordigt het veld FinishText (taak). |
| DurationText | `116` | Vertegenwoordigt het veld DurationText (taak). |
| CalendarUid | `117` | Vertegenwoordigt het veld CalendarUid (taak). |
| ParentTaskUid | `118` | Vertegenwoordigt het veld ParentTaskUid (taak). |
| DisplayOnTimeline | `119` | Vertegenwoordigt het veld DisplayOnTimeline (taak). |
| TaskIsAssigned | `120` | Vertegenwoordigt het veld TaskIsAssigned (taak). |
| TaskOriginalStart | `121` | Vertegenwoordigt het oorspronkelijke startveld (taak). |
| TaskOriginalFinish | `122` | Vertegenwoordigt het oorspronkelijke voltooiingsveld (taak). |
| IsShowBeforeProjectStartDateWarning | `123` | Vertegenwoordigt de vlag waardoor MSP de waarschuwing laat zien dat een taak begint vóór de startdatum van het project. |
| Warning | `124` | Vertegenwoordigt de vlag die aangeeft dat de taak planningsverschillen heeft. |
| ParentTaskGuid | `125` | Vertegenwoordigt het veld ParentTaskGuid (taak). |
| ActivityId | `126` | Vertegenwoordigt het veld ActivityId (alleen van toepassing op Primavera-projecten). |
| FreeSlackTimeSpan | `127` | Vertegenwoordigt het veld FreeSlack (taak). |
| TotalSlackTimeSpan | `128` | Vertegenwoordigt het veld TotalSlack (taak). |
| StartSlackTimeSpan | `129` | Vertegenwoordigt het veld StartSlack (taak). |
| FinishSlackTimeSpan | `130` | Vertegenwoordigt het veld FinishSlack (taak). |
| ExternalUid | `131` | Vertegenwoordigt de unieke id van externe taak. |

### Zie ook

* naamruimte [Aspose.Tasks](../../aspose.tasks/)
* montage [Aspose.Tasks](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Tasks.dll -->
