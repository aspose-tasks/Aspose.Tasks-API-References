---
title: "Aspose::Tasks::ExtendedAttributeDefinition::CreateExtendedAttribute methode"
linktitle: "CreateExtendedAttribute"
articleTitle: "CreateExtendedAttribute"
second_title: "Aspose.Tasks voor C++"
description: "Maakt een nieuw uitgebreid attribuut aan met de veld-ID die gelijk is aan de veld-ID-waarde van dit object."
type: docs
weight: 20
url: /nl/cpp/aspose.tasks/extendedattributedefinition/createextendedattribute/
---

## CreateExtendedAttribute (1 of 7) {#createextendedattribute_1}

Maakt een nieuw uitgebreid attribuut aan met de veld-ID die gelijk is aan de veld-ID-waarde van dit object.

**Returns:** returns created instance of the ExtendedAttribute class with the fieldID which equals to this object's fieldID value.

```cpp
CreateExtendedAttribute()
```

---

## CreateExtendedAttribute (2 of 7) {#createextendedattribute_2}

Maakt een nieuw uitgebreid attribuut aan met de veld-ID die gelijk is aan de veld-ID-waarde van dit object en de opgegeven vlagwaarde.

**Returns:** returns created instance of the ExtendedAttribute class with the fieldID which equals to this object's fieldID value.

```cpp
CreateExtendedAttribute(bool flagValue)
```

| Parameter | Beschrijving |
| --- | --- |
| flagValue | De opgegeven vlagwaarde. |

---

## CreateExtendedAttribute (3 of 7) {#createextendedattribute_3}

Maakt een nieuw uitgebreid attribuut aan dat gekoppeld is aan het opgegeven waarde-item.

**Returns:** returns created instance of the ExtendedAttribute class linked with specified Value item.

```cpp
CreateExtendedAttribute(const System::SharedPtr< Value > & lookupValue)
```

| Parameter | Beschrijving |
| --- | --- |
| lookupValue | Het opgegeven waarde-item. |

---

## CreateExtendedAttribute (4 of 7) {#createextendedattribute_4}

Maakt een nieuw uitgebreid attribuut aan met de veld-ID die gelijk is aan de veld-ID-waarde van dit object en de opgegeven tekstwaarde.

**Returns:** returns created instance of the ExtendedAttribute class with the fieldID which equals to this object's fieldID value.

```cpp
CreateExtendedAttribute(const System::String & textValue)
```

| Parameter | Beschrijving |
| --- | --- |
| textValue | De opgegeven tekstwaarde. |

---

## CreateExtendedAttribute (5 of 7) {#createextendedattribute_5}

Maakt een nieuw uitgebreid attribuut aan met de veld-ID die gelijk is aan de veld-ID-waarde van dit object en de opgegeven duurwaarde.

**Returns:** returns created instance of the ExtendedAttribute class with the fieldID which equals to this object's fieldID value.

```cpp
CreateExtendedAttribute(Duration durationValue)
```

| Parameter | Beschrijving |
| --- | --- |
| durationValue | De opgegeven duurwaarde. |

---

## CreateExtendedAttribute (6 of 7) {#createextendedattribute_6}

Maakt een nieuw uitgebreid attribuut aan met de veld-ID die gelijk is aan de veld-ID-waarde van dit object en de opgegeven datumwaarde.

**Returns:** returns created instance of the ExtendedAttribute class with the fieldID which equals to this object's fieldID value.

```cpp
CreateExtendedAttribute(System::DateTime dateTimeValue)
```

| Parameter | Beschrijving |
| --- | --- |
| dateTimeValue | De opgegeven datum‑tijdwaarde. |

---

## CreateExtendedAttribute (7 of 7) {#createextendedattribute_7}

Maakt een nieuw uitgebreid attribuut met de veld‑ID die gelijk is aan de veld‑ID‑waarde van dit object en de opgegeven numerieke waarde.

**Returns:** returns created instance of the ExtendedAttribute class with the fieldID which equals to this object's fieldID value.

```cpp
CreateExtendedAttribute(System::Decimal numericValue)
```

| Parameter | Beschrijving |
| --- | --- |
| numericValue | De opgegeven numerieke waarde. |

