---
title: "Aspose::Tasks::View::CompareTo methode"
linktitle: "CompareTo"
articleTitle: "CompareTo"
second_title: "Aspose.Tasks voor C++"
description: "Vergelijkt de huidige instantie met een ander object van hetzelfde type en retourneert een geheel getal dat aangeeft of de huidige instantie voorafgaat, volgt of occu"
type: docs
weight: 10
url: /nl/cpp/aspose.tasks/view/compareto/
---

## CompareTo {#compareto}

Vergelijkt de huidige instantie met een ander object van hetzelfde type en retourneert een integer die aangeeft of de huidige instantie voorafgaat, volgt of zich op dezelfde positie in de sorteervolgorde bevindt als het andere object.

**Returns:** A 32-bit signed integer that indicates the relative order of the objects being compared. The return value has these meanings: Value Meaning Less than zero This instance precedes other in the sort order. Zero This instance occurs in the same position in the sort order as other . Greater than zero This instance follows other in the sort order.

```cpp
CompareTo(System::SharedPtr< View > other)
```

| Parameter | Beschrijving |
| --- | --- |
| anders | het opgegeven View-object waarmee deze instantie wordt vergeleken. |

