---
title: "PdfPermissions"
second_title: "Aspose.Tasks for Java API-referentie"
description: "Specificeert de rechten die gebruikt mogen worden voor toegang tot een PDF-document."
type: docs
weight: 190
url: /nl/java/com.aspose.tasks/pdfpermissions/
---

**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class PdfPermissions extends System.Enum
```

Specificeert de rechten die gebruikt mogen worden voor toegang tot een PDF-document.
## Velden

| Veld | Beschrijving |
| --- | --- |
| [AllowAll](#AllowAll) | Specificeert de rechten die gebruikt mogen worden voor toegang tot een PDF-document. |
| [ContentCopy](#ContentCopy) | Specificeert de rechten die gebruikt mogen worden voor toegang tot een PDF-document. |
| [ContentCopyForAccessibility](#ContentCopyForAccessibility) | Specificeert de rechten die gebruikt mogen worden voor toegang tot een PDF-document. |
| [DocumentAssembly](#DocumentAssembly) | Specificeert de rechten die gebruikt mogen worden voor toegang tot een PDF-document. |
| [FillIn](#FillIn) | Specificeert de rechten die gebruikt mogen worden voor toegang tot een PDF-document. |
| [HighResolutionPrinting](#HighResolutionPrinting) | Specificeert de rechten die gebruikt mogen worden voor toegang tot een PDF-document. |
| [ModifyAnnotations](#ModifyAnnotations) | Specificeert de rechten die gebruikt mogen worden voor toegang tot een PDF-document. |
| [ModifyContents](#ModifyContents) | Specificeert de rechten die gebruikt mogen worden voor toegang tot een PDF-document. |
| [None](#None) | Specificeert de rechten die gebruikt mogen worden voor toegang tot een PDF-document. |
| [Printing](#Printing) | Specificeert de rechten die gebruikt mogen worden voor toegang tot een PDF-document. |
### AllowAll {#AllowAll}
```
public static final int AllowAll
```


Specificeert de rechten die gebruikt mogen worden voor toegang tot een PDF-document.

### ContentCopy {#ContentCopy}
```
public static final int ContentCopy
```


Specificeert de rechten die gebruikt mogen worden voor toegang tot een PDF-document.

### ContentCopyForAccessibility {#ContentCopyForAccessibility}
```
public static final int ContentCopyForAccessibility
```


Specificeert de rechten die gebruikt mogen worden voor toegang tot een PDF-document.

### DocumentAssembly {#DocumentAssembly}
```
public static final int DocumentAssembly
```


Specificeert de rechten die gebruikt mogen worden voor toegang tot een PDF-document.

### FillIn {#FillIn}
```
public static final int FillIn
```


Specificeert de rechten die gebruikt mogen worden voor toegang tot een PDF-document.

### HighResolutionPrinting {#HighResolutionPrinting}
```
public static final int HighResolutionPrinting
```


Specificeert de rechten die gebruikt mogen worden voor toegang tot een PDF-document.

### ModifyAnnotations {#ModifyAnnotations}
```
public static final int ModifyAnnotations
```


Specificeert de rechten die gebruikt mogen worden voor toegang tot een PDF-document.

### ModifyContents {#ModifyContents}
```
public static final int ModifyContents
```


Specificeert de rechten die gebruikt mogen worden voor toegang tot een PDF-document.

### None {#None}
```
public static final int None
```


Specificeert de rechten die gebruikt mogen worden voor toegang tot een PDF-document.

### Printing {#Printing}
```
public static final int Printing
```


Specificeert de rechten die gebruikt mogen worden voor toegang tot een PDF-document.

