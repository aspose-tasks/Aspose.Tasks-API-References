---
title: "RiskPatternCollection"
second_title: "Aspose.Tasks for Java API-referentie"
description: "Stelt een collectie voor die de instanties van de RiskPattern-klasse bevat."
type: docs
weight: 269
url: /nl/java/com.aspose.tasks/riskpatterncollection/
---

**Inheritance:**
java.lang.Object, java.util.AbstractCollection
```
public class RiskPatternCollection extends AbstractCollection<RiskPattern>
```

Vertegenwoordigt een collectie die de instanties van de `RiskPattern`-klasse bevat.
## Methoden

| Methode | Beschrijving |
| --- | --- |
| [add(RiskPattern item)](#add-com.aspose.tasks.RiskPattern-) | Voegt een instantie van de `RiskPattern`-klasse toe aan deze collectie. |
| [clear()](#clear--) | Verwijdert alle items uit deze collectie. |
| [contains(Object item)](#contains-java.lang.Object-) | Retourneert true als het opgegeven item in deze collectie wordt gevonden; anders false. |
| [copyTo(RiskPattern[] array, int arrayIndex)](#copyTo-com.aspose.tasks.RiskPattern---int-) | Kopieert de elementen van deze collectie naar de opgegeven array, beginnend bij de opgegeven array-index. |
| [get_Item(Task task)](#get-Item-com.aspose.tasks.Task-) | Haalt de instantie van de `RiskPattern`-klasse op voor de opgegeven taak. |
| [iterator()](#iterator--) | Retourneert een enumerator voor deze collectie. |
| [remove(Object item)](#remove-java.lang.Object-) | Verwijdert de eerste instantie van een specifiek object uit deze collectie. |
| [size()](#size--) | Retourneert het aantal elementen dat in deze collectie zit. |
### add(RiskPattern item) {#add-com.aspose.tasks.RiskPattern-}
```
public boolean add(RiskPattern item)
```


Voegt een instantie van de `RiskPattern`-klasse toe aan deze collectie.

**Parameters:**
| Parameter | Type | Beschrijving |
| --- | --- | --- |
| item | [RiskPattern](../../com.aspose.tasks/riskpattern) | een instantie van de `RiskPattern`-klasse om toe te voegen aan deze collectie. |

**Returns:**
boolean - retourneert true als een instantie van de `RiskPattern`-klasse succesvol is toegevoegd; anders false.
### clear() {#clear--}
```
public void clear()
```


Verwijdert alle items uit deze collectie.

### contains(Object item) {#contains-java.lang.Object-}
```
public boolean contains(Object item)
```


Retourneert true als het opgegeven item in deze collectie wordt gevonden; anders false.

**Parameters:**
| Parameter | Type | Beschrijving |
| --- | --- | --- |
| item | java.lang.Object | het opgegeven item om te vinden. |

**Returns:**
boolean - true als het opgegeven item in deze collectie wordt gevonden; anders false.
### copyTo(RiskPattern[] array, int arrayIndex) {#copyTo-com.aspose.tasks.RiskPattern---int-}
```
public void copyTo(RiskPattern[] array, int arrayIndex)
```


Kopieert de elementen van deze collectie naar de opgegeven array, beginnend bij de opgegeven array-index.

**Parameters:**
| Parameter | Type | Beschrijving |
| --- | --- | --- |
| array | [RiskPattern\[\]](../../com.aspose.tasks/riskpattern) | de opgegeven één-dimensionale array om elementen naartoe te kopiëren. |
| arrayIndex | int | de nulgebaseerde index van de opgegeven array waarop het kopiëren begint. |

### get_Item(Task task) {#get-Item-com.aspose.tasks.Task-}
```
public RiskPattern get_Item(Task task)
```


Haalt de instantie van de `RiskPattern`-klasse op voor de opgegeven taak.

**Parameters:**
| Parameter | Type | Beschrijving |
| --- | --- | --- |
| task | [Task](../../com.aspose.tasks/task) | de opgegeven taak. |

**Returns:**
[RiskPattern](../../com.aspose.tasks/riskpattern) - the pattern for the specified task.
### iterator() {#iterator--}
```
public Iterator<RiskPattern> iterator()
```


Retourneert een enumerator voor deze collectie.

**Returns:**
java.util.Iterator&lt;com.aspose.tasks.RiskPattern&gt; - een enumerator voor deze collectie.
### remove(Object item) {#remove-java.lang.Object-}
```
public boolean remove(Object item)
```


Verwijdert de eerste instantie van een specifiek object uit deze collectie.

**Parameters:**
| Parameter | Type | Beschrijving |
| --- | --- | --- |
| item | java.lang.Object | het opgegeven object om te verwijderen. |

**Returns:**
boolean - true als het opgegeven object succesvol uit deze collectie is verwijderd; anders false.
### size() {#size--}
```
public int size()
```


Retourneert het aantal elementen dat in deze collectie zit.

**Returns:**
int - het aantal elementen dat in deze collectie zit.
