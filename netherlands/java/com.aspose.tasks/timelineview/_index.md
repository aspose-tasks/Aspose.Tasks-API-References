---
title: "TimelineView"
second_title: "Aspose.Tasks for Java API-referentie"
description: "Stelt een tijdlijnweergave van een project voor."
type: docs
weight: 319
url: /nl/java/com.aspose.tasks/timelineview/
---

**Inheritance:**
java.lang.Object, [com.aspose.tasks.View](../../com.aspose.tasks/view)
```
public class TimelineView extends View
```

Stelt een tijdlijnweergave van een project voor.
## Constructors

| Constructor | Beschrijving |
| --- | --- |
| [TimelineView()](#TimelineView--) | Initialiseert een nieuw exemplaar van de [TimelineView](../../com.aspose.tasks/timelineview) klasse. |
## Methoden

| Methode | Beschrijving |
| --- | --- |
| [getDateFormat()](#getDateFormat--) | Haalt een waarde op die aangeeft hoe datums op de Timeline-weergave worden opgemaakt. |
| [getDisplayOverlapped()](#getDisplayOverlapped--) | Haalt een waarde op die aangeeft of overlappende taken op meerdere rijen worden weergegeven. |
| [getShowDates()](#getShowDates--) | Haalt een waarde op die aangeeft of datums worden getoond. |
| [getShowPanZoom()](#getShowPanZoom--) | Haalt een waarde op die aangeeft of pan- en zoombesturing worden getoond. |
| [getShowTimescale()](#getShowTimescale--) | Haalt een waarde op die aangeeft of de tijdschaal wordt getoond. |
| [getShowToday()](#getShowToday--) | Haalt een waarde op die aangeeft of een lijn die vandaag weergeeft wordt weergegeven. |
| [getTextLinesCount()](#getTextLinesCount--) | Haalt een waarde op die aangeeft hoeveel lijnen worden gebruikt om een taak in een tijdlijn weer te geven. |
| [setDateFormat(int value)](#setDateFormat-int-) | Stelt een waarde in die aangeeft hoe datums op de Timeline-weergave worden opgemaakt. |
| [setDisplayOverlapped(boolean value)](#setDisplayOverlapped-boolean-) | Stelt een waarde in die aangeeft of overlappende taken op meerdere rijen worden weergegeven. |
| [setShowPanZoom(boolean value)](#setShowPanZoom-boolean-) | Stelt een waarde in die aangeeft of pan- en zoombesturing worden getoond. |
| [setShowTimescale(boolean value)](#setShowTimescale-boolean-) | Stelt een waarde in die aangeeft of de tijdschaal wordt getoond. |
| [setShowToday(boolean value)](#setShowToday-boolean-) | Stelt een waarde in die aangeeft of een lijn die vandaag weergeeft wordt weergegeven. |
| [setTextLinesCount(int value)](#setTextLinesCount-int-) | Stelt een waarde in die aangeeft hoeveel lijnen worden gebruikt om een taak in een tijdlijn weer te geven. |
### TimelineView() {#TimelineView--}
```
public TimelineView()
```


Initialiseert een nieuw exemplaar van de [TimelineView](../../com.aspose.tasks/timelineview) klasse.

### getDateFormat() {#getDateFormat--}
```
public final int getDateFormat()
```


Haalt een waarde op die aangeeft hoe datums op de Timeline-weergave worden opgemaakt.

**Returns:**
int - een waarde die aangeeft hoe datums op de Timeline-weergave worden opgemaakt.
### getDisplayOverlapped() {#getDisplayOverlapped--}
```
public final boolean getDisplayOverlapped()
```


Haalt een waarde op die aangeeft of overlappende taken op meerdere rijen worden weergegeven.

**Returns:**
boolean - een waarde die aangeeft of overlappende taken op meerdere rijen worden weergegeven.
### getShowDates() {#getShowDates--}
```
public final boolean getShowDates()
```


Haalt een waarde op die aangeeft of datums worden getoond.

**Returns:**
boolean - een waarde die aangeeft of datums worden getoond.
### getShowPanZoom() {#getShowPanZoom--}
```
public final boolean getShowPanZoom()
```


Haalt een waarde op die aangeeft of pan- en zoombesturing worden getoond.

**Returns:**
boolean - een waarde die aangeeft of pan- en zoombesturing worden getoond.
### getShowTimescale() {#getShowTimescale--}
```
public final boolean getShowTimescale()
```


Haalt een waarde op die aangeeft of de tijdschaal wordt getoond.

**Returns:**
boolean - een waarde die aangeeft of de tijdschaal wordt getoond.
### getShowToday() {#getShowToday--}
```
public final boolean getShowToday()
```


Haalt een waarde op die aangeeft of een lijn die vandaag weergeeft wordt weergegeven.

**Returns:**
boolean - een waarde die aangeeft of een lijn die vandaag weergeeft wordt weergegeven.
### getTextLinesCount() {#getTextLinesCount--}
```
public final int getTextLinesCount()
```


Haalt een waarde op die aangeeft hoeveel lijnen worden gebruikt om een taak in een tijdlijn weer te geven.

**Returns:**
int - een waarde die aangeeft hoeveel lijnen worden gebruikt om een taak in een tijdlijn weer te geven.
### setDateFormat(int value) {#setDateFormat-int-}
```
public final void setDateFormat(int value)
```


Stelt een waarde in die aangeeft hoe datums op de Timeline-weergave worden opgemaakt.

**Parameters:**
| Parameter | Type | Beschrijving |
| --- | --- | --- |
| waarde | int | een waarde die aangeeft hoe datums op de Timeline-weergave worden opgemaakt. |

### setDisplayOverlapped(boolean value) {#setDisplayOverlapped-boolean-}
```
public final void setDisplayOverlapped(boolean value)
```


Stelt een waarde in die aangeeft of overlappende taken op meerdere rijen worden weergegeven.

**Parameters:**
| Parameter | Type | Beschrijving |
| --- | --- | --- |
| waarde | boolean | een waarde die aangeeft of overlappende taken op meerdere rijen worden weergegeven. |

### setShowPanZoom(boolean value) {#setShowPanZoom-boolean-}
```
public final void setShowPanZoom(boolean value)
```


Stelt een waarde in die aangeeft of pan- en zoombesturing worden getoond.

**Parameters:**
| Parameter | Type | Beschrijving |
| --- | --- | --- |
| waarde | boolean | een waarde die aangeeft of de pan- en zoombediening moet worden weergegeven. |

### setShowTimescale(boolean value) {#setShowTimescale-boolean-}
```
public final void setShowTimescale(boolean value)
```


Stelt een waarde in die aangeeft of de tijdschaal wordt getoond.

**Parameters:**
| Parameter | Type | Beschrijving |
| --- | --- | --- |
| waarde | boolean | een waarde die aangeeft of de tijdschaal moet worden weergegeven. |

### setShowToday(boolean value) {#setShowToday-boolean-}
```
public final void setShowToday(boolean value)
```


Stelt een waarde in die aangeeft of een lijn die vandaag weergeeft wordt weergegeven.

**Parameters:**
| Parameter | Type | Beschrijving |
| --- | --- | --- |
| waarde | boolean | een waarde die aangeeft of een lijn die vandaag weergeeft moet worden getoond. |

### setTextLinesCount(int value) {#setTextLinesCount-int-}
```
public final void setTextLinesCount(int value)
```


Stelt een waarde in die aangeeft hoeveel lijnen worden gebruikt om een taak in een tijdlijn weer te geven.

**Parameters:**
| Parameter | Type | Beschrijving |
| --- | --- | --- |
| waarde | int | een waarde die aangeeft hoeveel lijnen worden gebruikt om taken in een tijdlijn weer te geven. |

