---
title: "ByYearWeekDayRepetition"
second_title: "Aspose.Tasks for Java API-referentie"
description: "Stelt een patroon voor dat gebaseerd is op de positie van een weekdag in een maand."
type: docs
weight: 38
url: /nl/java/com.aspose.tasks/byyearweekdayrepetition/
---

**Inheritance:**
java.lang.Object, [com.aspose.tasks.YearlyRepetitionBase](../../com.aspose.tasks/yearlyrepetitionbase)
```
public class ByYearWeekDayRepetition extends YearlyRepetitionBase
```

Stelt een patroon voor dat gebaseerd is op de positie van een weekdag in een maand.
## Constructors

| Constructor | Beschrijving |
| --- | --- |
| [ByYearWeekDayRepetition()](#ByYearWeekDayRepetition--) | Initialiseert een nieuwe instantie van de [ByYearWeekDayRepetition](../../com.aspose.tasks/byyearweekdayrepetition) klasse. |
## Methoden

| Methode | Beschrijving |
| --- | --- |
| [getMonth()](#getMonth--) | Haalt de maand op waarop de taak moet terugkeren. |
| [getPosition()](#getPosition--) | Haalt de positie van de dag in een week van een maand op waarop de taak moet terugkeren. |
| [getWeekDay()](#getWeekDay--) | Haalt het type weekdag op waarop de taak moet terugkeren. |
| [setMonth(int value)](#setMonth-int-) | Stelt de maand in waarop de taak moet terugkeren. |
| [setPosition(int value)](#setPosition-int-) | Stelt de positie van de dag in een week van een maand in waarop de taak moet terugkeren. |
| [setWeekDay(int value)](#setWeekDay-int-) | Stelt het type weekdag in waarop de taak moet terugkeren. |
### ByYearWeekDayRepetition() {#ByYearWeekDayRepetition--}
```
public ByYearWeekDayRepetition()
```


Initialiseert een nieuwe instantie van de [ByYearWeekDayRepetition](../../com.aspose.tasks/byyearweekdayrepetition) klasse.

### getMonth() {#getMonth--}
```
public final int getMonth()
```


Haalt de maand op waarop de taak moet terugkeren.

Lezen/Schrijven `Month`.

**Returns:**
int - een maand waarop de taak moet terugkeren.
### getPosition() {#getPosition--}
```
public final int getPosition()
```


Haalt de positie van de dag in een week van een maand op waarop de taak moet terugkeren.

Lezen/Schrijven `OrdinalNumber`.

**Returns:**
int - een positie van de dag in een week van een maand waarop de taak moet terugkeren.
### getWeekDay() {#getWeekDay--}
```
public final int getWeekDay()
```


Haalt het type weekdag op waarop de taak moet terugkeren.

Lezen/Schrijven `DayOfWeek`.

**Returns:**
int - een type weekdag waarop de taak moet terugkeren.
### setMonth(int value) {#setMonth-int-}
```
public final void setMonth(int value)
```


Stelt de maand in waarop de taak moet terugkeren.

Lezen/Schrijven `Month`.

**Parameters:**
| Parameter | Type | Beschrijving |
| --- | --- | --- |
| waarde | int | een maand waarop de taak moet terugkeren. |

### setPosition(int value) {#setPosition-int-}
```
public final void setPosition(int value)
```


Stelt de positie van de dag in een week van een maand in waarop de taak moet terugkeren.

Lezen/Schrijven `OrdinalNumber`.

**Parameters:**
| Parameter | Type | Beschrijving |
| --- | --- | --- |
| waarde | int | een positie van de dag in een week van een maand waarop de taak moet terugkeren. |

### setWeekDay(int value) {#setWeekDay-int-}
```
public final void setWeekDay(int value)
```


Stelt het type weekdag in waarop de taak moet terugkeren.

Lezen/Schrijven `DayOfWeek`.

**Parameters:**
| Parameter | Type | Beschrijving |
| --- | --- | --- |
| waarde | int | een type weekdag waarop de taak moet terugkeren. |

