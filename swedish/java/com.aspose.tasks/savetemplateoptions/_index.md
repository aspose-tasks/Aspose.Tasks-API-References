---
title: "SaveTemplateOptions"
second_title: "Aspose.Tasks for Java API-referens"
description: "Tillåter att ange ytterligare alternativ när ett projekt sparas som en mall."
type: docs
weight: 275
url: /sv/java/com.aspose.tasks/savetemplateoptions/
---

**Inheritance:**
java.lang.Object
```
public class SaveTemplateOptions
```

Tillåter att ange ytterligare alternativ när ett projekt sparas som en mall.
## Konstruktörer

| Konstruktor | Beskrivning |
| --- | --- |
| [SaveTemplateOptions()](#SaveTemplateOptions--) |  |
## Metoder

| Metod | Beskrivning |
| --- | --- |
| [getRemoveActualValues()](#getRemoveActualValues--) | Hämtar ett värde som indikerar om alla faktiska värden från en projekttmall ska tas bort. |
| [getRemoveBaselineValues()](#getRemoveBaselineValues--) | Hämtar ett värde som indikerar om alla baslinjevärden från en projekttmall ska tas bort. |
| [getRemoveFixedCosts()](#getRemoveFixedCosts--) | Hämtar ett värde som indikerar om alla fasta kostnader från en projekttmall ska tas bort. |
| [getRemoveResourceRates()](#getRemoveResourceRates--) | Hämtar ett värde som indikerar om resurspriser från en projekttmall ska tas bort. |
| [setRemoveActualValues(boolean value)](#setRemoveActualValues-boolean-) | Ställer in ett värde som indikerar om alla faktiska värden från en projekttmall ska tas bort. |
| [setRemoveBaselineValues(boolean value)](#setRemoveBaselineValues-boolean-) | Ställer in ett värde som indikerar om alla baslinjevärden från en projekttmall ska tas bort. |
| [setRemoveFixedCosts(boolean value)](#setRemoveFixedCosts-boolean-) | Ställer in ett värde som indikerar om alla fasta kostnader från en projekttmall ska tas bort. |
| [setRemoveResourceRates(boolean value)](#setRemoveResourceRates-boolean-) | Ställer in ett värde som indikerar om resurspriser från en projekttmall ska tas bort. |
### SaveTemplateOptions() {#SaveTemplateOptions--}
```
public SaveTemplateOptions()
```


### getRemoveActualValues() {#getRemoveActualValues--}
```
public final boolean getRemoveActualValues()
```


Hämtar ett värde som indikerar om alla faktiska värden från en projekttmall ska tas bort.

**Returns:**
boolean - ett värde som indikerar om alla faktiska värden från en projekttmall ska tas bort.
### getRemoveBaselineValues() {#getRemoveBaselineValues--}
```
public final boolean getRemoveBaselineValues()
```


Hämtar ett värde som indikerar om alla baslinjevärden från en projekttmall ska tas bort.

**Returns:**
boolean - ett värde som indikerar om alla baslinjevärden från en projekttmall ska tas bort.
### getRemoveFixedCosts() {#getRemoveFixedCosts--}
```
public final boolean getRemoveFixedCosts()
```


Hämtar ett värde som indikerar om alla fasta kostnader från en projekttmall ska tas bort.

**Returns:**
boolean - ett värde som indikerar om alla fasta kostnader från en projekttmall ska tas bort.
### getRemoveResourceRates() {#getRemoveResourceRates--}
```
public final boolean getRemoveResourceRates()
```


Hämtar ett värde som indikerar om resurspriser från en projekttmall ska tas bort.

**Returns:**
boolean - ett värde som indikerar om resurspriser från en projekttmall ska tas bort.
### setRemoveActualValues(boolean value) {#setRemoveActualValues-boolean-}
```
public final void setRemoveActualValues(boolean value)
```


Ställer in ett värde som indikerar om alla faktiska värden från en projekttmall ska tas bort.

**Parameters:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| värde | boolean | ett värde som indikerar om alla faktiska värden från en projekttmall ska tas bort. |

### setRemoveBaselineValues(boolean value) {#setRemoveBaselineValues-boolean-}
```
public final void setRemoveBaselineValues(boolean value)
```


Ställer in ett värde som indikerar om alla baslinjevärden från en projekttmall ska tas bort.

**Parameters:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| värde | boolean | ett värde som indikerar om alla baslinjevärden från en projekttmall ska tas bort. |

### setRemoveFixedCosts(boolean value) {#setRemoveFixedCosts-boolean-}
```
public final void setRemoveFixedCosts(boolean value)
```


Ställer in ett värde som indikerar om alla fasta kostnader från en projekttmall ska tas bort.

**Parameters:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| värde | boolean | ett värde som indikerar om alla fasta kostnader från en projekttmall ska tas bort. |

### setRemoveResourceRates(boolean value) {#setRemoveResourceRates-boolean-}
```
public final void setRemoveResourceRates(boolean value)
```


Ställer in ett värde som indikerar om resurspriser från en projekttmall ska tas bort.

**Parameters:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| värde | boolean | ett värde som indikerar om resurspriser från en projekttmall ska tas bort. |

