---
title: "TimelineView"
second_title: "Aspose.Tasks for Java API-referens"
description: "Representerar en tidslinjavy av ett projekt."
type: docs
weight: 319
url: /sv/java/com.aspose.tasks/timelineview/
---

**Inheritance:**
java.lang.Object, [com.aspose.tasks.View](../../com.aspose.tasks/view)
```
public class TimelineView extends View
```

Representerar en tidslinjavy av ett projekt.
## Konstruktörer

| Konstruktor | Beskrivning |
| --- | --- |
| [TimelineView()](#TimelineView--) | Initierar en ny instans av klassen [TimelineView](../../com.aspose.tasks/timelineview). |
## Metoder

| Metod | Beskrivning |
| --- | --- |
| [getDateFormat()](#getDateFormat--) | Hämtar ett värde som anger hur datum ska formateras i Timeline-vyn. |
| [getDisplayOverlapped()](#getDisplayOverlapped--) | Hämtar ett värde som anger om överlappande uppgifter ska visas på flera rader. |
| [getShowDates()](#getShowDates--) | Hämtar ett värde som anger om datum ska visas. |
| [getShowPanZoom()](#getShowPanZoom--) | Hämtar ett värde som anger om panorera- och zoomkontrollen ska visas. |
| [getShowTimescale()](#getShowTimescale--) | Hämtar ett värde som anger om tidslinjen ska visas. |
| [getShowToday()](#getShowToday--) | Hämtar ett värde som anger om en linje som representerar idag ska visas. |
| [getTextLinesCount()](#getTextLinesCount--) | Hämtar ett värde som anger hur många rader som används för att visa uppgifter i en tidslinje. |
| [setDateFormat(int value)](#setDateFormat-int-) | Ställer in ett värde som anger hur datum ska formateras i Timeline-vyn. |
| [setDisplayOverlapped(boolean value)](#setDisplayOverlapped-boolean-) | Ställer in ett värde som anger om överlappande uppgifter ska visas på flera rader. |
| [setShowPanZoom(boolean value)](#setShowPanZoom-boolean-) | Ställer in ett värde som anger om panorera- och zoomkontrollen ska visas. |
| [setShowTimescale(boolean value)](#setShowTimescale-boolean-) | Ställer in ett värde som anger om tidslinjen ska visas. |
| [setShowToday(boolean value)](#setShowToday-boolean-) | Ställer in ett värde som anger om en linje som representerar idag ska visas. |
| [setTextLinesCount(int value)](#setTextLinesCount-int-) | Ställer in ett värde som anger hur många rader som används för att visa uppgifter i en tidslinje. |
### TimelineView() {#TimelineView--}
```
public TimelineView()
```


Initierar en ny instans av klassen [TimelineView](../../com.aspose.tasks/timelineview).

### getDateFormat() {#getDateFormat--}
```
public final int getDateFormat()
```


Hämtar ett värde som anger hur datum ska formateras i Timeline-vyn.

**Returns:**
int - ett värde som anger hur datum ska formateras i Timeline-vyn.
### getDisplayOverlapped() {#getDisplayOverlapped--}
```
public final boolean getDisplayOverlapped()
```


Hämtar ett värde som anger om överlappande uppgifter ska visas på flera rader.

**Returns:**
boolean - ett värde som indikerar om överlappande uppgifter ska visas på flera rader.
### getShowDates() {#getShowDates--}
```
public final boolean getShowDates()
```


Hämtar ett värde som anger om datum ska visas.

**Returns:**
boolean - ett värde som indikerar om datum ska visas.
### getShowPanZoom() {#getShowPanZoom--}
```
public final boolean getShowPanZoom()
```


Hämtar ett värde som anger om panorera- och zoomkontrollen ska visas.

**Returns:**
boolean - ett värde som indikerar om panorera- och zoomkontrollen ska visas.
### getShowTimescale() {#getShowTimescale--}
```
public final boolean getShowTimescale()
```


Hämtar ett värde som anger om tidslinjen ska visas.

**Returns:**
boolean - ett värde som indikerar om tidslinjen ska visas.
### getShowToday() {#getShowToday--}
```
public final boolean getShowToday()
```


Hämtar ett värde som anger om en linje som representerar idag ska visas.

**Returns:**
boolean - ett värde som indikerar om en linje som representerar idag ska visas.
### getTextLinesCount() {#getTextLinesCount--}
```
public final int getTextLinesCount()
```


Hämtar ett värde som anger hur många rader som används för att visa uppgifter i en tidslinje.

**Returns:**
int - ett värde som indikerar hur många rader som används för att visa uppgiften i en tidslinje.
### setDateFormat(int value) {#setDateFormat-int-}
```
public final void setDateFormat(int value)
```


Ställer in ett värde som anger hur datum ska formateras i Timeline-vyn.

**Parameters:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| värde | int | ett värde som indikerar hur datum ska formateras i tidslinjevyn. |

### setDisplayOverlapped(boolean value) {#setDisplayOverlapped-boolean-}
```
public final void setDisplayOverlapped(boolean value)
```


Ställer in ett värde som anger om överlappande uppgifter ska visas på flera rader.

**Parameters:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| värde | boolean | ett värde som indikerar om överlappande uppgifter ska visas på flera rader. |

### setShowPanZoom(boolean value) {#setShowPanZoom-boolean-}
```
public final void setShowPanZoom(boolean value)
```


Ställer in ett värde som anger om panorera- och zoomkontrollen ska visas.

**Parameters:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| värde | boolean | ett värde som indikerar om panorera- och zoomkontrollen ska visas. |

### setShowTimescale(boolean value) {#setShowTimescale-boolean-}
```
public final void setShowTimescale(boolean value)
```


Ställer in ett värde som anger om tidslinjen ska visas.

**Parameters:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| värde | boolean | ett värde som indikerar om tidslinjen ska visas. |

### setShowToday(boolean value) {#setShowToday-boolean-}
```
public final void setShowToday(boolean value)
```


Ställer in ett värde som anger om en linje som representerar idag ska visas.

**Parameters:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| värde | boolean | ett värde som indikerar om en linje som representerar idag ska visas. |

### setTextLinesCount(int value) {#setTextLinesCount-int-}
```
public final void setTextLinesCount(int value)
```


Ställer in ett värde som anger hur många rader som används för att visa uppgifter i en tidslinje.

**Parameters:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| värde | int | ett värde som indikerar hur många rader som används för att visa uppgiften i en tidslinje. |

