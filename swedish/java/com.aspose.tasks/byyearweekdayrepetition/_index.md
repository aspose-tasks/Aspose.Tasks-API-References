---
title: "ByYearWeekDayRepetition"
second_title: "Aspose.Tasks for Java API-referens"
description: "Representerar ett mönster som baseras på positionen för en veckodag i en månad."
type: docs
weight: 38
url: /sv/java/com.aspose.tasks/byyearweekdayrepetition/
---

**Inheritance:**
java.lang.Object, [com.aspose.tasks.YearlyRepetitionBase](../../com.aspose.tasks/yearlyrepetitionbase)
```
public class ByYearWeekDayRepetition extends YearlyRepetitionBase
```

Representerar ett mönster som baseras på positionen för en veckodag i en månad.
## Konstruktörer

| Konstruktor | Beskrivning |
| --- | --- |
| [ByYearWeekDayRepetition()](#ByYearWeekDayRepetition--) | Initierar en ny instans av klassen [ByYearWeekDayRepetition](../../com.aspose.tasks/byyearweekdayrepetition). |
## Metoder

| Metod | Beskrivning |
| --- | --- |
| [getMonth()](#getMonth--) | Hämtar en månad då uppgiften måste återkomma. |
| [getPosition()](#getPosition--) | Hämtar positionen för dagen i en vecka i en månad då uppgiften måste återkomma. |
| [getWeekDay()](#getWeekDay--) | Hämtar typen av veckodag då uppgiften måste återkomma. |
| [setMonth(int value)](#setMonth-int-) | Ställer in en månad då uppgiften måste återkomma. |
| [setPosition(int value)](#setPosition-int-) | Ställer in positionen för dagen i en vecka i en månad då uppgiften måste återkomma. |
| [setWeekDay(int value)](#setWeekDay-int-) | Ställer in typen av veckodag då uppgiften måste återkomma. |
### ByYearWeekDayRepetition() {#ByYearWeekDayRepetition--}
```
public ByYearWeekDayRepetition()
```


Initierar en ny instans av klassen [ByYearWeekDayRepetition](../../com.aspose.tasks/byyearweekdayrepetition).

### getMonth() {#getMonth--}
```
public final int getMonth()
```


Hämtar en månad då uppgiften måste återkomma.

Läs/skriv `Month`.

**Returns:**
int – en månad då uppgiften måste återkomma.
### getPosition() {#getPosition--}
```
public final int getPosition()
```


Hämtar positionen för dagen i en vecka i en månad då uppgiften måste återkomma.

Läs/skriv `OrdinalNumber`.

**Returns:**
int - en position av dagen i en vecka i en månad då uppgiften måste återkomma.
### getWeekDay() {#getWeekDay--}
```
public final int getWeekDay()
```


Hämtar typen av veckodag då uppgiften måste återkomma.

Läs/skriv `DayOfWeek`.

**Returns:**
int - en typ av veckodag då uppgiften måste återkomma.
### setMonth(int value) {#setMonth-int-}
```
public final void setMonth(int value)
```


Ställer in en månad då uppgiften måste återkomma.

Läs/skriv `Month`.

**Parameters:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| värde | int | en månad då uppgiften måste återkomma. |

### setPosition(int value) {#setPosition-int-}
```
public final void setPosition(int value)
```


Ställer in positionen för dagen i en vecka i en månad då uppgiften måste återkomma.

Läs/skriv `OrdinalNumber`.

**Parameters:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| värde | int | en position av dagen i en vecka i en månad då uppgiften måste återkomma. |

### setWeekDay(int value) {#setWeekDay-int-}
```
public final void setWeekDay(int value)
```


Ställer in typen av veckodag då uppgiften måste återkomma.

Läs/skriv `DayOfWeek`.

**Parameters:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| värde | int | en typ av veckodag då uppgiften måste återkomma. |

