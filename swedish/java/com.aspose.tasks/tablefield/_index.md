---
title: "TableField"
second_title: "Aspose.Tasks for Java API-referens"
description: "Representerar ett fält i en tabell i ett projekt."
type: docs
weight: 286
url: /sv/java/com.aspose.tasks/tablefield/
---

**Inheritance:**
java.lang.Object
```
public class TableField
```

Representerar ett fält i en tabell i ett projekt.
## Konstruktörer

| Konstruktor | Beskrivning |
| --- | --- |
| [TableField()](#TableField--) | Initierar en ny instans av klassen [TableField](../../com.aspose.tasks/tablefield). |
## Metoder

| Metod | Beskrivning |
| --- | --- |
| [getAlignData()](#getAlignData--) | Hämtar justeringen av data i ett tabellfält. |
| [getAlignTitle()](#getAlignTitle--) | Hämtar justeringen av rubriken i ett tabellfält. |
| [getField()](#getField--) | Hämtar typen av ett tabellfält. |
| [getTitle()](#getTitle--) | Hämtar titeln på fältet i en tabell. |
| [getWidth()](#getWidth--) | Hämtar bredden i punkter för fältkolumnen i en tabell. |
| [getWrapHeader()](#getWrapHeader--) | Hämtar ett värde som anger om tabellkolumnrubriken kan radbrytas till flera rader, eller om den ska trunkeras när den överskrider kolumnens bredd. |
| [getWrapText()](#getWrapText--) | Hämtar ett värde som anger om kolumntexten kan radbrytas till flera rader, eller om den ska trunkeras när den överskrider kolumnens bredd. |
| [setAlignData(int value)](#setAlignData-int-) | Ställer in justeringen av data i ett tabellfält. |
| [setAlignTitle(int value)](#setAlignTitle-int-) | Ställer in justeringen av rubriken i ett tabellfält. |
| [setField(int value)](#setField-int-) | Ställer in typen av ett tabellfält. |
| [setTitle(String value)](#setTitle-java.lang.String-) | Ställer in titeln på fältet i en tabell. |
| [setWidth(int value)](#setWidth-int-) | Ställer in bredden i punkter för fältkolumnen i en tabell. |
| [setWrapHeader(boolean value)](#setWrapHeader-boolean-) | Ställer in ett värde som anger om tabellkolumnrubriken kan radbrytas till flera rader, eller om den ska trunkeras när den överskrider kolumnens bredd. |
| [setWrapText(boolean value)](#setWrapText-boolean-) | Ställer in ett värde som anger om kolumntexten kan radbrytas till flera rader, eller om den ska trunkeras när den överskrider kolumnens bredd. |
### TableField() {#TableField--}
```
public TableField()
```


Initierar en ny instans av klassen [TableField](../../com.aspose.tasks/tablefield).

### getAlignData() {#getAlignData--}
```
public final int getAlignData()
```


Hämtar justeringen av data i ett tabellfält.

**Returns:**
int - justeringen av data i ett tabellfält.
### getAlignTitle() {#getAlignTitle--}
```
public final int getAlignTitle()
```


Hämtar justeringen av rubriken i ett tabellfält.

**Returns:**
int - justeringen av rubriken i ett tabellfält.
### getField() {#getField--}
```
public final int getField()
```


Hämtar typen av ett tabellfält.

**Returns:**
int - typen av ett tabellfält.
### getTitle() {#getTitle--}
```
public final String getTitle()
```


Hämtar titeln på fältet i en tabell.

**Returns:**
java.lang.String - titeln på fältet i en tabell.
### getWidth() {#getWidth--}
```
public final int getWidth()
```


Hämtar bredden i punkter för fältkolumnen i en tabell.

**Returns:**
int - bredden i punkter för fältkolumnen i en tabell.
### getWrapHeader() {#getWrapHeader--}
```
public final boolean getWrapHeader()
```


Hämtar ett värde som anger om tabellkolumnrubriken kan radbrytas till flera rader, eller om den ska trunkeras när den överskrider kolumnens bredd.

**Returns:**
boolean - ett värde som indikerar om tabellkolumnrubriken kan radbrytas till flera rader, eller om den ska trunkeras när den överskrider kolumnens bredd.
### getWrapText() {#getWrapText--}
```
public final boolean getWrapText()
```


Hämtar ett värde som anger om kolumntexten kan radbrytas till flera rader, eller om den ska trunkeras när den överskrider kolumnens bredd.

--------------------

Stöds av MSP 2010-versionen och senare.

**Returns:**
boolean - ett värde som indikerar om kolumntexten kan radbrytas till flera rader, eller om den ska trunkeras när den överskrider kolumnens bredd.
### setAlignData(int value) {#setAlignData-int-}
```
public final void setAlignData(int value)
```


Ställer in justeringen av data i ett tabellfält.

**Parameters:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| värde | int | justeringen av data i ett tabellfält. |

### setAlignTitle(int value) {#setAlignTitle-int-}
```
public final void setAlignTitle(int value)
```


Ställer in justeringen av rubriken i ett tabellfält.

**Parameters:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| värde | int | justeringen av titeln i ett tabellfält. |

### setField(int value) {#setField-int-}
```
public final void setField(int value)
```


Ställer in typen av ett tabellfält.

**Parameters:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| värde | int | typen av ett tabellfält. |

### setTitle(String value) {#setTitle-java.lang.String-}
```
public final void setTitle(String value)
```


Ställer in titeln på fältet i en tabell.

**Parameters:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| värde | java.lang.String | titeln på fältet i en tabell. |

### setWidth(int value) {#setWidth-int-}
```
public final void setWidth(int value)
```


Ställer in bredden i punkter för fältkolumnen i en tabell.

**Parameters:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| värde | int | bredden i punkter för fältkolumnen i en tabell. |

### setWrapHeader(boolean value) {#setWrapHeader-boolean-}
```
public final void setWrapHeader(boolean value)
```


Ställer in ett värde som anger om tabellkolumnrubriken kan radbrytas till flera rader, eller om den ska trunkeras när den överskrider kolumnens bredd.

**Parameters:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| värde | boolean | ett värde som indikerar om tabellkolumnrubriken kan radbrytas till flera rader, eller om den ska trunkeras när den överskrider kolumnens bredd. |

### setWrapText(boolean value) {#setWrapText-boolean-}
```
public final void setWrapText(boolean value)
```


Ställer in ett värde som anger om kolumntexten kan radbrytas till flera rader, eller om den ska trunkeras när den överskrider kolumnens bredd.

--------------------

Stöds av MSP 2010-versionen och senare.

**Parameters:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| värde | boolean | ett värde som indikerar om kolumntexten kan radbrytas till flera rader, eller om den ska trunkeras när den överskrider kolumnens bredd. |

