---
title: "PageViewSettings"
second_title: "Aspose.Tasks for Java API-referens"
description: "Representerar utskriftsinställningar för en projektvy."
type: docs
weight: 183
url: /sv/java/com.aspose.tasks/pageviewsettings/
---

**Inheritance:**
java.lang.Object
```
public class PageViewSettings
```

Representerar utskriftsinställningar för en projektvy.
## Konstruktörer

| Konstruktor | Beskrivning |
| --- | --- |
| [PageViewSettings()](#PageViewSettings--) |  |
## Metoder

| Metod | Beskrivning |
| --- | --- |
| [getFirstColumnsCount()](#getFirstColumnsCount--) | Hämtar antalet första kolumner som ska skrivas ut på alla sidor. |
| [getFitTimescaleToEndOfPage()](#getFitTimescaleToEndOfPage--) | Hämtar ett värde som indikerar om tidslinjen ska anpassas till slutet av en sida vid utskrift. |
| [getPrintAllSheetColumns()](#getPrintAllSheetColumns--) | Hämtar ett värde som indikerar om alla bladkolumner i en vy ska skrivas ut. |
| [getPrintBlankPages()](#getPrintBlankPages--) | Hämtar ett värde som indikerar om tomma sidor i en vy ska skrivas ut. |
| [getPrintFirstColumnsCountOnAllPages()](#getPrintFirstColumnsCountOnAllPages--) | Hämtar ett värde som indikerar om ett specificerat antal första kolumner ska skrivas ut på alla sidor. |
| [getPrintNotes()](#getPrintNotes--) | Hämtar ett värde som indikerar om anteckningar ska skrivas ut. |
| [setFirstColumnsCount(int value)](#setFirstColumnsCount-int-) | Ställer in antalet första kolumner som ska skrivas ut på alla sidor. |
| [setFitTimescaleToEndOfPage(boolean value)](#setFitTimescaleToEndOfPage-boolean-) | Ställer in ett värde som indikerar om tidslinjen ska anpassas till slutet av en sida vid utskrift. |
| [setPrintAllSheetColumns(boolean value)](#setPrintAllSheetColumns-boolean-) | Ställer in ett värde som indikerar om alla bladkolumner i en vy ska skrivas ut. |
| [setPrintBlankPages(boolean value)](#setPrintBlankPages-boolean-) | Ställer in ett värde som indikerar om tomma sidor i en vy ska skrivas ut. |
| [setPrintFirstColumnsCountOnAllPages(boolean value)](#setPrintFirstColumnsCountOnAllPages-boolean-) | Ställer in ett värde som indikerar om ett specificerat antal första kolumner ska skrivas ut på alla sidor. |
| [setPrintNotes(boolean value)](#setPrintNotes-boolean-) | Ställer in ett värde som indikerar om anteckningar ska skrivas ut. |
### PageViewSettings() {#PageViewSettings--}
```
public PageViewSettings()
```


### getFirstColumnsCount() {#getFirstColumnsCount--}
```
public final int getFirstColumnsCount()
```


Hämtar antalet första kolumner som ska skrivas ut på alla sidor.

**Returns:**
int - antalet första kolumner som ska skrivas ut på alla sidor.
### getFitTimescaleToEndOfPage() {#getFitTimescaleToEndOfPage--}
```
public final boolean getFitTimescaleToEndOfPage()
```


Hämtar ett värde som indikerar om tidslinjen ska anpassas till slutet av en sida vid utskrift.

**Returns:**
boolean - ett värde som indikerar om tidslinjen ska anpassas till slutet av en sida vid utskrift.
### getPrintAllSheetColumns() {#getPrintAllSheetColumns--}
```
public final boolean getPrintAllSheetColumns()
```


Hämtar ett värde som indikerar om alla bladkolumner i en vy ska skrivas ut.

**Returns:**
boolean - ett värde som indikerar om alla bladkolumner i en vy ska skrivas ut.
### getPrintBlankPages() {#getPrintBlankPages--}
```
public final boolean getPrintBlankPages()
```


Hämtar ett värde som indikerar om tomma sidor i en vy ska skrivas ut.

**Returns:**
boolean - ett värde som indikerar om tomma sidor i en vy ska skrivas ut.
### getPrintFirstColumnsCountOnAllPages() {#getPrintFirstColumnsCountOnAllPages--}
```
public final boolean getPrintFirstColumnsCountOnAllPages()
```


Hämtar ett värde som indikerar om ett specificerat antal första kolumner ska skrivas ut på alla sidor.

**Returns:**
boolean - ett värde som indikerar om ett specificerat antal första kolumner ska skrivas ut på alla sidor.
### getPrintNotes() {#getPrintNotes--}
```
public final boolean getPrintNotes()
```


Hämtar ett värde som indikerar om anteckningar ska skrivas ut.

**Returns:**
boolean - ett värde som indikerar om anteckningar ska skrivas ut.
### setFirstColumnsCount(int value) {#setFirstColumnsCount-int-}
```
public final void setFirstColumnsCount(int value)
```


Ställer in antalet första kolumner som ska skrivas ut på alla sidor.

**Parameters:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| värde | int | antalet första kolumner som ska skrivas ut på alla sidor. |

### setFitTimescaleToEndOfPage(boolean value) {#setFitTimescaleToEndOfPage-boolean-}
```
public final void setFitTimescaleToEndOfPage(boolean value)
```


Ställer in ett värde som indikerar om tidslinjen ska anpassas till slutet av en sida vid utskrift.

**Parameters:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| värde | boolean | ett värde som indikerar om tidslinjen ska anpassas till slutet av en sida vid utskrift. |

### setPrintAllSheetColumns(boolean value) {#setPrintAllSheetColumns-boolean-}
```
public final void setPrintAllSheetColumns(boolean value)
```


Ställer in ett värde som indikerar om alla bladkolumner i en vy ska skrivas ut.

**Parameters:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| värde | boolean | ett värde som indikerar om alla bladkolumner i en vy ska skrivas ut. |

### setPrintBlankPages(boolean value) {#setPrintBlankPages-boolean-}
```
public final void setPrintBlankPages(boolean value)
```


Ställer in ett värde som indikerar om tomma sidor i en vy ska skrivas ut.

**Parameters:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| värde | boolean | ett värde som indikerar om tomma sidor i en vy ska skrivas ut. |

### setPrintFirstColumnsCountOnAllPages(boolean value) {#setPrintFirstColumnsCountOnAllPages-boolean-}
```
public final void setPrintFirstColumnsCountOnAllPages(boolean value)
```


Ställer in ett värde som indikerar om ett specificerat antal första kolumner ska skrivas ut på alla sidor.

**Parameters:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| värde | boolean | ett värde som indikerar om ett specificerat antal första kolumner ska skrivas ut på alla sidor. |

### setPrintNotes(boolean value) {#setPrintNotes-boolean-}
```
public final void setPrintNotes(boolean value)
```


Ställer in ett värde som indikerar om anteckningar ska skrivas ut.

**Parameters:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| värde | boolean | ett värde som indikerar om anteckningar ska skrivas ut. |

