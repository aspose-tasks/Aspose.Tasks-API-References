name: Trigger Tasks Netherlands Production Build

on:
  push:
    branches: [ production ]
    paths: [ 'netherlands/**' ]
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - uses: peter-evans/repository-dispatch@v2
        with:
          token: ${{ secrets.REPO_TOKEN }}
          repository: Aspose/apireference.aspose.com
          event-type: build-tasks-netherlands-production
