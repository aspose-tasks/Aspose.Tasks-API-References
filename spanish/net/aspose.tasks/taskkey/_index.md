---
title: "Enum TaskKey"
second_title: "Referencia de API de Aspose.Tasks para .NET"
description: "Aspose.Tasks.TaskKey enum. Representa una lista de campos de tarea compatibles"
type: docs
weight: 2400
url: /es/net/aspose.tasks/taskkey/
---
## TaskKey enumeration

Representa una lista de campos de tarea compatibles.

```csharp
public enum TaskKey : byte
```

### Valores

| Nombre | Valor | Descripción |
| --- | --- | --- |
| Uid | `0` | Representa el campo Unique ID (Task). |
| Id | `1` | Representa el campo Id (Task). |
| Name | `2` | Representa el campo Name (Task). |
| Type | `3` | Representa el campo Type (Task). |
| IsNull | `4` | Representa el campo IsNull (Task). |
| Created | `5` | Representa el campo Created (Task). |
| Contact | `6` | Representa el campo Contact (Task). |
| WBS | `7` | Representa el campo WBS (Task). |
| WBSLevel | `8` | Representa el campo WBSLevel (Task). |
| OutlineNumber | `9` | Representa el campo OutlineNumber (Task). |
| OutlineLevel | `10` | Representa el campo OutlineLevel (Task). |
| Priority | `11` | Representa el campo Priority (Task). |
| Start | `12` | Representa el campo Start (Task). |
| Finish | `13` | Representa el campo Finish (Task). |
| Duration | `14` | Representa el campo Duration (Task). |
| DurationVariance | `15` | Representa el campo DurationVariance (Task). |
| Work | `16` | Representa el campo Work (Task). |
| Stop | `17` | Representa el campo Stop (Task). |
| Resume | `18` | Representa el campo Resume (Task). |
| IsResumeValid | `19` | Representa el campo IsResumeValid (Task). |
| IsEffortDriven | `20` | Representa el campo IsEffortDriven (Task). |
| IsRecurring | `21` | Representa el campo IsRecurring (Task). |
| IsOverallocated | `22` | Representa el campo IsOverallocated (Task). |
| HasOverallocatedResource | `23` | Representa el campo HasOverallocatedResource (Task). |
| IsEstimated | `24` | Representa el campo IsEstimated (Task). |
| IsMilestone | `25` | Representa el campo IsMilestone (Task). |
| IsCritical | `26` | Representa el campo IsCritical (Task). |
| IsSubproject | `27` | Representa el campo IsSubproject (Task). |
| IsSubprojectReadOnly | `28` | Representa el campo IsSubprojectReadOnly (Task). |
| IsMarked | `29` | Representa el campo IsMarked (Task). |
| IgnoreWarnings | `30` | Representa el campo IgnoreWarnings (Task). |
| SubprojectName | `31` | Representa el campo SubprojectName (Task). |
| IsExternalTask | `32` | Representa el campo IsExternalTask (Task). |
| IsSummary | `33` | Representa el campo IsSummary (Task). |
| ExternalTaskProject | `34` | Representa el campo ExternalTaskProject (Task). |
| ExternalId | `35` | Representa el campo ExternalId (Task). |
| EarlyStart | `36` | Representa el campo EarlyStart (Task). |
| EarlyFinish | `37` | Representa el campo EarlyFinish (Task). |
| LateStart | `38` | Representa el campo LateStart (Task). |
| LateFinish | `39` | Representa el campo LateFinish (Task). |
| StartVariance | `40` | Representa el campo StartVariance (Task). |
| FinishVariance | `41` | Representa el campo FinishVariance (Task). |
| WorkVariance | `42` | Representa el campo WorkVariance (Task). |
| CostVariance | `43` | Representa el campo CostVariance (Task). |
| FreeSlack | `44` | Representa el campo FreeSlack (Task). |
| TotalSlack | `45` | Representa el campo TotalSlack (Task). |
| StartSlack | `46` | Representa el campo StartSlack (Task). |
| FinishSlack | `47` | Representa el campo FinishSlack (Task). |
| FixedCost | `48` | Representa el campo FixedCost (Task). |
| FixedCostAccrual | `49` | Representa el campo FixedCostAccrual (Task). |
| PercentComplete | `50` | Representa el PercentComplete (Task) campo. |
| PercentWorkComplete | `51` | Representa el PercentWorkComplete (Task) campo. |
| Cost | `52` | Representa el Cost (Task) campo. |
| OvertimeCost | `53` | Representa el OvertimeCost (Task) campo. |
| ActualStart | `54` | Representa el ActualStart (Task) campo. |
| ActualFinish | `55` | Representa el ActualFinish (Task) campo. |
| ActualDuration | `56` | Representa el ActualDuration (Task) campo. |
| ActualCost | `57` | Representa el ActualCost (Task) campo. |
| ActualOvertimeCost | `58` | Representa el ActualOvertimeCost (Task) campo. |
| ActualWork | `59` | Representa el ActualWork (Task) campo. |
| ActualOvertimeWork | `60` | Representa el ActualOvertimeWork (Task) campo. |
| OvertimeWork | `61` | Representa el OvertimeWork (Task) campo. |
| RegularWork | `62` | Representa el RegularWork (Task) campo. |
| RemainingDuration | `63` | Representa el RemainingDuration (Task) campo. |
| RemainingCost | `64` | Representa el RemainingCost (Task) campo. |
| RemainingWork | `65` | Representa el RemainingWork (Task) campo. |
| RemainingOvertimeWork | `66` | Representa el RemainingOvertimeWork (Task) campo. |
| RemainingOvertimeCost | `67` | Representa el RemainingOvertimeCost (Task) campo. |
| ACWP | `68` | Representa el ACWP (Task) campo. |
| CV | `69` | Representa el CV (Task) campo. |
| SV | `70` | Representa el SV (Task) campo. |
| ConstraintType | `71` | Representa el ConstraintType (Task) campo. |
| Calendar | `72` | Representa el Calendar (Task) campo. |
| ConstraintDate | `73` | Representa el ConstraintDate (Task) campo. |
| Deadline | `74` | Representa el Deadline (Task) campo. |
| LevelAssignments | `75` | Representa el campo LevelAssignments (Task). |
| LevelingCanSplit | `76` | Representa el campo LevelingCanSplit (Task). |
| LevelingDelay | `77` | Representa el campo LevelingDelay (Task). |
| PreleveledStart | `78` | Representa el campo PreleveledStart (Task). |
| PreleveledFinish | `79` | Representa el campo PreleveledFinish (Task). |
| Hyperlink | `80` | Representa el campo Hyperlink (Task). |
| HyperlinkAddress | `81` | Representa el campo HyperlinkAddress (Task). |
| HyperlinkSubAddress | `82` | Representa el campo HyperlinkSubAddress (Task). |
| IgnoreResourceCalendar | `83` | Representa el campo IgnoreResourceCalendar (Task). |
| HideBar | `84` | Representa el campo HideBar (Task). |
| IsRollup | `85` | Representa el campo IsRollup (Task). |
| BCWS | `86` | Representa el campo BCWS (Task). |
| BCWP | `87` | Representa el campo BCWP (Task). |
| PhysicalPercentComplete | `88` | Representa el campo PhysicalPercentComplete (Task). |
| EarnedValueMethod | `89` | Representa el campo EarnedValueMethod (Task). |
| ActualWorkProtected | `90` | Representa el campo ActualWorkProtected (Task). |
| ActualOvertimeWorkProtected | `91` | Representa el campo ActualOvertimeWorkProtected (Task). |
| IsPublished | `92` | Representa el campo IsPublished (Task). |
| IsScheduled | `93` | Representa el campo IsScheduled (Task). |
| StatusManager | `94` | Representa el campo StatusManager (Task). |
| CommitmentStart | `95` | Representa el campo CommitmentStart (Task). |
| CommitmentFinish | `96` | Representa el campo CommitmentFinish (Task). |
| CommitmentType | `97` | Representa el campo CommitmentType (Task). |
| IsManual | `98` | Representa el campo IsManual (Task). |
| IsExpanded | `99` | Representa el campo IsExpanded (Task). |
| Guid | `100` | Representa el Guid (Task) campo. |
| NotesText | `101` | Representa el NotesText (Task) campo. |
| NotesRTF | `102` | Representa el NotesRTF (Task) campo. |
| ManualStart | `103` | Representa el ManualStart (Task) campo. |
| ManualFinish | `104` | Representa el ManualFinish (Task) campo. |
| ManualDuration | `105` | Representa el ManualDuration (Task) campo. |
| BudgetWork | `106` | Representa el BudgetWork (Task) campo. |
| BudgetCost | `107` | Representa el BudgetCost (Task) campo. |
| DisplayAsSummary | `108` | Representa el DisplayAsSummary (Task) campo. |
| SummaryProgress | `109` | Representa el Summary Progress (Task) campo. |
| IsActive | `110` | Representa el IsActive (Task) campo. |
| StartText | `111` | Representa el StartText (Task) campo. |
| FinishText | `112` | Representa el FinishText (Task) campo. |
| DurationText | `113` | Representa el DurationText (Task) campo. |
| CalendarUid | `114` | Representa el CalendarUid (Task) campo. |
| ParentTaskUid | `115` | Representa el ParentTaskUid (Task) campo. |
| DisplayOnTimeline | `116` | Representa el DisplayOnTimeline (Task) campo. |
| TaskIsAssigned | `117` | Representa el TaskIsAssigned (Task) campo. |
| TaskOriginalStart | `118` | Representa el inicio original (Task) campo. |
| TaskOriginalFinish | `119` | Representa el fin original (Task) campo. |
| IsShowBeforeProjectStartDateWarning | `120` | Representa la bandera que hace que MSP muestre la advertencia de que una tarea comienza antes de la fecha de inicio del proyecto. |
| Warning | `121` | Representa la bandera que indica que la tarea tiene discrepancias de programación. |
| ParentTaskGuid | `122` | Representa el ParentTaskGuid (Task) campo. |
| ActivityId | `123` | Representa el campo ActivityId (solo aplicable a proyectos Primavera). |
| FreeSlackTimeSpan | `124` | Representa el campo FreeSlack (Task). |
| TotalSlackTimeSpan | `125` | Representa el campo TotalSlack (Task). |
| StartSlackTimeSpan | `126` | Representa el campo StartSlack (Task). |
| FinishSlackTimeSpan | `127` | Representa el campo FinishSlack (Task). |
| ExternalUid | `128` | Representa el Identificador único de la tarea externa. |

## Ejemplos

Muestra cómo leer/escribir propiedades de tareas.

```csharp
var project = new Project();

// Agregar tarea y establecer propiedades de la tarea
var task = project.RootTask.Children.Add();
task.Set(Tsk.Name, "Task1");
task.Set(Tsk.Start, new DateTime(2020, 3, 31, 8, 0, 0));
task.Set(Tsk.Finish, new DateTime(2020, 3, 31, 17, 0, 0));

var collector = new ChildTasksCollector();
TaskUtils.Apply(project.RootTask, collector, 0);

// Analizar todas las tareas recopiladas
foreach (var tsk in collector.Tasks)
{
    Console.WriteLine("Task Id: {0}", tsk.Get(Tsk.Id));
    Console.WriteLine("Task Uid: {0}", tsk.Get(Tsk.Uid));
    Console.WriteLine("Task Name: {0}", tsk.Get(Tsk.Name));
    Console.WriteLine("Task Start: {0}", tsk.Get(Tsk.Start));
    Console.WriteLine("Task Finish: {0}", tsk.Get(Tsk.Finish));
}
```

### Ver también

* namespace [Aspose.Tasks](../../aspose.tasks/)
* assembly [Aspose.Tasks](../../)


