---
title: Enum AsnKey
second_title: Referencia de Aspose.Tasks para la API de .NET
description: Aspose.Tasks.AsnKey enumeración. Representa una lista de campos de asignación admitidos.
type: docs
weight: 40
url: /es/net/aspose.tasks/asnkey/
---
## AsnKey enumeration

Representa una lista de campos de asignación admitidos.

```csharp
public enum AsnKey
```

### Valores

| Nombre | Valor | Descripción |
| --- | --- | --- |
| Guid | `0` | Representa el campo Guid (ResourceAssignment). |
| Uid | `1` | Representa el campo UID (Asignación de recursos). |
| Task | `2` | Representa el campo Tarea (Asignación de recursos). |
| Resource | `3` | Representa el campo Recurso (ResourceAssignment). |
| PercentWorkComplete | `4` | Representa el campo PercentWorkComplete (ResourceAssignment). |
| ActualCost | `5` | Representa el campo ActualCost (ResourceAssignment). |
| ActualFinish | `6` | Representa el campo ActualFinish (ResourceAssignment). |
| ActualOvertimeCost | `7` | Representa el campo ActualOvertimeCost (ResourceAssignment). |
| ActualStart | `8` | Representa el campo ActualStart (ResourceAssignment). |
| ActualWork | `9` | Representa el campo ActualWork (ResourceAssignment). |
| ActualOvertimeWork | `10` | Representa el campo ActualOvertimeWork (ResourceAssignment). |
| ACWP | `11` | Representa el campo ACWP (ResourceAssignment). |
| Confirmed | `12` | Representa el campo Confirmado (Asignación de recursos). |
| Cost | `13` | Representa el campo Costo (Asignación de recursos). |
| CostRateTableType | `14` | Representa el campo CostRateTableType (ResourceAssignment). |
| CostVariance | `15` | Representa el campo CostVariance (ResourceAssignment). |
| CV | `16` | Representa el campo CV (Asignación de recursos). |
| Delay | `17` | Representa el campo Retraso (Asignación de recursos). |
| Finish | `18` | Representa el campo Finalizar (Asignación de recursos). |
| FinishVariance | `19` | Representa el campo FinishVariance (ResourceAssignment). |
| Hyperlink | `20` | Representa el campo Hipervínculo (Asignación de recursos). |
| HyperlinkAddress | `21` | Representa el campo HyperlinkAddress (ResourceAssignment). |
| HyperlinkSubAddress | `22` | Representa el campo HyperlinkSubAddress (ResourceAssignment). |
| WorkVariance | `23` | Representa el campo WorkVariance (ResourceAssignment). |
| HasFixedRateUnits | `24` | Representa el campo HasFixedRateUnits (ResourceAssignment). |
| FixedMaterial | `25` | Representa el campo FixedMaterial (ResourceAssignment). |
| LevelingDelay | `26` | Representa el campo LevelingDelay (ResourceAssignment). |
| LevelingDelayFormat | `27` | Representa el campo LevelingDelayFormat (ResourceAssignment). |
| LinkedFields | `28` | Representa el campo LinkedFields (ResourceAssignment). |
| Milestone | `29` | Representa el campo Milestone (ResourceAssignment). |
| Notes | `30` | Representa el campo Notas (ResourceAssignment). |
| Overallocated | `31` | Representa el campo Sobreasignado (Asignación de recursos). |
| OvertimeCost | `32` | Representa el campo OvertimeCost (ResourceAssignment). |
| OvertimeWork | `33` | Representa el campo OvertimeWork (ResourceAssignment). |
| PeakUnits | `34` | Representa el campo PeakUnits (ResourceAssignment). |
| RegularWork | `35` | Representa el campo RegularWork (ResourceAssignment). |
| RemainingCost | `36` | Representa el campo RemainingCost (ResourceAssignment). |
| RemainingOvertimeCost | `37` | Representa el campo RemainingOvertimeCost (ResourceAssignment). |
| RemainingOvertimeWork | `38` | Representa el campo RemainingOvertimeWork (ResourceAssignment). |
| RemainingWork | `39` | Representa el campo Trabajo restante (Asignación de recursos). |
| ResponsePending | `40` | Representa el campo ResponsePending (ResourceAssignment). |
| Start | `41` | Representa el campo Inicio (Asignación de recursos). |
| Stop | `42` | Representa el campo Detener (Asignación de recursos). |
| Resume | `43` | Representa el campo Curriculum Vitae (Asignación de recursos). |
| StartVariance | `44` | Representa el campo StartVariance (ResourceAssignment). |
| Summary | `45` | Representa el campo Resumen (Asignación de recursos). |
| SV | `46` | Representa el campo SV (ResourceAssignment). |
| Units | `47` | Representa el campo Unidades (Asignación de recursos). |
| UpdateNeeded | `48` | Representa el campo UpdateNeeded (ResourceAssignment). |
| VAC | `49` | Representa el campo VAC (Asignación de recursos). |
| Work | `50` | Representa el campo Trabajo (Asignación de recursos). |
| WorkContour | `51` | Representa el campo WorkContour (Asignación de recursos). |
| BCWS | `52` | Representa el campo BCWS (Asignación de recursos). |
| BCWP | `53` | Representa el campo BCWP (ResourceAssignment). |
| BookingType | `54` | Representa el campo BookingType (ResourceAssignment). |
| ActualWorkProtected | `55` | Representa el campo ActualWorkProtected (ResourceAssignment). |
| ActualOvertimeWorkProtected | `56` | Representa el campo ActualOvertimeWorkProtected (ResourceAssignment). |
| Created | `57` | Representa el campo Creado (Asignación de recursos). |
| AssignmentOwner | `58` | Representa el campo AssignmentOwner (ResourceAssignment). |
| AssignmentOwnerGuid | `59` | Representa el campo AssignmentOwnerGuid (ResourceAssignment). |
| BudgetWork | `60` | Representa el campo BudgetWork (ResourceAssignment). |
| BudgetCost | `61` | Representa el campo BudgetCost (ResourceAssignment). |
| RateScale | `62` | Representa el campo RateScale (Asignación de recursos). |
| TaskUid | `63` | Representa el campo TaskUid (ResourceAssignment). |
| ResourceUid | `64` | Representa el campo ResourceUid (ResourceAssignment). |

### Ver también

* espacio de nombres [Aspose.Tasks](../../aspose.tasks/)
* asamblea [Aspose.Tasks](../../)


