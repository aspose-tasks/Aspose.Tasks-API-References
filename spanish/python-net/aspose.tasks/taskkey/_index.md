---
title: "TaskKey"
second_title: "Referencia de API de Aspose.Tasks para Python vía .NET"
description: 
type: docs
weight: 2770
url: /es/python-net/aspose.tasks/taskkey/
---

## TaskKey enumeration

Representa una lista de campos de tarea compatibles.

## Members
| Nombre del miembro | Descripción |
| :- | :- |
| UID | Representa el campo ID Único (Tarea). |
| ID | Representa el campo Id (Tarea). |
| NAME | Representa el campo Nombre (Tarea). |
| TYPE | Representa el campo Tipo (Tarea). |
| IS_NULL | Representa el campo IsNull (Task). |
| CREATED | Representa el campo Created (Task). |
| CONTACT | Representa el campo Contact (Task). |
| WBS | Representa el campo WBS (Task). |
| WBS_LEVEL | Representa el campo WBSLevel (Task). |
| OUTLINE_NUMBER | Representa el campo OutlineNumber (Task). |
| OUTLINE_LEVEL | Representa el campo OutlineLevel (Task). |
| PRIORITY | Representa el campo Priority (Task). |
| START | Representa el campo Start (Task). |
| FINISH | Representa el campo Finish (Task). |
| DURATION | Representa el campo Duration (Task). |
| DURATION_VARIANCE | Representa el campo DurationVariance (Task). |
| WORK | Representa el campo Work (Task). |
| STOP | Representa el campo Stop (Task). |
| RESUME | Representa el campo Resume (Task). |
| IS_RESUME_VALID | Representa el campo IsResumeValid (Task). |
| IS_EFFORT_DRIVEN | Representa el campo IsEffortDriven (Task). |
| IS_RECURRING | Representa el campo IsRecurring (Task). |
| IS_OVERALLOCATED | Representa el campo IsOverallocated (Task). |
| HAS_OVERALLOCATED_RESOURCE | Representa el campo HasOverallocatedResource (Task). |
| IS_ESTIMATED | Representa el campo IsEstimated (Task). |
| IS_MILESTONE | Representa el campo IsMilestone (Task). |
| IS_CRITICAL | Representa el campo IsCritical (Task). |
| IS_SUBPROJECT | Representa el campo IsSubproject (Task). |
| IS_SUBPROJECT_READ_ONLY | Representa el campo IsSubprojectReadOnly (Task). |
| IS_MARKED | Representa el campo IsMarked (Task). |
| IGNORE_WARNINGS | Representa el campo IgnoreWarnings (Task). |
| SUBPROJECT_NAME | Representa el campo SubprojectName (Task). |
| IS_EXTERNAL_TASK | Representa el campo IsExternalTask (Task). |
| IS_SUMMARY | Representa el campo IsSummary (Task). |
| EXTERNAL_TASK_PROJECT | Representa el campo ExternalTaskProject (Task). |
| EXTERNAL_ID | Representa el campo ExternalId (Task). |
| EARLY_START | Representa el campo EarlyStart (Task). |
| EARLY_FINISH | Representa el campo EarlyFinish (Task). |
| LATE_START | Representa el campo LateStart (Task). |
| LATE_FINISH | Representa el campo LateFinish (Task). |
| START_VARIANCE | Representa el campo StartVariance (Task). |
| FINISH_VARIANCE | Representa el campo FinishVariance (Task). |
| WORK_VARIANCE | Representa el campo WorkVariance (Task). |
| COST_VARIANCE | Representa el campo CostVariance (Task). |
| FREE_SLACK | Representa el FreeSlack (Task) campo. |
| TOTAL_SLACK | Representa el TotalSlack (Task) campo. |
| START_SLACK | Representa el StartSlack (Task) campo. |
| FINISH_SLACK | Representa el FinishSlack (Task) campo. |
| FIXED_COST | Representa el FixedCost (Task) campo. |
| FIXED_COST_ACCRUAL | Representa el FixedCostAccrual (Task) campo. |
| PERCENT_COMPLETE | Representa el PercentComplete (Task) campo. |
| PERCENT_WORK_COMPLETE | Representa el PercentWorkComplete (Task) campo. |
| COST | Representa el Cost (Task) campo. |
| OVERTIME_COST | Representa el OvertimeCost (Task) campo. |
| ACTUAL_START | Representa el ActualStart (Task) campo. |
| ACTUAL_FINISH | Representa el ActualFinish (Task) campo. |
| ACTUAL_DURATION | Representa el ActualDuration (Task) campo. |
| ACTUAL_COST | Representa el campo ActualCost (Task). |
| ACTUAL_OVERTIME_COST | Representa el campo ActualOvertimeCost (Task). |
| ACTUAL_WORK | Representa el campo ActualWork (Task). |
| ACTUAL_OVERTIME_WORK | Representa el campo ActualOvertimeWork (Task). |
| OVERTIME_WORK | Representa el campo OvertimeWork (Task). |
| REGULAR_WORK | Representa el campo RegularWork (Task). |
| REMAINING_DURATION | Representa el campo RemainingDuration (Task). |
| REMAINING_COST | Representa el campo RemainingCost (Task). |
| REMAINING_WORK | Representa el campo RemainingWork (Task). |
| REMAINING_OVERTIME_WORK | Representa el campo RemainingOvertimeWork (Task). |
| REMAINING_OVERTIME_COST | Representa el campo RemainingOvertimeCost (Task). |
| ACWP | Representa el campo ACWP (Task). |
| CV | Representa el campo CV (Task). |
| SV | Representa el campo SV (Task). |
| CONSTRAINT_TYPE | Representa el campo ConstraintType (Task). |
| CALENDAR | Representa el campo Calendar (Task). |
| CONSTRAINT_DATE | Representa el campo ConstraintDate (Task). |
| DEADLINE | Representa el campo Deadline (Task). |
| LEVEL_ASSIGNMENTS | Representa el campo LevelAssignments (Task). |
| LEVELING_CAN_SPLIT | Representa el campo LevelingCanSplit (Task). |
| LEVELING_DELAY | Representa el campo LevelingDelay (Task). |
| PRELEVELED_START | Representa el campo PreleveledStart (Task). |
| PRELEVELED_FINISH | Representa el campo PreleveledFinish (Task). |
| HYPERLINK | Representa el campo Hyperlink (Task). |
| HYPERLINK_ADDRESS | Representa el campo HyperlinkAddress (Task). |
| HYPERLINK_SUB_ADDRESS | Representa el campo HyperlinkSubAddress (Task). |
| IGNORE_RESOURCE_CALENDAR | Representa el campo IgnoreResourceCalendar (Task). |
| HIDE_BAR | Representa el campo HideBar (Task). |
| IS_ROLLUP | Representa el campo IsRollup (Task). |
| BCWS | Representa el campo BCWS (Task). |
| BCWP | Representa el campo BCWP (Task). |
| PHYSICAL_PERCENT_COMPLETE | Representa el campo PhysicalPercentComplete (Task). |
| EARNED_VALUE_METHOD | Representa el campo EarnedValueMethod (Task). |
| ACTUAL_WORK_PROTECTED | Representa el campo ActualWorkProtected (Task). |
| ACTUAL_OVERTIME_WORK_PROTECTED | Representa el campo ActualOvertimeWorkProtected (Task). |
| IS_PUBLISHED | Representa el campo IsPublished (Task). |
| IS_SCHEDULED | Representa el campo IsScheduled (Task). |
| STATUS_MANAGER | Representa el campo StatusManager (Task). |
| COMMITMENT_START | Representa el campo CommitmentStart (Task). |
| COMMITMENT_FINISH | Representa el campo CommitmentFinish (Task). |
| COMMITMENT_TYPE | Representa el campo CommitmentType (Task). |
| IS_MANUAL | Representa el campo IsManual (Task). |
| IS_EXPANDED | Representa el campo IsExpanded (Task). |
| GUID | Representa el campo Guid (Task). |
| NOTES_TEXT | Representa el campo NotesText (Task). |
| NOTES_RTF | Representa el campo NotesRTF (Task). |
| MANUAL_START | Representa el campo ManualStart (Task). |
| MANUAL_FINISH | Representa el campo ManualFinish (Task). |
| MANUAL_DURATION | Representa el campo ManualDuration (Task). |
| BUDGET_WORK | Representa el campo BudgetWork (Task). |
| BUDGET_COST | Representa el campo BudgetCost (Task). |
| DISPLAY_AS_SUMMARY | Representa el campo DisplayAsSummary (Task). |
| SUMMARY_PROGRESS | Representa el campo Summary Progress (Task). |
| IS_ACTIVE | Representa el campo IsActive (Task). |
| START_TEXT | Representa el campo StartText (Task). |
| FINISH_TEXT | Representa el campo FinishText (Task). |
| DURATION_TEXT | Representa el campo DurationText (Task). |
| CALENDAR_UID | Representa el campo CalendarUid (Task). |
| PARENT_TASK_UID | Representa el campo ParentTaskUid (Task). |
| DISPLAY_ON_TIMELINE | Representa el campo DisplayOnTimeline (Task). |
| TASK_IS_ASSIGNED | Representa el campo TaskIsAssigned (Task). |
| TASK_ORIGINAL_START | Representa el campo de inicio original (Task). |
| TASK_ORIGINAL_FINISH | Representa el campo de fin original (Task). |
| IS_SHOW_BEFORE_PROJECT_START_DATE_WARNING | Representa la bandera que hace que MSP muestre la advertencia de que una tarea comienza antes de la fecha de inicio del proyecto. |
| WARNING | Representa la bandera que indica que la tarea tiene discrepancias de programación. |
| PARENT_TASK_GUID | Representa el campo ParentTaskGuid (Task). |
| ACTIVITY_ID | Representa el campo ActivityId (solo aplicable a proyectos Primavera). |
| FREE_SLACK_TIME_SPAN | Representa el FreeSlack (Task) campo. |
| TOTAL_SLACK_TIME_SPAN | Representa el TotalSlack (Task) campo. |
| START_SLACK_TIME_SPAN | Representa el StartSlack (Task) campo. |
| FINISH_SLACK_TIME_SPAN | Representa el FinishSlack (Task) campo. |
| EXTERNAL_UID | Representa el Id único de la tarea externa. |

### Ver también

* namespace [aspose.tasks](/tasks/python-net/aspose.tasks/)
* assembly [Aspose.Tasks](/tasks/python-net/)

