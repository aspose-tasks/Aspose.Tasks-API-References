---
title: "ByYearDayRepetition"
second_title: "Referencia de API de Aspose.Tasks para Java"
description: "Representa un patrón basado en la posición absoluta de un día en un mes."
type: docs
weight: 37
url: /es/java/com.aspose.tasks/byyeardayrepetition/
---

**Inheritance:**
java.lang.Object, [com.aspose.tasks.YearlyRepetitionBase](../../com.aspose.tasks/yearlyrepetitionbase)
```
public class ByYearDayRepetition extends YearlyRepetitionBase
```

Representa un patrón basado en la posición absoluta de un día en un mes.
## Constructores

| Constructor | Descripción |
| --- | --- |
| [ByYearDayRepetition()](#ByYearDayRepetition--) | Inicializa una nueva instancia de la clase [ByYearDayRepetition](../../com.aspose/tasks/byyeardayrepetition). |
## Métodos

| Método | Descripción |
| --- | --- |
| [getDayPosition()](#getDayPosition--) | Obtiene una posición de día en un mes en la que la tarea debe ser recurrente. |
| [getMonth()](#getMonth--) | Obtiene el mes en el que la tarea debe repetirse. |
| [setDayPosition(int value)](#setDayPosition-int-) | Establece una posición de día en un mes en la que la tarea debe ser recurrente. |
| [setMonth(int value)](#setMonth-int-) | Establece el mes en el que la tarea debe repetirse. |
### ByYearDayRepetition() {#ByYearDayRepetition--}
```
public ByYearDayRepetition()
```


Inicializa una nueva instancia de la clase [ByYearDayRepetition](../../com.aspose/tasks/byyeardayrepetition).

### getDayPosition() {#getDayPosition--}
```
public final int getDayPosition()
```


Obtiene una posición de día en un mes en la que la tarea debe ser recurrente.

**Returns:**
int - una posición de día en un mes en la que la tarea debe repetirse.
### getMonth() {#getMonth--}
```
public final int getMonth()
```


Obtiene el mes en el que la tarea debe repetirse.

Leer/Escribir `Month`.

**Returns:**
int - un mes en el que la tarea debe repetirse.
### setDayPosition(int value) {#setDayPosition-int-}
```
public final void setDayPosition(int value)
```


Establece una posición de día en un mes en la que la tarea debe ser recurrente.

**Parameters:**
| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| valor | int | una posición de día en un mes en la que la tarea debe repetirse. |

### setMonth(int value) {#setMonth-int-}
```
public final void setMonth(int value)
```


Establece el mes en el que la tarea debe repetirse.

Leer/Escribir `Month`.

**Parameters:**
| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| valor | int | un mes en el que la tarea debe repetirse. |

