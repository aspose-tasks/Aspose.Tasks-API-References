---
title: "ByYearWeekDayRepetition"
second_title: "Referencia de API de Aspose.Tasks para Java"
description: "Representa un patrón basado en la posición de un día de la semana en un mes."
type: docs
weight: 38
url: /es/java/com.aspose.tasks/byyearweekdayrepetition/
---

**Inheritance:**
java.lang.Object, [com.aspose.tasks.YearlyRepetitionBase](../../com.aspose.tasks/yearlyrepetitionbase)
```
public class ByYearWeekDayRepetition extends YearlyRepetitionBase
```

Representa un patrón basado en la posición de un día de la semana en un mes.
## Constructores

| Constructor | Descripción |
| --- | --- |
| [ByYearWeekDayRepetition()](#ByYearWeekDayRepetition--) | Inicializa una nueva instancia de la clase [ByYearWeekDayRepetition](../../com.aspose.tasks/byyearweekdayrepetition). |
## Métodos

| Método | Descripción |
| --- | --- |
| [getMonth()](#getMonth--) | Obtiene el mes en el que la tarea debe repetirse. |
| [getPosition()](#getPosition--) | Obtiene la posición del día en una semana de un mes en la que la tarea debe repetirse. |
| [getWeekDay()](#getWeekDay--) | Obtiene el tipo de día de la semana en el que la tarea debe repetirse. |
| [setMonth(int value)](#setMonth-int-) | Establece el mes en el que la tarea debe repetirse. |
| [setPosition(int value)](#setPosition-int-) | Establece la posición del día en una semana de un mes en la que la tarea debe repetirse. |
| [setWeekDay(int value)](#setWeekDay-int-) | Establece el tipo de día de la semana en el que la tarea debe repetirse. |
### ByYearWeekDayRepetition() {#ByYearWeekDayRepetition--}
```
public ByYearWeekDayRepetition()
```


Inicializa una nueva instancia de la clase [ByYearWeekDayRepetition](../../com.aspose.tasks/byyearweekdayrepetition).

### getMonth() {#getMonth--}
```
public final int getMonth()
```


Obtiene el mes en el que la tarea debe repetirse.

Leer/Escribir `Month`.

**Returns:**
int - un mes en el que la tarea debe repetirse.
### getPosition() {#getPosition--}
```
public final int getPosition()
```


Obtiene la posición del día en una semana de un mes en la que la tarea debe repetirse.

Leer/Escribir `OrdinalNumber`.

**Returns:**
int - una posición del día en una semana de un mes en la que la tarea debe repetirse.
### getWeekDay() {#getWeekDay--}
```
public final int getWeekDay()
```


Obtiene el tipo de día de la semana en el que la tarea debe repetirse.

Leer/Escribir `DayOfWeek`.

**Returns:**
int - un tipo de día de la semana en el que la tarea debe repetirse.
### setMonth(int value) {#setMonth-int-}
```
public final void setMonth(int value)
```


Establece el mes en el que la tarea debe repetirse.

Leer/Escribir `Month`.

**Parameters:**
| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| valor | int | un mes en el que la tarea debe repetirse. |

### setPosition(int value) {#setPosition-int-}
```
public final void setPosition(int value)
```


Establece la posición del día en una semana de un mes en la que la tarea debe repetirse.

Leer/Escribir `OrdinalNumber`.

**Parameters:**
| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| valor | int | una posición del día en una semana de un mes en la que la tarea debe repetirse. |

### setWeekDay(int value) {#setWeekDay-int-}
```
public final void setWeekDay(int value)
```


Establece el tipo de día de la semana en el que la tarea debe repetirse.

Leer/Escribir `DayOfWeek`.

**Parameters:**
| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| valor | int | un tipo de día de la semana en el que la tarea debe repetirse. |

