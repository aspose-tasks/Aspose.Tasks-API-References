---
title: "ByMonthWeekDayRepetition"
second_title: "Referencia de API de Aspose.Tasks para Java"
description: "Representa un patrón basado en la posición del día de la semana en un mes."
type: docs
weight: 36
url: /es/java/com.aspose.tasks/bymonthweekdayrepetition/
---

**Inheritance:**
java.lang.Object, [com.aspose.tasks.MonthlyRepetitionBase](../../com.aspose.tasks/monthlyrepetitionbase)
```
public class ByMonthWeekDayRepetition extends MonthlyRepetitionBase
```

Representa un patrón basado en la posición del día de la semana en un mes.
## Constructores

| Constructor | Descripción |
| --- | --- |
| [ByMonthWeekDayRepetition()](#ByMonthWeekDayRepetition--) | Inicializa una nueva instancia de la clase [ByMonthWeekDayRepetition](../../com.aspose.tasks/bymonthweekdayrepetition). |
## Métodos

| Método | Descripción |
| --- | --- |
| [getPosition()](#getPosition--) | Obtiene una posición del día de la semana en un mes en el que la tarea debe repetirse. |
| [getWeekDay()](#getWeekDay--) | Obtiene un tipo de día de la semana en el que la tarea debe repetirse. |
| [setPosition(int value)](#setPosition-int-) | Establece una posición del día de la semana en un mes en el que la tarea debe repetirse. |
| [setWeekDay(int value)](#setWeekDay-int-) | Establece un tipo de día de la semana en el que la tarea debe repetirse. |
### ByMonthWeekDayRepetition() {#ByMonthWeekDayRepetition--}
```
public ByMonthWeekDayRepetition()
```


Inicializa una nueva instancia de la clase [ByMonthWeekDayRepetition](../../com.aspose.tasks/bymonthweekdayrepetition).

### getPosition() {#getPosition--}
```
public final int getPosition()
```


Obtiene una posición del día de la semana en un mes en el que la tarea debe repetirse.

Leer/Escribir `OrdinalNumber`.

**Returns:**
int - una posición del día de la semana en un mes en el que la tarea debe repetirse.
### getWeekDay() {#getWeekDay--}
```
public final int getWeekDay()
```


Obtiene un tipo de día de la semana en el que la tarea debe repetirse.

Leer/Escribir `DayOfWeek`.

**Returns:**
int - un tipo de día de la semana en el que la tarea debe repetirse.
### setPosition(int value) {#setPosition-int-}
```
public final void setPosition(int value)
```


Establece una posición del día de la semana en un mes en el que la tarea debe repetirse.

Leer/Escribir `OrdinalNumber`.

**Parameters:**
| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| valor | int | una posición del día de la semana en un mes en el que la tarea debe repetirse. |

### setWeekDay(int value) {#setWeekDay-int-}
```
public final void setWeekDay(int value)
```


Establece un tipo de día de la semana en el que la tarea debe repetirse.

Leer/Escribir `DayOfWeek`.

**Parameters:**
| Parámetro | Tipo | Descripción |
| --- | --- | --- |
| valor | int | un tipo de día de la semana en el que la tarea debe repetirse. |

