---
title: "Aspose::Tasks::View::CompareTo método"
linktitle: "CompareTo"
articleTitle: "CompareTo"
second_title: "Aspose.Tasks for C++"
description: "Compara la instancia actual con otro objeto del mismo tipo y devuelve un entero que indica si la instancia actual precede, sigue o occu"
type: docs
weight: 10
url: /es/cpp/aspose.tasks/view/compareto/
---

## CompareTo {#compareto}

Compara la instancia actual con otro objeto del mismo tipo y devuelve un entero que indica si la instancia actual precede, sigue o ocurre en la misma posición en el orden de clasificación que el otro objeto.

**Returns:** A 32-bit signed integer that indicates the relative order of the objects being compared. The return value has these meanings: Value Meaning Less than zero This instance precedes other in the sort order. Zero This instance occurs in the same position in the sort order as other . Greater than zero This instance follows other in the sort order.

```cpp
CompareTo(System::SharedPtr< View > other)
```

| Parámetro | Descripción |
| --- | --- |
| otro | el objeto View especificado para comparar esta instancia. |

